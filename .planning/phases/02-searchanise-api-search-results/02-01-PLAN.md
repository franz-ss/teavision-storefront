---
phase: 02-searchanise-api-search-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/conventions.md
  - src/lib/searchanise/types.ts
  - src/lib/searchanise/search.ts
  - src/lib/searchanise/params.ts
  - src/app/(storefront)/search/page.tsx
  - src/app/(storefront)/pages/search-results/page.tsx
  - src/app/(storefront)/pages/search-results-page/page.tsx
  - src/components/search/index.ts
  - src/components/search/search-results-view/search-results-view.tsx
  - src/components/search/search-results-view/search-results-view.stories.tsx
  - src/components/search/search-results-view/index.ts
  - src/components/search/search-filter-panel/search-filter-panel.tsx
  - src/components/search/search-filter-panel/search-filter-panel.stories.tsx
  - src/components/search/search-filter-panel/index.ts
  - src/components/search/search-sort-select/search-sort-select.tsx
  - src/components/search/search-sort-select/search-sort-select.stories.tsx
  - src/components/search/search-sort-select/index.ts
  - src/components/search/search-pagination/search-pagination.tsx
  - src/components/search/search-pagination/search-pagination.stories.tsx
  - src/components/search/search-pagination/index.ts
  - src/components/product/searchanise-recommendations/searchanise-script-loader.tsx
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05
  - SEARCH-06
  - SEARCH-07
user_setup: []
must_haves:
  truths:
    - /search uses Searchanise JSON data as the primary search source instead of Searchanise-injected DOM.
    - Search results, filters, sort, pagination, empty, loading, and error states are rendered with Teavision-owned components and token classes.
    - Search query state is encoded in URL search params and converted into Searchanise API parameters server-side.
    - Legacy /pages/search-results-page and /pages/search-results routes no longer render the Shopify Searchanise app body.
    - Searchanise PDP recommendations remain available and keep their existing fallback behavior.
  artifacts:
    - src/lib/searchanise/search.ts
    - src/components/search/search-results-view/search-results-view.tsx
    - src/components/search/search-results-view/search-results-view.stories.tsx
  key_links:
    - HeaderSearchForm -> /search?q=... -> Searchanise API -> SearchResultsView
    - URL filter params -> parseSearchParams -> restrictBy[...] -> Searchanise facets
    - legacy search-results pages -> redirect('/search')
---

<objective>
Implement a headless Searchanise search results experience for the Next storefront.

Purpose: preserve Searchanise ranking, facets, and merchandising while replacing the legacy Shopify widget page and injected `snize-*` DOM with Teavision-owned, accessible, fully styled React UI.
Output: Searchanise data adapter, URL-param parser, `/search` route replacement, search UI components with stories, legacy redirect routes, isolated Searchanise script loading, and verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-searchanise-api-search-results/02-CONTEXT.md
@.planning/phases/02-searchanise-api-search-results/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@docs/conventions.md
@node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md
@node_modules/next/dist/docs/01-app/01-getting-started/06-fetching-data.md
@node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
@node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-search-params.md
@src/app/(storefront)/search/page.tsx
@src/lib/shopify/operations/search.ts
@src/lib/shopify/types/index.ts
@src/components/layout/header/header-search-form.tsx
@src/components/collection/collection-filter-panel/collection-filter-panel.tsx
@src/components/collection/sort-select/sort-select.tsx
@src/components/ui/product-card/product-card.tsx
@src/components/product/searchanise-recommendations/searchanise-script-loader.tsx
@src/components/product/searchanise-recommendations/searchanise-recommendations.tsx
@src/app/(storefront)/pages/[...slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed Searchanise API data boundary</name>
  <files>src/lib/searchanise/types.ts, src/lib/searchanise/search.ts, src/lib/searchanise/params.ts, src/lib/shopify/types/index.ts</files>
  <read_first>.planning/phases/02-searchanise-api-search-results/02-RESEARCH.md, src/lib/shopify/operations/search.ts, src/lib/shopify/types/index.ts, src/lib/shopify/client.ts</read_first>
  <action>Create a `src/lib/searchanise/` module for Searchanise search results. Define local public types for results, facets, buckets, categories/pages, request params, pagination, and errors. Implement a server-only fetch helper that builds `https://searchserverapi1.com/getresults` URLs with `apiKey`, `output=json`, `items=true`, `facets=true`, bounded `maxResults`, `startIndex`, `q`, sort, and filter restrictions. Read `NEXT_PUBLIC_SEARCHANISE_API_KEY` and `NEXT_PUBLIC_SEARCHANISE_ENABLED`; do not write the key into logs or planning docs. Parse JSON from `unknown` with small narrowing helpers, never `any`. Map valid Searchanise products into existing `ProductSummary` shape by extracting handles from product links and normalizing price/image data. Add URL-param helpers that convert app params (`q`, `page`, `sort`, repeated filters) into Searchanise request params and build stable hrefs for toggles, sort, clear, and pagination.</action>
  <verify>pnpm lint</verify>
  <acceptance_criteria>
    - `src/lib/searchanise/search.ts` imports `server-only`.
    - Searchanise response parsing uses `unknown` narrowing and contains no `any`.
    - API requests use HTTPS and bounded `maxResults`.
    - Searchanise products map to `ProductSummary` without direct imports from `src/lib/shopify/types/generated/`.
    - URL helpers cover `q`, `page`, `sort`, filter toggle, clear filters, and pagination href construction.
    - Missing/disabled Searchanise config produces a typed unavailable result or controlled route state rather than exposing a secret or crashing a no-query page.
  </acceptance_criteria>
  <done>The search route can ask one local operation for Searchanise results using typed, safe, server-side data.</done>
</task>

<task type="auto">
  <name>Task 2: Build owned search results UI components</name>
  <files>docs/conventions.md, src/components/search/index.ts, src/components/search/search-results-view/search-results-view.tsx, src/components/search/search-results-view/search-results-view.stories.tsx, src/components/search/search-results-view/index.ts, src/components/search/search-filter-panel/search-filter-panel.tsx, src/components/search/search-filter-panel/search-filter-panel.stories.tsx, src/components/search/search-filter-panel/index.ts, src/components/search/search-sort-select/search-sort-select.tsx, src/components/search/search-sort-select/search-sort-select.stories.tsx, src/components/search/search-sort-select/index.ts, src/components/search/search-pagination/search-pagination.tsx, src/components/search/search-pagination/search-pagination.stories.tsx, src/components/search/search-pagination/index.ts</files>
  <read_first>docs/conventions.md, src/components/collection/collection-toolbar/collection-toolbar.tsx, src/components/collection/collection-filter-panel/collection-filter-panel.tsx, src/components/collection/sort-select/sort-select.tsx, src/components/ui/product-card/product-card.tsx, src/components/ui/section/section.tsx, src/components/ui/button/button.tsx, src/components/ui/select/select.tsx</read_first>
  <action>Add a `src/components/search/` domain and update `docs/conventions.md` so search-specific feature components have an explicit home. Scaffold search components using `pnpm create:component -- search/<name>` where practical. Build `SearchResultsView` as the composed presentational experience: hero/summary, result count, product grid, empty state, unavailable/error state, categories/pages secondary results if returned, desktop and mobile filters, sort control, active filter chips or clear action, and pagination. Build filters from Searchanise facets using accessible links/forms or small client leaves patterned after collection filters. Build sort and pagination so they preserve current URL state. Use `Section.Root`/`Section.Container`, existing UI primitives, `ProductCard`, `ProductQuickView`, token class names, and `cn()` for conditional classes. Keep cards for repeated items only; do not create nested card layouts.</action>
  <verify>pnpm lint</verify>
  <acceptance_criteria>
    - `docs/conventions.md` includes `src/components/search/` in the folder map.
    - Every new component under `src/components/search/` has a co-located `.stories.tsx`.
    - Search UI renders products with `ProductCard` and quick view actions when product data is valid.
    - Empty, unavailable/error, filtered, and multi-page states are represented in Storybook.
    - Filter controls have labels/counts, keyboard-accessible focus styles, and clear behavior.
    - Sort and pagination preserve `q` and active filters in generated hrefs or router updates.
    - Styling uses Tailwind token classes only; no raw hex/rgb, CSS modules, inline styles, or className string concatenation.
  </acceptance_criteria>
  <done>Search results have Teavision-owned UI with Storybook coverage and no dependency on Searchanise widget DOM.</done>
</task>

<task type="auto">
  <name>Task 3: Replace /search and clean legacy Searchanise routes</name>
  <files>src/app/(storefront)/search/page.tsx, src/app/(storefront)/pages/search-results/page.tsx, src/app/(storefront)/pages/search-results-page/page.tsx, src/components/product/searchanise-recommendations/searchanise-script-loader.tsx</files>
  <read_first>src/app/(storefront)/search/page.tsx, src/app/(storefront)/layout.tsx, src/app/(storefront)/pages/[...slug]/page.tsx, src/components/product/searchanise-recommendations/searchanise-script-loader.tsx, src/components/product/searchanise-recommendations/searchanise-recommendations.tsx, node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md</read_first>
  <action>Replace the current `/search` implementation with the new Searchanise-backed data flow. Keep the Next 16 `searchParams: Promise<...>` pattern and wrap runtime search content in `Suspense`. Preserve metadata with `robots: { index: false }`. Render no-query guidance without calling Searchanise. For query requests, call the Searchanise operation and pass mapped products/facets/pagination into `SearchResultsView`. Add explicit routes for `/pages/search-results-page` and `/pages/search-results` that redirect to `/search`, preserving `q` and other useful query params. Update `SearchaniseScriptLoader` so it skips `/search`, `/pages/search-results-page`, and `/pages/search-results`, while PDP `SearchaniseRecommendations` continues to use it on product pages.</action>
  <verify>pnpm lint; pnpm build</verify>
  <acceptance_criteria>
    - `src/app/(storefront)/search/page.tsx` no longer renders the simple native Shopify predictive-search grid as the primary result view.
    - `/search` renders Searchanise-backed results when `q` is present and no-query guidance when `q` is absent.
    - `/pages/search-results-page` and `/pages/search-results` redirect to `/search` instead of falling through to `pages/[...slug]`.
    - `SearchaniseScriptLoader` does not inject the widget script on search result routes.
    - PDP recommendation behavior is not removed or disabled globally.
    - `pnpm build` succeeds with any `useSearchParams()` client leaves wrapped in `Suspense`.
  </acceptance_criteria>
  <done>Customers and legacy URLs land on the owned search page, and Searchanise no longer owns the search-results DOM.</done>
</task>

<task type="auto">
  <name>Task 4: Verify search behavior and document completion</name>
  <files>.planning/phases/02-searchanise-api-search-results/02-01-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/phases/02-searchanise-api-search-results/02-01-PLAN.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/codebase/TESTING.md</read_first>
  <action>Run final verification. Use `pnpm lint` and `pnpm build`. Start `pnpm dev` and use the in-app browser to verify `/search?q=chai`, a filtered/sorted/paginated search URL, `/search` with no query, `/pages/search-results-page?q=chai` redirect behavior, and one product page with Searchanise recommendations enabled or fallback behavior. Inspect the search page DOM enough to confirm Searchanise is not injecting a `snize-search-results` widget above or below the owned UI. Update ROADMAP and STATE only after verification passes. Create the phase summary with files changed, verification output, deviations, and self-check.</action>
  <verify>pnpm lint; pnpm build; browser check /search?q=chai; browser check /pages/search-results-page?q=chai; browser check one PDP recommendation section</verify>
  <acceptance_criteria>
    - Lint passes.
    - Production build passes.
    - Browser check shows owned `/search` UI for a real query.
    - Browser check shows no legacy static-page hero/body under Searchanise results.
    - Browser check confirms legacy Searchanise page redirects to `/search`.
    - Browser check confirms PDP recommendations are still present or fallback safely.
    - `.planning/phases/02-searchanise-api-search-results/02-01-SUMMARY.md` exists and records exact verification results.
  </acceptance_criteria>
  <done>Phase 2 is verified, summarized, and ready for user review.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Storybook stories exist for all new `src/components/search/*` components.
- [ ] `/search?q=chai` renders owned search UI from Searchanise API data.
- [ ] A filtered/sorted/paginated URL preserves state and fetches the expected Searchanise params.
- [ ] `/pages/search-results-page?q=chai` redirects to `/search?q=chai`.
- [ ] `/search` and legacy search routes do not contain injected Searchanise search-results widget DOM.
- [ ] A PDP still renders Searchanise recommendations or the existing fallback.
</verification>

<success_criteria>

- All tasks completed.
- All SEARCH requirements are covered.
- Searchanise API is used as a data source, not a DOM renderer, for search results.
- Search UI follows Teavision design/component conventions.
- Legacy Searchanise page body no longer appears below injected results.
- PDP recommendation integration remains intact.
- Verification checks pass or any external Searchanise/API blocker is documented with exact output.

</success_criteria>

<output>
After completion, create `.planning/phases/02-searchanise-api-search-results/02-01-SUMMARY.md`.
</output>
