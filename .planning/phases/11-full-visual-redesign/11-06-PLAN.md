---
phase: 11-full-visual-redesign
plan: 06
type: execute
wave: 4
depends_on: ['11-02', '11-03']
files_modified:
  - src/app/globals.css
  - scripts/component-contracts/button-system.test.mjs
  - src/components/homepage/hero
  - src/components/homepage/proof-points
  - src/components/homepage/product-range
  - src/components/homepage/private-label
  - src/components/homepage/overlay-image-card
  - src/components/homepage/organic-herbs
  - src/components/homepage/certification-coverage
autonomous: true
requirements: [RD-04]

must_haves:
  truths:
    - 'The homepage hero is a full-bleed editorial image with dark green gradient scrim, gold eyebrow, paper serif display headline with italic accent, and inverse/inverseSecondary CTAs'
    - 'Proof points render as a green-deep stat band: white serif stats, gold icons, white/12 column rules'
    - 'Product range renders as image tiles with bottom scrim, gold mono tag, white serif title, hover-reveal Shop now'
    - 'Private label renders as 3 card-surface service cards with mono gold-deep numbering and link-arrow'
    - 'Organic herbs is a sunken two-column split with hairline check-list rows and a brand CTA'
    - 'Certification marquee scrolls with CSS only, pauses on hover, and stops under reduced motion'
  artifacts:
    - path: 'src/components/homepage/hero/hero.tsx'
      provides: 'HeroA editorial full-bleed'
      contains: 'type-display'
    - path: 'src/components/homepage/certification-coverage/certification-coverage.tsx'
      provides: 'reduced-motion-safe marquee'
      contains: 'motion-reduce'
  key_links:
    - from: 'src/components/homepage/hero/hero.tsx'
      to: 'scripts/component-contracts/button-system.test.mjs'
      via: 'contract test asserts variant="brand" exists in hero CTAs — keep at least one brand-variant Button'
      pattern: 'variant="brand"'
    - from: 'src/components/homepage/proof-points/proof-points.tsx'
      to: 'src/components/ui/section'
      via: 'Section.Root tone="brand" (green-deep band)'
      pattern: 'tone='
---

<objective>
Restyle the upper homepage sections — hero, proof-points stat band, product range tiles, private-label service cards, overlay image card, organic split, and certification marquee — to the redesign's HeroA composition (mockup default; hero variants B/C are excluded per CONTEXT).

Purpose: first half of RD-04; these sections carry the strongest brand impression.
Output: upper homepage matching homepage.js sections, real Shopify data kept under mockup treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/homepage.js
@design/extracted/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero A + proof-points stat band</name>
  <files>src/components/homepage/hero, src/components/homepage/proof-points</files>
  <read_first>src/components/homepage/hero/hero.tsx (current CTAs — contract test asserts variant="brand" presence; check scripts/component-contracts/button-system.test.mjs hero assertion before changing CTA variants), src/components/homepage/proof-points/proof-points.tsx, design/extracted/homepage.js HeroA + heroA__strip JSX, 11-UI-SPEC.md §5.3 rows 1–2</read_first>
  <action>
**Hero (HeroA)**: full-bleed image (keep the existing `public/images/homepage-hero.jpg` or Shopify-sourced image and next/image usage), `min-h-[min(92vh,860px)]`, gradient scrim overlay defined as a globals.css utility (PRIMARY instruction — the class checker does NOT catch raw color values, so do not inline the oklch arbitrary value): add `@utility hero-scrim { background: linear-gradient(105deg, oklch(0.225 0.016 167 / 0.82), oklch(0.225 0.016 167 / 0.15)); }` to `src/app/globals.css` and apply the `hero-scrim` class on the overlay element. Never inline raw oklch/hex in className arbitrary values, never `style={{}}`. Content: `Eyebrow tone="gold"`, headline `type-display text-paper max-w-[16ch]` with one line in `italic` Spectral (`<em className="italic">`), lede `type-lede text-paper/90`, CTA row: `<Button variant="inverse" size="lg">` + `<Button variant="inverseSecondary" size="lg">`. IMPORTANT: the contract test asserts `variant="brand"` appears in hero.tsx — read the assertion first; if it requires variant="brand", keep one CTA as `variant="brand"` (it reads well on the scrim) OR update the contract test expectation in the same commit and note it. Preserve any aria/landmark structure.

**Proof points**: `Section.Root tone="brand"` (green-deep) band; 4-col grid (2-col mobile), columns separated by `border-l border-paper/12` rules; each stat: gold lucide icon, `font-display text-[2.4rem] text-paper` value, `text-paper/78` label. Keep the existing stats content/data.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:contracts</automated>
</verify>
<acceptance_criteria> - `grep -c "type-display" src/components/homepage/hero/hero.tsx` returns >= 1 - `pnpm test:contracts` exits 0 (hero variant assertion satisfied) - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|type-display-0\|brandStrong" src/components/homepage/hero src/components/homepage/proof-points` returns no files - `rg -n "-\[(#|rgb|oklch|hsl)" src/components/homepage/hero src/components/homepage/proof-points` returns no matches (same regex as the 11-14 phase-wide RD-08 gate; the scrim gradient lives in globals.css as `hero-scrim`) - `grep -c "hero-scrim" src/app/globals.css` returns >= 1
</acceptance_criteria>
<done>Hero A + green stat band live with contract test green</done>
</task>

<task type="auto">
  <name>Task 2: Product range tiles + private-label service cards + overlay image card</name>
  <files>src/components/homepage/product-range, src/components/homepage/private-label, src/components/homepage/overlay-image-card</files>
  <read_first>all files in the three component folders (current data sources, image handling, story files), design/extracted/homepage.js RangeSection + ServicesSection JSX, design/extracted/design-system.css .rtile/.svc blocks, 11-UI-SPEC.md §5.3 rows 3–4</read_first>
  <action>
**Product range (.rtile grid)**: section head split — left `Eyebrow` + `type-heading-01`, right `text-ink-soft max-w-[34ch]`; tiles in 4-col grid (`lg:grid-cols-4 md:grid-cols-2 grid-cols-1 gap-4.5`); each tile `relative aspect-[1/1.08] rounded-lg overflow-hidden`, bottom gradient scrim (`bg-gradient-to-t from-ink/70 via-ink/20 to-transparent`), gold mono tag (`font-mono text-[10px] tracking-[0.14em] uppercase text-gold`), `font-display text-paper` title, hover-reveal "Shop now" link-arrow (`opacity-0 group-hover:opacity-100 focus-within:opacity-100 transition-opacity motion-reduce:transition-none` — must remain reachable by keyboard and visible on touch: also `max-lg:opacity-100`). Keep real collection links/images.

**Private label (.svc cards)**: 3-col grid of card-surface cards (`bg-card border border-hairline-2 rounded-lg p-7.5`), 150px media block, `font-mono text-xs tracking-[0.14em] text-gold-deep` numbering ("01"…), `font-display text-[1.45rem]` title, link-arrow CTA; hover `hover:-translate-y-1 hover:shadow-3 motion-reduce:hover:translate-y-0`.

**Overlay image card**: `.rtile` treatment; PRESERVE the exact motion-reduce trio `motion-reduce:transform-none motion-reduce:transition-none motion-reduce:group-hover:scale-100` on its `group-hover:scale` line (contract-tested file).
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:contracts</automated>
</verify>
<acceptance_criteria> - `grep -rc "rounded-lg" src/components/homepage/product-range` returns >= 1; `grep -rc "text-gold-deep" src/components/homepage/private-label` returns >= 1 - `grep -c "motion-reduce:group-hover:scale-100" src/components/homepage/overlay-image-card/overlay-image-card.tsx` returns >= 1 - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle" src/components/homepage/product-range src/components/homepage/private-label src/components/homepage/overlay-image-card` returns no files - `pnpm test:contracts` exits 0
</acceptance_criteria>
<done>Range tiles, service cards, overlay card restyled with motion-reduce contracts intact</done>
</task>

<task type="auto">
  <name>Task 3: Organic split + certification marquee</name>
  <files>src/components/homepage/organic-herbs, src/components/homepage/certification-coverage</files>
  <read_first>both component folders (current markup + any animation), design/extracted/homepage.js OrganicSplit + CertsMarquee JSX, design/extracted/design-system.css .split/.certs + marquee keyframes, 11-UI-SPEC.md §5.3 rows 5–6, src/app/globals.css (where to put the marquee keyframes)</read_first>
  <action>
**Organic split**: `Section.Root tone="sunken"` (paper-2); two-column grid; check-list rows `border-t border-hairline py-4 flex gap-3` with brand check icon (lucide Check `text-brand`); CTA `<Button variant="brand">`. Keep real copy/images.

**Certs marquee**: wrapper `border-y border-hairline overflow-hidden`; define a `marquee` keyframe (translateX 0 → −50%) in globals.css with a `animate-marquee` utility (38s linear infinite) — duplicated item track for seamless loop; pause on hover (`hover:[animation-play-state:paused]`) and `motion-reduce:animate-none` so reduced-motion users see a static row. Items `type-mono-meta text-ink-soft inline-flex items-center gap-2.5` + brand icon. Keep the existing certification content. Implement as a Server Component (pure CSS animation — no 'use client', no IntersectionObserver; the mockup useReveal pattern is excluded per RESEARCH).
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `grep -rc "motion-reduce:animate-none" src/components/homepage/certification-coverage` returns >= 1 - `grep -rc "use client" src/components/homepage/certification-coverage` returns 0 - `grep -c "marquee" src/app/globals.css` returns >= 1 - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle" src/components/homepage/organic-herbs src/components/homepage/certification-coverage` returns no files
</acceptance_criteria>
<done>Sunken organic split + CSS-only reduced-motion-safe marquee</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                  |
| -------- | ------------------------------------------------------------ |
| none new | presentational restyle of server-rendered marketing sections |

## STRIDE Threat Register

| Threat ID | Category                     | Component                    | Disposition | Mitigation Plan                                                                                                                |
| --------- | ---------------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-11-13   | DoS (a11y/motion regression) | certs marquee, hover reveals | mitigate    | motion-reduce:animate-none + motion-reduce trios asserted by grep + contract tests; hover-reveal CTAs keyboard/touch reachable |
| T-11-14   | Elevation (XSS)              | mockup markup porting        | mitigate    | no dangerouslySetInnerHTML; icons via lucide-react components                                                                  |

</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind`, `pnpm test:contracts` exit 0
- Dev-server check at desktop + 375px against extracted-design.html homepage upper sections
</verification>

<success_criteria>

- Upper homepage matches HeroA composition; zero old tokens in owned folders; motion contracts green
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-06-SUMMARY.md`
</output>
