---
phase: 11-full-visual-redesign
plan: 16
type: execute
wave: 2
depends_on: ['11-15']
autonomous: true
gap_closure: true
requirements: [RD-03, RD-04, RD-06]
files_modified:
  - src/app/(storefront)/page.tsx
  - src/components/homepage/hero/hero.tsx
  - src/components/homepage/content.ts
  - src/components/homepage/proof-points/proof-points.tsx
  - src/components/homepage/private-label/private-label.tsx
  - src/components/homepage/product-range/product-range.tsx
  - src/components/homepage/overlay-image-card/overlay-image-card.tsx
  - src/components/homepage/certification-coverage/certification-coverage.tsx
  - src/components/homepage/testimonials/testimonials.tsx
  - src/components/homepage/newsletter/newsletter.tsx
  - src/components/homepage/newsletter/newsletter-form.tsx
  - src/components/homepage/faq/faq.tsx
  - src/lib/contact/actions.ts
  - .env.example

user_setup:
  - service: resend
    why: 'Newsletter and contact form email delivery (sendNewsletterSignupAction / submitContactFormAction). Without the key, every newsletter/contact submission returns the user-safe send-error state.'
    env_vars:
      - name: RESEND_API_KEY
        source: 'Resend Dashboard -> API Keys (https://resend.com/api-keys). Add to .env.local; .env.example already documents the name.'

must_haves:
  truths:
    - "Hero shows the original 'Australia's #1 tea company' headline and approved lede"
    - 'The certification marquee is visible on the homepage and scrolls seamlessly'
    - 'Stat band shows the Australian flag, larger gold icons, and matches the design layout'
    - 'Private-label, product-range, testimonials, and FAQ sections use the approved verbatim copy'
    - 'Newsletter band matches the design (decorative image layer) and a configured RESEND_API_KEY lets signup succeed'
  artifacts:
    - path: 'src/app/(storefront)/page.tsx'
      provides: 'CertificationCoverage mounted between OrganicHerbs and Testimonials'
    - path: 'src/components/homepage/proof-points/proof-points.tsx'
      provides: 'image branch (flag) + 40px icons + left-aligned layout'
    - path: 'src/lib/contact/actions.ts'
      provides: 'console.warn distinguishing missing-key from provider failure'
  key_links:
    - from: 'src/components/homepage/hero/hero.tsx'
      to: 'src/components/homepage/content.ts'
      via: 'consumes HOMEPAGE_HERO.title/.copy'
      pattern: 'HOMEPAGE_HERO|HERO'
    - from: 'src/app/(storefront)/page.tsx'
      to: 'certification-coverage'
      via: 'renders <CertificationCoverage />'
      pattern: 'CertificationCoverage'
---

<objective>
Close the two homepage parity gaps (UAT tests 6 and 7) covering upper and lower sections, fix the verbatim copy, mount the missing certification marquee, and resolve the newsletter signup failure (environment + server-side observability). Copy exact values from design/extracted-design.html — do NOT approximate.

Purpose: The homepage is the primary brand surface and currently diverges from the design in copy, layout, and components, and the newsletter/contact forms fail deterministically because RESEND_API_KEY is unprovisioned.
Output: Homepage upper sections (hero, stat band, range tiles, private-label, certs marquee, buttons), lower sections (testimonials, newsletter, FAQ) match the design with approved copy, and newsletter/contact send paths are correctly gated with provisioning documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative design source — copy exact values, do not approximate (owner directive)

@design/extracted-design.html

# Diagnosed evidence

@.planning/debug/homepage-upper-parity.md
@.planning/debug/homepage-lower-parity.md
@.planning/debug/newsletter-signup-failure.md

# Button .btn metrics are now finalized by 11-15 — consume them, do not re-edit button.tsx

@src/components/ui/button/button.tsx
</context>

<approved_copy>

<!-- Carry these strings VERBATIM. Source: 11-UAT.md gap notes (owner-approved). -->

Hero h1: "Australia's #1 tea company"
Hero p: "Discover a world of tea mastery in every cup. Handpicked from the finest leaves, our loose leaf teas, bulk tea bags, and organic herbs deliver rich flavor and freshness. Trusted by Australia's leading cafes, retailers, and wellness brands."

Private-label title: "Private Label & Custom Tea Solutions"
Private-label p: "We partner with you to develop custom blends, manufacture tea bags, and deliver fully packaged private label tea products."

Product-range title: "Explore Our Product Range" (replaces "Wholesale online, direct to business or doorstep.")
Product-range p: "We offer Wholesale products online direct to consumers and businesses or you can apply for a bulk wholesale account and receive further discounts on 100kg+ orders."

Testimonials title: "Teavision Testimonials"
Testimonials p: "We're proud to be the trusted tea supplier for Australia's biggest and most loved brands. Our clients value our ability to source fresh, organic ingredients and provide flexible solutions for bulk tea bags, loose tea in bulk, and custom blends."

FAQ title: "Frequently asked questions" (replaces "Frequently asked.")
FAQ p: "You may have questions about sourcing wholesale tea, tea bags, and bulk spices. Below are answers to some of the most common queries from our partners." (keep the original accordion Q/A contents which already match the theme verbatim)
</approved_copy>

<tasks>

<task type="auto">
  <name>Task 1: Homepage upper sections — copy, marquee mount, stat band, range tiles</name>
  <files>src/app/(storefront)/page.tsx, src/components/homepage/hero/hero.tsx, src/components/homepage/content.ts, src/components/homepage/proof-points/proof-points.tsx, src/components/homepage/private-label/private-label.tsx, src/components/homepage/product-range/product-range.tsx, src/components/homepage/overlay-image-card/overlay-image-card.tsx, src/components/homepage/certification-coverage/certification-coverage.tsx</files>
  <action>
    Per .planning/debug/homepage-upper-parity.md, matching the corresponding sections in design/extracted-design.html exactly:
    - HERO: hero.tsx currently hardcodes the h1/copy; the approved copy already sits in content.ts (~lines 104-106). Make hero.tsx consume HOMEPAGE_HERO.title/.copy and ensure those equal the approved hero strings above. h1 must be "Australia's #1 tea company".
    - MARQUEE: CertificationCoverage is never mounted on the homepage. Import and render it in page.tsx between OrganicHerbs and Testimonials. Add `w-max` to the marquee track in certification-coverage.tsx so the -50% loop is seamless; match the design's item type/gap.
    - STAT BAND (proof-points.tsx): implement the unimplemented image branch so the Australian flag (/images/australian-flag.svg, already defined in content) renders for that point. Match the design: ~40px gold icons (currently 24px), left-aligned 44px/30px layout with dividers (currently centered). Copy the .stat/proof layout values from design/extracted-design.html.
    - PRIVATE-LABEL (private-label.tsx + content.ts): set section title "Private Label & Custom Tea Solutions" and paragraph (approved copy above); add the missing card body copy; match the design card look and 350ms hover animation.
    - PRODUCT-RANGE (product-range.tsx + content.ts): set heading "Explore Our Product Range" and the approved paragraph (replacing "Wholesale online, direct to business or doorstep.").
    - RANGE TILES (overlay-image-card.tsx): remove the redundant static gold "Shop Now" text that misuses the design's category-tag slot; restyle the hover CTA to mono 11px uppercase with a translateY reveal (currently serif), matching the design. Preserve the existing motion-reduce hover-zoom classes required by the contract test (`group-hover:scale` lines must keep motion-reduce:transform-none / transition-none / group-hover:scale-100).
    - Buttons already carry the finalized .btn metrics from 11-15 — do NOT edit button.tsx here; just use the correct variants.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts</automated>
    <human-check>Homepage hero reads "Australia's #1 tea company"; certification marquee is visible and scrolls seamlessly; stat band shows the Australian flag with larger gold icons left-aligned; private-label and product-range headings/paragraphs match the approved copy; range tiles have no redundant gold "Shop Now" and a mono uppercase hover CTA.</human-check>
  </verify>
  <done>Upper-section copy is the approved verbatim text; marquee renders seamlessly; stat band matches design with flag + larger icons; range tiles fixed; contract + lint + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Homepage lower sections — testimonials, newsletter visual, FAQ copy</name>
  <files>src/components/homepage/content.ts, src/components/homepage/testimonials/testimonials.tsx, src/components/homepage/newsletter/newsletter.tsx, src/components/homepage/newsletter/newsletter-form.tsx, src/components/homepage/faq/faq.tsx</files>
  <action>
    Per .planning/debug/homepage-lower-parity.md, copying values from design/extracted-design.html:
    - TESTIMONIALS: content.ts currently feeds full 100-230-word raw testimonials into the large serif scale (wall of text). Replace with the design's condensed editorial pull-quotes (25-40 words) — use the verbatim pull-quotes captured in the homepage-lower-parity debug session. Fix the St. Ali entry's swapped name/role. In testimonials.tsx: replace the 4rem text-glyph quote mark with the design's 50x50 gold-stroke SVG at opacity .5; change the centered header to the design's split `range__head` layout; set title "Teavision Testimonials" + approved paragraph. Long quotes must be handled gracefully (clamp/scale) per the design.
    - NEWSLETTER (newsletter.tsx): add the design's absolutely-positioned decorative image layer using the design's EXACT offsets (right:-6%, top:-20%, width:46%, height:140%, opacity:.5) — these are computed dynamic values Tailwind cannot statically extract, so use `style={{}}` per the conventions exception. Set padding clamp(40px,6vw,72px). In newsletter-form.tsx: fix the placeholder/gap and add the arrow icon on the Subscribe button per the design.
    - FAQ (faq.tsx): set title "Frequently asked questions" (replacing "Frequently asked.") + approved paragraph; constrain the container to 880px (currently 1280px) to match the design. Keep the original accordion Q/A contents unchanged.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories</automated>
    <human-check>Testimonials use short pull-quotes with an SVG gold quote mark and split header titled "Teavision Testimonials"; St. Ali name/role correct; newsletter band has the decorative image layer matching the design; FAQ title is "Frequently asked questions" in an 880px container.</human-check>
  </verify>
  <done>Lower-section copy is the approved verbatim text; testimonials, newsletter, and FAQ visually match the design; stories + contract + lint + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 3: Newsletter/contact send — env provisioning doc + server-side warn</name>
  <files>src/lib/contact/actions.ts, .env.example</files>
  <action>
    Root cause (.planning/debug/newsletter-signup-failure.md): RESEND_API_KEY is unset in .env.local, so both the missing-key branch (newsletter at actions.ts:310-313, contact at :212-215) returns the user-safe send-error string before any network call — indistinguishable from a real provider failure.
    - CODE: in actions.ts, in BOTH missing-key branches, add a `console.warn` BEFORE returning the error, clearly stating the email provider is not configured (RESEND_API_KEY absent) so logs distinguish "not configured" from a genuine Resend failure. Do not change the returned user-facing error strings or the validation/rate-limit/honeypot flow. Keep `unknown` typing; no `any`.
    - DOC: confirm .env.example documents `RESEND_API_KEY=` (it does at line 34); leave a brief inline comment noting it is required for newsletter/contact email delivery if not already present.
    - This gap's environment provisioning is USER SETUP (declared in frontmatter) — Claude cannot obtain the Resend key. After the code change, surface the env requirement via the execute-plan user-setup flow; do not fabricate a key.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:integration</automated>
    <human-check>With no local RESEND_API_KEY, submitting the newsletter logs a clear "provider not configured" server warning and still shows the safe error. With a real key in .env.local, newsletter signup succeeds.</human-check>
  </verify>
  <done>actions.ts logs a distinct warning in the missing-key branch for both newsletter and contact; .env.example documents RESEND_API_KEY; user-setup flagged for the owner to provision the key; typecheck + integration tests pass.</done>
</task>

</tasks>

<verification>
- pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories && pnpm test:integration pass
- Manual: homepage upper + lower parity against design; newsletter succeeds once RESEND_API_KEY is set.
</verification>

<success_criteria>
Homepage UAT tests 6 and 7 are closed with approved verbatim copy and design-faithful layout; the newsletter signup failure is resolved (env documented + warn added).
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-16-SUMMARY.md` when done.
</output>
