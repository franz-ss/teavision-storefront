---
phase: 11-full-visual-redesign
plan: 11
type: execute
wave: 4
depends_on: ['11-02', '11-03']
files_modified:
  - src/app/(storefront)/cart/page.tsx
  - src/app/(storefront)/cart/error.tsx
  - src/app/(storefront)/cart/_components/cart-view.tsx
  - src/app/(storefront)/cart/_components/cart-view.test.tsx
  - src/app/(storefront)/cart/_components/cart-view.stories.tsx
  - src/app/(storefront)/cart/_components/cart-loading-skeleton.tsx
  - src/app/(storefront)/cart/_components/cart-loading-skeleton.test.tsx
  - src/app/(storefront)/cart/_components/cart-line-actions.tsx
  - src/app/(storefront)/cart/_components/cart-line-actions.stories.tsx
  - src/app/(storefront)/cart/_components/cart-checkout-form.tsx
  - src/app/(storefront)/cart/_components/cart-recommendations.tsx
autonomous: true
requirements: [RD-07]

must_haves:
  truths:
    - 'The /cart page wears the mockup cart-drawer visual language: hairline-divided lines with rounded thumbs, serif names, mono meta, pill steppers, mono remove links, card summary with serif subtotal and brand checkout button'
    - 'No cart drawer is built — the existing /cart page is restyled (orchestrator decision #3)'
    - 'Shopify line discount allocations still display as brand-colored savings lines (BULK-07)'
    - 'Cart Server Actions, optimistic states, and checkout handoff are unchanged'
    - 'The loading skeleton mirrors the new cart-view structure'
  artifacts:
    - path: 'src/app/(storefront)/cart/_components/cart-view.tsx'
      provides: 'redesigned cart lines + summary'
      contains: 'border-hairline'
    - path: 'src/app/(storefront)/cart/_components/cart-loading-skeleton.tsx'
      provides: 'skeleton mirroring new structure'
  key_links:
    - from: 'src/app/(storefront)/cart/_components/cart-view.tsx'
      to: 'cart-view.test.tsx'
      via: 'h3 class-string assertion updated in lockstep'
      pattern: 'font-display'
    - from: 'src/app/(storefront)/cart/_components/cart-checkout-form.tsx'
      to: 'src/lib/cart/actions.ts'
      via: 'unchanged checkout Server Action'
      pattern: 'action'
---

<objective>
Restyle the cart page to the mockup cart-drawer's line-item/summary visual language (applied to the existing page — no drawer), updating the cart-view and skeleton tests in lockstep, and preserving all cart Server Actions, discount display, optimistic updates, and the checkout handoff.

Output: redesigned /cart per header.js CartDrawer styles + UI-SPEC §5.7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/header.js
@design/extracted/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cart view + lockstep test + stories</name>
  <files>src/app/(storefront)/cart/_components/cart-view.tsx, src/app/(storefront)/cart/_components/cart-view.test.tsx, src/app/(storefront)/cart/_components/cart-view.stories.tsx, src/app/(storefront)/cart/_components/cart-line-actions.tsx, src/app/(storefront)/cart/_components/cart-line-actions.stories.tsx</files>
  <read_first>cart-view.tsx (68 old-class occurrences; line/summary/discount structure, optimistic state, aria), cart-view.test.tsx (h3 class-string assertion — exact string), cart-line-actions.tsx (remove action wiring), design/extracted/header.js CartDrawer JSX + design-system.css .cart block, 11-UI-SPEC.md §5.7</read_first>
  <action>
**Lines**: each `flex gap-3.5 py-5 border-b border-hairline`; 76px thumb `rounded-lg bg-paper-2 overflow-hidden`; name `font-display text-[1.05rem]`; variant/meta `type-mono-meta text-ink-faint`; line total `font-bold`; pill quantity stepper (restyled primitive from 11-03); discounts/savings lines `text-brand` (replacing success-* classes per UI-SPEC §3 — BULK-07 display logic untouched).

**Summary (.cart\_\_foot)**: `bg-card border-t border-hairline rounded-lg p-6` (page placement, not drawer-pinned); subtotal row `font-display text-2xl`; freight note `type-mono-meta text-ink-faint` with truck lucide icon; full-width `<Button variant="brand" size="lg">` checkout.

**Empty state**: centered leaf icon, `text-ink-soft` copy, `<Button variant="ghost">`/link-arrow "Browse teas".

**cart-line-actions**: remove link `type-mono-meta text-ink-faint hover:text-gold-deep` — keep the remove Server Action + pending state.

**Lockstep**: update the cart-view.test.tsx h3 class-string assertion to the new classes; update cart-view.stories.tsx + cart-line-actions.stories.tsx visuals. Keep all aria-live updates, optimistic quantity behavior, and discount-allocation rendering logic byte-equivalent (classes only).
</action>
<verify>
<automated>pnpm test:unit && pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `grep -c "text-brand" src/app/(storefront)/cart/_components/cart-view.tsx` returns >= 1 (discount lines) - `grep -c "success-" src/app/(storefront)/cart/_components/cart-view.tsx` returns 0 - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|action-" "src/app/(storefront)/cart/_components/cart-view.tsx" "src/app/(storefront)/cart/_components/cart-line-actions.tsx"` returns no files - `pnpm test:unit` exits 0 (cart-view.test.tsx assertion updated in lockstep)
</acceptance_criteria>
<done>Cart lines/summary in drawer visual language; discount display + actions intact; tests lockstepped</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton mirror + checkout form + recommendations + page/error</name>
  <files>src/app/(storefront)/cart/_components/cart-loading-skeleton.tsx, src/app/(storefront)/cart/_components/cart-loading-skeleton.test.tsx, src/app/(storefront)/cart/_components/cart-checkout-form.tsx, src/app/(storefront)/cart/_components/cart-recommendations.tsx, src/app/(storefront)/cart/page.tsx, src/app/(storefront)/cart/error.tsx</files>
  <read_first>cart-loading-skeleton.tsx + its test (structure mirrored from cart-view — copy the NEW cart-view structure from Task 1), cart-checkout-form.tsx (checkout handoff action), cart/page.tsx + error.tsx, 11-UI-SPEC.md §5.7 + §5.11</read_first>
  <action>
- **Skeleton**: mirror the new cart-view structure exactly — `bg-paper-2 animate-pulse motion-reduce:animate-none rounded-lg` blocks matching line/thumb/summary geometry; update cart-loading-skeleton.test.tsx assertions in lockstep with the new class strings.
- **Checkout form**: `<Button variant="brand" size="lg">` full-width; keep the checkout Server Action/handoff untouched (no real-Shopify checkout testing — AGENTS.md restriction).
- **Recommendations**: class swap per UI-SPEC §3; rail mirrors related-rail language.
- **page.tsx**: page heading `font-display type-heading-01` + `type-mono-meta` item count; container `max-w-base`.
- **error.tsx**: paper bg, serif heading, `text-ink-soft` copy, `<Button variant="brand">` retry (keep reset wiring).
  </action>
  <verify>
    <automated>pnpm test:unit && pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "motion-reduce:animate-none" src/app/(storefront)/cart/_components/cart-loading-skeleton.tsx` returns >= 1
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|type-display-0" "src/app/(storefront)/cart"` returns no files
    - `pnpm test:unit` exits 0 (skeleton test lockstepped)
    - `pnpm test:e2e` exits 0 (fake-Shopify cart-to-checkout handoff intact)
  </acceptance_criteria>
  <done>Skeleton mirrors new structure; checkout handoff verified by e2e; cart surface fully on new tokens</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                 |
| --------------------- | ------------------------------------------- |
| user → cart mutations | quantity/remove/checkout via Server Actions |

## STRIDE Threat Register

| Threat ID | Category    | Component        | Disposition | Mitigation Plan                                                                                         |
| --------- | ----------- | ---------------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| T-11-26   | Tampering   | cart mutations   | mitigate    | Server Actions + cookie cart untouched; classes only; unit + e2e gate                                   |
| T-11-27   | Repudiation | discount display | mitigate    | Shopify-reported allocations remain the display authority (BULK-07); no client-side discount math added |

</threat_model>

<verification>
- `pnpm test:unit`, `pnpm test:e2e`, `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind` exit 0
- Dev-server check: cart with discounted lines, empty cart, loading state at desktop/375px
</verification>

<success_criteria>

- /cart wears the drawer visual language (no drawer built); BULK-07 + cart contracts green; zero old tokens in cart files
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-11-SUMMARY.md`
</output>
