---
phase: 11-full-visual-redesign
plan: 10
type: execute
wave: 5
depends_on: ["11-08"]
files_modified:
  - src/app/(storefront)/products/[handle]/page.tsx
  - src/app/(storefront)/products/[handle]/_components/related-products.tsx
  - src/app/(storefront)/products/[handle]/_components/customers-also-bought.tsx
  - src/components/product/product-gallery
  - src/components/product/product-form
  - src/components/product/bulk-savings
  - src/components/product/product-quick-view
  - src/components/product/recommendation-product-card
  - src/components/product/searchanise-recommendations
  - src/components/product/related-products-carousel
autonomous: true
requirements: [RD-06]

must_haves:
  truths:
    - "The PDP is a two-column layout (sticky gallery left, info right) with mono breadcrumb, origin/type eyebrow, serif title, gold stars, serif price with mono unit"
    - "Variant tiles are bordered .size tiles where the selected tile is border-brand bg-brand-tint; variant-selection logic unchanged"
    - "Buy row keeps quantity validation, the existing Server Action, and the aria-live success announcement"
    - "Bulk savings renders the .tiers grid — bordered centered tiles, serif price, brand save line, best tier with floating Best value chip — only when tier data exists (BULK-03)"
    - "Quick view, recommendation cards, and related rails wear the new system with data flow untouched"
  artifacts:
    - path: "src/components/product/bulk-savings/bulk-savings.tsx"
      provides: ".tiers grid + Best value chip"
      contains: "Best value"
    - path: "src/components/product/product-form"
      provides: ".size variant tiles + redesigned buy row"
  key_links:
    - from: "src/components/product/product-form"
      to: "src/lib/cart/actions.ts"
      via: "unchanged add-to-cart Server Action (BULK-05)"
      pattern: "action"
    - from: "src/components/product/recommendation-product-card"
      to: "scripts/component-contracts/button-system.test.mjs"
      via: "motion-reduce trio assertion on its group-hover:scale line"
      pattern: "motion-reduce:group-hover:scale-100"
---

<objective>
Restyle the product detail page to the redesign — breadcrumb, sticky gallery with thumbs, info column (eyebrow/title/rating/pills/price), .size variant tiles, buy row, assurance row, bulk-savings tiers grid, spec accordions, quick view, and recommendation rails — preserving bulk savings (BULK-01..05), variant selection, add-to-cart, and Searchanise recommendation behavior.

CAUTION: `src/app/(storefront)/products/[handle]/page.tsx` has pre-existing intentionally-preserved unstaged edits (STATE.md blocker). Work from the current working-tree content and preserve those edits' intent — never checkout/revert this file.

Output: redesigned PDP per product-page.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/product-page.js
@design/extracted/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDP layout, breadcrumb, gallery, info column</name>
  <files>src/app/(storefront)/products/[handle]/page.tsx, src/components/product/product-gallery</files>
  <read_first>src/app/(storefront)/products/[handle]/page.tsx CURRENT WORKING TREE (run `git status`/`git diff` on it first to see the preserved unstaged edits; build on top of them), src/components/product/product-gallery files, design/extracted/product-page.js JSX, design/extracted/design-system.css .pdp block, 11-UI-SPEC.md §5.6</read_first>
  <action>
**page.tsx** (preserve existing unstaged layout edits — restyle around them):
- Breadcrumb `type-mono-meta text-ink-faint py-5.5`, current item `text-ink`.
- Grid `lg:grid-cols-[1.05fr_1fr] gap-[clamp(28px,4vw,64px)]`.
- Info column top: `Eyebrow` line with origin · productType · grade (globe lucide icon); h1 `font-display text-[clamp(2rem,3.4vw,2.9rem)]`; rating row gold stars + `type-mono-meta` count; tag pills (Badge); price block `font-display text-[2.2rem]` + `font-mono text-[11px] text-ink-faint` unit + optional `bg-brand-tint text-brand` mono pill for base/kg; description `text-ink-soft text-[1.02rem]`.
- Spec accordions (.spec-acc): `border-t border-hairline`, `font-display text-[1.15rem]` headers, chevron `text-brand` rotate when open; spec table rows `border-b border-hairline-2`, first col `type-mono-meta text-ink-faint w-2/5`. Keep keyboard access (AUDIT-02).
- Keep JSON-LD via serializeInlineJson, metadata, and `await params` patterns untouched.

**Gallery**: `lg:sticky lg:top-30`; main image `aspect-[1/1.05] rounded-lg overflow-hidden bg-paper-2`; thumbs row `rounded-lg border-2 border-transparent` with selected `border-brand`; keep existing selection state logic and image alt handling.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type-mono-meta" "src/app/(storefront)/products/[handle]/page.tsx"` returns >= 1
    - `grep -c "serializeInlineJson" "src/app/(storefront)/products/[handle]/page.tsx"` unchanged from before (no JSON-LD plumbing change)
    - `grep -rc "border-brand" src/components/product/product-gallery` returns >= 1
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|type-display-0" "src/app/(storefront)/products/[handle]/page.tsx" src/components/product/product-gallery` returns no files
  </acceptance_criteria>
  <done>PDP shell + gallery restyled on top of preserved edits</done>
</task>

<task type="auto">
  <name>Task 2: Variant tiles, buy row, assurance, bulk-savings tiers</name>
  <files>src/components/product/product-form, src/components/product/bulk-savings</files>
  <read_first>all files in both folders (variant selection logic, quantity validation/max-cap logic, tier data flow from quantityPriceBreaks/metafield fallback, aria-live announcement), design/extracted/product-page.js size tiles + tiers JSX, design/extracted/design-system.css .size/.tiers blocks, 11-UI-SPEC.md §5.6</read_first>
  <action>
**Variant tiles (.size)**: `border-[1.5px] border-hairline rounded-sm px-4.5 py-3 min-w-23 text-center`, selected `border-brand bg-brand-tint` with mono sub-price `text-brand`. KEEP the existing variant-selection state logic, disabled/sold-out handling, and aria semantics — classes only.

**Buy row**: restyled pill quantity stepper (from 11-03) + flex-1 `<Button variant="brand" size="lg">` add-to-cart. KEEP: quantity validation against quantity rules/inventory cap (Phase 1 contract), the cart Server Action, pending/success/error feedback, and the aria-live success announcement (AUDIT-01).

**Assurance row**: `border-y border-hairline py-5` flex items — brand lucide icons + `text-ink-soft text-[0.86rem]` text.

**Bulk savings (.tiers)**: heading `font-mono text-[11px] tracking-[0.12em] uppercase text-ink-faint`; `grid-cols-3 gap-2.5` (stack on mobile) of tiles `border border-hairline rounded-sm p-3.5 text-center` — mono qty range, `font-display text-[1.3rem]` price, `text-brand` "Save n%" line; best-value tier `border-brand relative` + floating chip `absolute -top-2 bg-brand text-paper rounded-full px-2 py-0.5 font-mono text-[9px] uppercase` "Best value"; link-arrow to /pages/wholesale below. RENDER ONLY when tier data exists (BULK-03) — keep the existing conditional + tier-selection behavior (selecting a tier still feeds the validated quantity path, BULK-04/05).
  </action>
  <verify>
    <automated>pnpm test:unit && pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "Best value" src/components/product/bulk-savings` returns >= 1
    - `grep -rc "bg-brand-tint" src/components/product/product-form` returns >= 1 (selected tile)
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|success-\|action-" src/components/product/product-form src/components/product/bulk-savings` returns no files
    - `pnpm test:unit` exits 0 (product-form/bulk-savings behavior tests green)
  </acceptance_criteria>
  <done>Variant tiles, buy row, tiers grid restyled with all BULK contracts intact</done>
</task>

<task type="auto">
  <name>Task 3: Quick view, recommendation cards, related rails</name>
  <files>src/components/product/product-quick-view, src/components/product/recommendation-product-card, src/components/product/searchanise-recommendations, src/components/product/related-products-carousel, src/app/(storefront)/products/[handle]/_components/related-products.tsx, src/app/(storefront)/products/[handle]/_components/customers-also-bought.tsx</files>
  <read_first>all listed files (quick-view dialog data flow + failure behavior, Searchanise recommendation loading/degradation, carousel wiring), 11-UI-SPEC.md §5.5 card + §5.6 related rail, design/extracted/product-card-footer.js ProductCard</read_first>
  <action>
- **Quick view**: dialog already restyled (11-03) — restyle the inner content to mirror the PDP info column (eyebrow, serif title, price, size tiles, brand add button); keep open/close, data fetch, and failure behavior (AUDIT-05).
- **Recommendation product card**: apply the `.pcard` body language (mono origin line, serif 1.2rem title, price row); PRESERVE the motion-reduce trio on its `group-hover:scale` line (contract-tested file).
- **Searchanise recommendations + related rails**: class swaps per UI-SPEC §3; rail head `type-heading-02` + link-arrow; 4-col card rail (carousel wiring untouched). Update stories in these folders alongside.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:contracts && pnpm test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "motion-reduce:group-hover:scale-100" src/components/product/recommendation-product-card/recommendation-product-card.tsx` returns >= 1
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default" src/components/product/product-quick-view src/components/product/recommendation-product-card src/components/product/searchanise-recommendations src/components/product/related-products-carousel "src/app/(storefront)/products/[handle]/_components"` returns no files
    - `pnpm test:contracts && pnpm test:unit` exit 0
  </acceptance_criteria>
  <done>Quick view + rails restyled; trio + degradation behavior intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → add-to-cart | quantity + variant flow into cart Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-23 | Tampering | buy row / quantity | mitigate | existing quantity validation + Server Action untouched (unit tests gate); restyle only |
| T-11-24 | Elevation (XSS) | JSON-LD / product HTML | mitigate | serializeInlineJson + sanitized HTML plumbing untouched (acceptance grep) |
| T-11-25 | Repudiation | preserved unstaged edits | mitigate | build on working-tree content; never revert page.tsx; diff reviewed in summary |
</threat_model>

<verification>
- `pnpm test:unit`, `pnpm test:contracts`, `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind` exit 0
- Dev-server check: PDP with bulk tiers product + simple product at desktop/375px; add-to-cart announcement; quick view
</verification>

<success_criteria>
- PDP matches product-page.js presentation; BULK/variant/announcement contracts intact; zero old tokens in owned files
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-10-SUMMARY.md`
</output>
