---
phase: 11-full-visual-redesign
plan: 21
type: execute
wave: 2
depends_on: ["11-17"]
autonomous: true
gap_closure: true
requirements: [RD-03, RD-07]
files_modified:
  - src/app/(storefront)/pages/bulk-wholesale-supply/page.tsx
  - src/app/(storefront)/pages/bulk-wholesale-supply/_components/
  - src/app/(storefront)/pages/bulk-wholesale-supply/_lib/

must_haves:
  truths:
    - "/pages/bulk-wholesale-supply renders the 10-section composition with the supplied production copy"
    - "The page is built on the Phase 11 design system (Section/Eyebrow/Button/Accordion primitives, tokens)"
    - "The page reuses the shared Contact section and has metadata + JSON-LD per sibling supporting pages"
  artifacts:
    - path: "src/app/(storefront)/pages/bulk-wholesale-supply/page.tsx"
      provides: "the route with metadata, noindex/cache plumbing, JSON-LD, and section composition"
  key_links:
    - from: "src/app/(storefront)/pages/bulk-wholesale-supply/page.tsx"
      to: "Contact section component"
      via: "renders <Contact action={submitContactFormAction} /> as the final section"
      pattern: "Contact"
---

<objective>
Close the new-page UAT gap (UAT test 15): build /pages/bulk-wholesale-supply, which never existed and currently falls through to the generic Shopify [...slug] renderer. Implement the authoritative 10-section content spec on the Phase 11 design system — copy the supplied texts/links/media verbatim, strip production classes/styles/scripts.

Purpose: The owner added this page during UAT with full production content supplied; it must be reproduced faithfully on the new design system.
Output: A new App Router route at src/app/(storefront)/pages/bulk-wholesale-supply/ with the 10 sections, metadata + JSON-LD matching sibling supporting pages, and the reused contact section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative content spec — exact copy, links, media, section order (build from THIS)
@.planning/phases/11-full-visual-redesign/bulk-wholesale-supply-content.md

# Authoritative design source for primitive styling
@design/extracted-design.html

# Sibling supporting pages — match their metadata/noindex/cache/JSON-LD plumbing and hero/eyebrow patterns
@src/app/(storefront)/pages/wholesale
@src/app/(storefront)/pages/our-story

<interfaces>
- Use Section primitives (Section.Root tones, Section.Container), Eyebrow, Button, and Accordion from src/components/ui. Reuse the shared Contact section (relocated to src/components/contact/ by plan 11-17) with action={submitContactFormAction} for section 10.
- Route-only components go in _components/, route-only constants/copy/metadata helpers in _lib/ per docs/conventions.md.
- Outbound links (wholesale-account-request, appi-compliance) reference production pages — keep as-is relative paths even though those routes are not yet built locally.
- Media assets are Shopify CDN / production URLs in the spec; use next/image with proper sizes for images; the mobile hero video is autoplay/loop/muted with a poster.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold route + metadata/JSON-LD + sections 1-5</name>
  <files>src/app/(storefront)/pages/bulk-wholesale-supply/page.tsx, src/app/(storefront)/pages/bulk-wholesale-supply/_lib/, src/app/(storefront)/pages/bulk-wholesale-supply/_components/</files>
  <action>
    Build from .planning/phases/11-full-visual-redesign/bulk-wholesale-supply-content.md, copying primitive styling from design/extracted-design.html. Match the metadata/noindex/cache/JSON-LD plumbing of a sibling supporting page (e.g. /pages/wholesale).
    - Create page.tsx with `await params` if dynamic (this is a static route, so no params), metadata export, noindex/cache handling consistent with siblings, and JSON-LD via the existing safe helper.
    - Section 1 — Wholesale account banner: full-width image band (CDN image from spec, 2000x675) with H2 "BULK WHOLESALE ACCOUNT", P "Access 1500+ Ingredients Direct from Farms and Save", CTA "Apply Now" -> /pages/appi-compliance?view=bulk-wholesale-supply.
    - Section 2 — Hero split (content left, media right): green eyebrow "Bulk Imports • Containers • Volume Discounts"; H2 "Large-Scale Tea Imports to Australia - Delivered to Your Warehouse"; the supplied paragraph; primary Button "Apply Now - Wholesale Account" -> /pages/wholesale-account-request; secondary Button with phone icon "Call 1300 729 617" -> tel:+611300729617; desktop image Section_Image.avif (1500x1130), mobile autoplay/loop/muted video with poster.
    - Section 3 — Features grid 3-col (ACO/HACCP Certified; Container Imports; Price Advantage) with the supplied descriptions.
    - Section 4 — Media-with-text (sunken band, image-ship.png left, content right): H2 "Freight & Logistics — Done For You"; supplied paragraph; 4 check items (verbatim).
    - Section 5 — Features grid 2-col (check-icon cards): H2 "What You Can Import With Us" + supplied paragraph; 4 cards (Tea Varieties; Botanicals & Herbs; Organic Options; Custom Solutions) verbatim.
    - Extract repeated card markup into route-only _components only where it creates a real reuse boundary; keep copy in _lib. One component per file. cn() for classNames; tokens only.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts</automated>
    <human-check>/pages/bulk-wholesale-supply renders sections 1-5 with verbatim copy, brand hero/eyebrow styling, working CTAs, and responsive hero media (image desktop, video mobile).</human-check>
  </verify>
  <done>Route exists with metadata/JSON-LD/noindex per siblings; sections 1-5 render verbatim copy on the design system; lint + typecheck + contracts pass.</done>
</task>

<task type="auto">
  <name>Task 2: Sections 6-10 (accordion, process, FAQ, CTA, contact)</name>
  <files>src/app/(storefront)/pages/bulk-wholesale-supply/page.tsx, src/app/(storefront)/pages/bulk-wholesale-supply/_components/, src/app/(storefront)/pages/bulk-wholesale-supply/_lib/</files>
  <action>
    Continue from the content spec:
    - Section 6 — Media with numbered accordion (image ship.avif left, Accordion right): H2 "Why Choose Teavision for Your Imported Tea Needs?"; 5 numbered accordion items (Global Reach…; Superior Product Quality…; Wide Selection…; Cost-Effective…; Expertise…) with verbatim bodies. Use the Accordion primitive.
    - Section 7 — Process steps "How It Works": 5 numbered cards (Get in Touch; Confirm Shipment Options; Approve Scheduling & Pricing; Product Arrival & Inspection; Order Fulfillment) verbatim. On mobile the production uses a horizontal swiper — follow the design's responsive pattern but do NOT introduce a horizontal scrollbar the owner dislikes (prefer wrap/stack on mobile, consistent with the 11-15 mobile-chip decision).
    - Section 8 — FAQ (Accordion): H2 "Frequently asked questions" + supplied paragraph; 6 Q/A items verbatim.
    - Section 9 — CTA banner (centered light band, green heading): H2 "Ready to Get Started?" + supplied paragraph; primary Button "Apply Now - Wholesale Account" -> /pages/wholesale-account-request; secondary phone Button "Call 1300 729 617" -> tel:+611300729617.
    - Section 10 — Need-help contact: reuse the shared Contact section (from src/components/contact/) with action={submitContactFormAction}, heading "Need help? Speak with our Ingredients Experts Today."
    - The Searchanise recommendations slot before contact is OPTIONAL — only add it if it cleanly follows existing site patterns; otherwise skip per the spec note.
    - cn() for classNames; tokens only; one component per file.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm build</automated>
    <human-check>Sections 6-10 render verbatim copy with working accordions, process steps (no horizontal scrollbar on mobile), CTA buttons, and the reused contact form; page builds.</human-check>
  </verify>
  <done>All 10 sections render with verbatim copy on the design system; the page builds; contract + lint + typecheck pass.</done>
</task>

</tasks>

<verification>
- pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm build pass
- Manual: full /pages/bulk-wholesale-supply against the content spec; metadata/JSON-LD consistent with siblings.
</verification>

<success_criteria>
UAT test 15 is closed: /pages/bulk-wholesale-supply exists with the supplied 10-section production content, built on the Phase 11 design system, reusing the contact section, with sibling-consistent metadata.
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-21-SUMMARY.md` when done.
</output>
