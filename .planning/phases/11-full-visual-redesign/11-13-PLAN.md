---
phase: 11-full-visual-redesign
plan: 13
type: execute
wave: 4
depends_on: ['11-02', '11-03', '11-12']
files_modified:
  - src/app/(storefront)/pages/wholesale/_components
  - src/app/(storefront)/pages/contact/_components
  - src/app/(storefront)/pages/our-story/_components
  - src/app/(storefront)/pages/certifications/_components
  - src/app/(storefront)/pages/custom-tea-blends/_components
  - src/app/(storefront)/pages/[...slug]/_components
  - src/components/contact/contact-form
autonomous: true
requirements: [RD-07]

must_haves:
  truths:
    - 'Wholesale, contact, our-story, certifications, custom-tea-blends, and generic [...slug] pages all wear the new system — brand hero bands, card-surface forms, .split layouts, pill badges'
    - 'Wholesale and contact forms keep their Server Actions, validation, honeypot, and rate limiting'
    - 'Generic Shopify pages render their body through the restyled html-content mapper at prose width'
  artifacts:
    - path: 'src/app/(storefront)/pages/wholesale/_components'
      provides: 'redesigned wholesale page (benefits split + card application form)'
    - path: 'src/components/contact/contact-form'
      provides: '.field-styled shared contact form'
  key_links:
    - from: 'src/components/contact/contact-form'
      to: 'src/lib/contact/actions.ts'
      via: 'unchanged contact Server Action'
      pattern: 'action'
    - from: 'src/app/(storefront)/pages/[...slug]/_components'
      to: 'src/lib/shopify/html-content.ts'
      via: 'page body via restyled class map (11-12)'
      pattern: 'html-content|htmlContent'
---

<objective>
Restyle every supporting/static page surface — wholesale, contact, our-story, certifications, custom-tea-blends, and the generic `[...slug]` Shopify-page route — plus the shared contact-form component, completing the per-surface portion of RD-07.

Output: supporting pages per supporting-pages.js + UI-SPEC §5.10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/supporting-pages.js
@design/extracted/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wholesale + contact pages + shared contact form</name>
  <files>src/app/(storefront)/pages/wholesale/_components, src/app/(storefront)/pages/contact/_components, src/components/contact/contact-form</files>
  <read_first>all files in the three locations (form action/validation/honeypot/rate-limit wiring — must not change; wholesale has 60 old-class occurrences across 4 files), design/extracted/supporting-pages.js WholesalePage JSX, 11-UI-SPEC.md §5.10 + §5.3 HelpSection</read_first>
  <action>
**Wholesale**: page hero per `.coll__hero` pattern (`Section.Root tone="brand"`, gold Eyebrow, `type-display`-scale title max-w-[16ch], `text-paper/85` copy ≤54ch); body two-column — left: benefits list rows `border-t border-hairline py-4 flex gap-3` with brand lucide icons + cert pills (`Badge variant="organic"/"gold"`); right: application form in `bg-card border border-hairline-2 rounded-lg p-7` using restyled field primitives, submit `<Button variant="brand" size="lg">`. KEEP form action, validation, honeypot, rate limiting (never log form values — rate-limit rule).

**Contact page**: `.help` pattern — `Section.Root tone="inverse"`; left contact rows (44px `bg-brand-tint text-brand rounded-full` icon circles, `type-mono-meta` labels, `font-display text-paper` values with real tel/mailto); right card form.

**Shared contact-form component**: `.field` treatment via the restyled primitives; `type-mono-meta` labels; submit `<Button variant="brand">`; keep action/honeypot/aria feedback; update its story.
</action>
<verify>
<automated>pnpm test:integration && pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|action-\|brandStrong\|type-display-0" "src/app/(storefront)/pages/wholesale/_components" "src/app/(storefront)/pages/contact/_components" src/components/contact/contact-form` returns no files - contact + wholesale forms keep their Server Action imports (no diff to action wiring per git diff review) - `pnpm test:integration` exits 0 (form boundaries + rate limiting intact)
</acceptance_criteria>
<done>Wholesale + contact redesigned; form protections untouched</done>
</task>

<task type="auto">
  <name>Task 2: Our-story + certifications pages</name>
  <files>src/app/(storefront)/pages/our-story/_components, src/app/(storefront)/pages/certifications/_components</files>
  <read_first>all files in both folders (our-story: 9 files/67 occurrences; certifications: 5 files/29), design/extracted/supporting-pages.js SimplePage/story sections, 11-UI-SPEC.md §5.10</read_first>
  <action>
**Our-story**: hero per `.coll__hero`; `.split` two-column sections with values Eyebrow ("Mindfulness · Sincerity · Wholesomeness" if that copy exists in current content — keep real copy), cert pills via Badge, `<Button variant="brand">` CTA; prose blocks `text-ink-soft`; serif `type-heading-02` section titles with Eyebrow leads.

**Certifications**: hero band; certification entries as card-surface cards (`bg-card border border-hairline-2 rounded-lg`) with gold/organic Badge pills and `type-mono-meta` meta lines.
Mechanical class swaps per UI-SPEC §3 everywhere else. Keep all real copy, images, links.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|brandStrong\|type-display-0" "src/app/(storefront)/pages/our-story/_components" "src/app/(storefront)/pages/certifications/_components"` returns no files - `pnpm lint:tailwind` exits 0
</acceptance_criteria>
<done>Our-story + certifications on new tokens with real content preserved</done>
</task>

<task type="auto">
  <name>Task 3: Custom-tea-blends + generic [...slug] pages</name>
  <files>src/app/(storefront)/pages/custom-tea-blends/_components, src/app/(storefront)/pages/[...slug]/_components</files>
  <read_first>all files in both folders (custom-tea-blends is the largest single consumer: 12 files/117 occurrences; [...slug] renders Shopify page HTML via html-content), 11-UI-SPEC.md §5.10 + §3 rename table</read_first>
  <action>
**Custom-tea-blends**: mechanical §3 class sweep across all 12 component files, plus structural alignment where sections map to mockup patterns (hero → `.coll__hero`; process/steps → `.svc__card` numbering language; CTA bands → Section tone `brand`/`inverse` with Eyebrow + serif headline). Keep all copy/forms/links; any form keeps its action wiring.

**[...slug] generic pages**: hero per `.coll__hero` page-hero pattern; body `Section.Container variant="compact"` (prose width) rendered through the restyled html-content mapper (11-12); class swaps per §3. Keep `'use cache'`/cacheTag invalidation and noindex metadata untouched.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm build</automated>
</verify>
<acceptance_criteria> - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|action-\|brandStrong\|type-display-0" "src/app/(storefront)/pages/custom-tea-blends/_components" "src/app/(storefront)/pages/[...slug]/_components"` returns no files - `pnpm build` exits 0
</acceptance_criteria>
<done>All remaining static surfaces on new tokens; caching/noindex untouched</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                        |
| ------------------------------ | ---------------------------------- |
| user → wholesale/contact forms | untrusted input via restyled forms |
| Shopify CMS → [...slug] HTML   | third-party-authored page bodies   |

## STRIDE Threat Register

| Threat ID | Category        | Component                 | Disposition | Mitigation Plan                                                                                            |
| --------- | --------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-11-30   | Tampering/DoS   | wholesale + contact forms | mitigate    | Server Actions, validation, honeypot, rate limits untouched; integration tests gate; no form-value logging |
| T-11-31   | Elevation (XSS) | [...slug] page bodies     | mitigate    | rendering stays behind the sanitized html-content pipeline (restyled in 11-12, plumbing unchanged)         |

</threat_model>

<verification>
- `pnpm test:integration`, `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind`, `pnpm build` exit 0
- Dev-server check: /pages/wholesale, /pages/contact, /pages/our-story, one generic Shopify page at desktop/375px
</verification>

<success_criteria>

- Every supporting page on the new system; form protections + caching + noindex intact; zero old tokens in owned files
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-13-SUMMARY.md`
</output>
