---
phase: 11-full-visual-redesign
plan: 17
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
requirements: [RD-03, RD-06]
files_modified:
  - src/app/(storefront)/collections/[handle]/_components/hero.tsx
  - src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts
  - src/app/(storefront)/collections/[handle]/_components/page-content.tsx
  - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
  - src/components/collection/product-card/product-card.tsx
  - src/components/collection/product-card/product-card.stories.tsx
  - src/app/(storefront)/collections/page.tsx
  - src/components/homepage/contact/contact.tsx
  - src/components/homepage/contact/index.ts
  - src/components/homepage/contact/contact.stories.tsx
  - src/components/homepage/contact-form/contact-form.tsx
  - src/components/homepage/contact-form/index.ts
  - src/components/homepage/contact-form/contact-form.stories.tsx
  - src/components/contact/index.ts
  - src/components/search/search-results-view/search-results-view.tsx
  - src/components/search/search-results-view/product-results.tsx

must_haves:
  truths:
    - 'Collection pages with a banner image show the full-opacity art banner + visible title + read-more disclosure; others keep the green band'
    - 'Product cards render the gold star rating when rating data is present'
    - 'Collections index shows only the card grid (All first, linking to /collections/all) in production order, plus the contact section'
    - 'Search empty/no-results shows only the hero and a No-matches card'
  artifacts:
    - path: 'src/app/(storefront)/collections/[handle]/_components/hero.tsx'
      provides: 'conditional banner-art vs green-band hero'
    - path: 'src/components/collection/product-card/product-card.tsx'
      provides: 'StarRating rendered between title and price'
    - path: 'src/app/(storefront)/collections/page.tsx'
      provides: 'single card grid + contact section, directory removed'
  key_links:
    - from: 'src/app/(storefront)/collections/[handle]/_components/hero.tsx'
      to: 'getDescriptionHeroImage()'
      via: 'branch on banner detection'
      pattern: 'getDescriptionHeroImage'
    - from: 'src/app/(storefront)/collections/page.tsx'
      to: 'Contact section component'
      via: 'renders <Contact action={submitContactFormAction} />'
      pattern: 'Contact'
---

<objective>
Close three collection/search UAT gaps (UAT tests 9, 10, 11): PLP hero banner logic + product-card star rating + grid spacing; collections index card-grid-only + contact section + correct ordering; and the search empty-state simplification. Copy exact values from design/extracted-design.html and preserve the original site's collection-banner logic (reference ../teavision-theme).

Purpose: Collection surfaces drive catalogue discovery; the PLP hero lost the original banner logic, cards are missing the star rating, the collections index renders the wrong structure, and the search empty state shows unwanted chrome.
Output: PLP hero conditional banner + star ratings + mobile grid spacing; collections index as a single grid with All-first + reusable contact section; search empty state reduced to hero + No-matches card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative design source — copy exact values, do not approximate (owner directive)

@design/extracted-design.html

# Original collection-banner logic reference (Liquid theme)

# ../teavision-theme — inspect the collection template's full-width descriptionHtml banner + "Read More About {collection}" toggle

# Diagnosed evidence

@.planning/debug/plp-parity.md
@.planning/debug/collections-index.md

<interfaces>
- getDescriptionHeroImage() already exists in the [handle] _lib helpers and detects an <img> embedded in collection.descriptionHtml — the banner-mode signal exists but is not branched on.
- HERO_IMAGE_OVERRIDES / HIDDEN_HERO_INTRO_HANDLES are hardcoded in _lib/page-helpers.ts (~lines 53-75) — these handle-specific hacks must be DELETED in favor of the general signal.
- StarRating lives at src/components/ui/star-rating/ with a drop-in precedent in src/components/product/recommendation-product-card/. CollectionProductSummary already carries rating/reviewCount (collection.graphql metafields flow end-to-end).
- The reusable Contact section currently lives at src/components/homepage/contact/ (its only prop is `action`). Contact imports its form leaf HomepageContactForm via the relative path `../contact-form` (src/components/homepage/contact-form/). A contact DOMAIN already exists at src/components/contact/ (the form leaf's own domain home). submitContactFormAction is the Server Action.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PLP hero banner logic, product-card star rating, mobile grid spacing</name>
  <files>src/app/(storefront)/collections/[handle]/_components/hero.tsx, src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts, src/app/(storefront)/collections/[handle]/_components/page-content.tsx, src/app/(storefront)/collections/[handle]/_components/product-list.tsx, src/components/collection/product-card/product-card.tsx, src/components/collection/product-card/product-card.stories.tsx</files>
  <action>
    Per .planning/debug/plp-parity.md and the original Liquid theme at ../teavision-theme:
    - HERO: branch on `getDescriptionHeroImage() !== null`. When a banner is detected (e.g. "Tea Masters Selection"), render the full-opacity art banner with a VISIBLE title and the "Read More About {collection}" StoryDisclosure BELOW the banner — mirroring the original theme's full-width descriptionHtml banner + read-more toggle. When no banner, keep the green 35%-opacity band as the regular hero. DELETE the hardcoded HERO_IMAGE_OVERRIDES and HIDDEN_HERO_INTRO_HANDLES hacks (page-helpers.ts ~53-75) and the demote-to-background special-casing. In page-content.tsx, move StoryDisclosure out of the green band to below the banner for banner collections.
    - STAR RATING: in product-card.tsx, render `<StarRating rating={...} count={reviewCount} size="sm" />` between the title and price when rating is defined (data already flows via CollectionProductSummary.rating/reviewCount). Follow the recommendation-product-card precedent. Add a stories case covering the rating state in product-card.stories.tsx. Compare against the `.pcard` spec in design/extracted-design.html and add any other clearly-missing elements; for ambiguous deltas (origin segment, unit, grade, fav) leave a note in the SUMMARY for owner triage rather than inventing.
    - GRID: in product-list.tsx add the mobile gap tightening (gap 16px / 12px at <=620px); desktop gaps already match the design — do not change them.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories</automated>
    <human-check>Visit a banner collection (e.g. /collections/tea-masters-selection) — full art banner + visible title + read-more below. Visit a non-banner collection — green band hero. Product cards show gold stars when rated. Mobile grid gaps are tighter at <=620px.</human-check>
  </verify>
  <done>Hero branches correctly on banner detection (hacks deleted); star rating renders on rated cards with story coverage; mobile grid spacing tightened; contract + stories + lint + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Collections index — card-grid-only with All-first + reusable contact section (move Contact + its form leaf to the contact domain)</name>
  <files>src/app/(storefront)/collections/page.tsx, src/components/homepage/contact/contact.tsx, src/components/homepage/contact/index.ts, src/components/homepage/contact/contact.stories.tsx, src/components/homepage/contact-form/contact-form.tsx, src/components/homepage/contact-form/index.ts, src/components/homepage/contact-form/contact-form.stories.tsx, src/components/contact/index.ts</files>
  <action>
    Per .planning/debug/collections-index.md and the production page at https://www.teavision.com.au/collections, matching the design's collections-index treatment:
    - GRID: replace the current "8 featured menu tiles + text directory" with a SINGLE full card grid sourced from getCollectionSummaries() filtered to exclude only 'frontpage', sorted by title so "All" lands first and links to /collections/all. Delete the featured/menu-cap helpers and the Directory section (page.tsx ~262-300). Rebuild the JSON-LD to reflect the full grid (use the existing safe JSON-LD helper).
    - CONTACT SECTION (move BOTH directories together): Contact lives in src/components/homepage/contact/ and imports its form leaf HomepageContactForm from `../contact-form` (src/components/homepage/contact-form/). Because that import is relative-sibling, the Contact directory CANNOT move alone — physically move BOTH src/components/homepage/contact/ AND src/components/homepage/contact-form/ into src/components/contact/ (so they remain siblings and the `./contact-form` relative import still resolves). Concretely: relocate contact.tsx + index.ts + contact.stories.tsx and contact-form.tsx + index.ts + contact-form.stories.tsx under src/components/contact/<name>/; update both story `title` fields to the new domain path (e.g. 'contact/Contact' and 'contact/ContactForm' instead of 'homepage/...'); update src/components/contact/index.ts to export Contact (and the form leaf if it was barrel-exported); and update the homepage importer that consumes Contact to import from src/components/contact. Keep one component per file. Then mount `<Contact action={submitContactFormAction} />` as the final section of /collections with the heading "Need help? Speak with our Ingredients Experts Today."
    - Verify /collections/all still resolves and grid order matches production.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories && pnpm build</automated>
    <human-check>/collections shows only the card grid with "All" first (links to /collections/all), order matching production, no Directory section, and the contact section at the bottom. Homepage still renders its contact section unchanged.</human-check>
  </verify>
  <done>Collections index is a single grid with All-first in production order, Directory removed, JSON-LD rebuilt, and the reusable contact section mounted; Contact + its form leaf moved together to src/components/contact/ with stories retitled and barrels/importers updated; homepage still renders; build + lint + typecheck + stories pass.</done>
</task>

<task type="auto">
  <name>Task 3: Search empty/no-results state — hero + No-matches card only</name>
  <files>src/components/search/search-results-view/search-results-view.tsx, src/components/search/search-results-view/product-results.tsx</files>
  <action>
    Per UAT test 11: when the result count is 0 (and no active-filter context requires the chrome), render only SearchHero + the No-matches card. Currently search-results-view.tsx renders the full toolbar/filters/sort chrome around the empty-state card and product-results.tsx nests the card inside the full layout. Short-circuit the empty path to suppress the toolbar/filter/sort chrome and show just the hero and the No-matches card. Do not change the Searchanise fetch, URL-param helpers, or the populated-results layout.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts</automated>
    <human-check>Visit /search?q=zzzznotamatch — only the hero and a "No matches" card render; no filter/sort/toolbar chrome.</human-check>
  </verify>
  <done>Empty/no-results search shows only the hero and No-matches card; populated results unchanged; contract + lint + typecheck pass.</done>
</task>

</tasks>

<verification>
- pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories && pnpm build pass
- Manual: PLP banner vs green-band heroes; star ratings; collections-index grid + contact; search empty state.
</verification>

<success_criteria>
UAT tests 9, 10, 11 are closed: PLP hero preserves original banner logic with star-rated cards, collections index is a single ordered grid with contact, and search empty state is reduced to hero + No-matches card.
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-17-SUMMARY.md` when done.
</output>
