---
phase: 11-full-visual-redesign
plan: 14
type: execute
wave: 6
depends_on:
  [
    '11-01',
    '11-02',
    '11-03',
    '11-04',
    '11-05',
    '11-06',
    '11-07',
    '11-08',
    '11-09',
    '11-10',
    '11-11',
    '11-12',
    '11-13',
  ]
files_modified:
  - src/app/globals.css
  - src/components/ui/section/section.tsx
  - scripts/eslint-rules/no-section-root-tone-class.mjs
  - docs/conventions.md
  - AGENTS.md
autonomous: true
requirements: [RD-02, RD-08]

must_haves:
  truths:
    - 'grep for tv-/steep-/stone- across src/ and .storybook/ returns zero design-token matches — the old system is deleted, not aliased (RD-02)'
    - 'No orphaned old utility classes (bg-canvas, text-default, action-*, success-bg, footer-*, type-display-01/02, brandStrong) remain anywhere in src/'
    - 'No raw hex/rgb/oklch/hsl arbitrary values appear in any className in src/ (RD-08)'
    - 'pnpm lint, typecheck, test:contracts, test:unit, test:integration, build, and build-storybook all pass (RD-08)'
    - 'docs/conventions.md and AGENTS.md token examples reference the new system'
  artifacts:
    - path: 'src/app/globals.css'
      provides: 'new-system-only @theme (old tokens deleted, --color-*: initial wipe)'
      contains: '--color-*: initial'
    - path: 'docs/conventions.md'
      provides: 'updated token-class examples'
  key_links:
    - from: 'src/app/globals.css'
      to: 'scripts/check-tailwind-classes.mjs'
      via: 'guard recompiles the theme — any straggler old class fails lint repo-wide'
      pattern: 'lint:tailwind'
---

<objective>
Final sweep: migrate any straggler old-token usages, retire the Section `brandStrong` variant, DELETE the entire old design system from `globals.css` (all `--tv-*` vars, steep/stone/ink-scale/amber palettes, legacy footer tokens, stale `@utility` definitions including `type-display-01/02`), add the Tailwind default-palette wipe, update the `no-section-root-tone-class` rule regex, refresh docs, and run the full phase verification gate.

Purpose: RD-02 demands complete removal with no aliasing; the repo's own guard tooling machine-enforces it once old tokens leave globals.css.
Output: single-system codebase with all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@src/app/globals.css
@scripts/check-tailwind-classes.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Straggler migration + retire Section brandStrong</name>
  <files>src/components/ui/section/section.tsx, (any straggler files found by census)</files>
  <read_first>11-UI-SPEC.md §3 rename table, src/components/ui/section/section.tsx (temporary brandStrong alias from 11-02)</read_first>
  <action>
Run a full census BEFORE deleting anything (Pitfall 1 — deletion before migration breaks lint repo-wide):

```
rg -n "bg-canvas|bg-surface|surface-sunken|surface-raised|text-default|text-strong|text-muted|text-subtle|text-on-brand|text-link|text-accent|border-default|border-subtle|border-strong|bg-inverse|bg-strong|footer-|action-|success-bg|success-text|success-border|danger-bg|danger-text|danger-border|brand-strong|brand-subtle|bg-accent|type-display-01|type-display-02|brandStrong" src .storybook --glob '!src/app/globals.css'
```

Migrate every hit per the UI-SPEC §3 rename table (these are stragglers the surface plans missed — expect few; ~40 small story files with 1–4 occurrences each migrate with the same table if their owning plan missed them). For `tone="brandStrong"` call sites → `tone="brand"`. Then REMOVE the `brandStrong` variant from section.tsx (the temporary alias from 11-02) and any brandStrong story. Re-run the census until it returns zero hits outside globals.css.
</action>
<verify>
<automated>bash -c "! rg -n 'bg-canvas|text-default|text-on-brand|surface-sunken|action-primary|success-bg|type-display-01|brandStrong' src .storybook --glob '!src/app/globals.css' | grep -q ." && pnpm lint:tailwind && pnpm test:contracts</automated>
</verify>
<acceptance_criteria> - the census command returns zero matches outside src/app/globals.css - `grep -c "brandStrong" src/components/ui/section/section.tsx` returns 0 - `pnpm lint:tailwind && pnpm test:contracts` exit 0 (old tokens still defined, so any missed straggler would still pass — the census grep is the real gate here)
</acceptance_criteria>
<done>Zero old-class consumers remain anywhere; brandStrong variant gone</done>
</task>

<task type="auto">
  <name>Task 2: Delete the old system from globals.css + palette wipe + guard-rule update</name>
  <files>src/app/globals.css, scripts/eslint-rules/no-section-root-tone-class.mjs</files>
  <read_first>src/app/globals.css (FULL — identify every old block: --tv-* vars, steep/stone/ink-scale/amber scales, legacy footer tokens, old @utility defs), scripts/eslint-rules/no-section-root-tone-class.mjs (the text-on-brand regex limb), scripts/check-tailwind-classes.mjs, 11-RESEARCH.md §Token Mapping (--color-*: initial note, assumption A2)</read_first>
  <action>
1. DELETE from globals.css: every `--tv-*` variable, the entire `steep-*`/`stone-*`/ink-scale/amber palette scales, legacy footer color tokens, `type-display-01`/`type-display-02` utilities, and any `@utility`/`@layer` rules that only served the old system. NO aliasing — old names must not exist pointing at new values (CONTEXT locked decision).
2. ADD `--color-*: initial;` as the FIRST line of the `@theme` color block to wipe Tailwind's default palette, so only Teavision tokens generate color utilities (machine-enforces RD-02). If `pnpm lint:tailwind` or the build rejects this syntax (RESEARCH assumption A2), remove the wipe line and note it in the summary — the grep gates still enforce removal.
3. Keep: the global `prefers-reduced-motion` block, `@utility searchanise-recommendations`, the marquee/float keyframes added in 11-06/11-07, and all new tokens/utilities.
4. Update `scripts/eslint-rules/no-section-root-tone-class.mjs`: the regex limb matching `text-on-brand` is dead after the rename — update it to `text-paper` (per RESEARCH Open Q3) so the rule still catches tone-class misuse on Section.Root.
  </action>
  <verify>
    <automated>bash -c "! rg -n 'tv-|steep-|stone-' src/app/globals.css | grep -q ." && pnpm lint:tailwind && pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tv-|steep-|stone-" src .storybook` returns zero design-token matches (incidental word hits like "milestone" do not count; expect literally zero in class/var positions)
    - `grep -c "color-\*: initial" src/app/globals.css` returns 1 (or summary documents the A2 fallback)
    - `grep -c "text-on-brand" scripts/eslint-rules/no-section-root-tone-class.mjs` returns 0
    - `pnpm lint:tailwind`, `pnpm lint`, `pnpm typecheck`, `pnpm test:contracts`, `pnpm test:unit` all exit 0
  </acceptance_criteria>
  <done>Old system deleted; default palette wiped; guard rule current; all gates green</done>
</task>

<task type="auto">
  <name>Task 3: Docs refresh + full phase verification gate</name>
  <files>docs/conventions.md, AGENTS.md</files>
  <read_first>docs/conventions.md styling-rules section (old token examples: bg-canvas, text-default, text-brand, border-default), AGENTS.md design-token bullet, 11-RESEARCH.md §Validation Architecture (phase-gate command list)</read_first>
  <action>
1. Update `docs/conventions.md` styling examples to the new vocabulary: `bg-paper`, `bg-card`, `text-ink`, `text-ink-soft`, `text-brand`, `border-hairline`, `ring-ring`; note the Eyebrow component, `px-gutter`/`py-section` rhythm utilities, and `max-w-base`/`max-w-wide` containers. Update the AGENTS.md token bullet (`bg-background`, `text-primary` examples → real new tokens). Keep the warm/botanical palette rule.
2. Run the full phase gate and record results in the summary:
   - `rg -n "tv-|steep-|stone-" src .storybook scripts/component-contracts` → zero token matches (RD-02)
   - `rg -n "-\[(#|rgb|oklch|hsl)" src` → zero matches (RD-08 raw-value gate; lint cannot catch these — Pitfall 5)
   - `pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:unit && pnpm test:integration && pnpm build && pnpm build-storybook && pnpm test:stories && pnpm test:e2e`
3. Note in the summary that a manual visual pass per surface (desktop + 375px vs extracted-design.html) is required at phase verification (`/gsd-verify-work`) — no visual-regression tooling is configured.
  </action>
  <verify>
    <automated>bash -c "! rg -n '\-\[(#|rgb|oklch|hsl)' src | grep -q ." && pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:unit && pnpm test:integration && pnpm build && pnpm build-storybook</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-canvas\|text-default" docs/conventions.md` returns 0; `grep -c "bg-paper\|text-ink" docs/conventions.md` returns >= 1
    - `rg -n "-\[(#|rgb|oklch|hsl)" src` returns zero matches
    - full suite (lint, typecheck, contracts, unit, integration, build, build-storybook) exits 0
  </acceptance_criteria>
  <done>Docs current; every RD-02/RD-08 machine gate green; phase ready for verification</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                      |
| -------- | ------------------------------------------------ |
| none new | token deletion + docs; no runtime surface change |

## STRIDE Threat Register

| Threat ID | Category          | Component              | Disposition | Mitigation Plan                                                                            |
| --------- | ----------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-11-32   | DoS (build break) | old-token deletion     | mitigate    | census-before-delete ordering (Task 1 before Task 2); lint:tailwind gate after each commit |
| T-11-33   | Tampering         | eslint guard-rule edit | mitigate    | rule updated (not removed); test:contracts + lint prove guards still fire                  |

</threat_model>

<verification>
- All RD-02 and RD-08 grep gates return zero matches
- `pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:unit && pnpm test:integration && pnpm build && pnpm build-storybook && pnpm test:stories && pnpm test:e2e` all exit 0
- Manual visual pass deferred to phase verification (no visual-regression tooling)
</verification>

<success_criteria>

- Old design system completely removed (RD-02); all quality gates pass with no raw color values in classNames (RD-08); docs reflect the new system
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-14-SUMMARY.md`
</output>
