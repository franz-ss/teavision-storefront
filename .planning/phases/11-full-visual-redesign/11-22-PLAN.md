---
phase: 11-full-visual-redesign
plan: 22
type: execute
wave: 1
depends_on: []
autonomous: false
gap_closure: true
requirements: [RD-04]
files_modified:
  - src/components/layout/header/search-overlay.tsx
  - src/components/layout/header/search-form.tsx
  - src/app/not-found.tsx

must_haves:
  truths:
    - "The search overlay design is previewed and owner-approved before re-implementation"
    - "The re-implemented search overlay has a comfortably sized input and a clean soft focus treatment"
    - "The 404 page is a tasteful on-brand concept, previewed and owner-approved before implementation"
  artifacts:
    - path: "src/components/layout/header/search-overlay.tsx"
      provides: "calmer overlay matching the approved mockup"
    - path: "src/app/not-found.tsx"
      provides: "approved on-brand 404 treatment"
  key_links: []
---

<objective>
Close the two preview-first design-direction UAT gaps (UAT test 4 search overlay, UAT test 16 404 page). Both were rejected as design-direction issues, NOT code defects, and have no counterpart in design/extracted-design.html. Per the owner directive, do NOT blindly reimplement — produce a previewable mockup, get explicit owner approval, THEN implement.

Purpose: The search overlay was designed ad hoc (oversized input, invented focus border) and the 404 is plain; the owner wants to preview a direction before code is written.
Output: Owner-approved search-overlay and 404 concepts, then their implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design system reference for tokens/field treatments (the overlay has NO counterpart in the mockup)
@design/extracted-design.html

# Current implementations to revise
@src/components/layout/header/search-overlay.tsx
@src/components/layout/header/search-form.tsx
@src/app/not-found.tsx

<interfaces>
- search-overlay.tsx input is clamp(1.4rem,3vw,2.2rem) serif (reads oversized); the border-b-2 border-ink focus rule was an invention. search-form.tsx inputClassName carries the oversized serif/borderless treatment. The calmer target: smaller input, soft focus ring consistent with the shared `.field` treatment (see Field primitive / design field tokens).
- not-found.tsx is a plain centered 404 (green serif numeral + heading + two buttons). Brand motif assets (BrushCircle, Stamp, tea illustrations) are available for a tasteful on-brand treatment; keep helpful links to collections/search.
- Mockup workflow: produce a previewable artifact (e.g. a Storybook story, a static preview route, or /gsd-sketch output) the owner can view at a URL — NOT a screenshot description.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce previewable mockups (search overlay + 404)</name>
  <files>src/components/layout/header/search-overlay.tsx, src/app/not-found.tsx</files>
  <action>
    Produce previewable mockups WITHOUT committing to the production components yet:
    - SEARCH OVERLAY: create 1-2 calmer overlay concepts — smaller input (drop the clamp(1.4rem,3vw,2.2rem) serif scale to a comfortable size), a soft focus RING consistent with the shared .field treatment (not the invented border-b-2 border-ink), and a tidier "Popular" + suggestion-pill layout. Expose each concept as a previewable artifact the owner can open at a URL (a Storybook story for the overlay is the lightest-weight option; use a temporary preview route only if Storybook cannot host the full-width overlay interaction).
    - 404: create 1-2 tasteful on-brand 404 concepts using existing brand motifs (e.g. BrushCircle/Stamp/tea illustration) plus helpful links to collections and search. Be creative but do NOT overdesign (owner directive). Expose as a previewable artifact (Storybook story or temporary preview route).
    - Use tokens only, cn() for classNames, Spectral/Hanken/Space Mono per the type system. Do not delete the current production behavior yet — keep the live overlay/404 working until approval.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:stories</automated>
    <human-check>The owner can open each concept at a URL (Storybook story or preview route) and evaluate input sizing, focus treatment, and overall design for the overlay, and the creativity/restraint of the 404.</human-check>
  </verify>
  <done>1-2 previewable concepts each for the search overlay and 404 exist at openable URLs; lint + typecheck + stories pass; production behavior still intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Owner approves search-overlay and 404 concepts</name>
  <action>
    Present the Task 1 previewable concepts to the owner and pause for an explicit direction decision before any production implementation. Do not proceed to Task 2 until the owner approves a direction for each.
  </action>
  <what-built>1-2 previewable mockup concepts each for the redesigned search overlay (calmer input + soft focus ring) and the on-brand 404 page, viewable at URLs (Storybook stories / preview route).</what-built>
  <how-to-verify>
    1. Open the search-overlay concept(s) at the provided URL(s); confirm the input size is comfortable, the focus treatment is a clean soft ring, and the overall layout is acceptable.
    2. Open the 404 concept(s) at the provided URL(s); confirm it is creative but not overdesigned, on-brand, with helpful links.
    3. Pick one direction for each (or request changes).
  </how-to-verify>
  <resume-signal>Approve a direction for each ("overlay: concept A, 404: concept B"), or describe changes to iterate on.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement the approved search overlay + 404</name>
  <files>src/components/layout/header/search-overlay.tsx, src/components/layout/header/search-form.tsx, src/app/not-found.tsx</files>
  <action>
    Implement the owner-approved concepts from the checkpoint:
    - SEARCH OVERLAY: apply the approved overlay design to search-overlay.tsx and update search-form.tsx inputClassName to the approved comfortable size + soft focus ring (replacing the oversized serif + border-b-2 border-ink). Keep Escape-to-close, auto-focus, Searchanise autocomplete, and submit-to-/search behavior unchanged.
    - 404: apply the approved 404 concept to not-found.tsx, keeping the brand/secondary action buttons and helpful links to collections/search.
    - Remove any temporary preview route created in Task 1 (keep Storybook stories if they are useful documentation). Tokens only; cn() for classNames.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories && pnpm build</automated>
    <human-check>The live search overlay matches the approved concept (comfortable input, soft focus ring, Escape/auto-focus/autocomplete/submit all work); navigating to a non-existent URL shows the approved 404.</human-check>
  </verify>
  <done>The approved overlay and 404 are implemented in the production components with behavior preserved; temporary preview routes removed; contract + stories + build + lint + typecheck pass.</done>
</task>

</tasks>

<verification>
- pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories && pnpm build pass
- Manual: search overlay and 404 match the owner-approved concepts; search behavior intact.
</verification>

<success_criteria>
UAT tests 4 and 16 are closed via the preview-first workflow: owner-approved concepts implemented for the search overlay and 404 page, with no blind reimplementation.
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-22-SUMMARY.md` when done.
</output>
