---
phase: 11-full-visual-redesign
plan: 09
type: execute
wave: 5
depends_on: ["11-08"]
files_modified:
  - src/app/(storefront)/search/page.tsx
  - src/components/search/search-results-view
  - src/components/search/search-filter-panel
  - src/components/search/search-pagination
  - src/components/search/search-sort-select
autonomous: true
requirements: [RD-07]

must_haves:
  truths:
    - "The search page shows an eyebrow + serif query heading + mono result count, with facets/sort/chips/grid styled identically to the redesigned PLP"
    - "Pagination renders as pill buttons with the active page bg-brand text-paper"
    - "Query, filter, sort, and page state still round-trip through URL search params (SEARCH-03)"
    - "Searchanise data flow and typed-unknown narrowing are untouched (SEARCH-01/06)"
  artifacts:
    - path: "src/components/search/search-results-view"
      provides: "redesigned results grid + empty/loading states"
    - path: "src/components/search/search-pagination"
      provides: "pill pagination"
      contains: "bg-brand"
  key_links:
    - from: "src/components/search/search-results-view"
      to: "src/components/collection/product-card"
      via: "shared redesigned ProductCard (restyled in 11-08)"
      pattern: "ProductCard"
    - from: "src/app/(storefront)/search/page.tsx"
      to: "URL search params"
      via: "unchanged server-rendered param handling"
      pattern: "searchParams"
---

<objective>
Restyle the search surface to match the redesigned PLP language — hero band, facet panel, sort, chips, grid, pagination, empty/loading/error states — reusing the patterns established in 11-08 while leaving the Searchanise integration untouched.

Purpose: search slice of RD-07; visual consistency with collections.
Output: redesigned /search per UI-SPEC §5.8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/collection-page.js
@.planning/phases/11-full-visual-redesign/11-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search page + results view</name>
  <files>src/app/(storefront)/search/page.tsx, src/components/search/search-results-view</files>
  <read_first>src/app/(storefront)/search/page.tsx (Suspense boundaries around searchParams — Next 16 Cache Components constraint, do not restructure), all files in src/components/search/search-results-view (result grid, count, empty/loading/error states, Searchanise data mapping), 11-UI-SPEC.md §5.8 + §5.4, the redesigned PLP files from 11-08 for pattern reuse</read_first>
  <action>
**Search hero**: paper band (or `Section.Root tone="brand"` if the current page uses a hero band — match the PLP hero pattern): `Eyebrow` "Search", query heading `font-display text-[clamp(2rem,4vw,3.4rem)]` ("Results for “{q}”"), result count `type-mono-meta text-ink-faint`.

**Results view**: grid identical to PLP (`lg:grid-cols-3 sm:grid-cols-2 grid-cols-2 gap-y-5.5 gap-x-4.5`) — the shared ProductCard is already redesigned (11-08); swap any remaining old token classes in the view per UI-SPEC §3 table. Empty state: centered leaf icon + `font-display` heading + muted copy + ghost clear/search-again button. Loading skeletons: `bg-paper-2 rounded-lg animate-pulse motion-reduce:animate-none` blocks. Error state: muted copy + `<Button variant="secondary">` retry. DO NOT touch the Searchanise fetch, response narrowing, Suspense placement, or URL-param plumbing.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|type-display-0" "src/app/(storefront)/search/page.tsx" src/components/search/search-results-view` returns no files
    - `grep -rc "font-display" src/components/search/search-results-view` returns >= 1
    - `pnpm test:integration` exits 0 (search route + param behavior intact)
  </acceptance_criteria>
  <done>Search results page matches PLP language; data flow untouched</done>
</task>

<task type="auto">
  <name>Task 2: Search facets, sort, pagination</name>
  <files>src/components/search/search-filter-panel, src/components/search/search-sort-select, src/components/search/search-pagination</files>
  <read_first>all files in the three folders (facet state → URL params, sort options, pagination math), 11-UI-SPEC.md §5.8 + §5.4 filter/toolbar specs, the redesigned src/components/collection/filter-panel + sort-select from 11-08 (mirror their classes)</read_first>
  <action>
Mirror the PLP treatments exactly (same class vocabulary as 11-08 Task 2):
- Filter panel: groups `border-b border-hairline py-5.5`; mono 11px uppercase ink-faint headings; restyled checkboxes; counts mono right; active chips `bg-brand-tint text-brand rounded-full px-3 py-1.75 text-xs font-semibold` with ×; "Clear" `type-mono-meta text-gold-deep`.
- Sort: pill `border border-hairline bg-card rounded-full px-4 py-2.25 type-label`.
- Pagination: pill buttons `rounded-full border border-hairline min-h-11 min-w-11 type-label hover:bg-brand-tint hover:text-brand`; active page `bg-brand text-paper border-brand`; prev/next with lucide arrows. Keep all aria-current/labels and the URL-param page state.
Update any stories in these folders alongside.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "bg-brand text-paper" src/components/search/search-pagination` returns >= 1
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|action-" src/components/search/search-filter-panel src/components/search/search-sort-select src/components/search/search-pagination` returns no files
    - `grep -rc "min-h-11" src/components/search/search-pagination` returns >= 1 (touch targets kept)
    - `pnpm test:unit` exits 0
  </acceptance_criteria>
  <done>Facets/sort/pagination match PLP; URL state + a11y intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → search params | query/facets/page flow via URL into server rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-21 | Tampering | Searchanise response handling | mitigate | restyle only; existing typed-unknown narrowing untouched (SEARCH-06); integration tests gate |
| T-11-22 | Information Disclosure | query echo in heading | mitigate | query rendered as React text (auto-escaped); no dangerouslySetInnerHTML |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind`, `pnpm test:unit`, `pnpm test:integration` exit 0
- Dev-server check: /search?q=tea with facets, sort, pagination at desktop/375px
</verification>

<success_criteria>
- Search visually matches the redesigned PLP; SEARCH-01..07 behaviors intact; zero old tokens in owned files
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-09-SUMMARY.md`
</output>
