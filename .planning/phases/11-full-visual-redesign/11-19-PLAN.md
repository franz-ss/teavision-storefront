---
phase: 11-full-visual-redesign
plan: 19
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
requirements: [RD-03]
files_modified:
  - src/app/(storefront)/products/[handle]/page.tsx
  - src/components/product/product-form/product-form.tsx
  - src/components/ui/quantity-stepper/quantity-stepper.tsx
  - src/components/product/bulk-savings/bulk-savings.tsx

must_haves:
  truths:
    - "PDP info column uses the design's varied vertical rhythm rather than a uniform gap"
    - 'PDP buy-row stepper is a 4px-radius rectangle (not a rounded-full pill)'
    - 'PDP post-grid section spacing and grid top padding match the design'
  artifacts:
    - path: 'src/app/(storefront)/products/[handle]/page.tsx'
      provides: 'varied info-column rhythm + corrected post-grid spacing'
    - path: 'src/components/ui/quantity-stepper/quantity-stepper.tsx'
      provides: 'rectangle (rounded-sm) variant for the PDP buy row'
  key_links: []
---

<objective>
Close the PDP polish UAT gap (UAT test 12): 17 spacing/alignment deltas against the PDP section of design/extracted-design.html, none structural. This is a diff-and-correct pass — copy the design's exact spacing values, do NOT re-implement or approximate.

Purpose: The PDP is functionally correct but its uniform spacing diverges from the design's varied rhythm; the owner wants pixel parity.
Output: PDP info-column rhythm, stepper shape, post-grid spacing, and remaining 2px-class deltas brought to parity with the design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative design source — copy exact values, do not approximate (owner directive)

@design/extracted-design.html

# Diagnosed evidence — lists all 17 deltas with exact target values

@.planning/debug/pdp-spacing.md

# product-form test exists under test:unit — keep it green

@src/components/product/product-form/product-form.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDP spacing/alignment parity sweep</name>
  <files>src/app/(storefront)/products/[handle]/page.tsx, src/components/product/product-form/product-form.tsx, src/components/ui/quantity-stepper/quantity-stepper.tsx, src/components/product/bulk-savings/bulk-savings.tsx</files>
  <action>
    Per .planning/debug/pdp-spacing.md, apply the diagnosed deltas using the design's exact values from design/extracted-design.html. The four medium deltas:
    - INFO-COLUMN RHYTHM (page.tsx): replace the uniform gap-6 (24px) with the design's varied rhythm — 14px title block, 16px rating->tags, 22px price, 26px options/tiers, 18px buy->assurance. Apply per-element spacing rather than a single gap utility.
    - STEPPER SHAPE (quantity-stepper.tsx): the PDP buy row needs a rectangle (rounded-sm / 4px radius) variant instead of the rounded-full pill. Add a variant/prop so the PDP buy row uses the rectangle shape; do not change the stepper shape where other consumers (e.g. cart) rely on the pill. (Note: the cart stepper styling is owned by plan 11-18; this plan only touches the PDP buy-row variant.)
    - POST-GRID SPACING (page.tsx): post-grid sections use mt-12 (48px) vs the design's clamp(50px,7vw,90px); add the missing 8px (pt-2) top pad on the grid.
    - PRODUCT-FORM (product-form.tsx): buy->assurance gap 24px -> 18px.
    - BULK-SAVINGS (bulk-savings.tsx): heading->grid gap 16px -> 12px.
    - Then sweep the remaining eleven 2px-class deltas listed in the debug session for pixel parity. Tokens/grid/sticky offset are already verified matching — do not touch them. This is a polish diff; do not restructure layout.
  </action>
  <verify>
    <automated>pnpm test:unit && pnpm lint && pnpm typecheck && pnpm test:contracts</automated>
    <human-check>Compare a PDP (e.g. /products/<any handle>) side-by-side with the PDP section of design/extracted-design.html at desktop and 375px — spacing and alignment match (info-column rhythm, rectangle stepper, post-grid gaps).</human-check>
  </verify>
  <done>All diagnosed PDP spacing/alignment deltas corrected to the design's exact values; buy-row stepper is a rectangle; product-form test + contract + lint + typecheck pass.</done>
</task>

</tasks>

<verification>
- pnpm test:unit && pnpm lint && pnpm typecheck && pnpm test:contracts pass
- Manual: PDP visual parity vs design at desktop + mobile.
</verification>

<success_criteria>
UAT test 12 is closed: PDP spacing/alignment matches the design with no structural changes.
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-19-SUMMARY.md` when done.
</output>
