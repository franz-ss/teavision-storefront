---
phase: 11-full-visual-redesign
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - src/components/layout/header/header.tsx
  - src/components/layout/header/header.stories.tsx
  - src/components/layout/header/mega-nav.tsx
  - src/components/layout/header/mega-nav.stories.tsx
  - src/components/layout/header/mega-nav-styles.ts
  - src/components/layout/header/mega-nav-data.ts
  - src/components/layout/header/shop-mega-panel.tsx
  - src/components/layout/header/services-mega-panel.tsx
  - src/components/layout/header/services-links.tsx
  - src/components/layout/header/catalogue-links.tsx
  - src/components/layout/header/mobile-mega-nav.tsx
  - src/components/layout/header/mobile-shop-panel.tsx
  - src/components/layout/header/mobile-services-panel.tsx
  - src/components/layout/header/search.tsx
  - src/components/layout/header/search-form.tsx
  - src/components/layout/header/search-form.stories.tsx
  - src/components/layout/header/search-autocomplete.tsx
  - src/components/layout/header/search-autocomplete.stories.tsx
  - src/components/layout/header/search-suggestions.tsx
  - src/components/layout/header/cart-badge.tsx
  - src/components/layout/header/cart-badge.stories.tsx
  - src/components/layout/header/cart-count.tsx
autonomous: true
requirements: [RD-03]

must_haves:
  truths:
    - "The header shows a 38px ink utility bar with mono ticker copy and wholesale/phone links above a 76px translucent paper main bar"
    - "Nav links render as pills that tint green on hover; the cart count is a gold round badge"
    - "Mega menu opens as a full-width paper panel (intro column + link columns + feature card) over an ink scrim"
    - "Search overlay shows a large serif input over a 2px ink rule with popular-suggestion pills"
    - "Mobile menu is a fullscreen paper panel with serif accordion rows"
    - "All existing nav links/hrefs, search autocomplete behavior, and cart count data flow are unchanged"
  artifacts:
    - path: "src/components/layout/header/header.tsx"
      provides: "utility bar + sticky translucent main bar"
      contains: "backdrop-blur"
    - path: "src/components/layout/header/cart-count.tsx"
      provides: "gold mono count badge"
      contains: "bg-gold"
  key_links:
    - from: "src/components/layout/header/header.tsx"
      to: "src/components/ui (Button, Eyebrow, icon-button)"
      via: "primitives restyled in 11-02/11-03"
      pattern: "Button"
    - from: "src/components/layout/header/search-form.tsx"
      to: "existing search route/actions"
      via: "unchanged form submission to /search"
      pattern: "search"
---

<objective>
Restyle the entire header chrome to the redesign: new ink utility/announcement bar (RD-03 announcement surface, mockup static copy per orchestrator decision #2), translucent sticky main bar with pill nav and gold cart badge, full-width mega menus, serif search overlay, and fullscreen mobile menu — preserving every link destination, the Searchanise autocomplete flow, and the cart-count data flow.

Purpose: the header is the highest-visibility chrome; it sets the new system's tone on every page.
Output: redesigned header per design/extracted/header.js + design-system.css HEADER block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/header.js
@design/extracted/design-system.css
@src/components/layout/header/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utility bar + main bar + cart badge</name>
  <files>src/components/layout/header/header.tsx, src/components/layout/header/header.stories.tsx, src/components/layout/header/cart-badge.tsx, src/components/layout/header/cart-badge.stories.tsx, src/components/layout/header/cart-count.tsx</files>
  <read_first>src/components/layout/header/header.tsx (current structure, landmark roles, 'use client' boundaries), design/extracted/header.js lines 1–110 (utility bar + main bar JSX), design/extracted/design-system.css HEADER block, 11-UI-SPEC.md §5.1, src/components/layout/footer/data.ts (real phone number)</read_first>
  <action>
**Utility bar** (new, above the main bar): `bg-ink text-paper font-mono text-[11.5px] tracking-[0.08em] h-9.5` with a `max-w-wide px-gutter` row. Left ticker — exact mockup static copy (orchestrator decision): `EST. MELBOURNE 2014 · ACO + USDA CERTIFIED ORGANIC · FREIGHT-INSURED, WORLDWIDE` (separator `·` spans). Right: link "Apply for wholesale" → `/pages/wholesale`, and `tel:1300729617` rendered as "1300 729 617" (matches existing footer contact data). Links `text-paper/85 hover:text-paper`. Hide the right group on small screens if it overflows (`hidden sm:flex`).

**Main bar**: `h-19 sticky top-0 z-60 bg-paper/80 backdrop-blur-md border-b border-hairline`. Logo left: serif wordmark `font-display text-2xl` (keep the existing logo link/alt). Nav links as pills: `rounded-full px-3.5 py-2.5 type-label text-ink hover:bg-brand-tint hover:text-brand` with the same classes on active/open state (chevron rotates when open). Right cluster: existing 42px round IconButtons (search/cart) now `hover:bg-brand-tint hover:text-brand`; wholesale CTA as `<Button variant="brand" size="sm">`; burger toggle on mobile. Keep skip-link target ids, `<header>` landmark, nav aria attributes, and all link hrefs exactly as they are today.

**Cart badge/count**: `absolute top-1 right-1 min-w-4.5 h-4.5 rounded-full bg-gold text-ink font-mono text-[10px] font-bold` centered count. Keep the data flow (server count / optimistic refresh) untouched. Update both stories files for the new visuals.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:contracts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "EST. MELBOURNE 2014" src/components/layout/header/header.tsx` returns 1
    - `grep -c "tel:1300729617" src/components/layout/header/header.tsx` returns >= 1
    - `grep -c "backdrop-blur" src/components/layout/header/header.tsx` returns >= 1
    - `grep -c "bg-gold" src/components/layout/header/cart-count.tsx src/components/layout/header/cart-badge.tsx` total >= 1
    - No raw `<section>`/raw `<button>` violations: `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Utility bar + translucent pill-nav main bar + gold cart badge live; all hrefs and landmarks unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Mega menus (shop + services) and scrim</name>
  <files>src/components/layout/header/mega-nav.tsx, src/components/layout/header/mega-nav.stories.tsx, src/components/layout/header/mega-nav-styles.ts, src/components/layout/header/mega-nav-data.ts, src/components/layout/header/shop-mega-panel.tsx, src/components/layout/header/services-mega-panel.tsx, src/components/layout/header/services-links.tsx, src/components/layout/header/catalogue-links.tsx</files>
  <read_first>all eight files (current panel structure, link data, open/close + outside-close behavior in use-outside-close.ts), design/extracted/header.js mega-menu JSX (.mega blocks), design/extracted/design-system.css .mega CSS, 11-UI-SPEC.md §5.1 Mega menu</read_first>
  <action>
Restyle the mega panels to the mockup `.mega` pattern, KEEPING every link label/href in mega-nav-data, the hover/intent open-close timing, focus management, and aria wiring:
- Panel: full-width under the header, `bg-paper border-b border-hairline shadow-4`; inner `max-w-wide px-gutter grid grid-cols-[1.1fr_2.4fr_1.3fr] gap-10 py-10` (collapse to stacked below lg).
- Intro column: `Eyebrow` (tone brand) + `font-display text-[1.7rem]` heading + `text-ink-soft` blurb + link-arrow CTA (`inline-flex items-center gap-2 type-label border-b-[1.5px] border-hairline pb-1 hover:border-brand hover:text-brand` + ArrowRight 15px from lucide).
- Link columns: heading `font-mono text-[10.5px] tracking-[0.14em] uppercase text-ink-faint`; links `text-ink-soft hover:bg-brand-tint hover:text-brand rounded-md px-2.5 py-1.75 block`.
- Feature card (right): `rounded-lg overflow-hidden` image card with bottom gradient scrim (`bg-gradient-to-t from-ink/70 to-transparent`), Badge pill + `font-display text-paper` title — use real collection imagery already available to the panel (keep existing data source).
- Page scrim while open: `bg-ink/35 backdrop-blur-[2px]`.
mega-nav-styles.ts: replace its old-token class constants with new-token equivalents per UI-SPEC §3 (this file centralizes panel classes — keep the export shape).
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-4" src/components/layout/header/mega-nav-styles.ts src/components/layout/header/mega-nav.tsx` total >= 1
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|text-link" src/components/layout/header/mega-nav.tsx src/components/layout/header/mega-nav-styles.ts src/components/layout/header/shop-mega-panel.tsx src/components/layout/header/services-mega-panel.tsx src/components/layout/header/services-links.tsx src/components/layout/header/catalogue-links.tsx` returns no files
    - mega-nav-data.ts link hrefs byte-identical except styling fields (verify via `git diff --stat` showing no href changes)
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>Mega menus match the mockup grid/treatment; data, links, and interaction behavior unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Search overlay + mobile menu</name>
  <files>src/components/layout/header/search.tsx, src/components/layout/header/search-form.tsx, src/components/layout/header/search-form.stories.tsx, src/components/layout/header/search-autocomplete.tsx, src/components/layout/header/search-autocomplete.stories.tsx, src/components/layout/header/search-suggestions.tsx, src/components/layout/header/mobile-mega-nav.tsx, src/components/layout/header/mobile-shop-panel.tsx, src/components/layout/header/mobile-services-panel.tsx</files>
  <read_first>all nine files (Searchanise autocomplete data flow, debounce, submission to /search, mobile accordion behavior), design/extracted/header.js search overlay + mobile menu JSX, design/extracted/design-system.css .searchbar/.mmenu CSS, 11-UI-SPEC.md §5.1</read_first>
  <action>
**Search overlay**: panel styled like the mega panel (`bg-paper border-b border-hairline shadow-4`); the input becomes `font-display text-[clamp(1.4rem,3vw,2.2rem)] bg-transparent border-0 border-b-2 border-ink rounded-none focus:ring-0 focus:border-brand placeholder:text-ink-faint`; "Popular" label = `Eyebrow tone="muted" rule={false}`; suggestion pills `rounded-full border border-hairline bg-card px-3.5 py-2 type-label hover:bg-brand hover:text-paper`. KEEP: form submission to /search with query param, Searchanise autocomplete fetch/debounce/keyboard navigation, aria-combobox semantics, and the rate-limit-safe suggestion behavior. Autocomplete dropdown: `bg-card border border-hairline rounded-lg shadow-2`, result rows `hover:bg-brand-tint`.

**Mobile menu**: fullscreen `bg-paper` panel under the 76px bar; top-level rows `border-b border-hairline font-display text-2xl py-5`; expanded children as pill links (`rounded-full px-3.5 py-2.5 hover:bg-brand-tint hover:text-brand`); footer of panel: `<Button variant="brand">` wholesale CTA + `<Button variant="secondary">` phone link. Keep accordion expand/collapse state handling, focus trapping, and close-on-route-change behavior exactly as today.

Update the two stories files for new visuals.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "font-display" src/components/layout/header/search-form.tsx src/components/layout/header/search.tsx` total >= 1
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default" src/components/layout/header/search.tsx src/components/layout/header/search-form.tsx src/components/layout/header/search-autocomplete.tsx src/components/layout/header/search-suggestions.tsx src/components/layout/header/mobile-mega-nav.tsx src/components/layout/header/mobile-shop-panel.tsx src/components/layout/header/mobile-services-panel.tsx` returns no files
    - `pnpm test:integration` exits 0 (search route behavior intact)
  </acceptance_criteria>
  <done>Search overlay and mobile menu match mockup; autocomplete/data flows untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → search input | query text flows to Searchanise autocomplete + /search route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | search autocomplete | mitigate | restyle only; existing typed-unknown narrowing of Searchanise JSON and debounce/abuse protections untouched (integration tests gate) |
| T-11-09 | Spoofing (phishing-adjacent) | utility-bar links | mitigate | hardcoded first-party hrefs only (/pages/wholesale, tel:) — no user-controlled URLs |
| T-11-10 | Elevation (XSS) | mega feature card imagery | mitigate | next/image with existing data source; no dangerouslySetInnerHTML when porting mockup SVG/markup |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind`, `pnpm test:integration` exit 0
- Dev-server check at desktop + 375px: utility bar, sticky blur bar, mega menu open/close, search overlay, mobile menu
</verification>

<success_criteria>
- Header chrome matches design/extracted/header.js structure and styling; zero old-token classes under src/components/layout/header/; all behavior contracts intact
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-04-SUMMARY.md`
</output>
