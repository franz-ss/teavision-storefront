---
phase: 11-full-visual-redesign
plan: 18
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
requirements: [RD-03, RD-05]
files_modified:
  - src/app/(storefront)/cart/_components/cart-view.tsx
  - src/app/(storefront)/cart/_components/cart-line-actions.tsx
  - src/app/(storefront)/cart/page.tsx
  - src/components/ui/price/price.tsx

must_haves:
  truths:
    - 'Cart line columns (price, quantity, total) align with the table headers'
    - 'Discount slashed (compare-at) prices are visible on cart lines'
    - 'Changing quantity updates the stepper and totals optimistically without a visible loading cycle'
    - 'Cart container width matches adjacent sections (wide variant)'
  artifacts:
    - path: 'src/app/(storefront)/cart/_components/cart-view.tsx'
      provides: 'header + rows unified on one layout system; visible compare-at prices'
    - path: 'src/app/(storefront)/cart/_components/cart-line-actions.tsx'
      provides: 'useOptimistic + useTransition quantity updates'
  key_links:
    - from: 'src/app/(storefront)/cart/_components/cart-line-actions.tsx'
      to: 'cart Server Action'
      via: 'useOptimistic over existing updateCartLine action'
      pattern: 'useOptimistic|useTransition'
---

<objective>
Close the cart UAT gap (UAT test 13): two regressions from the 11-11 restyle (header/column alignment broke, compare-at slashed prices became invisible) plus an optimistic-quantity improvement and a container-width fix. Copy exact values from the cart/drawer treatment in design/extracted-design.html.

Purpose: The cart is revenue-critical; misaligned columns, missing discount prices, and a loading cycle on every quantity change all hurt conversion.
Output: Unified cart line layout aligned to headers, visible compare-at prices, optimistic quantity updates, and a consistent wide container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative design source — copy exact values, do not approximate (owner directive)

@design/extracted-design.html

# Diagnosed evidence

@.planning/debug/cart-regressions.md

# Cart view test exists and runs under test:unit — keep it green

@src/app/(storefront)/cart/\_components/cart-view.test.tsx

<interfaces>
- Regression source commit c4888d1 (11-11) replaced each line's grid (xl:grid-cols-[6rem_minmax(0,1fr)_7rem_10rem_7rem] — which matched the 5-col header) with drawer-style flex rows but left the 5-col header at cart-view.tsx ~line 331.
- compareAtAmountPerQuantity still flows (queried/mapped/passed) but 4d9d062 restyled compare-at to font-mono text-[10px] text-ink-faint (price.tsx) and c4888d1 demoted unit price to faint mono meta — perceptually invisible at cart density.
- cart-line-actions.tsx uses useActionState only; each click runs the Shopify mutation + revalidatePath('/cart'), re-suspending the page-level Suspense skeleton. React 19 useOptimistic + useTransition is available.
- cart/page.tsx is the only route using Section.Container variant='base' (1280px); the rest of the site uses max-w-wide (1480px).
- cart-view.test.tsx (runs under pnpm test:unit) renders CartView and its line components, so it exercises the optimistic-stepper component path — keep it green and extend it if a deterministic optimistic-update assertion is feasible.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify cart line layout, restore visible compare-at prices, fix container width</name>
  <files>src/app/(storefront)/cart/_components/cart-view.tsx, src/components/ui/price/price.tsx, src/app/(storefront)/cart/page.tsx</files>
  <behavior>
    - cart-view.test.tsx continues to pass.
    - The cart line price and quantity columns align under their respective table headers (single shared layout system).
    - When a line has a compareAtAmountPerQuantity greater than the unit price, the slashed compare-at price is visibly rendered on the line (not the invisible 10px faint-mono treatment).
  </behavior>
  <action>
    Per .planning/debug/cart-regressions.md, copying values from the cart treatment in design/extracted-design.html:
    - ALIGNMENT: unify the header and the rows on ONE layout system. Either restore the shared grid template (xl:grid-cols-[6rem_minmax(0,1fr)_7rem_10rem_7rem]) on each line so it lines up under the 5-col header, OR redesign the header to match the new flex rows. Choose whichever matches the design; both header and rows must share the same column model so price/qty/total align.
    - COMPARE-AT: re-emphasize the compare-at slashed price at cart sizes — revert the invisible font-mono text-[10px] text-ink-faint treatment to a legible struck-through price (use the design's cart price treatment / the existing Price compare-at variant at a readable size). Consider restoring the per-discount savings line that c4888d1 deleted if the design shows it. Keep the per-unit pricing context.
    - CONTAINER: change cart/page.tsx from Section.Container variant='base' to the default wide variant (max-w-wide / 1480px) to match recommendations and footer.
  </action>
  <verify>
    <automated>pnpm test:unit && pnpm lint && pnpm typecheck</automated>
    <human-check>On /cart with a discounted line: price/qty/total columns sit under their headers; the slashed compare-at price is clearly visible; the cart container width matches the sections above/below it.</human-check>
  </verify>
  <done>Header and rows share one column model and align; compare-at prices are visible at cart density; container uses the wide variant; cart-view test + lint + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Optimistic quantity updates</name>
  <files>src/app/(storefront)/cart/_components/cart-line-actions.tsx</files>
  <action>
    Per the cart-regressions debug session: wrap the quantity change in React 19 `useOptimistic` + `useTransition` around the existing cart Server Action so the stepper and displayed quantity update instantly on click, without disabling the stepper (isPending currently disables it) and without re-triggering the page-level Suspense skeleton. The cookie-backed cart remains the source of truth — the Server Action still runs and revalidates; only the perceived update is immediate. Preserve remove-line behavior and existing accessibility/aria. Do not introduce client-side cart storage. No `any`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration</automated>
    <human-check>This change is client-side, so the human-check is the primary proof of the optimistic behavior: on /cart, click +/- on a line quantity repeatedly — the value updates instantly with no loading flash/skeleton; totals reconcile after the action resolves; remove still works. (pnpm test:unit runs cart-view.test.tsx, which exercises the component path; pnpm test:integration covers the server-action boundary only.)</human-check>
  </verify>
  <done>Quantity changes update optimistically with no visible loading cycle; Server Action still authoritative; lint + typecheck + unit + integration pass; human-check confirms the optimistic stepper behavior.</done>
</task>

</tasks>

<verification>
- pnpm test:unit && pnpm test:integration && pnpm lint && pnpm typecheck pass
- Manual: cart column alignment, visible slashed prices, optimistic quantity, container width.
</verification>

<success_criteria>
UAT test 13 is closed: cart columns align to headers, compare-at prices are visible, quantity updates optimistically, and the container width is consistent.
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-18-SUMMARY.md` when done.
</output>
