---
phase: 11-full-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - src/components/ui/button/button.tsx
  - src/components/ui/button/button.stories.tsx
  - scripts/component-contracts/button-system.test.mjs
  - src/components/ui/eyebrow/eyebrow.tsx
  - src/components/ui/eyebrow/eyebrow.stories.tsx
  - src/components/ui/eyebrow/index.ts
  - src/components/ui/index.ts
  - src/components/ui/section/section.tsx
  - src/components/ui/section/section.stories.tsx
  - src/components/ui/badge/badge.tsx
  - src/components/ui/badge/badge.stories.tsx
autonomous: true
requirements: [RD-01]

must_haves:
  truths:
    - 'Buttons render as pills (rounded-full) with the mockup variant mapping: brand=green, primary=ink, secondary=hairline ghost, inverse=paper, inverseSecondary=ghost-on-dark'
    - 'An Eyebrow component renders Space Mono 11px uppercase 0.22em text with a leading rule line, in brand/muted/gold tones'
    - 'Section tones map to the new palette: surface=paper, sunken=paper-2, brand=brand-deep, inverse=ink'
    - 'Badges render as mono uppercase pills with organic and gold variants'
    - 'pnpm test:contracts passes with updated button-system assertions'
  artifacts:
    - path: 'src/components/ui/eyebrow/eyebrow.tsx'
      provides: 'Signature eyebrow element (cva tone: brand/muted/gold, rule boolean)'
      exports: ['Eyebrow']
    - path: 'src/components/ui/button/button.tsx'
      provides: 'Pill button variants mapped to mockup .btn-* styles'
      contains: 'rounded-full'
    - path: 'src/components/ui/section/section.tsx'
      provides: 'New tone/spacing/container maps (py-section, px-gutter, max-w-wide)'
    - path: 'src/components/ui/badge/badge.tsx'
      provides: '.pill treatment with organic/gold variants'
  key_links:
    - from: 'src/components/ui/button/button.tsx'
      to: 'scripts/component-contracts/button-system.test.mjs'
      via: 'contract test asserts exact class strings — updated in lockstep'
      pattern: 'rounded-full'
    - from: 'src/components/ui/section/section.tsx'
      to: 'src/app/globals.css'
      via: 'tone classes consume new tokens'
      pattern: 'bg-paper-2|bg-brand-deep'
---

<objective>
Restyle the four structural primitives every surface depends on — Button (pill variants), Section (tone/spacing/container), Badge (pill), plus the NEW Eyebrow signature component — updating the button-system contract test and stories in lockstep so `pnpm test:contracts` stays green.

Purpose: ESLint guards (`no-raw-button`, `no-button-style-class`, `no-section-root-tone-class`) force all mockup button/section styling to live INSIDE these primitives; every later surface plan consumes them.
Output: redesigned core primitives + new Eyebrow with Storybook coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/ui-primitives.js
@design/extracted/design-system.css
@src/components/ui/button/button.tsx
@src/components/ui/section/section.tsx
@scripts/component-contracts/button-system.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Button pill restyle + lockstep contract test + stories</name>
  <files>src/components/ui/button/button.tsx, src/components/ui/button/button.stories.tsx, scripts/component-contracts/button-system.test.mjs</files>
  <read_first>src/components/ui/button/button.tsx (current cva structure, sizes, loading state), scripts/component-contracts/button-system.test.mjs (EVERY assertion — rounded-md count, sm size string, inverseSecondary, motion-reduce trios, variant="brand"), src/components/ui/button/button.stories.tsx, 11-UI-SPEC.md §4 Button, design/extracted/design-system.css lines 128–162</read_first>
  <action>
In `button.tsx` (the ONLY legal place for button visual styling per eslint no-button-style-class):

Base: `inline-flex items-center justify-center gap-2.5 rounded-full transition-[background-color,color,box-shadow,transform] cursor-pointer focus-visible:ring-ring focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:outline-none disabled:cursor-not-allowed disabled:opacity-40` — `rounded-full` REPLACES `rounded-md` (mockup border-radius:100px). Add hover lift to filled variants: `hover:-translate-y-0.5 hover:shadow-2 motion-reduce:hover:translate-y-0 motion-reduce:hover:shadow-0` (or equivalent motion-reduce guards preserving the existing pattern in the file).

Variants (exact mapping per UI-SPEC §4; keep ALL existing variant names — `inverseSecondary` is contract-asserted):

- `brand` (mockup .btn-primary): `bg-brand text-paper hover:bg-brand-deep active:bg-brand-deep`
- `primary` (mockup .btn-gold, which is ink): `bg-ink text-paper hover:bg-ink/90 active:bg-ink/90`
- `secondary` (mockup .btn-ghost): `border-[1.5px] border-hairline bg-transparent text-ink hover:border-ink hover:bg-ink hover:text-paper`
- `inverse` (mockup .btn-light): `bg-paper text-ink hover:bg-card`
- `inverseSecondary` (ghost-on-dark): `border-[1.5px] border-paper/35 bg-transparent text-paper hover:bg-paper hover:text-ink`
- `ghost`: `text-brand hover:bg-brand-tint hover:text-brand-deep`

Sizes — KEEP `min-h-11` (sm/md) and `min-h-12` (lg/cta) touch targets regardless of mockup paddings (AA contract, Pitfall 9): `sm` ≈ `type-label min-h-11 px-4.5 text-[0.86rem]`, `md` `type-label min-h-11 px-6.5`, `lg` `min-h-12 px-8.5`, `cta` `min-h-12 px-8.5`. Keep the existing loading/LoaderCircle behavior untouched.

Lockstep `button-system.test.mjs`: update the `rounded-md` exact-count assertion to `rounded-full`; update the `sm: 'type-label min-h-11 px-3'` verbatim string to the new sm string; keep `inverseSecondary` and motion-reduce-trio assertions intact (update expected strings only where the class text changed). Do not delete any assertion — update values in lockstep, never remove coverage.

Lockstep `button.stories.tsx`: replace `var(--tv-bg-inverse)` story background with `var(--color-ink)`.
</action>
<verify>
<automated>pnpm test:contracts && pnpm lint:tailwind && pnpm lint</automated>
</verify>
<acceptance_criteria> - `grep -c "rounded-md" src/components/ui/button/button.tsx` returns 0; `grep -c "rounded-full" src/components/ui/button/button.tsx` returns >= 1 - `grep -c "min-h-11" src/components/ui/button/button.tsx` returns >= 1 and `grep -c "min-h-12" src/components/ui/button/button.tsx` returns >= 1 - `grep -c "inverseSecondary" src/components/ui/button/button.tsx` returns >= 1 - `grep -c "tv-bg-inverse" src/components/ui/button/button.stories.tsx` returns 0 - `pnpm test:contracts` exits 0
</acceptance_criteria>
<done>Pill buttons with mockup variant mapping; contract test and stories updated in lockstep; contracts green</done>
</task>

<task type="auto">
  <name>Task 2: Create the Eyebrow signature component</name>
  <files>src/components/ui/eyebrow/eyebrow.tsx, src/components/ui/eyebrow/eyebrow.stories.tsx, src/components/ui/eyebrow/index.ts, src/components/ui/index.ts</files>
  <read_first>design/extracted/design-system.css lines 90–109 (.eyebrow), 11-UI-SPEC.md §2 Eyebrow component, src/components/ui/badge/badge.tsx (existing cva component shape to mirror), docs/conventions.md component anatomy</read_first>
  <action>
Scaffold with `pnpm create:component -- ui/eyebrow` (then fill in). Server component (no 'use client'). Named export `Eyebrow`, rendering a `<span>`:

Base classes: `type-eyebrow inline-flex items-center gap-2.5` + rule line `before:h-px before:w-5.5 before:bg-current before:opacity-60` (replaces mockup ::before 22×1px — 22px canonicalizes to w-5.5 per the spacing checker).
cva variants — `tone`: `brand` → `text-brand` (default), `muted` → `text-ink-faint`, `gold` → `text-gold` (for dark bands); boolean prop `rule` (default `true`; when `false` omit all `before:*` classes — compose with cn()).
Accept `className` merged via `cn()`, children = label text. Add `eyebrow.stories.tsx` covering: default brand, muted, gold-on-dark (story with `bg-ink` decorator), and no-rule. Export from `src/components/ui/eyebrow/index.ts` and add to the `src/components/ui/index.ts` barrel.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `src/components/ui/eyebrow/eyebrow.tsx` exists with `export function Eyebrow` and contains `type-eyebrow` and `before:w-5.5` - `grep -c "use client" src/components/ui/eyebrow/eyebrow.tsx` returns 0 - eyebrow.stories.tsx exists with >= 4 stories; `grep -c "Eyebrow" src/components/ui/index.ts` returns >= 1 - `pnpm lint` exits 0 (cn() composition, no banned patterns)
</acceptance_criteria>
<done>Eyebrow component + story + barrel export; lint green</done>
</task>

<task type="auto">
  <name>Task 3: Section tone/spacing/container remap + Badge pill</name>
  <files>src/components/ui/section/section.tsx, src/components/ui/section/section.stories.tsx, src/components/ui/badge/badge.tsx, src/components/ui/badge/badge.stories.tsx</files>
  <read_first>src/components/ui/section/section.tsx (tone/spacing/container cva maps + Section.Intro), src/components/ui/badge/badge.tsx, 11-UI-SPEC.md §4 Section + Badge, design/extracted/design-system.css .section-pad/.wrap/.pill</read_first>
  <action>
`section.tsx` — remap cva values (API names unchanged):
- Tones: `surface` → `bg-paper text-ink`; `sunken` → `bg-paper-2 text-ink`; `brand` → `bg-brand-deep text-paper`; `inverse` → `bg-ink text-paper`; `brandStrong` → `bg-brand-deep text-paper` (TEMPORARY alias of brand — the variant is deleted in plan 11-14 after call sites migrate; keep it so unmigrated surfaces compile); `transparent` → ''.
- Spacing: `default` → `py-section` (clamp(4rem,9vw,8.125rem)); `compact` → `py-8 md:py-12`; `none` → ''.
- Container: `mx-auto px-gutter` base; `default` → `max-w-wide` (1480px); `compact` → `max-w-prose`; add a `base` width variant → `max-w-base` (1280px, for FAQ/editorial).
- `Section.Intro`: use the new `Eyebrow` component above the title; title `type-heading-01`; copy `type-lede text-ink-soft`. Import Eyebrow via relative path `../eyebrow` (within-domain rule).
Update section.stories.tsx tone/spacing examples to render against the new tokens (keep a brandStrong story until 11-14 OR drop it now if the stories file enumerates tones dynamically — prefer dropping the explicit brandStrong story).

`badge.tsx` — mockup `.pill`: base `inline-flex items-center gap-2 rounded-full border border-hairline bg-card px-3 py-1.5 type-mono-meta text-ink-soft`. Variants: `organic` → `border-brand/30 bg-brand-tint text-brand before:size-1.5 before:rounded-full before:bg-current` (6px leading dot); `gold` → `border-gold-deep/40 bg-gold-tint text-gold-deep`; `onDark` (new) → `border-paper/30 bg-paper/15 text-paper`. Map/rename existing variant names conservatively — read the file first and keep existing consumer-facing variant names where they exist, adding organic/gold/onDark. Update badge.stories.tsx to cover all variants.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:contracts</automated>
</verify>
<acceptance_criteria> - `grep -c "bg-paper-2" src/components/ui/section/section.tsx` returns >= 1; `grep -c "bg-brand-deep" src/components/ui/section/section.tsx` returns >= 2 (brand + temporary brandStrong) - `grep -c "py-section" src/components/ui/section/section.tsx` returns >= 1; `grep -c "px-gutter" src/components/ui/section/section.tsx` returns >= 1 - `grep -c "type-mono-meta" src/components/ui/badge/badge.tsx` returns >= 1; `grep -c "rounded-full" src/components/ui/badge/badge.tsx` returns >= 1 - `pnpm test:contracts` exits 0 and `pnpm lint:tailwind` exits 0
</acceptance_criteria>
<done>Section tones/spacing on new tokens (brandStrong temporarily aliased), Badge is the mockup pill; all gates green</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                        |
| -------- | -------------------------------------------------- |
| none new | pure presentational restyle of internal primitives |

## STRIDE Threat Register

| Threat ID | Category                            | Component              | Disposition | Mitigation Plan                                                                                                        |
| --------- | ----------------------------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-11-04   | Tampering                           | button-system.test.mjs | mitigate    | assertions updated in lockstep, never deleted — coverage (touch targets, motion-reduce, variant presence) is preserved |
| T-11-05   | Denial of Service (a11y regression) | Button sizes           | mitigate    | min-h-11/min-h-12 touch targets kept despite smaller mockup paddings                                                   |

</threat_model>

<verification>
- `pnpm test:contracts`, `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind` all exit 0
- Storybook spot check: Button variants, Eyebrow tones, Section tones, Badge pills render with new palette
</verification>

<success_criteria>

- Core primitives express the mockup system; Eyebrow exists with story; contract tests updated in lockstep and green
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-02-SUMMARY.md`
</output>
