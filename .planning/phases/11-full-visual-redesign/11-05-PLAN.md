---
phase: 11-full-visual-redesign
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - src/components/layout/footer/footer.tsx
  - src/components/layout/footer/view/view.tsx
  - src/components/layout/footer/link-list
  - src/components/layout/footer/text-link
  - src/components/layout/footer/quality-column
  - src/components/layout/footer/newsletter-column
  - src/components/layout/footer/newsletter-form
  - src/components/layout/footer/payment-mark
  - src/components/layout/footer/payment-marks
  - src/components/layout/footer/icons
  - src/app/(storefront)/layout.tsx
  - src/app/error.tsx
  - src/app/global-error.tsx
  - src/app/not-found.tsx
autonomous: true
requirements: [RD-03, RD-07]

must_haves:
  truths:
    - "The footer is an ink (dark) band with gold mono column headings, paper-toned links, pill newsletter input, and bordered payment-mark chips"
    - "Every existing footer link label/href is preserved, including https://mrtea.com.au/account/login"
    - "Newsletter signup still submits through the existing Server Action with honeypot and accessible feedback"
    - "Error, global-error, and not-found pages render paper bg + serif heading + new-system buttons"
    - "Skip link still works and is styled with new tokens"
  artifacts:
    - path: "src/components/layout/footer/view/view.tsx"
      provides: "ink footer grid 1.6fr/1fr/1fr/1.4fr"
      contains: "bg-ink"
    - path: "src/app/not-found.tsx"
      provides: "restyled 404"
      contains: "font-display"
  key_links:
    - from: "src/components/layout/footer/newsletter-form"
      to: "existing newsletter Server Action"
      via: "unchanged action wiring"
      pattern: "action"
    - from: "src/components/layout/footer/data.ts"
      to: "rendered footer links"
      via: "data file unchanged — visual layer only"
      pattern: "mrtea.com.au"
---

<objective>
Restyle the footer to the redesign's ink `.ft` treatment (superseding Phase 4's 1:1 visual parity per CONTEXT — link destinations and newsletter behavior are kept), restyle the storefront layout skip-link, and restyle the root error/global-error/not-found pages.

Purpose: completes layout chrome (RD-03) and the error/empty surfaces slice of RD-07.
Output: redesigned footer + error pages per product-card-footer.js Footer + .ft CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/product-card-footer.js
@design/extracted/design-system.css
@src/components/layout/footer/data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer ink restyle (keep all links + newsletter action)</name>
  <files>src/components/layout/footer/footer.tsx, src/components/layout/footer/view/view.tsx, src/components/layout/footer/link-list, src/components/layout/footer/text-link, src/components/layout/footer/quality-column, src/components/layout/footer/newsletter-column, src/components/layout/footer/newsletter-form, src/components/layout/footer/payment-mark, src/components/layout/footer/payment-marks, src/components/layout/footer/icons</files>
  <read_first>src/components/layout/footer/view/view.tsx + data.ts + all subcomponent files (current structure, every link href, newsletter action/honeypot wiring), design/extracted/product-card-footer.js Footer JSX, design/extracted/design-system.css .ft block, 11-UI-SPEC.md §5.2</read_first>
  <action>
Apply the `.ft` treatment, changing ONLY presentation (data.ts hrefs/labels stay byte-identical):
- Wrapper: `bg-ink text-paper/75`; inner `max-w-wide px-gutter`; top grid `grid gap-10 py-section lg:grid-cols-[1.6fr_1fr_1fr_1.4fr]`.
- Column headings: `font-mono text-[10.5px] tracking-[0.16em] uppercase text-gold mb-4.5`.
- Links: `text-[0.95rem] text-paper/75 hover:text-paper` (replace `text-footer-muted`-family classes per UI-SPEC §3: `text-footer-muted`→`text-paper/75`, `border-footer-input`→`border-paper/20`, `bg-footer*`→`bg-ink`).
- Brand column: paper-toned logo, <=30ch blurb, quality pills `rounded-full border border-paper/15 bg-paper/5 text-paper/80 px-3 py-1.5 type-mono-meta`.
- Newsletter (KEEP Server Action, honeypot, pending/success/error feedback, aria-live): pill input `bg-paper/5 border border-paper/20 rounded-full px-4.5 py-3.5 text-paper placeholder:text-paper/60 focus:border-gold focus:ring-0`; submit `<Button variant="inverse" size="sm">` full-width; mono `tel:`/`mailto:` contact links below.
- Bottom row: `border-t border-paper/10 py-5.5 font-mono text-[11px] tracking-[0.06em]` — copyright + Popular Searches left, payment marks right as chips `border border-paper/15 rounded-[5px] px-2 py-1 text-[9.5px]`.
Update any footer stories alongside. The mrtea.com.au Login link and all tel/mailto/legacy URLs must remain.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-ink" src/components/layout/footer/view/view.tsx` returns >= 1; `grep -c "text-gold" src/components/layout/footer/view/view.tsx` (or quality/link-list subcomponents) total >= 1
    - `grep -rc "mrtea.com.au/account/login" src/components/layout/footer/` returns >= 1
    - `grep -rln "bg-footer\|text-footer\|border-footer\|bg-inverse\|text-on-brand" src/components/layout/footer/` returns no files
    - newsletter-form still references its Server Action and honeypot (grep identifiers found in read_first)
    - `pnpm test:integration` exits 0
  </acceptance_criteria>
  <done>Ink footer with gold headings and pill newsletter; links and actions unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Skip link + error/global-error/not-found pages</name>
  <files>src/app/(storefront)/layout.tsx, src/app/error.tsx, src/app/global-error.tsx, src/app/not-found.tsx</files>
  <read_first>all four files (skip-link markup, error-reset behavior, landmark structure), 11-UI-SPEC.md §5.11, 11-UI-SPEC.md §3 rename table</read_first>
  <action>
- `(storefront)/layout.tsx`: restyle the skip link with new tokens (e.g. `focus:bg-paper focus:text-ink border border-hairline rounded-sm`); keep its target id and landmark order (AUDIT-02 contract).
- `error.tsx` / `global-error.tsx` / `not-found.tsx`: paper background, `font-display` serif heading (`type-heading-02`), `text-ink-soft` copy, actions as `<Button variant="brand">` (retry/home) + `<Button variant="secondary">` where a second action exists. Keep the `reset()` handler wiring and any logging exactly as-is. Swap every old token class per UI-SPEC §3 table. global-error.tsx renders its own <html>/<body> — make sure body classes use `bg-paper text-ink font-sans`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|type-display-0" src/app/error.tsx src/app/global-error.tsx src/app/not-found.tsx "src/app/(storefront)/layout.tsx"` returns no files
    - `grep -c "font-display\|type-heading" src/app/not-found.tsx` returns >= 1
    - skip-link target id unchanged (`git diff` shows no id/href change in layout.tsx)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Skip link + all three error surfaces on the new system; reset/landmark behavior intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → footer newsletter form | untrusted input via restyled form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering/DoS | footer newsletter form | mitigate | Server Action + honeypot + rate-limit wiring untouched; integration tests gate the boundary |
| T-11-12 | Information Disclosure | error pages | mitigate | keep existing error-message handling; do not render raw error stacks while restyling |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind`, `pnpm test:integration`, `pnpm build` exit 0
- Dev-server check: footer at desktop/tablet/mobile; /nonexistent-url 404 page; skip link via keyboard Tab
</verification>

<success_criteria>
- Footer matches the redesign ink treatment with all Phase 4 links/actions preserved; error surfaces restyled; zero old tokens in owned files
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-05-SUMMARY.md`
</output>
