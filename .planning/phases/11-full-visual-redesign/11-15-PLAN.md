---
phase: 11-full-visual-redesign
plan: 15
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
requirements: [RD-04, RD-05]
files_modified:
  - src/components/layout/header/header.tsx
  - src/components/layout/header/mobile-mega-nav.tsx
  - src/components/layout/header/mobile-shop-panel.tsx
  - src/components/ui/toggle-button/toggle-button.tsx
  - src/components/ui/button/button.tsx
  - src/components/ui/button/button.stories.tsx
  - src/components/layout/header/mega-nav.tsx
  - src/components/layout/header/mega-nav-styles.ts
  - src/components/layout/header/shop-mega-panel.tsx
  - src/components/layout/header/services-mega-panel.tsx
  - .planning/phases/11-full-visual-redesign/11-UI-SPEC.md

must_haves:
  truths:
    - 'Header utility bar renders on a single line at all widths it is visible — no wrapping/collision at narrow viewports'
    - 'Mobile menu has a visible close (X) affordance and category chips wrap instead of horizontal-scrolling'
    - 'Mega menu stays open while the cursor moves from the nav trigger into the panel'
    - 'Brand/primary/inverse buttons no longer lift (translate-y) on hover'
  artifacts:
    - path: 'src/components/layout/header/header.tsx'
      provides: 'utility bar responsive visibility/nowrap guard'
    - path: 'src/components/layout/header/mobile-mega-nav.tsx'
      provides: 'close button + non-overlapping overlay + disambiguated link affordance'
    - path: 'src/components/layout/header/mega-nav.tsx'
      provides: 'hover-intent close grace timeout'
    - path: 'src/components/ui/button/button.tsx'
      provides: 'button variants without hover lift, with design .btn metrics'
  key_links:
    - from: 'src/components/layout/header/mega-nav.tsx'
      to: 'shop-mega-panel.tsx / services-mega-panel.tsx'
      via: 'shared open-state with close grace timeout / contiguous hover bridge'
      pattern: 'setTimeout|onMouseLeave|onMouseEnter'
---

<objective>
Close four header/navigation UAT gaps (UAT tests 3, 5, 17 + the global Button hover-lift sub-issue from test 5). Copy exact values from the design mockup — do NOT approximate.

Purpose: The header is the most-seen surface and currently breaks on mobile (utility bar wrap, no mobile-menu exit, horizontal-scroll chips) and on desktop (mega-menu hover dead zone). The global Button hover-lift effect is disliked by the owner and must be removed.
Output: Header utility bar, mobile mega nav, mobile shop panel, desktop mega-nav hover behavior, and the shared Button component all fixed and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative design source — copy exact values, do not approximate (owner directive)

@design/extracted-design.html

# Diagnosed evidence for these gaps

@.planning/debug/utility-bar-mobile-wrap.md
@.planning/debug/mobile-menu-ux.md
@.planning/debug/megamenu-hover-gap.md

# UI spec to update in lockstep when removing the hover lift

@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md

<interfaces>
button.tsx variant strings carry the hover lift to remove (current state):
- brand: '... hover:-translate-y-0.5 hover:shadow-2 motion-reduce:hover:translate-y-0 motion-reduce:hover:shadow-0'
- primary: '... hover:bg-ink/90 active:bg-ink/90 hover:-translate-y-0.5 hover:shadow-2 ...'
- inverse: '... hover:bg-card hover:-translate-y-0.5 hover:shadow-2 ...'
The button-system contract test (scripts/component-contracts/button-system.test.mjs) does NOT assert the hover-lift classes, so removing them is contract-safe. It DOES assert: sm size string exactly `'type-label min-h-11 px-4.5 text-[0.86rem]'`, presence of `inverseSecondary:`, exactly one `items-center justify-center gap-2.5`, `rounded-full` present, and that role-variant names (nav/menuItem/etc.) stay absent. Keep all of those intact.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix utility bar mobile wrap and disabled-button hover lift</name>
  <files>src/components/layout/header/header.tsx, src/components/ui/button/button.tsx, src/components/ui/button/button.stories.tsx, .planning/phases/11-full-visual-redesign/11-UI-SPEC.md</files>
  <action>
    Utility bar (header.tsx, currently the bar around lines 31-58, visible from sm/640px, fixed h-9.5):
    - The design mockup is desktop-only at this width; the bar needs ~1024px to fit on one line. Gate visibility to `lg` (only show the utility bar from 1024px) AND add a hard guard of `whitespace-nowrap overflow-hidden` on the bar's flex container so any future narrow render cannot wrap inside the fixed-height bar. Read the `.topbar`/utility-bar treatment in design/extracted-design.html and match its single-line layout exactly; do not invent a tablet trim ladder unless the lg-gate alone leaves a sub-lg state that still shows the bar.
    - Confirm no horizontal overflow is introduced at 375px / 768px / 1024px.

    Button hover lift removal (button.tsx): Remove `hover:-translate-y-0.5 hover:shadow-2 motion-reduce:hover:translate-y-0 motion-reduce:hover:shadow-0` from the brand, primary, and inverse variants (lines ~21, 23, 27). Keep the color/background hover states (e.g. hover:bg-brand-deep, hover:bg-ink/90, hover:bg-card) and keep the shared `transition-[background-color,color,box-shadow,transform]` frame. Do NOT change the sm size string, inverseSecondary presence, the single shared centered-frame class, or rounded-full — the contract test asserts these exactly.
    - Update 11-UI-SPEC.md line ~183 in lockstep to remove the documented hover-lift behavior (no contract-test change is needed). Adjust button.stories.tsx only if a story explicitly documents/asserts the lift.

  </action>
  <verify>
    <automated>pnpm test:contracts</automated>
  </verify>
  <done>Utility bar shows single-line only at lg+ with a nowrap/overflow guard; brand/primary/inverse buttons have no translate-y on hover; pnpm test:contracts passes; 11-UI-SPEC.md updated.</done>
</task>

<task type="auto">
  <name>Task 2: Fix mobile mega nav exit, chip wrap, alignment, and link affordance</name>
  <files>src/components/layout/header/mobile-mega-nav.tsx, src/components/layout/header/mobile-shop-panel.tsx, src/components/layout/header/header.tsx, src/components/ui/toggle-button/toggle-button.tsx</files>
  <action>
    Per .planning/debug/mobile-menu-ux.md and copying spacing/typography from the mobile-nav treatment in design/extracted-design.html:
    - CLOSE BUTTON: the burger toggles to an X but the fullscreen overlay (fixed inset-0 z-55) paints over the z-60 sticky header's main bar (which is z-auto). Either offset the overlay below the header (e.g. top-19) so the X stays visible and tappable, OR raise the main-bar z-index above the overlay, OR add an explicit in-menu close (X) button at the top of the overlay. Choose the approach that matches the design and keeps the X reachable; ensure tapping it closes the menu.
    - CHIP WRAP: mobile-shop-panel category chips use `overflow-x-auto + min-w-max` causing an always-visible native scrollbar and horizontal scroll the owner dislikes. Replace with `flex-wrap` and drop `min-w-max` so chips wrap; remove the horizontal-scroll pattern entirely.
    - CHIP STYLING: chips currently use the toggle-button `menuRow`/button-like variant. Restyle the mobile category selector toward a text-tab treatment (no border, no fill) so they do not look like buttons. Adjust toggle-button.tsx only as needed for this variant; keep existing variants intact for other consumers.
    - VIEW-ALL AFFORDANCE: the `ChevronRight` is overloaded as both a drill-in indicator and the "View all X" link decorator, which reads as a submenu. Replace the View-all ChevronRight with an underlined text link (or an ArrowRight, distinct from the drill-in chevron) so the affordance is unambiguous.
    - ALIGNMENT: normalize the four different left text-edges (px-0/px-2.5/px-3/px-3.5) to a single consistent edge using the design's negative-margin compensation pattern so menu items align.
    - Do not introduce horizontal overflow at 375px.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck</automated>
    <human-check>At ~375px: open the mobile menu, confirm a visible/working close (X), category chips wrap (no horizontal scrollbar), chips read as tabs not buttons, "View all" is a text/arrow link distinct from drill-in, and menu items share one left edge.</human-check>
  </verify>
  <done>Mobile menu is exitable via a visible close affordance; chips wrap with no horizontal scrollbar; chips are text-tab styled; View-all affordance is unambiguous; menu items left-aligned consistently; lint + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fix desktop mega-menu hover dead zone</name>
  <files>src/components/layout/header/mega-nav.tsx, src/components/layout/header/mega-nav-styles.ts, src/components/layout/header/shop-mega-panel.tsx, src/components/layout/header/services-mega-panel.tsx</files>
  <action>
    Per .planning/debug/megamenu-hover-gap.md: there is a ~16px pointer dead zone between the 44px-tall nav `<li>` hover area (bottom edge ~y=98px) and the panels fixed at `top-28.5` (y=114px). Crossing y=98 fires the li's onMouseLeave -> setOpenMenu(null) synchronously, and the panel's rescue onMouseEnter can never fire because the panel is hidden before the cursor arrives.
    Fix with BOTH of these for robustness:
    - Add a shared close-grace timeout (~150-300ms) so onMouseLeave on a trigger schedules the close, and onMouseEnter on either the trigger or the panel wrapper cancels it. Clear the timeout on unmount. mega-nav.tsx currently calls setOpenMenu(null) immediately at lines ~44 and ~69 and has an unreachable keep-open handler at ~104-107 — wire these into the grace timeout.
    - Make the hover surface contiguous across the gap: in mega-nav-styles.ts give the trigger li `h-full`/`self-stretch` so its hover area reaches the bar's bottom edge, and/or add an invisible bridge element covering the 16px gap down to the panel top. Match the panel offset to the actual header height from design/extracted-design.html.
    - Verify Shop and Services panels both stay open when moving the cursor trigger -> panel, and still close when the cursor leaves the panel area.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck</automated>
    <human-check>On desktop, hover the Shop trigger, then move the cursor straight down into the panel — the panel stays open and links are clickable. Repeat for Services. Moving away closes the panel after the grace period.</human-check>
  </verify>
  <done>Mega panels remain open while crossing from trigger into panel for both Shop and Services; close on leave after grace; lint + typecheck pass.</done>
</task>

</tasks>

<verification>
- pnpm lint && pnpm typecheck && pnpm test:contracts all pass
- Manual: utility bar single-line behavior across widths; mobile menu exit + chip wrap + alignment + link affordance; mega-menu hover continuity on desktop; no button hover lift.
</verification>

<success_criteria>
All four header/nav gaps (UAT tests 3, 5, 17, plus the button hover-lift sub-issue) are closed with values copied from design/extracted-design.html, and the contract suite still passes.
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-15-SUMMARY.md` when done.
</output>
