---
phase: 11-full-visual-redesign
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/ui/text-input/text-input.tsx
  - src/components/ui/textarea/textarea.tsx
  - src/components/ui/select/select.tsx
  - src/components/ui/checkbox/checkbox.tsx
  - src/components/ui/checkbox/checkbox.stories.tsx
  - src/components/ui/form-label/form-label.tsx
  - src/components/ui/quantity-stepper/quantity-stepper.tsx
  - src/components/ui/icon-button/icon-button.tsx
  - src/components/ui/toggle-button/toggle-button.tsx
  - src/components/ui/toggle-button/toggle-button.stories.tsx
  - src/components/ui/disclosure-button/disclosure-button.tsx
  - src/components/ui/star-rating/star-rating.tsx
  - src/components/ui/price/price.tsx
  - src/components/ui/card/card.tsx
  - src/components/ui/card/card.stories.tsx
  - src/components/ui/accordion/accordion.tsx
  - src/components/ui/accordion/accordion.stories.tsx
  - src/components/ui/dialog/dialog.tsx
  - src/components/ui/dialog/dialog.stories.tsx
  - src/components/ui/article-card/article-card.tsx
  - src/components/ui/rich-text/rich-text.tsx
  - src/components/ui/rich-text/rich-text.stories.tsx
  - src/components/ui/newsletter-signup/newsletter-signup.tsx
autonomous: true
requirements: [RD-01]

must_haves:
  truths:
    - "Form fields render the mockup .field treatment: card bg, hairline border, 4px radius, mono uppercase labels, green focus border + tint glow"
    - "Quantity stepper is a hairline pill with mono digits and size-11 touch-target buttons"
    - "Article-card and dialog/card/accordion express the new paper/ink/hairline surfaces"
    - "Newsletter signup keeps its Server Action, honeypot, and accessible feedback while wearing the new styling"
    - "No remaining old-token classes in the files this plan owns"
  artifacts:
    - path: "src/components/ui/text-input/text-input.tsx"
      provides: ".field input treatment"
      contains: "bg-card"
    - path: "src/components/ui/quantity-stepper/quantity-stepper.tsx"
      provides: "pill stepper"
      contains: "rounded-full"
    - path: "src/components/ui/article-card/article-card.tsx"
      provides: ".jcard treatment with motion-reduce zoom trio preserved"
      contains: "motion-reduce:"
  key_links:
    - from: "src/components/ui/newsletter-signup/newsletter-signup.tsx"
      to: "existing newsletter Server Action"
      via: "unchanged action/props wiring (restyle markup only)"
      pattern: "action"
    - from: "src/components/ui/article-card/article-card.tsx"
      to: "scripts/component-contracts/button-system.test.mjs"
      via: "motion-reduce trio assertion on group-hover:scale lines"
      pattern: "motion-reduce:group-hover:scale-100"
---

<objective>
Restyle the remaining UI primitives — form fields, controls, and container components — to the new design system, migrating every old-token class in these files to the new names (UI-SPEC §3 rename table) and applying the mockup component treatments (UI-SPEC §4).

Purpose: surfaces in waves 3–5 compose these primitives; finishing them now means surface plans only restructure page markup.
Output: all `src/components/ui/*` primitives (except those owned by 11-02) on the new system, stories updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/design-system.css
@design/extracted/ui-primitives.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Form-field family (.field treatment)</name>
  <files>src/components/ui/text-input/text-input.tsx, src/components/ui/textarea/textarea.tsx, src/components/ui/select/select.tsx, src/components/ui/checkbox/checkbox.tsx, src/components/ui/checkbox/checkbox.stories.tsx, src/components/ui/form-label/form-label.tsx</files>
  <read_first>each file being modified (current props/class structure), 11-UI-SPEC.md §4 Form fields, design/extracted/design-system.css .field/.fbox blocks, 11-UI-SPEC.md §3 rename table</read_first>
  <action>
Apply the mockup `.field` treatment, swapping old token classes per the §3 rename table (`bg-surface*`→`bg-card`, `border-default`→`border-hairline`, `text-subtle`→`text-ink-faint`, etc.):
- text-input/textarea/select: `bg-card border border-hairline rounded-sm px-4 py-3.5 text-ink placeholder:text-ink-faint`; focus state `focus:border-brand focus:shadow-focus focus:ring-0 focus:outline-none` (the glow IS the shadow-focus token — remove any old ring-based focus that double-renders).
- checkbox (mockup .fbox): `size-4.5 rounded-[5px] border-[1.5px] border-hairline`, checked `bg-brand border-brand text-paper`. Update checkbox.stories.tsx states.
- form-label: `type-mono-meta text-ink-faint` (mono uppercase label role).
Keep every prop, aria attribute, error-message slot, and forwardRef signature exactly as-is — class strings only. Where a component renders error text using `text-danger-text`, swap to `text-danger` (new functional token).
  </action>
  <verify>
    <automated>pnpm lint:tailwind && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "bg-card" src/components/ui/text-input/ src/components/ui/textarea/ src/components/ui/select/` each return >= 1
    - `grep -c "type-mono-meta" src/components/ui/form-label/form-label.tsx` returns >= 1
    - `grep -rln "bg-surface\|border-default\|text-subtle\|text-muted\|text-default\|danger-text\|danger-bg" src/components/ui/text-input/ src/components/ui/textarea/ src/components/ui/select/ src/components/ui/checkbox/ src/components/ui/form-label/` returns no files
    - `pnpm lint:tailwind` exits 0
  </acceptance_criteria>
  <done>All five form primitives wear .field styling with zero old-token classes remaining in them</done>
</task>

<task type="auto">
  <name>Task 2: Controls — stepper, icon/toggle/disclosure buttons, stars, price</name>
  <files>src/components/ui/quantity-stepper/quantity-stepper.tsx, src/components/ui/icon-button/icon-button.tsx, src/components/ui/toggle-button/toggle-button.tsx, src/components/ui/toggle-button/toggle-button.stories.tsx, src/components/ui/disclosure-button/disclosure-button.tsx, src/components/ui/star-rating/star-rating.tsx, src/components/ui/price/price.tsx</files>
  <read_first>each file being modified, 11-UI-SPEC.md §4 Quantity stepper, design/extracted/design-system.css .qty block, 11-UI-SPEC.md §3 rename table</read_first>
  <action>
- quantity-stepper: pill wrapper `inline-flex items-center rounded-full border border-hairline`; +/- buttons `size-11 text-ink-soft hover:text-brand` (KEEP the 44px touch target); count `min-w-7 text-center font-mono text-[13px]`.
- icon-button: 42px circle treatment — `rounded-full hover:bg-brand-tint hover:text-brand`; KEEP the existing `h-11 w-11` small-size class strings if a contract test asserts them (read the file + button-system.test.mjs first; touch target must not shrink).
- toggle-button: filter chip treatment (mockup .fopt/active chips): base `border border-hairline bg-card text-ink-soft rounded-full`, selected `bg-brand-tint text-brand border-brand/30`. Update its stories to the new states.
- disclosure-button: pure class swap per §3 table; keep all aria-expanded/controls behavior.
- star-rating: filled stars `text-gold`, empty `text-ink-faint/40`.
- price: serif money + mono unit pattern — amount `font-display`, unit/compare text `font-mono text-[11px] text-ink-faint`; keep all props (discount/compare-at rendering logic untouched), discount/savings text `text-brand`.
  </action>
  <verify>
    <automated>pnpm lint:tailwind && pnpm lint && pnpm typecheck && pnpm test:contracts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-full" src/components/ui/quantity-stepper/quantity-stepper.tsx` returns >= 1; `grep -c "size-11" src/components/ui/quantity-stepper/quantity-stepper.tsx` returns >= 1
    - `grep -c "text-gold" src/components/ui/star-rating/star-rating.tsx` returns >= 1
    - `grep -rln "bg-surface\|border-default\|border-strong\|text-subtle\|text-muted\|text-default\|text-accent\b\|action-" src/components/ui/quantity-stepper/ src/components/ui/icon-button/ src/components/ui/toggle-button/ src/components/ui/disclosure-button/ src/components/ui/star-rating/ src/components/ui/price/` returns no files
    - `pnpm test:contracts` exits 0 (icon-button touch-target strings intact)
  </acceptance_criteria>
  <done>Control primitives on new tokens with touch targets preserved; contracts green</done>
</task>

<task type="auto">
  <name>Task 3: Containers — card, accordion, dialog, article-card, rich-text, newsletter-signup</name>
  <files>src/components/ui/card/card.tsx, src/components/ui/card/card.stories.tsx, src/components/ui/accordion/accordion.tsx, src/components/ui/accordion/accordion.stories.tsx, src/components/ui/dialog/dialog.tsx, src/components/ui/dialog/dialog.stories.tsx, src/components/ui/article-card/article-card.tsx, src/components/ui/rich-text/rich-text.tsx, src/components/ui/rich-text/rich-text.stories.tsx, src/components/ui/newsletter-signup/newsletter-signup.tsx</files>
  <read_first>each file being modified (newsletter-signup especially: identify the Server Action/honeypot/aria-live wiring that must NOT change), 11-UI-SPEC.md §4 Card + §5.3 newsletter row, design/extracted/design-system.css .card-surface/.faq__item/.news blocks, scripts/component-contracts/button-system.test.mjs motion-reduce assertions</read_first>
  <action>
- card: `.card-surface` → `bg-card border border-hairline-2 rounded-lg`; swap remaining old tokens per §3 table; refresh stories.
- accordion: mockup FAQ/spec-acc treatment — rows `border-t border-hairline`, trigger `font-display text-[1.15rem]`, open-state chevron `text-brand` rotate; keep all disclosure aria behavior. Refresh stories.
- dialog: panel `bg-paper rounded-lg shadow-4`, scrim `bg-ink/35 backdrop-blur-[2px]`; keep focus-trap/close behavior. Refresh stories.
- article-card (.jcard): media `rounded-lg` with hover zoom — PRESERVE the exact trio `motion-reduce:transform-none motion-reduce:transition-none motion-reduce:group-hover:scale-100` on the `group-hover:scale` line (contract-tested); meta row `type-mono-meta` with `text-brand` category; title `font-display text-[1.25rem]`.
- rich-text: class-map swap only per §3 table (e.g. `text-default`→`text-ink`, `text-muted`→`text-ink-soft`, `border-default`→`border-hairline`, link classes → `text-brand hover:text-brand-deep`); rich-text.stories.tsx is the single largest old-class consumer (142 occurrences) — sweep it mechanically with the same table.
- newsletter-signup: restyle to the mockup pill-input pattern — on light: `bg-card border border-hairline rounded-full px-4.5 py-3.5 placeholder:text-ink-faint focus:border-brand`; if the component has a dark/footer variant prop, dark fields use `bg-paper/5 border-paper/20 text-paper placeholder:text-paper/60 focus:border-gold`. DO NOT touch the action prop, honeypot field, pending/success/error states, or aria-live announcements — markup classes only.
  </action>
  <verify>
    <automated>pnpm lint:tailwind && pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "motion-reduce:group-hover:scale-100" src/components/ui/article-card/article-card.tsx` returns >= 1
    - `grep -c "border-hairline-2" src/components/ui/card/card.tsx` returns >= 1
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|border-subtle\|type-display-0" src/components/ui/card/ src/components/ui/accordion/ src/components/ui/dialog/ src/components/ui/article-card/ src/components/ui/rich-text/ src/components/ui/newsletter-signup/` returns no files
    - newsletter-signup still contains its honeypot field and action wiring (grep the identifiers found in read_first — e.g. `grep -c "action" src/components/ui/newsletter-signup/newsletter-signup.tsx` >= 1)
    - `pnpm test:contracts && pnpm test:unit` exit 0
  </acceptance_criteria>
  <done>Container primitives restyled, motion-reduce trio intact, newsletter behavior untouched, zero old tokens in owned files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → newsletter form | untrusted input enters via the form this plan restyles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering/DoS | newsletter-signup | mitigate | restyle markup only; honeypot field, rate-limited Server Action, and validation wiring asserted by acceptance grep + unit tests |
| T-11-07 | Elevation (XSS) | rich-text | mitigate | class-string swap only; no change to sanitization or HTML rendering plumbing; no dangerouslySetInnerHTML introduced |
</threat_model>

<verification>
- `pnpm lint:tailwind`, `pnpm lint`, `pnpm typecheck`, `pnpm test:contracts`, `pnpm test:unit` all exit 0
- Storybook spot check of fields, stepper, dialog, article-card
</verification>

<success_criteria>
- Every primitive in files_modified uses only new tokens; behavior (aria, actions, honeypot, touch targets, motion-reduce) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-03-SUMMARY.md`
</output>
