---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 10
type: execute
wave: 7
depends_on: [17-09]
files_modified:
  - scripts/performance/probe-lighthouse.mjs
  - scripts/performance/probe-lighthouse.test.mjs
  - scripts/launch/run-final-readiness-audit.mjs
  - scripts/launch/run-final-readiness-audit.test.mjs
  - tests/mocks/run-next-production-server.mjs
  - src/app/layout.tsx
  - src/app/(storefront)/layout.tsx
  - src/components/layout/header/header.tsx
  - src/components/homepage/hero/hero.tsx
  - src/components/product/product-gallery/product-gallery.tsx
  - src/components/collection/product-card/product-card.tsx
  - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
  - src/app/(storefront)/cart/page.tsx
  - src/app/(storefront)/search/page.tsx
  - src/app/(storefront)/account/layout.tsx
  - src/app/(storefront)/account/loading.tsx
  - src/app/(storefront)/account/login/page.tsx
  - src/app/(storefront)/pages/privacy-policy/page.tsx
  - next.config.ts
  - docs/launch/performance-evidence.md
  - docs/launch/performance-acceptance.md
  - docs/launch/final-production-readiness-report.md
  - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
autonomous: false
requirements:
  - PERF-01
  - UX-01
  - QA-02
must_haves:
  truths:
    - "PERF-01: Home and PDP Lighthouse/Core Web Vitals evidence no longer shows launch-blocking LCP regressions, or valid dated owner/staging/field evidence accepts local lab failures as non-blocking"
    - "UX-01: existing mobile wrapping, skip-link, and account/cart/search accessibility polish remains non-regressed"
    - "QA-02: the final readiness audit stays honest and cannot print 100/100 while unaccepted required performance failures remain"
    - "D-15: performance blocking uses practical mobile Lighthouse/Web Vitals gates with LCP, CLS, INP/TBT evidence and documented mitigation"
    - "D-17: automated code readiness and owner-gated launch proof remain separate in the final report"
  artifacts:
    - path: "docs/launch/performance-evidence.md"
      provides: "Strict local lab route metrics plus timing, resource, and diagnostic evidence for PERF-01"
    - path: "docs/launch/performance-acceptance.md"
      provides: "Optional dated owner/staging/field acceptance artifact when strict local lab failures are accepted as non-blocking"
    - path: "docs/launch/final-production-readiness-report.md"
      provides: "Final launch-readiness decision after performance gap closure"
  key_links:
    - from: "scripts/performance/probe-lighthouse.mjs"
      to: "docs/launch/performance-evidence.md"
      via: "route metrics, LCP diagnostics, timing diagnostics, and warmed asset evidence"
    - from: "docs/launch/performance-acceptance.md"
      to: "scripts/launch/run-final-readiness-audit.mjs"
      via: "explicit dated acceptance gate only when local performance remains red"
    - from: "docs/launch/final-production-readiness-report.md"
      to: ".planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md"
      via: "post-gap verification truth update"
---

<objective>
Close the remaining Phase 17 PERF-01 gap after plans 17-08 and 17-09 left strict local performance red. This plan must determine whether the current local Lighthouse failures are caused by cold Next image transforms, font/shared-shell resource cost, true route regressions, or a lab/staging mismatch; then it must either produce strict green Home/PDP launch evidence or require a valid dated owner/staging/field Core Web Vitals acceptance artifact before final readiness can pass.
</objective>

<execution_context>
@AGENTS.md
@node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
@node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md
@node_modules/next/dist/docs/01-app/02-guides/production-checklist.md
@node_modules/next/dist/docs/01-app/01-getting-started/06-fetching-data.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-08-SUMMARY.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-09-SUMMARY.md
@docs/launch/performance-evidence.md
@docs/launch/final-production-readiness-report.md
@scripts/performance/probe-lighthouse.mjs
@scripts/performance/probe-lighthouse.test.mjs
@scripts/launch/run-final-readiness-audit.mjs
@scripts/launch/run-final-readiness-audit.test.mjs
@tests/mocks/run-next-production-server.mjs
@src/app/layout.tsx
@src/app/(storefront)/layout.tsx
@src/components/layout/header/header.tsx
@src/components/homepage/hero/hero.tsx
@src/components/product/product-gallery/product-gallery.tsx
@src/components/collection/product-card/product-card.tsx
@src/app/(storefront)/collections/[handle]/_components/product-list.tsx
@src/app/(storefront)/cart/page.tsx
@src/app/(storefront)/search/page.tsx
@src/app/(storefront)/account/layout.tsx
@src/app/(storefront)/account/loading.tsx
@src/app/(storefront)/account/login/page.tsx
@src/app/(storefront)/pages/privacy-policy/page.tsx
@next.config.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Make local performance evidence repeatable and diagnostic enough to act on</name>
  <files>scripts/performance/probe-lighthouse.mjs, scripts/performance/probe-lighthouse.test.mjs, tests/mocks/run-next-production-server.mjs, docs/launch/performance-evidence.md</files>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs
    - scripts/performance/probe-lighthouse.test.mjs
    - tests/mocks/run-next-production-server.mjs
    - docs/launch/performance-evidence.md
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md
    - node_modules/lighthouse/core/config/default-config.js
  </read_first>
  <action>
    Extend the performance probe so the next remediation is based on timing evidence, not only the LCP element selector:

    - Fix non-Windows process cleanup in `stopChild()` so `scripts/performance/probe-lighthouse.mjs` waits for child exit after `SIGTERM`, escalates to `SIGKILL` after 5000ms if needed, and cannot start a second lifecycle against stale `next start` or fake-provider ports.
    - Apply the same process-tree cleanup pattern to `tests/mocks/run-next-production-server.mjs`: spawn the child detached on non-Windows, kill the process group on shutdown, keep the Windows `taskkill /T /F` behavior, and wait for exit before the wrapper exits.
    - Keep existing `--warmup-runs` semantics, but upgrade warmup from "HTML only" to "HTML plus discovered same-origin assets". For every warmed route, fetch the route HTML and then fetch same-origin URLs found in `src`, `srcset`, and preload `href` attributes when they are under `/_next/image`, `/_next/static`, `/images/`, or `/_next/font`. Do not fetch cross-origin URLs. Record the count of warmed asset URLs.
    - Add a `--no-asset-warmup` flag for cold diagnostics; default asset warmup remains on when `--warmup-runs` is greater than `0`.
    - Add route timing fields to each summarized row: `fcpMs` from `first-contentful-paint`, `speedIndexMs` from `speed-index`, `ttfbMs` from `server-response-time`, and `totalByteWeight` from `total-byte-weight` when Lighthouse exposes them.
    - Extract the Lighthouse `lcp-breakdown-insight` values into a nullable `lcpBreakdown` object with keys `ttfbMs`, `resourceLoadDelayMs`, `resourceLoadDurationMs`, and `elementRenderDelayMs` when present.
    - Extract `layout-shifts` details for the `/account` row into `layoutShiftSources`, preserving `selector`, `nodeLabel`, and `score` when Lighthouse exposes them.
    - Render a new `## Timing Diagnostics` section in `docs/launch/performance-evidence.md` with columns `Route`, `FCP`, `LCP`, `TTFB`, `Speed Index`, `Bytes`, and `Primary Cause`. The primary cause must be one of `image-resource`, `server-response`, `render-delay`, `layout-shift`, `text-render`, or `unknown`, derived from the row diagnostics.
    - Extend JSON summary route objects with `fcpMs`, `speedIndexMs`, `ttfbMs`, `totalByteWeight`, `lcpBreakdown`, `layoutShiftSources`, and `warmedAssetCount`.
    - Add tests for same-origin asset discovery from `src`, `srcset`, and preload `href`, `--no-asset-warmup`, JSON timing fields, LCP breakdown extraction, layout-shift extraction, and non-Windows lifecycle exit waiting using injectable child/process helpers rather than sleeping in tests.
  </action>
  <verify>
    <automated>node --test scripts/performance/probe-lighthouse.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/performance/probe-lighthouse.mjs` contains `--no-asset-warmup`.
    - `scripts/performance/probe-lighthouse.mjs` contains `server-response-time`.
    - `scripts/performance/probe-lighthouse.mjs` contains `first-contentful-paint`.
    - `scripts/performance/probe-lighthouse.mjs` contains `lcp-breakdown-insight`.
    - `scripts/performance/probe-lighthouse.mjs` contains `layout-shifts`.
    - `scripts/performance/probe-lighthouse.test.mjs` asserts JSON summary rows include `fcpMs`, `speedIndexMs`, `ttfbMs`, `totalByteWeight`, `lcpBreakdown`, `layoutShiftSources`, and `warmedAssetCount`.
    - `tests/mocks/run-next-production-server.mjs` no longer relies only on `activeChild.kill()` for shutdown.
  </acceptance_criteria>
  <done>The executor can distinguish image-transform cold starts, shared shell/font cost, server response delay, text render delay, and account CLS causes before changing UI code.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce shared launch shell, font, and image-transform cost</name>
  <files>src/app/layout.tsx, src/app/(storefront)/layout.tsx, src/components/layout/header/header.tsx, src/components/homepage/hero/hero.tsx, src/components/product/product-gallery/product-gallery.tsx, src/components/collection/product-card/product-card.tsx, src/app/(storefront)/collections/[handle]/_components/product-list.tsx, next.config.ts, docs/launch/performance-evidence.md</files>
  <read_first>
    - docs/launch/performance-evidence.md
    - scripts/performance/probe-lighthouse.mjs
    - src/app/layout.tsx
    - src/app/(storefront)/layout.tsx
    - src/components/layout/header/header.tsx
    - src/components/homepage/hero/hero.tsx
    - src/components/product/product-gallery/product-gallery.tsx
    - src/components/collection/product-card/product-card.tsx
    - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
    - src/app/globals.css
    - next.config.ts
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md
    - node_modules/next/dist/docs/01-app/02-guides/production-checklist.md
  </read_first>
  <action>
    Run the upgraded probe from Task 1 first:

    ```bash
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary --allow-metric-failures
    ```

    Then apply measured fixes in this order:

    1. If image-route LCP (`/`, `/products/test-standard-tea`, `/collections/all`) improves after asset warmup compared with `--no-asset-warmup`, keep asset warmup as the strict local default and document the warmed asset counts in `docs/launch/performance-evidence.md`. Do not mark performance green merely because cold image transforms are noisy; the warmed strict run still has to pass or be accepted by dated evidence.
    2. In `src/app/layout.tsx`, reduce global font preload cost:
       - Keep `Hanken_Grotesk` loaded globally as the primary sans font.
       - Keep `Spectral` globally available, but reduce it to the weights and styles actually needed above the fold: `weight: ['400', '500']`, `style: ['normal']`, `display: 'swap'`.
       - Set `preload: false` on `Space_Mono`.
       - Set `preload: false` on `Caveat`.
       - Preserve the existing CSS variable names `--font-spectral`, `--font-hanken-grotesk`, `--font-space-mono`, and `--font-caveat` so Tailwind tokens keep working.
    3. If Task 1 timing diagnostics identify the header/search/menu bundle as a shared render or byte-weight contributor, split `src/components/layout/header/header.tsx` so the server-rendered logo/nav shell stays in the header file and interactive search/mobile/cart leaves remain client components in separate files. Do not put `'use client'` on `src/app/(storefront)/layout.tsx`.
    4. For the home hero image, compare the current `loading="eager"` plus `fetchPriority="high"` strategy with a Next 16-supported `preload={true}` strategy. Keep exactly one supported strategy; do not combine `preload` with `loading` or `fetchPriority`, and do not reintroduce deprecated `priority`.
    5. For PDP and collection image LCP, keep only the first above-the-fold image eager/high priority. If diagnostics show the local fake image transform is the cause, prefer warming the transformed `/_next/image` URL and tightening `sizes` over lowering product image quality below `68`.
    6. Do not add raw hex/rgb classes, inline styles, CSS modules, parent-level `'use client'` wrappers, or className string concatenation.
  </action>
  <verify>
    <automated>pnpm lint -- src/app/layout.tsx "src/app/(storefront)/layout.tsx" src/components/layout/header/header.tsx src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx src/components/collection/product-card/product-card.tsx "src/app/(storefront)/collections/[handle]/_components/product-list.tsx" next.config.ts</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` keeps `--font-space-mono` and `--font-caveat` variables but sets `preload: false` for both.
    - `src/app/layout.tsx` does not globally preload italic Spectral faces unless a measured route requires them above the fold.
    - `src/components/homepage/hero/hero.tsx` uses exactly one Next 16 LCP loading strategy for the hero image.
    - `ProductGallery` and `ProductCard` still make only the first above-the-fold image eager/high priority.
    - No changed component adds `style={{}}`, raw hex/rgb Tailwind classes, or className string concatenation.
  </acceptance_criteria>
  <done>Shared route cost and LCP image-transform noise are reduced or explicitly documented with measured diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Fix route-specific LCP and CLS blockers that remain after shared-shell work</name>
  <files>src/app/(storefront)/cart/page.tsx, src/app/(storefront)/search/page.tsx, src/app/(storefront)/account/layout.tsx, src/app/(storefront)/account/loading.tsx, src/app/(storefront)/account/login/page.tsx, src/app/(storefront)/pages/privacy-policy/page.tsx, docs/launch/performance-evidence.md</files>
  <read_first>
    - docs/launch/performance-evidence.md
    - scripts/performance/probe-lighthouse.mjs
    - src/app/(storefront)/cart/page.tsx
    - src/app/(storefront)/cart/_components/loading-skeleton.tsx
    - src/app/(storefront)/cart/_components/view.tsx
    - src/app/(storefront)/search/page.tsx
    - src/components/search/search-results-view/search-results-view.tsx
    - src/app/(storefront)/account/layout.tsx
    - src/app/(storefront)/account/loading.tsx
    - src/app/(storefront)/account/login/page.tsx
    - src/app/(storefront)/pages/privacy-policy/page.tsx
    - node_modules/next/dist/docs/01-app/01-getting-started/06-fetching-data.md
  </read_first>
  <action>
    Re-run the strict probe after Task 2. For each route that still has `status: FAIL`, use the Task 1 primary cause:

    - `/cart`: If the LCP element remains the empty-cart explanatory paragraph, do not edit images. If `ttfbMs` is high, keep the empty-cart shell above the Suspense boundary and move only cart-specific data work behind the boundary. If `fcpMs` is high with low TTFB, simplify above-the-fold empty-cart markup without changing copy semantics.
    - `/search?q=tea`: If LCP remains the trust-strip paragraph, avoid provider or image changes. If the result fetch delays the hero shell, render the search hero outside the async result boundary and stream results below it. Keep `searchParams: Promise<SearchParamsInput>` and await only inside the async leaf.
    - `/account`: If CLS remains above `0.1`, use `layoutShiftSources` to reserve the exact shifting region. The login bridge observed at `/account/login?returnTo=%2Faccount` must keep stable min-height across layout, page, and loading states.
    - `/pages/privacy-policy`: If the policy copy paragraph remains a text LCP with low TTFB and low byte weight, do not create cosmetic content churn. Record it as text-render lab evidence and rely on Task 4 for strict pass or dated acceptance. If TTFB is high, inspect whether root layout or legal policy helpers are forcing dynamic work and remove only the measured blocker.
    - Keep all route changes server-first unless the file is already an interactive leaf.
  </action>
  <verify>
    <automated>pnpm lint -- "src/app/(storefront)/cart/page.tsx" "src/app/(storefront)/search/page.tsx" "src/app/(storefront)/account/layout.tsx" "src/app/(storefront)/account/loading.tsx" "src/app/(storefront)/account/login/page.tsx" "src/app/(storefront)/pages/privacy-policy/page.tsx"</automated>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
    <automated>pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary --allow-metric-failures</automated>
  </verify>
  <acceptance_criteria>
    - `docs/launch/performance-evidence.md` includes `## Timing Diagnostics`.
    - Any remaining `/cart`, `/search?q=tea`, `/account`, or `/pages/privacy-policy` failure names a primary cause, not generic image advice.
    - `/account` CLS is `<= 0.1`, or the evidence names the exact remaining shifting selector/source.
    - No route-specific edit changes owner-gated Shopify, checkout, payment, tax, order, OAuth, protected-data, B2B, or Search Console behavior.
  </acceptance_criteria>
  <done>Non-image route failures are either remediated with route-specific changes or documented as local lab evidence requiring acceptance.</done>
</task>

<task type="manual">
  <name>Task 4: Choose the valid PERF-01 closure path</name>
  <files>docs/launch/performance-acceptance.md, scripts/launch/run-final-readiness-audit.mjs, scripts/launch/run-final-readiness-audit.test.mjs, docs/launch/performance-evidence.md</files>
  <read_first>
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
    - docs/launch/performance-evidence.md
    - docs/launch/final-production-readiness-report.md
    - scripts/launch/run-final-readiness-audit.mjs
    - scripts/launch/run-final-readiness-audit.test.mjs
  </read_first>
  <action>
    After Tasks 1-3, choose exactly one closure path:

    **Path A - strict green evidence:**
    - If `/` and `/products/test-standard-tea` pass strict local LCP and the final audit performance command exits `0`, do not create `docs/launch/performance-acceptance.md`.
    - Keep `performance` as an ordinary required automated `PASS` row in the final report.

    **Path B - dated acceptance evidence:**
    - If strict local rows still fail, stop for human input before treating performance as non-blocking.
    - Proceed only if the user supplies dated owner, staging, or field Core Web Vitals evidence.
    - Create `docs/launch/performance-acceptance.md` with these exact headings and fields:

    ```markdown
    # Performance Acceptance

    ## Decision
    Status: accepted-non-blocking
    Date: YYYY-MM-DD
    Approver: [owner/staging authority name]
    Evidence source: [staging Lighthouse URL, CrUX/Core Web Vitals export, RUM dashboard, or owner-approved monitoring artifact]

    ## Local Lab Failures Accepted
    - `/`: [accepted or not applicable with reason]
    - `/products/test-standard-tea`: [accepted or not applicable with reason]
    - `/collections/all`: [accepted or not applicable with reason]
    - `/cart`: [accepted or not applicable with reason]
    - `/search?q=tea`: [accepted or not applicable with reason]
    - `/account`: [accepted or not applicable with reason]
    - `/pages/privacy-policy`: [accepted or not applicable with reason]

    ## Rationale
    [Why local lab evidence is non-blocking for launch.]
    ```

    - If `scripts/launch/run-final-readiness-audit.mjs` does not already validate this format, add validation requiring `Status: accepted-non-blocking`, an ISO date, a non-empty approver, and a non-empty evidence source.
    - The final report may render performance as `PASS` with evidence starting exactly `Accepted non-blocking by dated performance acceptance:` only when the file validates and the audit was run with `--performance-acceptance docs/launch/performance-acceptance.md`.

    **Path C - no closure yet:**
    - If neither strict evidence nor valid acceptance exists, leave performance as `FAIL`, leave final readiness `Not launch-ready`, and keep Phase 17 `gaps_found`.

    Never invent an approver, date, or evidence source.
  </action>
  <verify>
    <automated>node --test scripts/launch/run-final-readiness-audit.test.mjs</automated>
    <automated>node -e "const fs=require('fs'); const p='docs/launch/performance-acceptance.md'; if(fs.existsSync(p)){const s=fs.readFileSync(p,'utf8'); for (const needle of ['Status: accepted-non-blocking','Date:','Approver:','Evidence source:']) if(!s.includes(needle)) throw new Error('missing '+needle)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - No final report contains `Accepted non-blocking by dated performance acceptance:` unless `docs/launch/performance-acceptance.md` exists and validates.
    - If no acceptance file exists and strict performance exits `1`, `pnpm audit:readiness` still reports performance `FAIL`.
    - Tests cover valid acceptance, missing date, missing approver, missing evidence source, and no-flag default blocking behavior.
  </acceptance_criteria>
  <done>PERF-01 has a valid closure path or remains honestly blocked.</done>
</task>

<task type="auto">
  <name>Task 5: Regenerate final readiness evidence and Phase 17 verification</name>
  <files>docs/launch/performance-evidence.md, docs/launch/final-production-readiness-report.md, .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md</files>
  <read_first>
    - docs/launch/performance-evidence.md
    - docs/launch/performance-acceptance.md
    - docs/launch/final-production-readiness-report.md
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
    - scripts/performance/probe-lighthouse.mjs
    - scripts/launch/run-final-readiness-audit.mjs
  </read_first>
  <action>
    Regenerate evidence from real command output:

    ```bash
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
    pnpm audit:readiness
    ```

    If Task 4 created a valid acceptance artifact, run the final audit with the explicit acceptance flag:

    ```bash
    pnpm audit:readiness -- --performance-acceptance docs/launch/performance-acceptance.md
    ```

    Then update `.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md`:

    - If final report has `100/100`, no required automated failures, no required skips, and performance is `PASS` by strict evidence or valid acceptance, set frontmatter `status: verified`, update `score` to `9/9 phase truths verified; automated code readiness green`, and clear `gaps_remaining`.
    - If performance still fails without valid acceptance, keep `status: gaps_found`, update `verified` to the current timestamp, keep `PERF-01 strict performance blocker` in `gaps_remaining`, and quote the current report/evidence status.
    - Keep all owner-gated Shopify/admin/Search Console rows separate as `approved`, `pending`, or `owner-blocked`; do not convert pending owner evidence into an automated code failure.
  </action>
  <verify>
    <automated>pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary</automated>
    <automated>pnpm audit:readiness</automated>
    <automated>gsd-sdk query verify.schema-drift "17"</automated>
    <automated>node -e "const fs=require('fs');const report=fs.readFileSync('docs/launch/final-production-readiness-report.md','utf8');const verification=fs.readFileSync('.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md','utf8'); if(report.includes('| performance | FAIL |') && !verification.includes('status: gaps_found')) throw new Error('verification must stay gaps_found while performance fails'); if(report.includes('100/100') && verification.includes('status: gaps_found')) throw new Error('verification still gaps_found despite 100/100 report'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/launch/performance-evidence.md` includes Home and PDP rows plus `## Timing Diagnostics`.
    - `docs/launch/final-production-readiness-report.md` does not say `100/100` while an unaccepted `| performance | FAIL |` row exists.
    - `17-VERIFICATION.md` status matches the final report launch decision.
    - `gsd-sdk query verify.schema-drift "17"` reports no blocking schema drift.
  </acceptance_criteria>
  <done>Phase 17 either verifies cleanly or preserves the exact remaining PERF-01 blocker with current evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local Lighthouse lab evidence to launch decision | Local lab measurements can be noisy, but failures must not be hidden without strict pass evidence or dated acceptance |
| Warmed local assets to real-user performance | Warmup may remove local Next image-transform noise, but it is not field Core Web Vitals proof |
| Performance acceptance to automated score | Acceptance must require explicit dated owner/staging/field evidence |
| Shared layout/font/header changes to storefront behavior | Performance work must preserve navigation, accessibility, consent, analytics gates, and owner-gated commerce boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-17-25 | Repudiation | performance evidence | mitigate | Add timing diagnostics, warmed asset counts, and primary-cause labels to evidence |
| T-17-26 | Tampering | readiness score | mitigate | Keep performance FAIL blocking unless strict evidence passes or acceptance validates |
| T-17-27 | Denial of Service | local performance lifecycle | mitigate | Wait for child process exit and kill process trees before starting new probes |
| T-17-28 | Information Disclosure | performance and final reports | mitigate | Do not expose secrets, cart IDs, checkout URLs, order IDs, customer PII, raw provider payloads, or owner-gated proof values |

</threat_model>

<verification>

```bash
node --test scripts/performance/probe-lighthouse.test.mjs
node --test scripts/launch/run-final-readiness-audit.test.mjs
pnpm lint -- src/app/layout.tsx "src/app/(storefront)/layout.tsx" src/components/layout/header/header.tsx src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx src/components/collection/product-card/product-card.tsx "src/app/(storefront)/collections/[handle]/_components/product-list.tsx" "src/app/(storefront)/cart/page.tsx" "src/app/(storefront)/search/page.tsx" "src/app/(storefront)/account/layout.tsx" "src/app/(storefront)/account/loading.tsx" "src/app/(storefront)/account/login/page.tsx" "src/app/(storefront)/pages/privacy-policy/page.tsx" next.config.ts
pnpm typecheck
pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts
pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
pnpm audit:readiness
gsd-sdk query verify.schema-drift "17"
```

</verification>

<success_criteria>

- Home and PDP no longer show launch-blocking local LCP regressions, or a valid dated owner/staging/field acceptance artifact marks local lab failures non-blocking.
- Performance evidence explains remaining failures with timing diagnostics and primary causes instead of generic image advice.
- Local performance lifecycle cleanup does not leave stale servers or fake providers across sequential probes.
- The final readiness report cannot claim `100/100` while performance remains unaccepted and failed.
- Phase 17 verification status follows the regenerated final report.

</success_criteria>

<output>

- Upgraded performance diagnostics and asset warmup.
- Shared font/layout/image remediation or measured evidence that a manual acceptance path is required.
- Optional validated performance acceptance artifact.
- Regenerated performance evidence, final readiness report, and Phase 17 verification.

</output>
