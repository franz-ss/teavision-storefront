---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 06
type: execute
wave: 4
depends_on: [17-04, 17-05]
files_modified:
  - scripts/performance/probe-lighthouse.mjs
  - scripts/performance/probe-lighthouse.test.mjs
  - scripts/launch/run-final-readiness-audit.mjs
  - scripts/launch/run-final-readiness-audit.test.mjs
  - docs/launch/performance-evidence.md
  - docs/launch/final-production-readiness-report.md
  - package.json
autonomous: true
requirements:
  - PERF-01
  - QA-02
must_haves:
  truths:
    - "PERF-01: Home and PDP Lighthouse/Core Web Vitals evidence no longer shows launch-blocking LCP regressions, or an explicit dated owner/staging/field-data exception marks the local lab failures non-blocking"
    - "QA-02: final readiness audit fails when required automated checks are skipped or when performance metric rows include FAIL, unless an explicit accepted non-blocking performance exception is present"
    - "D-15: performance blocking uses practical mobile Lighthouse/Web Vitals gates with LCP, CLS, INP/TBT evidence and documented mitigations"
    - "D-17: final 100/100 readiness report separates automated code readiness from owner-gated launch proof without excluding required automated checks from the denominator"
  artifacts:
    - path: "scripts/performance/probe-lighthouse.mjs"
      provides: "Readiness-grade Lighthouse/Web Vitals evidence command that exits nonzero on metric blockers by default"
    - path: "scripts/launch/run-final-readiness-audit.mjs"
      provides: "Final audit runner that owns required local live-server checks and scores performance metrics semantically"
    - path: "docs/launch/final-production-readiness-report.md"
      provides: "Honest final readiness report with no skipped required automated checks in a 100/100 result"
  key_links:
    - from: "scripts/launch/run-final-readiness-audit.mjs"
      to: "scripts/performance/probe-lighthouse.mjs"
      via: "performance metric blocker status"
    - from: "scripts/launch/run-final-readiness-audit.mjs"
      to: "scripts/security/probe-production-security.mjs"
      via: "owned local production server lifecycle for required live-header probe"
    - from: "scripts/launch/run-final-readiness-audit.mjs"
      to: "scripts/seo/probe-launch-seo.mjs"
      via: "owned local production server lifecycle for required enabled/disabled SEO probes"
---

<objective>
Close the Phase 17 verification gaps by making performance and final readiness evidence launch-blocking instead of hollow. This plan changes the performance probe and final audit so current Home/PDP LCP failures and skipped header/SEO live probes cannot still produce a 100/100 readiness result.
</objective>

<execution_context>
@AGENTS.md
@node_modules/next/dist/docs/01-app/02-guides/production-checklist.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-04-SUMMARY.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-05-SUMMARY.md
@scripts/performance/probe-lighthouse.mjs
@scripts/performance/probe-lighthouse.test.mjs
@scripts/launch/run-final-readiness-audit.mjs
@scripts/launch/run-final-readiness-audit.test.mjs
@scripts/security/probe-production-security.mjs
@scripts/seo/probe-launch-seo.mjs
@docs/launch/performance-evidence.md
@docs/launch/final-production-readiness-report.md
@playwright.production.config.ts
@tests/mocks/run-next-production-server.mjs
@package.json
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Make performance metric failures block readiness by default</name>
  <files>scripts/performance/probe-lighthouse.mjs, scripts/performance/probe-lighthouse.test.mjs, package.json, docs/launch/performance-evidence.md</files>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs
    - scripts/performance/probe-lighthouse.test.mjs
    - docs/launch/performance-evidence.md
    - package.json
  </read_first>
  <action>
    Update `scripts/performance/probe-lighthouse.mjs` so metric rows are readiness semantics, not just evidence text:

    - Add `--allow-metric-failures` with default `false`.
    - Keep `--stdout-only`, `--start-server`, `--base-url`, and repeated `--url` behavior unchanged.
    - After rows are produced, compute `blockingRows = rows.filter((row) => row.status === 'FAIL')`.
    - If `blockingRows.length > 0` and `allowMetricFailures` is `false`, set `process.exitCode = 1` after writing/printing the evidence.
    - Print a clear line exactly `Performance readiness failed: ${blockingRows.length} route(s) have FAIL metrics.` before exiting nonzero.
    - Preserve `WARN` rows as nonzero only when the row also has `status === 'FAIL'`; inconclusive WARN evidence remains a warning unless the final audit elects to block it separately.
    - Add `--json-summary` that prints one JSON object after the markdown table with fields `total`, `pass`, `warn`, `fail`, `blocking`, and `routes`. The `routes` array must contain `{ route, status, lcpMs, cls, tbtMs }` for each row.
    - Update tests to cover argument parsing for both new flags, nonzero readiness behavior when a row is `FAIL`, zero exit behavior with `--allow-metric-failures`, and the JSON summary shape.

    Keep `pnpm test:performance` mapped to the strict default command. If a developer needs evidence-only local diagnostics, document the exact command `pnpm test:performance -- --allow-metric-failures`.
  </action>
  <verify>
    <automated>node --test scripts/performance/probe-lighthouse.test.mjs</automated>
    <automated>node -e "import('./scripts/performance/probe-lighthouse.mjs').then((m)=>{const a=m.parseArgs(['--allow-metric-failures','--json-summary']); if(!a.allowMetricFailures || !a.jsonSummary) throw new Error('missing new flags'); console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/performance/probe-lighthouse.mjs` contains `--allow-metric-failures`.
    - `scripts/performance/probe-lighthouse.mjs` contains `Performance readiness failed:`.
    - `scripts/performance/probe-lighthouse.mjs` contains `--json-summary`.
    - `scripts/performance/probe-lighthouse.test.mjs` asserts strict mode fails on at least one `FAIL` row.
    - `package.json` still contains `"test:performance": "node scripts/performance/probe-lighthouse.mjs"`.
  </acceptance_criteria>
  <done>PERF-01 can no longer appear passed merely because the Lighthouse command generated evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Make the final audit own required live-server probes</name>
  <files>scripts/launch/run-final-readiness-audit.mjs, scripts/launch/run-final-readiness-audit.test.mjs, docs/launch/final-production-readiness-report.md</files>
  <read_first>
    - scripts/launch/run-final-readiness-audit.mjs
    - scripts/launch/run-final-readiness-audit.test.mjs
    - scripts/performance/probe-lighthouse.mjs
    - scripts/security/probe-production-security.mjs
    - scripts/seo/probe-launch-seo.mjs
    - tests/mocks/run-next-production-server.mjs
    - playwright.production.config.ts
  </read_first>
  <action>
    Change `scripts/launch/run-final-readiness-audit.mjs` so required automated live-server checks are run against an owned local production-like server by default:

    - Add `--start-server`, defaulting to `true`, and `--no-start-server`, setting it to `false`.
    - Reuse or extract the same fake-provider production lifecycle used by `scripts/performance/probe-lighthouse.mjs`: fake Shopify, fake Customer Account API, `corepack pnpm exec next build`, then `corepack pnpm exec next start -p ${PORT}` with `PLAYWRIGHT_PRODUCTION_TEST_MODE=true`, `DISABLE_INDEXING=true`, `NEXT_PUBLIC_ANALYTICS_MODE=fake`, `SHOPIFY_STOREFRONT_TEST_MODE=true`, `SHOPIFY_CUSTOMER_ACCOUNT_TEST_MODE=true`, and the same fake-provider URLs.
    - Start that lifecycle before running the matrix whenever any required check has `requiresBaseUrl: true` or the `performance` check needs `--start-server`.
    - Remove the preflight skip behavior for `security headers`, `seo disabled`, and `seo enabled` in the default path. If the server cannot start or the base URL is unreachable after the owned lifecycle starts, those checks must be `FAIL`, not `SKIPPED`.
    - Allow `SKIPPED` only for explicit user flags such as `--skip-performance` or a future explicit skip reason. Required automated checks skipped because a local server was absent must not be excluded from a 100/100 score.
    - Make `performance` run the strict default command from Task 1. It must fail the final audit when any Lighthouse row is `FAIL`.
    - In report rendering, replace `14/14 non-skipped automated checks passed. 3 automated check(s) were skipped...` style language with text that distinguishes `required`, `optional`, and `explicitly skipped by flag`. Required automated checks must be in the denominator unless an explicit flag marks them skipped with rationale.
    - Add tests that simulate an unreachable base URL in default `startServer` mode and assert the three live-server checks become `FAIL`, not `SKIPPED`.
    - Add tests that simulate a failed `performance` result and assert the report launch decision says `Not launch-ready`.
  </action>
  <verify>
    <automated>node --test scripts/launch/run-final-readiness-audit.test.mjs</automated>
    <automated>node scripts/launch/run-final-readiness-audit.mjs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/launch/run-final-readiness-audit.mjs` contains `--no-start-server`.
    - Default audit behavior does not call `skippedResult` for `security headers`, `seo disabled`, or `seo enabled` solely because `http://127.0.0.1:4173` is initially unreachable.
    - `scripts/launch/run-final-readiness-audit.test.mjs` asserts unreachable required live-server probes are `FAIL` in default mode.
    - `scripts/launch/run-final-readiness-audit.test.mjs` asserts a failing `performance` result prevents a `100/100` launch-ready report.
    - `docs/launch/final-production-readiness-report.md` generated after this task cannot say `100/100` when performance rows are `FAIL`.
  </acceptance_criteria>
  <done>QA-02 can no longer pass by excluding required security/SEO probes or by treating failed performance metrics as command success.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run strict evidence and record the honest launch decision</name>
  <files>docs/launch/performance-evidence.md, docs/launch/final-production-readiness-report.md</files>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs
    - scripts/launch/run-final-readiness-audit.mjs
    - docs/launch/performance-evidence.md
    - docs/launch/final-production-readiness-report.md
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
  </read_first>
  <action>
    Run the strict commands and update evidence based on actual output:

    ```bash
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
    pnpm audit:readiness
    ```

    Expected first run before performance remediation may fail because current local Lighthouse evidence has seven `FAIL` rows. That is acceptable only if the generated docs say launch is blocked:

    - `docs/launch/performance-evidence.md` must still include route rows for `/` and `/products/test-standard-tea`.
    - If `/` or `/products/test-standard-tea` is `FAIL`, `docs/launch/performance-evidence.md` must include `Launch-blocking: yes` or `Launch-blocking: accepted non-blocking by <owner/date/evidence>`. Do not invent owner acceptance.
    - `docs/launch/final-production-readiness-report.md` must say `Not launch-ready` if either strict performance or required live-server checks fail.
    - The report must list security headers, SEO disabled, and SEO enabled as `PASS` or `FAIL`, not `SKIPPED`, unless an explicit CLI skip flag was used and the report names that flag.
    - If Home/PDP LCP are remediated to `PASS`, update the report to a `100/100` automated score only after strict `pnpm audit:readiness` exits 0.

    Do not run real Shopify hosted checkout, payment, shipping-rate, tax, order-creation, success-redirect, live Customer Account OAuth, protected customer data, B2B pricing, or Search Console approval tests. Owner-gated rows must remain `approved`, `pending`, or `owner-blocked`.
  </action>
  <verify>
    <automated>pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary</automated>
    <automated>pnpm audit:readiness</automated>
    <automated>node -e "const fs=require('fs');const report=fs.readFileSync('docs/launch/final-production-readiness-report.md','utf8'); if(report.includes('| security headers | SKIPPED |')) throw new Error('security headers skipped'); if(report.includes('| seo disabled | SKIPPED |')) throw new Error('seo disabled skipped'); if(report.includes('| seo enabled | SKIPPED |')) throw new Error('seo enabled skipped'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/launch/performance-evidence.md` includes Home and PDP rows with strict status.
    - `docs/launch/final-production-readiness-report.md` does not list `security headers`, `seo disabled`, or `seo enabled` as `SKIPPED` in a default run.
    - If any performance route remains `FAIL`, final report says `Not launch-ready` and does not print `100/100`.
    - If final report prints `100/100`, strict `pnpm audit:readiness` exited 0 with no `FAIL` and no required automated `SKIPPED` rows.
  </acceptance_criteria>
  <done>The final report becomes an honest source of truth for the remaining launch decision: either strict automated readiness passes, or the report blocks launch with named evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lighthouse evidence to readiness score | Metric FAIL rows must not be converted into a passing launch score by command-exit semantics |
| Audit runner to local server lifecycle | Required live-server probes must run against an owned, production-like local server or fail visibly |
| Owner-gated launch proof to automated code score | Owner-only Shopify/admin evidence remains separate and must not be fabricated to compensate for automated failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Repudiation | performance readiness scoring | mitigate | Strict default performance command exits nonzero on `FAIL` rows and exposes JSON summary counts |
| T-17-14 | Repudiation | final readiness audit denominator | mitigate | Required live-server checks are PASS/FAIL in default runs and cannot be excluded merely because the base URL was initially down |
| T-17-15 | Information disclosure | final audit child output | mitigate | Preserve existing output tail redaction and do not print `.env.local` values while starting owned local lifecycle |

</threat_model>

<verification>

```bash
node --test scripts/performance/probe-lighthouse.test.mjs
node --test scripts/launch/run-final-readiness-audit.test.mjs
node scripts/launch/run-final-readiness-audit.mjs --dry-run
pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
pnpm audit:readiness
pnpm lint -- scripts/performance/probe-lighthouse.mjs scripts/launch/run-final-readiness-audit.mjs
pnpm typecheck
```

</verification>

<success_criteria>

- Performance metric `FAIL` rows make the strict performance command and final readiness audit fail by default.
- Security headers, SEO disabled, and SEO enabled are PASS or FAIL in the default final report, not skipped because no server was already running.
- The final report prints `100/100` only when all required automated checks pass and no strict performance blockers remain.
- Owner-gated Shopify/admin/Search Console proof remains separate and is not fabricated.

</success_criteria>

<output>

- Strict performance readiness semantics.
- Final audit local production lifecycle for required live-server probes.
- Regenerated performance and final readiness evidence.

</output>
