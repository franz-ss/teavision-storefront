---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 12
type: execute
wave: 9
depends_on: ["17-11"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - public/images/homepage/homepage-hero-tea-harvest-lcp.avif
  - public/images/homepage/bulk-wholesale-lcp.avif
  - src/components/homepage/content.ts
  - src/components/homepage/hero/hero.tsx
  - src/components/product/product-gallery/product-gallery.tsx
  - src/components/collection/product-card/product-card.tsx
  - src/app/(storefront)/collections/[handle]/_components/hero.tsx
  - src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx
  - tests/mocks/shopify-graphql-server.ts
  - scripts/component-contracts/launch-image-performance.test.mjs
autonomous: true
requirements:
  - PERF-01
  - QA-02
gap_closure: true
must_haves:
  truths:
    - "PERF-01: Home and PDP Lighthouse/Core Web Vitals evidence no longer shows launch-blocking LCP regressions, or a dated owner/staging/field acceptance marks local lab failures non-blocking"
    - "D-14: mandatory launch-critical representatives include home, PDP, /collections/all, /cart, /search, /account, and legal/static pages"
    - "D-15: performance blocking uses practical mobile Lighthouse/Web Vitals gates; LCP is under 2500ms where local evidence is reliable and CLS stays under 0.1"
    - "QA-02: final readiness evidence is generated from real commands and must not hide route-level metric failures"
    - "Plan 17-15 must regenerate docs/launch/performance-evidence.md, docs/launch/final-production-readiness-report.md, and 17-VERIFICATION.md after this remediation"
  artifacts:
    - path: "public/images/homepage/homepage-hero-tea-harvest-lcp.avif"
      provides: "Precompressed Home hero LCP source image"
    - path: "public/images/homepage/bulk-wholesale-lcp.avif"
      provides: "Precompressed fake Shopify PDP/PLP LCP source image"
    - path: "src/components/homepage/hero/hero.tsx"
      provides: "Next 16-compliant preloaded Home LCP image"
    - path: "src/components/product/product-gallery/product-gallery.tsx"
      provides: "Next 16-compliant first PDP gallery image discovery"
    - path: "src/components/collection/product-card/product-card.tsx"
      provides: "Next 16-compliant first PLP/search card image discovery"
    - path: "scripts/component-contracts/launch-image-performance.test.mjs"
      provides: "Static guard against invalid Next 16 image priority/preload combinations"
    - path: "docs/launch/performance-evidence.md"
      provides: "Regenerated strict performance evidence required to close PERF-01 in plan 17-15"
    - path: "docs/launch/final-production-readiness-report.md"
      provides: "Regenerated launch decision required to close PERF-01 in plan 17-15"
    - path: ".planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md"
      provides: "Updated verification record required after plan 17-15 reruns"
  key_links:
    - from: "src/components/homepage/content.ts"
      to: "src/components/homepage/hero/hero.tsx"
      via: "HOMEPAGE_HERO.image.src"
      pattern: "homepage-hero-tea-harvest-lcp\\.avif"
    - from: "tests/mocks/shopify-graphql-server.ts"
      to: "src/components/product/product-gallery/product-gallery.tsx"
      via: "fake Shopify product images"
      pattern: "bulk-wholesale-lcp\\.avif"
    - from: "tests/mocks/shopify-graphql-server.ts"
      to: "src/components/collection/product-card/product-card.tsx"
      via: "fake Shopify collection featuredImage"
      pattern: "bulk-wholesale-lcp\\.avif"
---

<objective>
Close the image-resource portion of the PERF-01 blocker. Current evidence identifies image-resource LCP on `/`, `/products/test-standard-tea`, and `/collections/all`; this plan creates smaller launch-critical image sources and updates only the above-the-fold image components to use Next.js 16-supported `preload` behavior without deprecated or conflicting props.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-RESEARCH.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-PATTERNS.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
@docs/launch/performance-evidence.md
@node_modules/next/dist/docs/01-app/01-getting-started/12-images.md
@node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
@.agents/skills/vercel-react-best-practices/SKILL.md
</execution_context>

<source_audit>

| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Prove storefront is operationally ready with performance evidence and final audit | Covered by image LCP remediation feeding plan 17-14 evidence |
| REQ | PERF-01 | Covered by Home/PDP/PLP image LCP source and preload remediation |
| REQ | QA-02 | Covered by contract tests and final evidence dependency |
| RESEARCH | Use mobile Lighthouse/Web Vitals representative routes and preserve failures honestly | Covered by retaining strict probe semantics; no threshold weakening |
| CONTEXT | D-14 representative routes | Covered for `/`, `/products/test-standard-tea`, and `/collections/all` image-resource routes |
| CONTEXT | D-15 strict LCP/CLS/TBT gate | Covered by targeting LCP <= 2500ms without changing thresholds |
| CONTEXT | D-17 final report separation | Deferred to plan 17-14; this plan does not edit final report |
| EXCLUDED | Dated owner/staging/field acceptance | Excluded here; no evidence was supplied and no acceptance artifact is fabricated |

</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Generate precompressed launch-critical LCP image sources</name>
  <read_first>
    - package.json
    - pnpm-lock.yaml
    - public/images/homepage/homepage-hero-tea-harvest.png
    - public/images/homepage/bulk-wholesale.jpg
    - node_modules/next/dist/docs/01-app/01-getting-started/12-images.md
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
  </read_first>
  <files>package.json, pnpm-lock.yaml, public/images/homepage/homepage-hero-tea-harvest-lcp.avif, public/images/homepage/bulk-wholesale-lcp.avif</files>
  <action>
    Add a direct dev dependency on `sharp@0.34.5` for deterministic local asset generation:

    ```bash
    pnpm add -D sharp@0.34.5
    ```

    Generate these exact files from the existing source assets:

    ```bash
    node -e "const sharp=require('sharp'); (async()=>{ await sharp('public/images/homepage/homepage-hero-tea-harvest.png').resize({width:1440,withoutEnlargement:true}).avif({quality:48,effort:6}).toFile('public/images/homepage/homepage-hero-tea-harvest-lcp.avif'); await sharp('public/images/homepage/bulk-wholesale.jpg').resize({width:800,withoutEnlargement:true}).avif({quality:52,effort:6}).toFile('public/images/homepage/bulk-wholesale-lcp.avif'); })().catch((error)=>{ console.error(error); process.exit(1); })"
    ```

    Keep the original PNG/JPG files in place because lower-page content and historical references may still use them. Do not replace unrelated image assets. Do not use real Shopify or external image URLs.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const checks=[['public/images/homepage/homepage-hero-tea-harvest-lcp.avif',350000],['public/images/homepage/bulk-wholesale-lcp.avif',120000]]; for (const [p,max] of checks){ if(!fs.existsSync(p)) throw new Error('missing '+p); const size=fs.statSync(p).size; if(size>max) throw new Error(p+' too large: '+size+' > '+max); } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `sharp` in `devDependencies`.
    - `public/images/homepage/homepage-hero-tea-harvest-lcp.avif` exists and is no larger than 350000 bytes.
    - `public/images/homepage/bulk-wholesale-lcp.avif` exists and is no larger than 120000 bytes.
    - The original `homepage-hero-tea-harvest.png` and `bulk-wholesale.jpg` files remain present.
  </acceptance_criteria>
  <done>Launch-critical local LCP image sources are smaller and reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LCP images with Next 16 preload semantics</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/12-images.md
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
    - src/components/homepage/content.ts
    - src/components/homepage/hero/hero.tsx
    - src/components/product/product-gallery/product-gallery.tsx
    - src/components/collection/product-card/product-card.tsx
    - src/app/(storefront)/collections/[handle]/_components/hero.tsx
    - src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx
    - tests/mocks/shopify-graphql-server.ts
  </read_first>
  <files>src/components/homepage/content.ts, src/components/homepage/hero/hero.tsx, src/components/product/product-gallery/product-gallery.tsx, src/components/collection/product-card/product-card.tsx, src/app/(storefront)/collections/[handle]/_components/hero.tsx, src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx, tests/mocks/shopify-graphql-server.ts</files>
  <action>
    Update image references and props without changing visual layout:

    - In `src/components/homepage/content.ts`, change `HOMEPAGE_HERO.image.src` to `/images/homepage/homepage-hero-tea-harvest-lcp.avif`, `width` to `1440`, and `height` to `810` per the generated asset.
    - In `tests/mocks/shopify-graphql-server.ts`, change `fakeProductImage.url` to `/images/homepage/bulk-wholesale-lcp.avif` while preserving width `800`, height `534`, and alt text.
    - In `src/components/homepage/hero/hero.tsx`, replace `loading="eager"` and `fetchPriority="high"` on the hero `<Image>` with `preload`. Keep `fill`, `sizes="100vw"`, and `quality={68}` only if the Image API accepts it for the new AVIF source; remove `quality` if it triggers a Next image warning for precompressed AVIF.
    - In `src/components/product/product-gallery/product-gallery.tsx`, for the first gallery image use `preload={i === 0}` and remove the first-image `loading="eager"`/`fetchPriority="high"` branch. Non-first images remain lazy by default.
    - In `src/components/collection/product-card/product-card.tsx`, keep the existing `priority` prop name as the local component API, but render it as `preload={priority}` on `<Image>` and remove `loading={...}` plus `fetchPriority={...}`.
    - In `src/app/(storefront)/collections/[handle]/_components/hero.tsx`, remove the invalid combination of `loading="eager"`, `fetchPriority="high"`, and `preload`; use `preload` only for the banner image.
    - In `src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx`, replace deprecated `priority` with `preload`.

    Do not use inline styles, raw color classes, default exports in component/lib modules, or `any`. Do not change Shopify GraphQL query shape.
  </action>
  <verify>
    <automated>pnpm lint -- src/components/homepage/content.ts src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx src/components/collection/product-card/product-card.tsx "src/app/(storefront)/collections/[handle]/_components/hero.tsx" "src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx" tests/mocks/shopify-graphql-server.ts</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/homepage/content.ts` contains `homepage-hero-tea-harvest-lcp.avif`.
    - `tests/mocks/shopify-graphql-server.ts` contains `bulk-wholesale-lcp.avif`.
    - No touched file contains `loading="eager"` and `fetchPriority="high"` on the same `<Image>`.
    - No touched file uses the deprecated Next `<Image priority` prop.
    - The first Home/PDP/PLP image discovery path uses `preload` per the installed Next 16 docs.
  </acceptance_criteria>
  <done>Image LCP discovery uses smaller sources and current Next 16 preload behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add a static contract for launch image LCP behavior</name>
  <read_first>
    - scripts/component-contracts/performance-fonts.test.mjs
    - src/components/homepage/content.ts
    - src/components/homepage/hero/hero.tsx
    - src/components/product/product-gallery/product-gallery.tsx
    - src/components/collection/product-card/product-card.tsx
    - src/app/(storefront)/collections/[handle]/_components/hero.tsx
    - src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx
    - tests/mocks/shopify-graphql-server.ts
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
  </read_first>
  <files>scripts/component-contracts/launch-image-performance.test.mjs</files>
  <action>
    Create `scripts/component-contracts/launch-image-performance.test.mjs` using `node:test`, `node:assert/strict`, and `fs/promises`. It must assert:

    - The two new AVIF files exist and stay within the byte ceilings from Task 1.
    - `src/components/homepage/content.ts` and `tests/mocks/shopify-graphql-server.ts` reference the new AVIF paths.
    - `src/components/homepage/hero/hero.tsx`, `src/components/product/product-gallery/product-gallery.tsx`, and `src/components/collection/product-card/product-card.tsx` contain `preload`.
    - No scanned source file contains `<Image` with `priority`.
    - No scanned source file contains both `loading="eager"` and `fetchPriority="high"`.
    - No scanned source file contains both `preload` and `fetchPriority="high"` on the same `<Image>` block.

    Use source scanning over this fixed file list only; do not scan `node_modules` or generated Shopify types.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/launch-image-performance.test.mjs</automated>
    <automated>pnpm test:contracts</automated>
  </verify>
  <acceptance_criteria>
    - The new contract fails if an above-the-fold image reintroduces deprecated `priority`.
    - The new contract fails if a touched image combines `preload` with `fetchPriority="high"`.
    - The new contract is included when `pnpm test:contracts` runs.
  </acceptance_criteria>
  <done>Future image edits cannot silently reintroduce the launch-blocking image priority pattern.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local asset generation to repository | Generated binary assets must come from existing local images, not untrusted external URLs |
| Fake Shopify provider to browser evidence | Fake product images must remain local and must not reach live Shopify checkout or provider URLs |
| Performance evidence to launch decision | Image changes must reduce strict local LCP evidence without weakening the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-17-33 | Tampering | generated LCP assets | mitigate | Generate from fixed local paths and enforce byte/exists contracts |
| T-17-34 | Information Disclosure | fake Shopify media path | mitigate | Use local `/images/...` assets only; do not add checkout URLs, cart IDs, PII, tokens, or raw provider payloads |
| T-17-35 | Denial of Service | image download weight | mitigate | Precompressed AVIF byte ceilings and Next 16 preload-only discovery |

</threat_model>

<verification>

```bash
node --test scripts/component-contracts/launch-image-performance.test.mjs
pnpm test:contracts
pnpm lint -- src/components/homepage/content.ts src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx src/components/collection/product-card/product-card.tsx "src/app/(storefront)/collections/[handle]/_components/hero.tsx" "src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx" tests/mocks/shopify-graphql-server.ts
pnpm typecheck
```

</verification>

<success_criteria>

- Home, PDP, and collection image LCP sources are precompressed local AVIF assets.
- Above-the-fold images use Next 16 `preload` without deprecated `priority` or conflicting `fetchPriority`.
- Fake-provider PDP and PLP performance evidence still exercises a real product image path.
- No performance threshold is weakened and no acceptance artifact is created.

</success_criteria>

<output>

After completion, create `.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-12-SUMMARY.md`.

</output>
