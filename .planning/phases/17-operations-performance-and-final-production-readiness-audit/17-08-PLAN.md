---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 08
type: execute
wave: 5
depends_on: [17-06]
files_modified:
  - scripts/performance/probe-lighthouse.mjs
  - scripts/performance/probe-lighthouse.test.mjs
  - scripts/launch/run-final-readiness-audit.mjs
  - scripts/launch/run-final-readiness-audit.test.mjs
  - src/components/homepage/hero/hero.tsx
  - src/components/product/product-gallery/product-gallery.tsx
  - src/components/collection/product-card/product-card.tsx
  - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
  - src/app/(storefront)/account/page.tsx
  - src/app/(storefront)/account/loading.tsx
  - docs/launch/performance-evidence.md
  - docs/launch/performance-acceptance.md
  - docs/launch/final-production-readiness-report.md
autonomous: false
requirements:
  - PERF-01
  - UX-01
  - QA-02
must_haves:
  truths:
    - "PERF-01: Home and PDP Lighthouse/Core Web Vitals evidence no longer shows launch-blocking LCP regressions, or a dated owner/staging/field-data exception marks local lab failures non-blocking"
    - "UX-01: audit UX/accessibility polish items remain resolved or documented non-blocking"
    - "QA-02: final readiness audit treats performance FAIL rows as blocking unless a validated acceptance artifact is present"
    - "D-14: launch-critical audit representatives include home, PDP, collection, cart, search, account, and legal/static pages"
    - "D-15: performance blocking uses practical mobile Lighthouse/Web Vitals gates with LCP, CLS, INP/TBT evidence and documented mitigations"
  artifacts:
    - path: "scripts/performance/probe-lighthouse.mjs"
      provides: "Route-level Lighthouse diagnostics that identify LCP element/resource and distinguish cold-start noise from steady-state blockers"
    - path: "docs/launch/performance-evidence.md"
      provides: "Strict local performance evidence after remediation"
    - path: "docs/launch/performance-acceptance.md"
      provides: "Optional dated owner/staging/field-data acceptance artifact if local lab failures are explicitly accepted as non-blocking"
  key_links:
    - from: "scripts/performance/probe-lighthouse.mjs"
      to: "docs/launch/performance-evidence.md"
      via: "strict route metrics and LCP diagnostics"
    - from: "docs/launch/performance-acceptance.md"
      to: "scripts/launch/run-final-readiness-audit.mjs"
      via: "validated exception only when local lab evidence remains non-green"
---

<objective>
Close the strict performance verification gap by diagnosing the seven failing Lighthouse rows, applying concrete route-level remediation, and rerunning evidence. If local Lighthouse remains non-green because it is not representative of staging or field Core Web Vitals, require a dated owner/staging/field-data acceptance artifact before the final audit can treat the local failures as non-blocking.
</objective>

<execution_context>
@AGENTS.md
@node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
@node_modules/next/dist/docs/01-app/02-guides/production-checklist.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-04-SUMMARY.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-06-SUMMARY.md
@scripts/performance/probe-lighthouse.mjs
@scripts/performance/probe-lighthouse.test.mjs
@scripts/launch/run-final-readiness-audit.mjs
@scripts/launch/run-final-readiness-audit.test.mjs
@src/components/homepage/hero/hero.tsx
@src/components/product/product-gallery/product-gallery.tsx
@src/components/collection/product-card/product-card.tsx
@src/app/(storefront)/collections/[handle]/_components/product-list.tsx
@src/app/(storefront)/account/page.tsx
@src/app/(storefront)/account/loading.tsx
@docs/launch/performance-evidence.md
@docs/launch/final-production-readiness-report.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add Lighthouse diagnostics for the current blockers</name>
  <files>scripts/performance/probe-lighthouse.mjs, scripts/performance/probe-lighthouse.test.mjs, docs/launch/performance-evidence.md</files>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs
    - scripts/performance/probe-lighthouse.test.mjs
    - docs/launch/performance-evidence.md
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
    - node_modules/next/dist/docs/01-app/02-guides/production-checklist.md
  </read_first>
  <action>
    Improve the performance probe so remediation targets the actual LCP/CLS causes instead of repeating generic image advice:

    - In `summarizeLhr`, extract `largest-contentful-paint-element` details when Lighthouse provides them. Record at least `selector`, `snippet`, and `nodeLabel` when available.
    - Extract the LCP resource URL from Lighthouse audit details when available. Record it as `lcpResourceUrl`; use `null` when Lighthouse does not expose one.
    - Add route-level `observedUrl` from `lhr.finalDisplayedUrl`.
    - Add optional `--warmup-runs <n>` with default `1`. Before measured Lighthouse runs, fetch each representative route `n` times against the started server. The warmup must not change pass/fail thresholds; it only removes first-request build/image-cache noise from the measured local evidence.
    - Add `--cold-run` as shorthand for `--warmup-runs 0`.
    - Extend the markdown evidence table or a new `## LCP Diagnostics` section with columns `Route`, `LCP Element`, `LCP Resource`, and `Observed URL`.
    - Update JSON summary route objects to include `lcpElement` and `lcpResourceUrl`.
    - Add tests for diagnostic extraction, `--warmup-runs`, `--cold-run`, and JSON summary shape.
  </action>
  <verify>
    <automated>node --test scripts/performance/probe-lighthouse.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/performance/probe-lighthouse.mjs` contains `largest-contentful-paint-element`.
    - `scripts/performance/probe-lighthouse.mjs` contains `--warmup-runs`.
    - `scripts/performance/probe-lighthouse.mjs` contains `--cold-run`.
    - `scripts/performance/probe-lighthouse.test.mjs` asserts JSON summary includes `lcpElement` and `lcpResourceUrl`.
    - `docs/launch/performance-evidence.md` can name the LCP element or explicitly says Lighthouse did not expose it.
  </acceptance_criteria>
  <done>The executor can tell whether the blocker is image loading, route cold-start, layout shift, font/render timing, or another route-specific element.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate route-level LCP and CLS causes</name>
  <files>src/components/homepage/hero/hero.tsx, src/components/product/product-gallery/product-gallery.tsx, src/components/collection/product-card/product-card.tsx, src/app/(storefront)/collections/[handle]/_components/product-list.tsx, src/app/(storefront)/account/page.tsx, src/app/(storefront)/account/loading.tsx, docs/launch/performance-evidence.md</files>
  <read_first>
    - docs/launch/performance-evidence.md
    - scripts/performance/probe-lighthouse.mjs
    - src/components/homepage/hero/hero.tsx
    - src/components/product/product-gallery/product-gallery.tsx
    - src/components/collection/product-card/product-card.tsx
    - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
    - src/app/(storefront)/account/page.tsx
    - src/app/(storefront)/account/loading.tsx
    - src/app/(storefront)/layout.tsx
    - src/app/globals.css
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
  </read_first>
  <action>
    Run the strict probe and use the diagnostics from Task 1 to make targeted code changes. Apply only changes supported by the measured route evidence:

    ```bash
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
    ```

    Required remediation rules:

    - For `/`, if the LCP element is the homepage hero image, change the hero image to one supported Next 16 LCP loading strategy: either `preload={true}` without `loading="eager"`/`fetchPriority="high"`, or keep `loading="eager"` plus `fetchPriority="high"` without `preload`. Do not use deprecated `priority`.
    - For `/products/test-standard-tea`, if the LCP element is the PDP gallery image, keep the first gallery image as the only eager/high-priority PDP image. Do not add a parent `'use client'` boundary to the product page.
    - For `/collections/all`, if the first product image is the LCP element, ensure only the first visible `ProductCard` receives `priority={true}` from `src/app/(storefront)/collections/[handle]/_components/product-list.tsx`; keep later cards lazy.
    - For `/account`, if CLS remains above `0.1`, reserve a stable min-height or skeleton geometry in `src/app/(storefront)/account/loading.tsx` and the account page wrapper so the login bridge/dashboard swap does not shift the viewport.
    - For `/cart`, `/search?q=tea`, and `/pages/privacy-policy`, do not make cosmetic redesign changes. If diagnostics show text or font LCP rather than image LCP, document the route-specific cause and prefer reducing route waterfalls or unstable layout over arbitrary image edits.
    - If the main cause is first-request server/image optimization cold-start and a warmed run passes, keep `--warmup-runs 1` as the strict default and record both the cold and warmed result in `docs/launch/performance-evidence.md`.
    - Any source edit must use `cn()` for class composition and Tailwind token classes; do not introduce inline styles, raw hex/rgb values, CSS modules, or broad parent `'use client'` wrappers.
  </action>
  <verify>
    <automated>pnpm lint -- src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx src/components/collection/product-card/product-card.tsx "src/app/(storefront)/collections/[handle]/_components/product-list.tsx" "src/app/(storefront)/account/page.tsx" "src/app/(storefront)/account/loading.tsx"</automated>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
    <automated>pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary</automated>
  </verify>
  <acceptance_criteria>
    - `/` and `/products/test-standard-tea` rows in `docs/launch/performance-evidence.md` are `PASS`, or each failing row has a route-specific diagnostic cause and mitigation.
    - If `/collections/all` was changed, only the first visible product card is eager/high-priority.
    - If `/account` CLS was changed, `docs/launch/performance-evidence.md` records CLS `<= 0.1` or a remaining route-specific cause.
    - No changed React file adds a new top-level `'use client'` directive unless it was already a client component.
    - No changed JSX uses `style={{}}`, raw hex/rgb Tailwind classes, or className string concatenation.
  </acceptance_criteria>
  <done>The current performance blockers have concrete fixes or concrete evidence explaining why code changes are not the right lever.</done>
</task>

<task type="manual">
  <name>Task 3: Validate any non-blocking performance exception before the audit may pass</name>
  <files>docs/launch/performance-acceptance.md, scripts/launch/run-final-readiness-audit.mjs, scripts/launch/run-final-readiness-audit.test.mjs, docs/launch/final-production-readiness-report.md</files>
  <read_first>
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
    - docs/launch/performance-evidence.md
    - docs/launch/final-production-readiness-report.md
    - scripts/launch/run-final-readiness-audit.mjs
    - scripts/launch/run-final-readiness-audit.test.mjs
  </read_first>
  <action>
    If strict local performance evidence is fully green after Task 2, do not create `docs/launch/performance-acceptance.md` and do not add an exception path.

    If one or more strict local rows remain `FAIL`, the executor must stop for a human checkpoint before treating the performance gate as non-blocking. Only proceed with an exception when the user provides dated owner, staging, or field Core Web Vitals evidence. Create `docs/launch/performance-acceptance.md` with these exact headings and required fields:

    ```markdown
    # Performance Acceptance

    ## Decision
    Status: accepted-non-blocking
    Date: YYYY-MM-DD
    Approver: [owner/staging authority name]
    Evidence source: [staging Lighthouse URL, CrUX/Core Web Vitals export, or owner-approved monitoring artifact]

    ## Local Lab Failures Accepted
    - `/`: [accepted or not applicable with reason]
    - `/products/test-standard-tea`: [accepted or not applicable with reason]
    - `/collections/all`: [accepted or not applicable with reason]
    - `/cart`: [accepted or not applicable with reason]
    - `/search?q=tea`: [accepted or not applicable with reason]
    - `/account`: [accepted or not applicable with reason]
    - `/pages/privacy-policy`: [accepted or not applicable with reason]

    ## Rationale
    [Why local lab evidence is non-blocking for launch.]
    ```

    Update `scripts/launch/run-final-readiness-audit.mjs` only if an acceptance file is present and validated:

    - Add an explicit CLI flag `--performance-acceptance docs/launch/performance-acceptance.md`.
    - Validate that the file contains `Status: accepted-non-blocking`, an ISO date, a non-empty approver, and a non-empty evidence source.
    - When performance command exits nonzero but the acceptance file validates, render the performance row as `PASS` with evidence text starting exactly `Accepted non-blocking by dated performance acceptance:`.
    - Without the flag or without a valid file, performance `FAIL` rows remain blocking.
    - Add tests for valid acceptance, missing approver, missing date, and the no-flag default failure behavior.

    Do not invent an approver, date, or evidence source. If the user cannot provide acceptance evidence and local rows remain `FAIL`, leave the final report `Not launch-ready`.
  </action>
  <verify>
    <automated>node --test scripts/launch/run-final-readiness-audit.test.mjs</automated>
    <automated>node -e "const fs=require('fs'); if(fs.existsSync('docs/launch/performance-acceptance.md')){const s=fs.readFileSync('docs/launch/performance-acceptance.md','utf8'); for(const n of ['Status: accepted-non-blocking','Date:','Approver:','Evidence source:']) if(!s.includes(n)) throw new Error('missing '+n)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - If `docs/launch/performance-acceptance.md` exists, it contains `Status: accepted-non-blocking`, `Date:`, `Approver:`, and `Evidence source:`.
    - If no valid acceptance file exists, `scripts/launch/run-final-readiness-audit.mjs` still treats performance command exit `1` as `FAIL`.
    - Tests prove the acceptance path is explicit and cannot be triggered accidentally.
    - The final report never claims a local performance failure is non-blocking without a dated acceptance artifact.
  </acceptance_criteria>
  <done>Performance can only be considered closed by strict green evidence or by an explicit dated acceptance artifact.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate strict performance evidence</name>
  <files>docs/launch/performance-evidence.md, docs/launch/final-production-readiness-report.md</files>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs
    - scripts/launch/run-final-readiness-audit.mjs
    - docs/launch/performance-evidence.md
    - docs/launch/performance-acceptance.md
  </read_first>
  <action>
    Regenerate performance and report evidence from real command output:

    ```bash
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
    pnpm audit:readiness
    ```

    If a valid acceptance file was created in Task 3, run:

    ```bash
    pnpm audit:readiness -- --performance-acceptance docs/launch/performance-acceptance.md
    ```

    Required evidence states:

    - `docs/launch/performance-evidence.md` must include route rows for `/` and `/products/test-standard-tea`.
    - If every route is `PASS`, `Launch-blocking: no` must be present.
    - If any route remains `FAIL` and no valid acceptance file is supplied, the final report must say `Not launch-ready`.
    - If local rows remain `FAIL` but a valid acceptance file is supplied, the final report must name `docs/launch/performance-acceptance.md` and include the exact phrase `Accepted non-blocking by dated performance acceptance:`.
  </action>
  <verify>
    <automated>pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary</automated>
    <automated>pnpm audit:readiness</automated>
  </verify>
  <acceptance_criteria>
    - `docs/launch/performance-evidence.md` includes `## LCP Diagnostics` or equivalent route-level LCP diagnostic details.
    - `docs/launch/performance-evidence.md` includes Home and PDP rows.
    - `docs/launch/final-production-readiness-report.md` does not print `100/100` while performance rows are `FAIL` unless a valid acceptance artifact is named.
    - If an acceptance artifact is named, `docs/launch/performance-acceptance.md` exists and validates.
  </acceptance_criteria>
  <done>PERF-01 is closed by strict pass evidence or remains honestly blocked with a validated manual acceptance path.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lighthouse local lab to launch decision | Local lab data can be noisy, but failures must not be hidden without evidence |
| Performance exception to automated score | Exception acceptance must require dated owner/staging/field-data proof |
| Route remediation to storefront behavior | Performance fixes must preserve commerce, accessibility, and server-first boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-19 | Repudiation | performance evidence | mitigate | Add LCP element/resource diagnostics and retain strict FAIL semantics |
| T-17-20 | Spoofing / Repudiation | performance acceptance | mitigate | Validate approver, date, status, and evidence source before an exception can pass the audit |
| T-17-21 | Tampering | storefront performance fixes | mitigate | Require route-specific diagnostics and project lint/contracts before evidence regeneration |

</threat_model>

<verification>

```bash
node --test scripts/performance/probe-lighthouse.test.mjs
node --test scripts/launch/run-final-readiness-audit.test.mjs
pnpm lint -- src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx src/components/collection/product-card/product-card.tsx "src/app/(storefront)/collections/[handle]/_components/product-list.tsx" "src/app/(storefront)/account/page.tsx" "src/app/(storefront)/account/loading.tsx"
pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts
pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
pnpm audit:readiness
pnpm typecheck
```

</verification>

<success_criteria>

- Performance evidence names the current LCP element/resource diagnostics for failing routes.
- Home and PDP no longer show launch-blocking local LCP regressions, or a valid dated acceptance artifact marks local lab failures non-blocking.
- Performance `FAIL` rows remain blocking by default.
- Final readiness cannot claim `100/100` for performance without strict green evidence or validated acceptance.

</success_criteria>

<output>

- Performance probe diagnostics and warmup controls.
- Route-level performance remediation.
- Optional validated performance acceptance artifact.
- Regenerated performance evidence.

</output>
