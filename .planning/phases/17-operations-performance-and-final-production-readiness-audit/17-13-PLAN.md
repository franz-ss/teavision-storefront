---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 13
type: execute
wave: 9
depends_on: ["17-11"]
files_modified:
  - src/app/layout.tsx
  - src/app/(storefront)/cart/page.tsx
  - src/app/(storefront)/cart/_components/loading-skeleton.tsx
  - src/app/(storefront)/search/page.tsx
  - src/components/search/search-results-view/search-results-view.tsx
  - src/components/search/search-results-view/search-hero.tsx
  - src/app/(storefront)/account/layout.tsx
  - src/app/(storefront)/account/loading.tsx
  - src/app/(storefront)/account/login/page.tsx
  - src/app/(storefront)/account/page.tsx
  - src/app/(storefront)/account/_components/login-panel/login-panel.tsx
  - src/app/(storefront)/pages/privacy-policy/page.tsx
  - scripts/component-contracts/performance-fonts.test.mjs
  - scripts/component-contracts/account-performance.test.mjs
  - scripts/component-contracts/render-shell-performance.test.mjs
autonomous: true
requirements:
  - PERF-01
  - UX-01
  - QA-02
gap_closure: true
must_haves:
  truths:
    - "PERF-01: Home and PDP Lighthouse/Core Web Vitals evidence no longer shows launch-blocking LCP regressions, or a dated owner/staging/field acceptance marks local lab failures non-blocking"
    - "Representative text-LCP routes `/cart`, `/search?q=tea`, and `/pages/privacy-policy` render their above-the-fold shell without waiting on avoidable provider/session work"
    - "`/account` local login bridge has CLS <= 0.1 in strict local Lighthouse evidence"
    - "UX-01 remains green: no duplicate skip link and no mobile horizontal overflow regression"
    - "Plan 17-14 must regenerate docs/launch/performance-evidence.md, docs/launch/final-production-readiness-report.md, and 17-VERIFICATION.md after this remediation"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Non-blocking launch font display configuration"
    - path: "src/app/(storefront)/cart/page.tsx"
      provides: "Empty-cart fast path that avoids account-session work when no cart exists"
    - path: "src/app/(storefront)/search/page.tsx"
      provides: "Search hero shell split from result provider fetch"
    - path: "src/app/(storefront)/account/_components/login-panel/login-panel.tsx"
      provides: "Stable account login bridge dimensions"
    - path: "scripts/component-contracts/render-shell-performance.test.mjs"
      provides: "Static guard for render-shell and font decisions"
    - path: "docs/launch/performance-evidence.md"
      provides: "Regenerated strict performance evidence required to close PERF-01 in plan 17-14"
    - path: "docs/launch/final-production-readiness-report.md"
      provides: "Regenerated launch decision required to close PERF-01 in plan 17-14"
    - path: ".planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md"
      provides: "Updated verification record required after plan 17-14 reruns"
  key_links:
    - from: "src/app/(storefront)/search/page.tsx"
      to: "src/components/search/search-results-view/search-results-view.tsx"
      via: "SearchHero rendered before result fetch"
      pattern: "SearchHero"
    - from: "src/app/(storefront)/cart/page.tsx"
      to: "src/lib/cart/actions.ts"
      via: "getCartAction called before session lookup"
      pattern: "getCartAction"
    - from: "src/app/layout.tsx"
      to: "scripts/component-contracts/performance-fonts.test.mjs"
      via: "font display optional contract"
      pattern: "display:\\s*'optional'"
---

<objective>
Close the render-delay and layout-shift portion of the PERF-01 blocker. Current evidence shows text/render-delay LCP on `/cart`, `/search?q=tea`, and `/pages/privacy-policy`, plus `/account` CLS 0.128. This plan reduces font-induced repaint risk, removes avoidable above-the-fold waterfalls, and stabilizes the account login bridge without changing checkout, owner-gated OAuth, or Shopify proof boundaries.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-RESEARCH.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-PATTERNS.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
@docs/launch/performance-evidence.md
@node_modules/next/dist/docs/01-app/02-guides/streaming.md
@node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
@node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-cache.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/webVitalsAttribution.md
@.agents/skills/vercel-react-best-practices/SKILL.md
@.agents/skills/next-cache-components/SKILL.md
</execution_context>

<source_audit>

| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Prove storefront is operationally ready with performance evidence and final audit | Covered by render-shell/CLS remediation feeding plan 17-14 evidence |
| REQ | PERF-01 | Covered by text LCP and account CLS remediation |
| REQ | UX-01 | Covered by preserving skip-link/overflow smoke gates |
| REQ | QA-02 | Covered by contract and production smoke verification |
| RESEARCH | Streaming guidance: keep LCP outside slow Suspense and reserve fallback dimensions | Covered by search/cart/account shell changes |
| CONTEXT | D-15 practical LCP/CLS/TBT gate | Covered by LCP <= 2500ms, CLS <= 0.1 target state |
| CONTEXT | D-16 fake-provider production lifecycle | Preserved; no real checkout/OAuth/provider testing added |
| EXCLUDED | Owner/staging/field acceptance | Excluded here; no dated evidence supplied |

</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Make launch fonts non-blocking for text LCP routes</name>
  <read_first>
    - src/app/layout.tsx
    - scripts/component-contracts/performance-fonts.test.mjs
    - node_modules/next/dist/docs/01-app/02-guides/streaming.md
    - node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
  </read_first>
  <files>src/app/layout.tsx, scripts/component-contracts/performance-fonts.test.mjs</files>
  <action>
    In `src/app/layout.tsx`, change all four `next/font/google` font configs (`Spectral`, `Hanken_Grotesk`, `Space_Mono`, `Caveat`) to `display: 'optional'`. Preserve the existing weight/style/subset/variable choices from 17-10:

    - `Spectral`: `weight: ['400', '500']`, `style: ['normal']`
    - `Hanken_Grotesk`: no explicit weight list
    - `Space_Mono`: `weight: ['400', '700']`, `preload: false`
    - `Caveat`: `preload: false`

    Update `scripts/component-contracts/performance-fonts.test.mjs` so it asserts `display: 'optional'` for all four font configs and continues to assert the narrowed Spectral weights/styles plus `preload: false` for `Space_Mono` and `Caveat`.

    Do not remove font variables from `<body>` and do not add raw CSS, inline styles, or a new font package.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/performance-fonts.test.mjs</automated>
    <automated>pnpm lint -- src/app/layout.tsx scripts/component-contracts/performance-fonts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains four `display: 'optional'` font configs.
    - `scripts/component-contracts/performance-fonts.test.mjs` fails if `display: 'swap'` is reintroduced for launch fonts.
    - `Space_Mono` and `Caveat` remain `preload: false`.
  </acceptance_criteria>
  <done>Font loading no longer creates avoidable late text repaint risk for static/text LCP routes.</done>
</task>

<task type="auto">
  <name>Task 2: Split text-LCP route shells from avoidable async work</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/02-guides/streaming.md
    - node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
    - node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-cache.md
    - src/app/(storefront)/cart/page.tsx
    - src/app/(storefront)/cart/_components/loading-skeleton.tsx
    - src/app/(storefront)/search/page.tsx
    - src/components/search/search-results-view/search-results-view.tsx
    - src/components/search/search-results-view/search-hero.tsx
    - src/app/(storefront)/pages/privacy-policy/page.tsx
  </read_first>
  <files>src/app/(storefront)/cart/page.tsx, src/app/(storefront)/cart/_components/loading-skeleton.tsx, src/app/(storefront)/search/page.tsx, src/components/search/search-results-view/search-results-view.tsx, src/components/search/search-results-view/search-hero.tsx, src/app/(storefront)/pages/privacy-policy/page.tsx</files>
  <action>
    Apply the installed streaming guidance without changing route semantics:

    - In `src/app/(storefront)/cart/page.tsx`, resolve `searchParams` and `getCartAction()` in parallel. Only call `getCustomerAccountSession()` when a cart exists or `checkout=identity-sync-failed` is present. This preserves signed-in checkout context for real carts while removing the account-session check from the local empty-cart LCP path.
    - In `src/app/(storefront)/cart/_components/loading-skeleton.tsx`, add a visible above-the-fold fallback block with the same empty-cart vertical footprint: icon placeholder, `h2` text `Checking your cart`, and one body line. Keep `role="status"` and `aria-live="polite"`. The fallback must reserve at least `min-h-72` for the main column so Suspense replacement does not shift the surrounding layout.
    - Refactor search so `SearchHero` renders before the Searchanise result fetch completes. Change `SearchHero` to accept `{ state, countLabel?: string }` instead of the full `SearchaniseSearchResult`. In `src/app/(storefront)/search/page.tsx`, create a small async shell component that awaits only `searchParams`, parses state, and renders `SearchHero` outside the result-fetching Suspense boundary. `SearchResultsView` should no longer render `SearchHero`; it receives the already parsed state/result for below-hero content.
    - In `src/app/(storefront)/pages/privacy-policy/page.tsx`, keep content static and reduce first-viewport LCP text size only within the compact policy route: change the first large policy body paragraph that Lighthouse identified from `type-body` to `type-body-sm` while preserving all legal-review copy and headings.

    Do not use `use cache` around runtime `searchParams`, cookies, or headers. Do not mark parent wrappers with `'use client'`. Do not change Searchanise fetch semantics or real Shopify checkout/account behavior.
  </action>
  <verify>
    <automated>pnpm lint -- "src/app/(storefront)/cart/page.tsx" "src/app/(storefront)/cart/_components/loading-skeleton.tsx" "src/app/(storefront)/search/page.tsx" src/components/search/search-results-view/search-results-view.tsx src/components/search/search-results-view/search-hero.tsx "src/app/(storefront)/pages/privacy-policy/page.tsx"</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(storefront)/cart/page.tsx` does not call `getCustomerAccountSession()` before knowing whether a cart exists or checkout identity-sync context is required.
    - `src/app/(storefront)/cart/_components/loading-skeleton.tsx` contains `Checking your cart` and `min-h-72`.
    - `src/components/search/search-results-view/search-results-view.tsx` does not render `<SearchHero`.
    - `src/app/(storefront)/search/page.tsx` renders `SearchHero` outside the result-fetching Suspense boundary.
    - Production smoke still passes for `/cart`, `/search?q=tea`, `/account`, and mobile overflow.
  </acceptance_criteria>
  <done>Text-LCP routes expose their stable first viewport before avoidable provider/session work completes.</done>
</task>

<task type="auto">
  <name>Task 3: Stabilize the account login bridge and guard render-shell contracts</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/02-guides/streaming.md
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/webVitalsAttribution.md
    - src/app/(storefront)/account/layout.tsx
    - src/app/(storefront)/account/loading.tsx
    - src/app/(storefront)/account/login/page.tsx
    - src/app/(storefront)/account/page.tsx
    - src/app/(storefront)/account/_components/login-panel/login-panel.tsx
    - scripts/component-contracts/account-performance.test.mjs
  </read_first>
  <files>src/app/(storefront)/account/layout.tsx, src/app/(storefront)/account/loading.tsx, src/app/(storefront)/account/login/page.tsx, src/app/(storefront)/account/page.tsx, src/app/(storefront)/account/_components/login-panel/login-panel.tsx, scripts/component-contracts/account-performance.test.mjs, scripts/component-contracts/render-shell-performance.test.mjs</files>
  <action>
    Keep the account route server-owned and fake-provider safe while eliminating the current source-less CLS:

    - In `src/app/(storefront)/account/layout.tsx`, `loading.tsx`, `login/page.tsx`, and `page.tsx`, replace the current mixed `min-h-136 md:min-h-128` geometry with a single stable `min-h-[34rem] md:min-h-[32rem]` class everywhere. The class must be identical across the layout, loading, login, and account page wrappers.
    - In `src/app/(storefront)/account/_components/login-panel/login-panel.tsx`, add fixed responsive spacing that reserves the button/copy footprint: `min-h-72` on the `Card` and `content-start` in the grid. Preserve `prefetch={false}` on the Shopify sign-in button.
    - Update `scripts/component-contracts/account-performance.test.mjs` to assert the identical bracketed min-height class across the four account wrapper files and `min-h-72` on `LoginPanel`.
    - Create `scripts/component-contracts/render-shell-performance.test.mjs` to assert the cart/search/privacy contracts from Task 2 and the account contracts from this task. Use fixed source file reads; do not scan generated files or `node_modules`.

    Do not run live Customer Account OAuth. Do not change `getAccountLoginStartHref`, callback/logout routes, protected-data access, or B2B/customer pricing behavior.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/account-performance.test.mjs scripts/component-contracts/render-shell-performance.test.mjs</automated>
    <automated>pnpm test:contracts</automated>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All account wrapper files contain the exact same `min-h-[34rem] md:min-h-[32rem]` class.
    - `src/app/(storefront)/account/_components/login-panel/login-panel.tsx` contains `min-h-72`, `content-start`, and `prefetch={false}`.
    - `scripts/component-contracts/render-shell-performance.test.mjs` fails if SearchHero moves back under `SearchResultsView`.
    - Production smoke still confirms `/account` does not navigate to live OAuth.
  </acceptance_criteria>
  <done>Account CLS risk and render-shell regressions are statically guarded before strict Lighthouse reruns.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runtime route params/cookies to static shell | Search params and cookies are untrusted request inputs and must not be cached incorrectly |
| Account route to live OAuth | Local browser evidence must not start live Shopify OAuth |
| Launch reports to privacy boundary | Evidence must not include secrets, PII, checkout URLs, cart IDs, or raw provider payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-17-36 | Information Disclosure | cart/search/account shell | mitigate | Do not print or render tokens, emails, cart IDs, checkout URLs, or raw provider data in fallbacks |
| T-17-37 | Spoofing | account login bridge | mitigate | Preserve `prefetch={false}` and fake-provider production smoke gate; do not run live OAuth |
| T-17-38 | Tampering | Cache Components usage | mitigate | Do not put cookies/searchParams inside `use cache`; use Suspense and argument passing only |
| T-17-39 | Denial of Service | render shell | mitigate | Avoid unnecessary session/provider calls on empty-cart and above-the-fold shell paths |

</threat_model>

<verification>

```bash
node --test scripts/component-contracts/performance-fonts.test.mjs scripts/component-contracts/account-performance.test.mjs scripts/component-contracts/render-shell-performance.test.mjs
pnpm test:contracts
pnpm lint -- src/app/layout.tsx "src/app/(storefront)/cart/page.tsx" "src/app/(storefront)/cart/_components/loading-skeleton.tsx" "src/app/(storefront)/search/page.tsx" src/components/search/search-results-view/search-results-view.tsx src/components/search/search-results-view/search-hero.tsx "src/app/(storefront)/account/layout.tsx" "src/app/(storefront)/account/loading.tsx" "src/app/(storefront)/account/login/page.tsx" "src/app/(storefront)/account/page.tsx" "src/app/(storefront)/account/_components/login-panel/login-panel.tsx" "src/app/(storefront)/pages/privacy-policy/page.tsx"
pnpm typecheck
pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts
```

</verification>

<success_criteria>

- Font rendering no longer delays first text paint through `font-display: swap`.
- Cart empty-state route avoids account-session work when no cart exists.
- Search hero renders outside Searchanise result fetch.
- Account login bridge has stable matching dimensions across loading, redirect, and login states.
- UX/accessibility smoke coverage remains green.

</success_criteria>

<output>

After completion, create `.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-13-SUMMARY.md`.

</output>
