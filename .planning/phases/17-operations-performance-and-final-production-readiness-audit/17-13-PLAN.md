---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 13
type: execute
wave: 9
depends_on: ["17-11"]
files_modified:
  - src/app/layout.tsx
  - src/app/(storefront)/cart/page.tsx
  - src/app/(storefront)/cart/_components/loading-skeleton.tsx
  - src/app/(storefront)/search/page.tsx
  - src/app/(storefront)/search/_components/results.tsx
  - src/components/search/search-results-view/search-results-view.tsx
  - src/components/search/search-results-view/search-hero.tsx
  - src/app/(storefront)/pages/privacy-policy/page.tsx
  - scripts/component-contracts/performance-fonts.test.mjs
autonomous: true
requirements:
  - PERF-01
  - UX-01
  - QA-02
gap_closure: true
must_haves:
  truths:
    - "PERF-01: render-delay LCP routes `/cart`, `/search?q=tea`, and `/pages/privacy-policy` expose stable above-the-fold shells before avoidable async work"
    - "Search page follows AGENTS.md: no extra React component declarations are created inside `src/app/(storefront)/search/page.tsx`"
    - "UX-01 remains green: no duplicate skip link and no mobile horizontal overflow regression"
    - "Plan 17-15 must regenerate docs/launch/performance-evidence.md, docs/launch/final-production-readiness-report.md, and 17-VERIFICATION.md after this remediation"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Non-blocking launch font display configuration"
    - path: "src/app/(storefront)/cart/page.tsx"
      provides: "Empty-cart fast path that avoids account-session work when no cart exists"
    - path: "src/app/(storefront)/cart/_components/loading-skeleton.tsx"
      provides: "Stable empty-cart loading footprint"
    - path: "src/app/(storefront)/search/page.tsx"
      provides: "Single-component route shell that renders SearchHero before result fetch completion"
    - path: "src/app/(storefront)/search/_components/results.tsx"
      provides: "Route-local async result boundary preserving search analytics without extra page-file components"
    - path: "src/components/search/search-results-view/search-hero.tsx"
      provides: "Search hero that can render from parsed search state before result data exists"
    - path: "src/components/search/search-results-view/search-results-view.tsx"
      provides: "Below-hero result/filter/pagination body"
    - path: "scripts/component-contracts/performance-fonts.test.mjs"
      provides: "Static guard for font-display choices"
    - path: "docs/launch/performance-evidence.md"
      provides: "Regenerated strict performance evidence required to close PERF-01 in plan 17-15"
    - path: "docs/launch/final-production-readiness-report.md"
      provides: "Regenerated launch decision required to close PERF-01 in plan 17-15"
    - path: ".planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md"
      provides: "Updated verification record required after plan 17-15 reruns"
  key_links:
    - from: "src/app/(storefront)/search/page.tsx"
      to: "src/components/search/search-results-view/search-hero.tsx"
      via: "SearchHero rendered from parsed state before result Suspense"
      pattern: "SearchHero"
    - from: "src/app/(storefront)/search/page.tsx"
      to: "src/app/(storefront)/search/_components/results.tsx"
      via: "resultPromise passed into route-local async result boundary"
      pattern: "resultPromise"
    - from: "src/app/(storefront)/cart/page.tsx"
      to: "src/lib/cart/actions.ts"
      via: "getCartAction starts before account-session lookup"
      pattern: "Promise\\.all.*getCartAction"
    - from: "src/app/layout.tsx"
      to: "scripts/component-contracts/performance-fonts.test.mjs"
      via: "font display optional contract"
      pattern: "display:\\s*'optional'"
---

<objective>
Close the font and render-delay portion of the PERF-01 blocker for text-LCP routes. This plan keeps the route shells server-owned, removes avoidable empty-cart/account-session and search-result waterfalls, and fixes the search-page task so it complies with AGENTS.md's one-component-per-file rule.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-RESEARCH.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-PATTERNS.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
@docs/launch/performance-evidence.md
@node_modules/next/dist/docs/01-app/02-guides/streaming.md
@node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
@node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-cache.md
@.agents/skills/vercel-react-best-practices/SKILL.md
@.agents/skills/vercel-react-best-practices/rules/async-parallel.md
@.agents/skills/vercel-react-best-practices/rules/async-suspense-boundaries.md
@.agents/skills/vercel-react-best-practices/rules/rerender-no-inline-components.md
@.agents/skills/next-cache-components/SKILL.md
</execution_context>

<context>
@src/app/layout.tsx
@src/app/(storefront)/cart/page.tsx
@src/app/(storefront)/cart/_components/loading-skeleton.tsx
@src/app/(storefront)/search/page.tsx
@src/components/search/search-results-view/search-results-view.tsx
@src/components/search/search-results-view/search-hero.tsx
@src/app/(storefront)/pages/privacy-policy/page.tsx

<interfaces>
Current search page shape:
```tsx
export default function SearchPage({ searchParams }: Props)
```

Current search hero contract to replace:
```tsx
export function SearchHero({
  result,
  state,
}: {
  result: SearchaniseSearchResult
  state: SearchRouteState
})
```

Target search hero contract:
```tsx
export function SearchHero({
  state,
  countLabel,
}: {
  state: SearchRouteState
  countLabel?: string
})
```

Target route-local results wrapper:
```tsx
export async function SearchResults({
  resultPromise,
  state,
}: {
  resultPromise: Promise<SearchaniseSearchResult>
  state: SearchRouteState
})
```
</interfaces>
</context>

<source_audit>

| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Prove storefront is operationally ready with performance evidence and final audit | Covered by text LCP shell remediation feeding plan 17-15 evidence |
| REQ | PERF-01 | Covered by font display, cart fast path, search shell split, and compact privacy first viewport |
| REQ | UX-01 | Covered by preserving production smoke/mobile overflow gates |
| REQ | QA-02 | Covered by final evidence dependency and automated lint/type/e2e checks |
| RESEARCH | Streaming guidance: keep LCP shell outside slow Suspense and reserve fallback dimensions | Covered by cart/search shell changes |
| CONTEXT | D-15 practical LCP/CLS/TBT gate | Covered by reducing render-delay LCP without changing thresholds |
| CONTEXT | D-16 fake-provider production lifecycle | Preserved; no real checkout/OAuth/provider testing added |
| CONTEXT | D-17 final report separation | Deferred to plan 17-15; this plan does not edit final report |
| EXCLUDED | Owner/staging/field acceptance | Excluded here; no dated evidence supplied |

</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Make launch fonts non-blocking for text LCP routes</name>
  <read_first>
    - src/app/layout.tsx
    - scripts/component-contracts/performance-fonts.test.mjs
    - node_modules/next/dist/docs/01-app/02-guides/streaming.md
    - node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
  </read_first>
  <files>src/app/layout.tsx, scripts/component-contracts/performance-fonts.test.mjs</files>
  <action>
    In `src/app/layout.tsx`, change all four `next/font/google` font configs (`Spectral`, `Hanken_Grotesk`, `Space_Mono`, `Caveat`) to `display: 'optional'`. Preserve the existing weight/style/subset/variable choices from 17-10:

    - `Spectral`: `weight: ['400', '500']`, `style: ['normal']`
    - `Hanken_Grotesk`: no explicit weight list
    - `Space_Mono`: `weight: ['400', '700']`, `preload: false`
    - `Caveat`: `preload: false`

    Update `scripts/component-contracts/performance-fonts.test.mjs` so it asserts `display: 'optional'` for all four font configs and continues to assert the narrowed Spectral weights/styles plus `preload: false` for `Space_Mono` and `Caveat`.

    Do not remove font variables from `<body>` and do not add raw CSS, inline styles, or a new font package.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/performance-fonts.test.mjs</automated>
    <automated>pnpm lint -- src/app/layout.tsx scripts/component-contracts/performance-fonts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains four `display: 'optional'` font configs.
    - `scripts/component-contracts/performance-fonts.test.mjs` fails if `display: 'swap'` is reintroduced for launch fonts.
    - `Space_Mono` and `Caveat` remain `preload: false`.
  </acceptance_criteria>
  <done>Font loading no longer creates avoidable late text repaint risk for static/text LCP routes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove empty-cart account-session waterfall and reserve fallback space</name>
  <read_first>
    - .agents/skills/vercel-react-best-practices/rules/async-parallel.md
    - src/app/(storefront)/cart/page.tsx
    - src/app/(storefront)/cart/_components/loading-skeleton.tsx
  </read_first>
  <files>src/app/(storefront)/cart/page.tsx, src/app/(storefront)/cart/_components/loading-skeleton.tsx</files>
  <action>
    Apply the existing server-component cart semantics without changing checkout behavior:

    - In `src/app/(storefront)/cart/page.tsx`, resolve `searchParams` and `getCartAction()` in parallel using `Promise.all`.
    - Only call `getCustomerAccountSession()` after the cart is known, and only when a cart exists or `params.checkout === 'identity-sync-failed'`. This preserves signed-in checkout context for real carts while removing account-session work from the local empty-cart LCP path.
    - Keep `CartRecommendations` gated behind `cart && cart.totalQuantity > 0`.
    - In `src/app/(storefront)/cart/_components/loading-skeleton.tsx`, add a visible above-the-fold fallback block with the same empty-cart vertical footprint: icon placeholder, `h2` text `Checking your cart`, and one body line. Keep `role="status"` and `aria-live="polite"`. The fallback must reserve at least `min-h-72` for the main column so Suspense replacement does not shift the surrounding layout.

    Do not run live Shopify hosted checkout and do not change cart cookie semantics, checkout terms, buyer identity sync, or Shopify operations.
  </action>
  <verify>
    <automated>pnpm lint -- "src/app/(storefront)/cart/page.tsx" "src/app/(storefront)/cart/_components/loading-skeleton.tsx"</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(storefront)/cart/page.tsx` starts `getCartAction()` in parallel with `searchParams`.
    - `src/app/(storefront)/cart/page.tsx` does not call `getCustomerAccountSession()` before knowing whether a cart exists or checkout identity-sync context is required.
    - `src/app/(storefront)/cart/_components/loading-skeleton.tsx` contains `Checking your cart` and `min-h-72`.
  </acceptance_criteria>
  <done>The empty-cart route exposes a stable shell without waiting on avoidable account-session work.</done>
</task>

<task type="auto">
  <name>Task 3: Split search shell into a real route-local result boundary</name>
  <read_first>
    - .agents/skills/vercel-react-best-practices/rules/async-suspense-boundaries.md
    - .agents/skills/vercel-react-best-practices/rules/rerender-no-inline-components.md
    - .agents/skills/next-cache-components/SKILL.md
    - src/app/(storefront)/search/page.tsx
    - src/components/search/search-results-view/search-results-view.tsx
    - src/components/search/search-results-view/search-hero.tsx
    - src/app/(storefront)/pages/privacy-policy/page.tsx
  </read_first>
  <files>src/app/(storefront)/search/page.tsx, src/app/(storefront)/search/_components/results.tsx, src/components/search/search-results-view/search-results-view.tsx, src/components/search/search-results-view/search-hero.tsx, src/app/(storefront)/pages/privacy-policy/page.tsx</files>
  <action>
    Refactor search without creating a small async shell component inside `src/app/(storefront)/search/page.tsx`:

    - Create `src/app/(storefront)/search/_components/results.tsx` as the single route-local async result boundary. Export named component `SearchResults` with props `{ resultPromise: Promise<SearchaniseSearchResult>; state: SearchRouteState }`. It awaits `resultPromise`, computes `resultCount`, renders `SearchAnalytics` when `result.status === 'success'`, then renders `<SearchResultsView result={result} state={state} />`.
    - In `src/app/(storefront)/search/page.tsx`, remove the existing `SearchContent` and `SearchFallback` component declarations. Keep only `generateMetadata` plus the default Next page export as React components in this file.
    - Make the default page export `async`, await `searchParams`, parse `state`, start `const resultPromise = getSearchaniseSearchResults(state)` without awaiting it before rendering the shell, render `<SearchHero state={state} />` before the result-fetching `<Suspense>` boundary, and render `<SearchResults resultPromise={resultPromise} state={state} />` inside that boundary.
    - Import `SearchHero` directly from `@/components/search/search-results-view/search-hero` unless a local barrel already exports it by the time this task runs. Do not add broad barrel exports solely for this route.
    - Change `SearchHero` to accept `{ state, countLabel?: string }`. It should render the title from `state.query`, render the existing no-query helper text/form when no query exists, and render `countLabel ?? 'Searching products...'` for query pages before results resolve.
    - Remove `<SearchHero ... />` from `SearchResultsView`; that component should own only the below-hero alert/filter/sort/product/pagination body.
    - In `src/app/(storefront)/pages/privacy-policy/page.tsx`, keep content static and reduce first-viewport LCP text size only within the compact policy route: change the first large policy body paragraph that Lighthouse identified from `type-body` to `type-body-sm` while preserving all legal-review copy and headings.

    The new `results.tsx` is route-local and not in `src/components`, so it does not need a Storybook story. Do not use `use cache` around runtime `searchParams`, cookies, or headers. Do not mark parent wrappers with `'use client'`. Do not change Searchanise fetch semantics.
  </action>
  <verify>
    <automated>pnpm lint -- "src/app/(storefront)/search/page.tsx" "src/app/(storefront)/search/_components/results.tsx" src/components/search/search-results-view/search-results-view.tsx src/components/search/search-results-view/search-hero.tsx "src/app/(storefront)/pages/privacy-policy/page.tsx"</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(storefront)/search/page.tsx` no longer declares `SearchContent` or `SearchFallback`.
    - `src/app/(storefront)/search/page.tsx` renders `SearchHero` before the result-fetching Suspense boundary.
    - `src/app/(storefront)/search/_components/results.tsx` renders `SearchAnalytics` and `SearchResultsView` after awaiting `resultPromise`.
    - `src/components/search/search-results-view/search-results-view.tsx` does not render `<SearchHero`.
    - Production smoke still passes for `/cart`, `/search?q=tea`, `/account`, and mobile overflow.
  </acceptance_criteria>
  <done>Search and privacy text-LCP routes expose their stable first viewport before avoidable provider work completes, while search-page component structure follows AGENTS.md.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runtime route params/cookies to static shell | Search params and cookies are untrusted request inputs and must not be cached incorrectly |
| Cart route to account session | Empty-cart local evidence must not force account/OAuth session checks |
| Launch reports to privacy boundary | Evidence must not include secrets, PII, checkout URLs, cart IDs, or raw provider payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-17-36 | Information Disclosure | cart/search shell | mitigate | Do not print or render tokens, emails, cart IDs, checkout URLs, or raw provider data in fallbacks |
| T-17-37 | Tampering | Cache Components usage | mitigate | Do not put cookies/searchParams inside `use cache`; use Suspense and argument passing only |
| T-17-38 | Denial of Service | render shell | mitigate | Avoid unnecessary session/provider calls on empty-cart and above-the-fold shell paths |

</threat_model>

<verification>

```bash
node --test scripts/component-contracts/performance-fonts.test.mjs
pnpm lint -- src/app/layout.tsx "src/app/(storefront)/cart/page.tsx" "src/app/(storefront)/cart/_components/loading-skeleton.tsx" "src/app/(storefront)/search/page.tsx" "src/app/(storefront)/search/_components/results.tsx" src/components/search/search-results-view/search-results-view.tsx src/components/search/search-results-view/search-hero.tsx "src/app/(storefront)/pages/privacy-policy/page.tsx" scripts/component-contracts/performance-fonts.test.mjs
pnpm typecheck
pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts
```

</verification>

<success_criteria>

- Font rendering no longer delays first text paint through `font-display: swap`.
- Cart empty-state route avoids account-session work when no cart exists.
- Search hero renders outside Searchanise result fetch without declaring extra route-file components.
- Privacy policy keeps static legal-review copy while reducing the first viewport text LCP target.
- UX/accessibility smoke coverage remains green.

</success_criteria>

<output>

After completion, create `.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-13-SUMMARY.md`.

</output>
