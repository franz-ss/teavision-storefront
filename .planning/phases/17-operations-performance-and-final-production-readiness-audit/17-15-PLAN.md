---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 15
type: execute
wave: 12
depends_on: ["17-12", "17-13", "17-14"]
files_modified:
  - src/app/(storefront)/account/loading.tsx
  - scripts/component-contracts/account-performance.test.mjs
  - scripts/component-contracts/render-shell-performance.test.mjs
  - src/components/homepage/hero/hero.tsx
  - src/components/product/product-gallery/product-gallery.tsx
  - src/components/collection/product-card/product-card.tsx
  - src/app/(storefront)/collections/[handle]/_components/hero.tsx
  - src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx
  - scripts/component-contracts/launch-image-performance.test.mjs
  - scripts/performance/probe-lighthouse.mjs
  - docs/launch/performance-evidence.md
  - docs/launch/final-production-readiness-report.md
  - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
autonomous: true
requirements:
  - PERF-01
  - UX-01
  - QA-02
gap_closure: true
must_haves:
  truths:
    - "PERF-01: strict local Lighthouse evidence has no launch-blocking Home/PDP LCP regression and no unaccepted route FAIL rows, or a valid dated owner/staging/field acceptance marks local lab failures non-blocking"
    - "Account CLS remediation from 17-14 is actually present in source and guarded by strict contracts that reject the old min-h-136/md:min-h-128 geometry"
    - "Home, PDP, and PLP launch-critical local AVIF images use current Next 16 preload semantics and are served directly, not through the local _next/image optimizer path"
    - "Performance evidence remediation notes describe the actual implemented image/font/shell strategy and never claim stale eager/fetchPriority behavior"
    - "Final readiness and Phase 17 verification are regenerated only from strict PASS evidence or validated pre-existing acceptance; no threshold edits, skip flags, or fabricated acceptance are allowed"
  artifacts:
    - path: "src/app/(storefront)/account/loading.tsx"
      provides: "Account loading fallback using the same stable bracketed geometry as layout, login, and account page wrappers"
    - path: "scripts/component-contracts/account-performance.test.mjs"
      provides: "Strict account geometry contract rejecting the old tokenized min-height form"
    - path: "scripts/component-contracts/render-shell-performance.test.mjs"
      provides: "Render-shell contract that reuses the strict account geometry assertion"
    - path: "src/components/homepage/hero/hero.tsx"
      provides: "Direct preloaded local Home AVIF LCP image"
    - path: "src/components/product/product-gallery/product-gallery.tsx"
      provides: "Direct preloaded fake-provider PDP AVIF LCP image when local launch media is used"
    - path: "src/components/collection/product-card/product-card.tsx"
      provides: "Direct preloaded fake-provider PLP AVIF card image for first visible products"
    - path: "scripts/component-contracts/launch-image-performance.test.mjs"
      provides: "Static guard for direct local launch AVIF delivery and invalid Next Image prop combinations"
    - path: "scripts/performance/probe-lighthouse.mjs"
      provides: "Accurate regenerated performance evidence notes and route diagnostics"
    - path: "docs/launch/performance-evidence.md"
      provides: "Strict post-remediation mobile Lighthouse evidence"
    - path: "docs/launch/final-production-readiness-report.md"
      provides: "Final automated readiness score and launch decision"
    - path: ".planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md"
      provides: "Authoritative Phase 17 truth status after final evidence"
  key_links:
    - from: "src/app/(storefront)/account/loading.tsx"
      to: "scripts/component-contracts/account-performance.test.mjs"
      via: "strict account loading geometry assertion"
      pattern: "min-h-\\[34rem\\].*md:min-h-\\[32rem\\]"
    - from: "src/components/collection/product-card/product-card.tsx"
      to: "scripts/component-contracts/launch-image-performance.test.mjs"
      via: "ProductCard local priority API renders as Next 16 preload plus direct AVIF delivery"
      pattern: "preload=\\{priority\\}"
    - from: "src/components/homepage/hero/hero.tsx"
      to: "docs/launch/performance-evidence.md"
      via: "Home LCP diagnostics should no longer report local launch AVIF through _next/image"
      pattern: "homepage-hero-tea-harvest-lcp\\.avif"
    - from: "docs/launch/performance-evidence.md"
      to: "docs/launch/final-production-readiness-report.md"
      via: "performance check in final audit matrix"
      pattern: "\\| performance \\| PASS \\|"
    - from: "docs/launch/final-production-readiness-report.md"
      to: ".planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md"
      via: "PERF-01 truth status"
      pattern: "PERF-01"
---

<objective>
Replace the blocked evidence-only 17-15 attempt with a concrete PERF-01 remediation pass. The current verification still has seven strict local Lighthouse FAIL rows and no dated owner/staging/field acceptance artifact. This plan first repairs source/contract drift from 17-12 and 17-14, then removes avoidable local image-optimizer latency for precompressed launch AVIFs, then regenerates strict performance, final readiness, and Phase 17 verification evidence.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-RESEARCH.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-PATTERNS.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-12-SUMMARY.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-13-SUMMARY.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-14-SUMMARY.md
@docs/launch/performance-evidence.md
@docs/launch/final-production-readiness-report.md
@node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
@node_modules/next/dist/docs/01-app/01-getting-started/12-images.md
@node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
@node_modules/next/dist/docs/01-app/02-guides/streaming.md
@scripts/performance/probe-lighthouse.mjs
@scripts/launch/run-final-readiness-audit.mjs
@scripts/launch/run-final-readiness-audit.test.mjs
</execution_context>

<context>
Current post-17-14 evidence, generated 2026-06-24, records seven strict local mobile Lighthouse FAIL rows:

| Route | Current issue |
| --- | --- |
| `/` | LCP 4841ms, image-resource, local Home AVIF still reported through `_next/image` |
| `/products/test-standard-tea` | LCP 4005ms, image-resource, fake-provider PDP image still reported through `_next/image` |
| `/collections/all` | LCP 4155ms, image-resource, fake-provider PLP card image still reported through `_next/image` |
| `/cart` | LCP 4016ms, render-delay |
| `/search?q=tea` | LCP 3854ms, render-delay |
| `/account` | LCP 5204ms and CLS 0.128, layout-shift |
| `/pages/privacy-policy` | LCP 3951ms, render-delay |

The source tree also currently contradicts two recent plan summaries:

- `src/app/(storefront)/account/loading.tsx` still contains `min-h-136 md:min-h-128`, while 17-14 says all account wrappers use `min-h-[34rem] md:min-h-[32rem]`.
- `scripts/component-contracts/account-performance.test.mjs` and `render-shell-performance.test.mjs` allow either old or new account geometry, so they do not guard the 17-14 acceptance criteria.
- `src/components/collection/product-card/product-card.tsx` still renders `loading={priority ? 'eager' : 'lazy'}` and `fetchPriority={priority ? 'high' : 'auto'}`, while 17-12 says launch card images render as Next 16 `preload`.
- `scripts/component-contracts/launch-image-performance.test.mjs` currently asserts the old ProductCard eager/fetchPriority behavior, so it must be corrected with the implementation.
</context>

<source_audit>

| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Repeatable final production-readiness audit reaches an honest launch-ready state | Covered by strict final performance and final audit rerun in Task 3 |
| REQ | PERF-01 | Covered by account CLS repair, direct local AVIF delivery, full strict performance rerun, or valid pre-existing acceptance |
| REQ | UX-01 | Covered by production smoke/mobile overflow and live OAuth boundary checks |
| REQ | QA-02 | Covered by contract tests, final audit tests, final readiness report, and verification schema drift |
| CONTEXT | D-14: representative routes | Covered by unchanged performance route matrix for home, PDP, collection, cart, search, account, and policy route |
| CONTEXT | D-15: practical Lighthouse/Web Vitals gate | Covered without threshold changes or skip flags |
| CONTEXT | D-16: fake-provider local production lifecycle | Preserved; no real Shopify checkout, payment, order, OAuth, protected data, B2B, or Search Console tests are added |
| CONTEXT | D-17: separated automated readiness and owner-gated proof | Covered by final readiness audit and owner-gated table preservation |
| RESEARCH | Next 16 Image preload for LCP and direct local media safety | Covered by Task 2 and launch-image contracts |
| CURRENT GAP | Seven strict local FAIL rows and no acceptance artifact | Covered by remediation plus strict evidence gate; if still red, execution must stop blocked |

</source_audit>

<decision_coverage>

The earlier Phase 17 implementation plans own D-01: through D-13:. This revised gap plan preserves those monitoring, health, safe diagnostics, runbook, and redacted logging decisions by not changing their source boundaries and by rerunning the final readiness audit that checks their evidence.

This plan actively exercises D-14: representative launch routes, D-15: practical Lighthouse/Web Vitals gates, D-16: fake-provider production lifecycle boundaries, and D-17: separated automated readiness versus owner-gated proof.

</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Restore strict account geometry contracts</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/02-guides/streaming.md
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-14-SUMMARY.md
    - src/app/(storefront)/account/layout.tsx
    - src/app/(storefront)/account/loading.tsx
    - src/app/(storefront)/account/login/page.tsx
    - src/app/(storefront)/account/page.tsx
    - src/app/(storefront)/account/_components/login-panel/login-panel.tsx
    - scripts/component-contracts/account-performance.test.mjs
    - scripts/component-contracts/render-shell-performance.test.mjs
  </read_first>
  <files>src/app/(storefront)/account/loading.tsx, scripts/component-contracts/account-performance.test.mjs, scripts/component-contracts/render-shell-performance.test.mjs</files>
  <action>
    Repair the account CLS source/contract drift from 17-14:

    - In `src/app/(storefront)/account/loading.tsx`, replace the old wrapper geometry `min-h-136 md:min-h-128` with the exact bracketed geometry `min-h-[34rem] md:min-h-[32rem]`.
    - In `scripts/component-contracts/account-performance.test.mjs`, remove the permissive `|| /min-h-136/` and `|| /md:min-h-128/` fallbacks. Every account wrapper source must match `min-h-[34rem]` and `md:min-h-[32rem]`, and the test must explicitly reject `min-h-136` and `md:min-h-128`.
    - In `scripts/component-contracts/render-shell-performance.test.mjs`, update `assertAccountShellContract` with the same strict assertions. It must fail if any of the four account wrapper files uses the old geometry.
    - Preserve `LoginPanel` `min-h-72`, `content-start`, and `prefetch={false}`. Do not change Customer Account login start, callback, logout, protected data, B2B pricing, or live OAuth behavior.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/account-performance.test.mjs scripts/component-contracts/render-shell-performance.test.mjs</automated>
    <automated>pnpm lint -- "src/app/(storefront)/account/loading.tsx" scripts/component-contracts/account-performance.test.mjs scripts/component-contracts/render-shell-performance.test.mjs</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(storefront)/account/loading.tsx` contains `min-h-[34rem] md:min-h-[32rem]`.
    - No account wrapper file contains `min-h-136` or `md:min-h-128`.
    - Account and render-shell contracts fail if the old account geometry returns.
    - Login panel still contains `min-h-72`, `content-start`, and `prefetch={false}`.
  </acceptance_criteria>
  <done>Account loading/login/dashboard geometry matches the 17-14 remediation and is guarded strictly before Lighthouse reruns.</done>
</task>

<task type="auto">
  <name>Task 2: Serve launch-critical local AVIF images directly and correct image evidence contracts</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
    - node_modules/next/dist/docs/01-app/01-getting-started/12-images.md
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-12-SUMMARY.md
    - src/components/homepage/content.ts
    - src/components/homepage/hero/hero.tsx
    - src/components/product/product-gallery/product-gallery.tsx
    - src/components/collection/product-card/product-card.tsx
    - src/app/(storefront)/collections/[handle]/_components/hero.tsx
    - src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx
    - src/lib/shopify/image-url.ts
    - tests/mocks/shopify-graphql-server.ts
    - scripts/component-contracts/launch-image-performance.test.mjs
    - scripts/performance/probe-lighthouse.mjs
  </read_first>
  <files>src/components/homepage/hero/hero.tsx, src/components/product/product-gallery/product-gallery.tsx, src/components/collection/product-card/product-card.tsx, src/app/(storefront)/collections/[handle]/_components/hero.tsx, src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx, scripts/component-contracts/launch-image-performance.test.mjs, scripts/performance/probe-lighthouse.mjs</files>
  <action>
    Keep the 17-12 local AVIF sources, but stop running them through the local Next image optimizer during launch lab evidence:

    - In `src/components/homepage/hero/hero.tsx`, keep `preload`, `fill`, and `sizes="100vw"` on the hero image, add `unoptimized`, and remove any stale `loading="eager"` or `fetchPriority="high"` if present. If `quality={68}` remains on this unoptimized local AVIF and Next warns during lint/build, remove `quality` from this image only.
    - In `src/components/product/product-gallery/product-gallery.tsx`, keep `preload={i === 0}` for the first image and add a local launch image predicate such as `image.url.startsWith('/images/homepage/') && image.url.endsWith('-lcp.avif')`. Pass `unoptimized={isLaunchLcpImage}` to the main gallery `<Image>`. Do not add `loading="eager"` or `fetchPriority="high"`.
    - In `src/components/collection/product-card/product-card.tsx`, keep the local `priority` prop API but render the card `<Image>` with `preload={priority}`. Remove `loading={priority ? 'eager' : 'lazy'}` and `fetchPriority={priority ? 'high' : 'auto'}`. Add `unoptimized={product.featuredImage.url.startsWith('/images/homepage/') && product.featuredImage.url.endsWith('-lcp.avif')}` so the fake-provider PLP image is served directly.
    - In `src/app/(storefront)/collections/[handle]/_components/hero.tsx` and `collection-rich-hero.tsx`, keep `preload` for above-the-fold collection art and add direct local AVIF delivery only when the image URL is a local `/images/...-lcp.avif` path. Do not add global image unoptimization.
    - In `scripts/component-contracts/launch-image-performance.test.mjs`, update the current stale ProductCard expectations. The fixed-file scan must assert:
      - the Home and fake Shopify AVIF files still exist within their byte budgets;
      - `src/components/homepage/content.ts` references `homepage-hero-tea-harvest-lcp.avif`;
      - `tests/mocks/shopify-graphql-server.ts` references `bulk-wholesale-lcp.avif`;
      - `HomepageHero`, `ProductGallery`, and `ProductCard` contain `preload`;
      - `HomepageHero`, `ProductGallery`, and `ProductCard` contain `unoptimized`;
      - no scanned source file uses deprecated `<Image priority`;
      - no scanned `<Image>` block combines `preload` with `fetchPriority="high"`;
      - `ProductCard` no longer contains `loading={priority ? 'eager' : 'lazy'}` or `fetchPriority={priority ? 'high' : 'auto'}`.
    - In `scripts/performance/probe-lighthouse.mjs`, update `renderRemediationNotes()` so regenerated evidence accurately describes the current strategy: precompressed local AVIF launch images, Next 16 `preload`, direct `unoptimized` local delivery for Home/PDP/PLP fake-provider launch media, optional fonts, and strict FAIL rows preserved when metrics miss. Remove stale prose claiming Home uses `loading="eager"` plus `fetchPriority="high"` without `preload`.

    Do not change `THRESHOLDS`, `DEFAULT_ROUTES`, `calculateStatus()`, `evaluateReadinessRows()`, final audit scoring, Shopify query shapes, or owner-gated proof handling.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/launch-image-performance.test.mjs</automated>
    <automated>pnpm test:contracts</automated>
    <automated>pnpm lint -- src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx src/components/collection/product-card/product-card.tsx "src/app/(storefront)/collections/[handle]/_components/hero.tsx" "src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx" scripts/component-contracts/launch-image-performance.test.mjs scripts/performance/probe-lighthouse.mjs</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --url / --url /products/test-standard-tea --url /collections/all --json-summary --allow-metric-failures</automated>
    <automated>node -e "const fs=require('fs');const text=fs.readFileSync('docs/launch/performance-evidence.md','utf8');for(const bad of ['_next/image?url=%2Fimages%2Fhomepage%2Fhomepage-hero-tea-harvest-lcp','_next/image?url=%2Fimages%2Fhomepage%2Fbulk-wholesale-lcp']){if(text.includes(bad)) throw new Error('local launch AVIF still routed through optimizer: '+bad)}if(text.includes('loading=\"eager\"')&&text.includes('fetchPriority=\"high\"')) throw new Error('stale image remediation note remains');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ProductCard` renders first-visible images through `preload={priority}` and does not render priority-driven eager/fetchPriority props.
    - Local launch AVIF Home/PDP/PLP image paths are served directly with `unoptimized`, not through `_next/image` in regenerated image-route diagnostics.
    - `launch-image-performance.test.mjs` fails if deprecated Next Image `priority`, preload/fetchPriority conflicts, or stale ProductCard eager/fetchPriority behavior returns.
    - Regenerated `docs/launch/performance-evidence.md` no longer contains stale remediation notes about Home using eager/high fetch priority without preload.
  </acceptance_criteria>
  <done>Launch-critical local AVIF image routes avoid the local optimizer path and image evidence contracts match the current Next 16 implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate strict performance, final readiness, and Phase 17 verification</name>
  <read_first>
    - docs/launch/performance-evidence.md
    - docs/launch/final-production-readiness-report.md
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md
    - scripts/performance/probe-lighthouse.mjs
    - scripts/launch/run-final-readiness-audit.mjs
    - scripts/launch/run-final-readiness-audit.test.mjs
    - docs/launch/performance-acceptance.md if it exists before this task starts
  </read_first>
  <files>docs/launch/performance-evidence.md, docs/launch/final-production-readiness-report.md, .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md</files>
  <action>
    Run the final evidence gate from the repaired source tree. First record whether `docs/launch/performance-acceptance.md` exists before this task starts. Do not create it in this task.

    Run the post-remediation quality gate:

    ```bash
    node --test scripts/component-contracts/launch-image-performance.test.mjs scripts/component-contracts/performance-fonts.test.mjs scripts/component-contracts/account-performance.test.mjs scripts/component-contracts/render-shell-performance.test.mjs
    pnpm test:contracts
    pnpm lint
    pnpm typecheck
    pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts
    node --test scripts/launch/run-final-readiness-audit.test.mjs
    ```

    Then run strict performance with no metric override:

    ```bash
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary
    ```

    Strict closure requires all of the following:
    - Command exits `0`.
    - `docs/launch/performance-evidence.md` contains `Launch-blocking: no`.
    - `docs/launch/performance-evidence.md` contains no `| FAIL |` route rows.
    - `/` and `/products/test-standard-tea` rows have LCP at or below `2500ms`.
    - `/account` row has CLS at or below `0.100`.
    - LCP diagnostics for local launch AVIFs do not contain `_next/image?url=%2Fimages%2Fhomepage%2Fhomepage-hero-tea-harvest-lcp` or `_next/image?url=%2Fimages%2Fhomepage%2Fbulk-wholesale-lcp`.

    If strict performance passes, run:

    ```bash
    pnpm audit:readiness
    ```

    If strict performance fails and a real operator-supplied `docs/launch/performance-acceptance.md` existed before this task started, validate the accepted path:

    ```bash
    node --test scripts/launch/run-final-readiness-audit.test.mjs
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary --allow-metric-failures
    pnpm audit:readiness -- --performance-acceptance docs/launch/performance-acceptance.md
    ```

    The acceptance artifact must be dated, must identify the owner/staging/field evidence source and approver, and must be accepted by the existing audit parser. If validation fails, treat this task as failed.

    If strict performance fails and no valid pre-existing acceptance artifact exists, regenerate red evidence for diagnostics only:

    ```bash
    pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary --allow-metric-failures
    pnpm audit:readiness
    ```

    Then stop execution as a blocker. Do not create `17-15-SUMMARY.md`, do not mark verification green, and do not change thresholds or use `--skip-performance`, `--skip-owner-gated`, `--no-start-server`, or any local lab acceptance shortcut.

    When strict performance passes or accepted evidence validates, update `.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md` from the final report:

    - If final report contains `| performance | PASS |`, no unaccepted required automated `FAIL` rows, and score `100/100`, set frontmatter `status: verified`, update `verified` to the current command timestamp, set `score` to `9/9 phase truths verified; automated code readiness green`, clear `gaps_remaining`, and mark the PERF-01 observable truth `VERIFIED`.
    - If final report contains `Accepted non-blocking by dated performance acceptance:`, set `status: verified` only if `docs/launch/performance-acceptance.md` exists and validates; cite the acceptance Date, Approver, and Evidence source.
    - Preserve existing verified OPS-01, OPS-02, OPS-03, OPS-04, UX-01, QA-01, QA-02, and QA-03 evidence. Owner-gated Shopify/admin/Search Console rows stay `approved`, `pending`, or `owner-blocked`; do not turn pending owner proof into automated failure.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/launch-image-performance.test.mjs scripts/component-contracts/performance-fonts.test.mjs scripts/component-contracts/account-performance.test.mjs scripts/component-contracts/render-shell-performance.test.mjs</automated>
    <automated>pnpm test:contracts</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
    <automated>node --test scripts/launch/run-final-readiness-audit.test.mjs</automated>
    <automated>powershell -NoProfile -Command "if (Test-Path 'docs/launch/performance-acceptance.md') { pnpm audit:readiness -- --performance-acceptance docs/launch/performance-acceptance.md } else { pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm audit:readiness }"</automated>
    <automated>gsd-sdk query verify.schema-drift "17"</automated>
    <automated>node -e "const fs=require('fs');const report=fs.readFileSync('docs/launch/final-production-readiness-report.md','utf8');const verification=fs.readFileSync('.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md','utf8');const accepted=report.includes('Accepted non-blocking by dated performance acceptance:');if(report.includes('| performance | FAIL |')&&!accepted) throw new Error('unaccepted performance FAIL cannot complete plan 17-15');if(report.includes('| performance | PASS |')&&!verification.includes('status: verified')) throw new Error('verification must be verified when performance passes');if(accepted&&!fs.existsSync('docs/launch/performance-acceptance.md')) throw new Error('acceptance referenced without file');if(accepted&&!verification.includes('status: verified')) throw new Error('verification must be verified when accepted performance is validated');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Strict pass path: performance evidence contains `Launch-blocking: no`, final report contains `| performance | PASS |`, and final report score is `100/100`.
    - Acceptance path: `docs/launch/performance-acceptance.md` existed before this task, validates through `pnpm audit:readiness -- --performance-acceptance docs/launch/performance-acceptance.md`, and final report contains `Accepted non-blocking by dated performance acceptance:`.
    - No-evidence strict failure is not accepted as done; it requires blocker escalation or replanning and must not produce a completion summary.
    - Final report never contains `100/100` while an unaccepted `| performance | FAIL |` row remains.
    - `17-VERIFICATION.md` is regenerated from passing or accepted evidence, not manually marked green.
  </acceptance_criteria>
  <done>Phase 17 verification follows strict passing performance evidence or validated dated acceptance only.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local launch AVIF delivery to Lighthouse evidence | Direct local asset serving must not weaken the performance gate or hide failed route rows |
| Fake-provider media to browser evidence | Local PDP/PLP media must stay fake-provider safe and must not call live Shopify checkout or external provider media |
| Optional owner acceptance to automated report | Acceptance can only be used if a valid pre-existing dated artifact exists |
| Final report to verification | Verification must reflect evidence, not desired launch state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-17-45 | Tampering | performance thresholds | mitigate | Do not edit `THRESHOLDS`, final audit scoring, skip flags, or metric failure semantics |
| T-17-46 | Repudiation | performance acceptance | mitigate | Do not create acceptance artifact; cite Date/Approver/Evidence source only when the file pre-exists and validates |
| T-17-47 | Information Disclosure | launch media and reports | mitigate | Use local `/images/...` fake-provider media; do not add secrets, tokens, PII, checkout URLs, cart IDs, order IDs, or raw provider payloads |
| T-17-48 | Denial of Service | local image optimizer path | mitigate | Serve precompressed launch AVIFs directly with `unoptimized` only for local launch media instead of globally disabling image optimization |
| T-17-49 | Spoofing | Customer Account and hosted checkout proof | mitigate | Preserve fake-provider production smoke boundaries and do not run live OAuth, payment, order, protected-data, B2B, or Search Console proof |

</threat_model>

<verification>

```bash
node --test scripts/component-contracts/account-performance.test.mjs scripts/component-contracts/render-shell-performance.test.mjs
node --test scripts/component-contracts/launch-image-performance.test.mjs
pnpm test:contracts
pnpm lint
pnpm typecheck
pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts
node --test scripts/launch/run-final-readiness-audit.test.mjs
powershell -NoProfile -Command "if (Test-Path 'docs/launch/performance-acceptance.md') { pnpm audit:readiness -- --performance-acceptance docs/launch/performance-acceptance.md } else { pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173 --json-summary; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm audit:readiness }"
gsd-sdk query verify.schema-drift "17"
```

</verification>

<success_criteria>

- Account loading, layout, login, and account page wrappers all use `min-h-[34rem] md:min-h-[32rem]`, and contracts reject the old geometry.
- Home, PDP, and collection fake-provider launch AVIF paths are served directly with `unoptimized` and current Next 16 `preload` behavior.
- Performance evidence notes accurately describe the implemented remediation and preserve strict route FAIL rows when metrics miss.
- `docs/launch/performance-evidence.md` and `docs/launch/final-production-readiness-report.md` are regenerated after the remediation.
- `.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VERIFICATION.md` is marked verified only for strict PASS or validated dated acceptance.
- No owner-gated Shopify/admin/Search Console proof is fabricated or moved into the automated code readiness score.

</success_criteria>

<output>

After completion, create `.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-15-SUMMARY.md`.

</output>
