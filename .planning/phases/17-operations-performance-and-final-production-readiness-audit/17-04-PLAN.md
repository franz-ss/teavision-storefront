---
phase: 17-operations-performance-and-final-production-readiness-audit
plan: 04
type: execute
wave: 2
depends_on: [17-03]
files_modified:
  - scripts/performance/probe-lighthouse.mjs
  - scripts/performance/probe-lighthouse.test.mjs
  - docs/launch/performance-evidence.md
  - src/components/homepage/hero/hero.tsx
  - src/components/product/product-gallery/product-gallery.tsx
  - src/app/(storefront)/products/[handle]/page.tsx
  - src/app/(storefront)/layout.tsx
  - src/components/layout/header/header.tsx
  - tests/e2e/production-smoke.spec.ts
autonomous: true
requirements:
  - PERF-01
  - UX-01
  - QA-02
must_haves:
  truths:
    - "PERF-01: Home and PDP Lighthouse/Core Web Vitals evidence no longer shows launch-blocking LCP regressions"
    - "UX-01: audit UX/accessibility polish items are resolved or documented as non-blocking, including mobile text wrapping and duplicate skip links"
    - "QA-02: final readiness audit includes performance and browser smoke evidence"
    - "D-14: launch-critical audit representatives include home, rich-media PDP, /collections/all, high-value collection, /cart, /search, account path, and legal/static pages"
    - "D-15: performance blocking uses practical mobile Lighthouse/Web Vitals gates with LCP, CLS, INP/TBT evidence and documented mitigations"
  artifacts:
    - path: "scripts/performance/probe-lighthouse.mjs"
      provides: "Repeatable mobile Lighthouse evidence"
    - path: "docs/launch/performance-evidence.md"
      provides: "Performance and UX/accessibility evidence table"
    - path: "tests/e2e/production-smoke.spec.ts"
      provides: "Route smoke and skip-link regression coverage"
  key_links:
    - from: "scripts/performance/probe-lighthouse.mjs"
      to: "docs/launch/performance-evidence.md"
      via: "metrics evidence output"
---

<objective>
Create repeatable performance and UX/accessibility evidence for launch. This plan adds a Lighthouse probe, records route-level evidence, fixes or documents launch-blocking Home/PDP LCP issues, and resolves the known duplicate skip-link problem.
</objective>

<execution_context>
@AGENTS.md
@https://web.dev/articles/vitals
@https://developer.chrome.com/docs/lighthouse/overview
@node_modules/next/dist/docs/01-app/02-guides/production-checklist.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/webVitalsAttribution.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-RESEARCH.md
@.planning/phases/17-operations-performance-and-final-production-readiness-audit/17-VALIDATION.md
@src/components/homepage/hero/hero.tsx
@src/components/product/product-gallery/product-gallery.tsx
@src/app/(storefront)/products/[handle]/page.tsx
@src/app/(storefront)/layout.tsx
@src/components/layout/header/header.tsx
@tests/e2e/production-smoke.spec.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add repeatable Lighthouse/Web Vitals evidence script</name>
  <files>scripts/performance/probe-lighthouse.mjs, scripts/performance/probe-lighthouse.test.mjs, docs/launch/performance-evidence.md, package.json</files>
  <read_first>
    - package.json
    - scripts/seo/probe-launch-seo.mjs
    - scripts/security/probe-production-security.mjs
    - .planning/phases/17-operations-performance-and-final-production-readiness-audit/17-CONTEXT.md
  </read_first>
  <action>
    Create `scripts/performance/probe-lighthouse.mjs` using the existing local `lighthouse` dependency. It must:

    - Accept `--base-url`, defaulting to `http://127.0.0.1:4173`.
    - Accept repeated `--url` overrides.
    - Accept `--start-server`. When present, start the same fake-provider production lifecycle from Plan 17-03 before running Lighthouse: fake Shopify, fake Customer Account API, `corepack pnpm exec next build`, and `corepack pnpm exec next start -p ${PORT}`. Shut those child processes down before exit.
    - Use default representative paths:
      - `/`
      - `/products/test-standard-tea`
      - `/collections/all`
      - `/cart`
      - `/search?q=tea`
      - `/account`
      - `/pages/privacy-policy`
    - Run mobile Lighthouse for categories `performance`, `accessibility`, `best-practices`, and `seo`.
    - Capture at minimum `largest-contentful-paint`, `cumulative-layout-shift`, `total-blocking-time`, and overall accessibility score.
    - Treat LCP <= 2500ms as pass, CLS <= 0.1 as pass, and TBT <= 300ms as pass for local launch evidence. If local noise makes a metric inconclusive, record `WARN` with mitigation instead of fabricating a pass.
    - Print a markdown table with columns `Route`, `LCP`, `CLS`, `TBT`, `A11y`, `Status`, `Mitigation`.
    - Write or update `docs/launch/performance-evidence.md` unless `--stdout-only` is passed.

    Create `scripts/performance/probe-lighthouse.test.mjs` with `node:test` coverage for argument parsing, `--start-server` command construction, threshold status calculation, markdown table rendering, and evidence headings.

    Add `package.json` script:

    - `"test:performance": "node scripts/performance/probe-lighthouse.mjs"`
  </action>
  <verify>
    <automated>node --test scripts/performance/probe-lighthouse.test.mjs</automated>
    <automated>node scripts/performance/probe-lighthouse.mjs --stdout-only --start-server --url / --base-url http://127.0.0.1:4173</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/performance/probe-lighthouse.mjs` contains `largest-contentful-paint`.
    - `scripts/performance/probe-lighthouse.mjs` contains `cumulative-layout-shift`.
    - `scripts/performance/probe-lighthouse.mjs` contains `total-blocking-time`.
    - `package.json` contains `test:performance`.
    - `docs/launch/performance-evidence.md` has heading `# Performance Evidence`.
  </acceptance_criteria>
  <done>PERF-01 has a repeatable evidence command for final audit.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate Home and PDP LCP blockers discovered by the probe</name>
  <files>src/components/homepage/hero/hero.tsx, src/components/product/product-gallery/product-gallery.tsx, src/app/(storefront)/products/[handle]/page.tsx, docs/launch/performance-evidence.md</files>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs
    - docs/launch/performance-evidence.md
    - src/components/homepage/hero/hero.tsx
    - src/components/product/product-gallery/product-gallery.tsx
    - src/app/(storefront)/products/[handle]/page.tsx
    - src/lib/shopify/image-url.ts
  </read_first>
  <action>
    Run the performance probe against the local production server from Plan 17-03. If home or PDP LCP is `FAIL` or a launch-blocking `WARN`, apply concrete remediations in the owned files:

    - Home hero: preserve `loading="eager"`, `fetchPriority="high"`, `sizes="100vw"`, and stable fill image dimensions. If missing, add them exactly.
    - PDP gallery: preserve first image `loading="eager"`, `fetchPriority="high"`, `preload={true}`, and a stable `sizes` string. If missing, add them exactly.
    - PDP page: keep product data fetching server-side and avoid adding new client wrappers around the product page. Do not add request-time APIs to root layouts.
    - If the probe identifies a specific oversized image URL, update the relevant `getSizedShopifyImageUrl` width or `sizes` value and document the before/after metric in `docs/launch/performance-evidence.md`.

    If the existing code already has the expected Home/PDP image priority controls and probe output is green, leave source code unchanged and record the green evidence. Do not make cosmetic redesign changes.
  </action>
  <verify>
    <automated>pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173</automated>
    <automated>pnpm lint -- src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx "src/app/(storefront)/products/[handle]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Home/PDP performance evidence is recorded in `docs/launch/performance-evidence.md`.
    - If source changed, Home hero or PDP gallery still contains `fetchPriority="high"` for the LCP image.
    - If source changed, no new client wrapper is added around `src/app/(storefront)/products/[handle]/page.tsx`.
    - Any remaining LCP miss is marked `WARN` or `FAIL` with mitigation, not silently passed.
  </acceptance_criteria>
  <done>Home/PDP LCP status is either green or honestly documented with mitigation.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve duplicate skip link and mobile wrapping launch polish</name>
  <files>src/app/(storefront)/layout.tsx, src/components/layout/header/header.tsx, tests/e2e/production-smoke.spec.ts, docs/launch/performance-evidence.md</files>
  <read_first>
    - src/app/(storefront)/layout.tsx
    - src/components/layout/header/header.tsx
    - tests/e2e/production-smoke.spec.ts
    - src/app/globals.css
  </read_first>
  <action>
    Resolve the known duplicate skip-link issue:

    - Keep the skip link in `src/app/(storefront)/layout.tsx`.
    - Remove the second `Skip to main content` anchor from `src/components/layout/header/header.tsx`.
    - Do not remove the `main id="main-content"` target.

    Add Playwright coverage to `tests/e2e/production-smoke.spec.ts`:

    - On `/`, assert `page.getByRole('link', { name: 'Skip to main content' })` has count `1`.
    - Press `Tab` and assert the skip link becomes focused or visible.
    - Assert `main#main-content` exists.

    Add mobile wrapping smoke coverage for at least one long-text surface already covered by existing stories/tests:

    - Prefer route-level smoke in `tests/e2e/production-smoke.spec.ts` that sets viewport width `375`, visits `/cart` and `/search?q=Organic%20ceremonial-grade%20green%20tea%20with%20a%20very%20long%20wholesale%20product%20title`, and asserts `document.documentElement.scrollWidth <= document.documentElement.clientWidth`.
    - If route data makes this unreliable, document the existing Storybook overflow tests in `docs/launch/performance-evidence.md` and add the route-level check only where deterministic.
  </action>
  <verify>
    <automated>pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts</automated>
    <automated>pnpm test:stories</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/header/header.tsx` no longer contains `Skip to main content`.
    - `src/app/(storefront)/layout.tsx` still contains `Skip to main content`.
    - `tests/e2e/production-smoke.spec.ts` asserts the skip link count is `1`.
    - Mobile overflow check or evidence note is present.
  </acceptance_criteria>
  <done>UX-01 duplicate skip-link issue is fixed and mobile wrapping has launch evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Record performance and UX evidence for final audit</name>
  <files>docs/launch/performance-evidence.md</files>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs
    - tests/e2e/production-smoke.spec.ts
    - docs/launch/production-e2e-evidence.md
  </read_first>
  <action>
    Ensure `docs/launch/performance-evidence.md` contains these exact headings:

    - `# Performance Evidence`
    - `## Command`
    - `## Representative Routes`
    - `## Mobile Lighthouse Results`
    - `## UX And Accessibility Polish`
    - `## Remaining Mitigations`

    The UX section must explicitly mention:

    - duplicate skip link resolved
    - mobile text wrapping checked
    - any remaining non-blocking UX/accessibility polish items, or `None - no launch-blocking UX/accessibility polish items remain`

    The representative route section must include `/`, `/products/test-standard-tea`, `/collections/all`, `/cart`, `/search?q=tea`, `/account`, and `/pages/privacy-policy`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/launch/performance-evidence.md','utf8');for(const n of ['# Performance Evidence','## Mobile Lighthouse Results','duplicate skip link resolved','/products/test-standard-tea']){if(!s.includes(n))throw new Error('missing '+n)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Performance evidence contains all required headings.
    - Evidence includes Home and PDP rows.
    - Evidence mentions duplicate skip link resolved.
    - Remaining performance/UX misses are documented with mitigation.
  </acceptance_criteria>
  <done>PERF-01 and UX-01 evidence is ready for the final readiness report.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lighthouse automation to launch score | Simulated local data can be noisy and must not fabricate field-data certainty |
| Accessibility navigation to header/layout composition | Duplicate skip links confuse keyboard users and launch audits |
| Performance remediation to storefront behavior | LCP fixes must not break server-first data flow or commerce interactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Repudiation | performance evidence | mitigate | Store command, route, metrics, status, and mitigation in evidence doc |
| T-17-10 | Accessibility / UX integrity | skip link and wrapping | mitigate | Keep one layout-level skip link and add production smoke count/overflow checks |

</threat_model>

<verification>

```bash
node --test scripts/performance/probe-lighthouse.test.mjs
pnpm test:performance -- --start-server --base-url http://127.0.0.1:4173
pnpm test:e2e:production -- tests/e2e/production-smoke.spec.ts
pnpm test:stories
pnpm lint -- scripts/performance src/app/(storefront)/layout.tsx src/components/layout/header/header.tsx src/components/homepage/hero/hero.tsx src/components/product/product-gallery/product-gallery.tsx "src/app/(storefront)/products/[handle]/page.tsx"
pnpm typecheck
```

</verification>

<success_criteria>

- Performance evidence records mobile Lighthouse/Web Vitals status for launch representative routes.
- Home and PDP LCP have green evidence or documented non-blocking mitigation.
- Duplicate skip-link issue is resolved with exactly one skip link.
- Mobile text wrapping has automated smoke or documented Storybook evidence.

</success_criteria>

<output>

- Lighthouse/Web Vitals probe.
- Performance evidence report.
- Duplicate skip-link fix.
- Production smoke coverage for skip link and mobile overflow.

</output>
