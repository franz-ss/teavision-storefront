---
phase: 18-seo-audit-remediation
plan: 06
type: execute
wave: gap-closure
depends_on:
  - '18-05'
files_modified:
  - 'scripts/seo/probe-launch-seo.mjs'
  - 'scripts/seo/probe-launch-seo.test.mjs'
  - 'docs/launch/seo-audit-remediation.md'
  - 'docs/launch/final-production-readiness-report.md'
autonomous: true
requirements:
  - SEO-AUDIT-05
gap_closure: true
must_haves:
  truths:
    - 'The enabled SEO probe must validate Product JSON-LD on an existing product page and must not misreport the fake fixture path as a schema regression when that fixture product is absent from a real Shopify-backed dev server.'
    - 'The default `/products/test-standard-tea` probe path remains valid for fake-provider production lifecycle evidence.'
    - 'Manual or real-Shopify enabled SEO probes require an explicit `--product-path` or `SEO_PROBE_PRODUCT_PATH` when the data source is not the fake Shopify fixture server.'
  artifacts:
    - path: 'scripts/seo/probe-launch-seo.mjs'
      provides: 'Enabled-mode Product JSON-LD probe with data-source-aware product-path handling'
    - path: 'scripts/seo/probe-launch-seo.test.mjs'
      provides: 'Regression tests for default fixture path, explicit product path, and missing Product JSON-LD'
    - path: 'docs/launch/seo-audit-remediation.md'
      provides: 'Structured-data evidence instructions that distinguish fake fixture and real Shopify product paths'
  key_links:
    - from: 'tests/fixtures/shopify/product.ts'
      to: 'scripts/seo/probe-launch-seo.mjs'
      via: 'default fake fixture product path'
      pattern: 'test-standard-tea'
    - from: 'src/app/(storefront)/products/[handle]/page.tsx'
      to: 'scripts/seo/probe-launch-seo.mjs'
      via: 'Product JSON-LD only exists after getProduct(handle) succeeds'
      pattern: 'Product'
---

<objective>
Close the Phase 18 UAT structured-data probe gap by making Product JSON-LD verification data-source-aware.

Purpose: UAT showed `probe-launch-seo --mode enabled --base-url http://127.0.0.1:3000` failing on `/products/test-standard-tea` because that fake fixture product was not present in the dev server's Shopify data. The fix must preserve hard failures for real product pages that lack Product JSON-LD, while making the default fixture-path behavior clear and non-misleading.

Output: Updated probe semantics, regression tests, and evidence docs that tell operators when to use fake-provider lifecycle evidence versus an explicit real product path.
</objective>

<source_audit>

| Source | Item | Coverage |
| --- | --- | --- |
| UAT | Test 4 Structured Data Coverage | Covered by data-source-aware Product JSON-LD probe handling |
| DEBUG | `.planning/debug/product-json-ld-probe-path.md` | Covered by tasks that address the diagnosed fake fixture path mismatch |
| REQ | SEO-AUDIT-05 | Covered by preserving Product JSON-LD validation on existing PDPs |
| PLAN 18-04 | Product schema is conditional only for `aggregateRating` | Covered by not changing PDP JSON-LD rendering |
| PLAN 18-05 | `/products/test-standard-tea` fake-provider evidence | Covered by keeping the fake fixture lifecycle path valid |

</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Make Product JSON-LD probe failures data-source-aware</name>
  <read_first>
    - scripts/seo/probe-launch-seo.mjs
    - src/app/(storefront)/products/[handle]/page.tsx
    - src/lib/shopify/env.ts
    - tests/fixtures/shopify/product.ts
  </read_first>
  <files>scripts/seo/probe-launch-seo.mjs</files>
  <action>
    Update `probeProductStructuredData()` so it distinguishes these cases:

    - Product page exists and has Product JSON-LD: `PASS`.
    - Product page exists and visible product content is present but Product JSON-LD is missing: `FAIL`.
    - Explicit `--product-path`/`SEO_PROBE_PRODUCT_PATH` points to a not-found or non-product page: `FAIL` with a clear "product route not found for selected data source" detail.
    - Default `/products/test-standard-tea` is used against a server that renders the product-not-found page: `WARN` with an actionable detail telling the operator to run the fake-provider lifecycle or pass a real product path.

    Do not weaken `aggregateRating` checks. Keep aggregateRating conditional on visible review data.
  </action>
  <verify>node --test scripts/seo/probe-launch-seo.test.mjs</verify>
  <acceptance_criteria>
    - `scripts/seo/probe-launch-seo.mjs` still fails when an existing product page has no Product JSON-LD.
    - `scripts/seo/probe-launch-seo.mjs` warns when the default fake fixture path renders the product-not-found page on a non-fixture data source.
    - The warning detail mentions `--product-path` or `SEO_PROBE_PRODUCT_PATH`.
  </acceptance_criteria>
  <done>Enabled SEO Product JSON-LD checks no longer confuse missing fixture data with a schema regression.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for fixture-path and real-product semantics</name>
  <read_first>
    - scripts/seo/probe-launch-seo.test.mjs
    - scripts/seo/probe-launch-seo.mjs
  </read_first>
  <files>scripts/seo/probe-launch-seo.test.mjs</files>
  <action>
    Add focused tests for the Product structured-data helper behavior:

    - A real product HTML document with Product JSON-LD passes.
    - A real product HTML document without Product JSON-LD fails.
    - The default `/products/test-standard-tea` not-found HTML warns with actionable product-path guidance.
    - An explicit non-default product path that renders not-found fails.

    Keep tests in the existing Node test style. Do not introduce browser dependencies.
  </action>
  <verify>node --test scripts/seo/probe-launch-seo.test.mjs</verify>
  <acceptance_criteria>
    - Tests cover both default fixture-path and explicit product-path behavior.
    - Tests assert `FAIL` remains possible for true Product JSON-LD omissions.
    - Tests assert `WARN` only applies to the default fixture path when the selected data source lacks that product.
  </acceptance_criteria>
  <done>The probe contract is protected against both false positives and false negatives.</done>
</task>

<task type="auto">
  <name>Task 3: Update structured-data evidence instructions</name>
  <read_first>
    - docs/launch/seo-audit-remediation.md
    - docs/launch/final-production-readiness-report.md
    - .planning/phases/18-seo-audit-remediation/18-UAT.md
  </read_first>
  <files>docs/launch/seo-audit-remediation.md, docs/launch/final-production-readiness-report.md</files>
  <action>
    Update evidence wording so Product structured-data checks are unambiguous:

    - Fake-provider production evidence may use default `/products/test-standard-tea`.
    - Real Shopify-backed dev/staging/prod probes must pass `--product-path` or `SEO_PROBE_PRODUCT_PATH` for a product known to exist in that data source.
    - A warning for the default fixture path on a real-data server is not Product schema proof; a real product path or fake-provider lifecycle proof is still required.

    Do not mark owner/operator gates approved and do not hide existing performance readiness failures.
  </action>
  <verify>pnpm exec prettier --check docs/launch/seo-audit-remediation.md docs/launch/final-production-readiness-report.md</verify>
  <acceptance_criteria>
    - `docs/launch/seo-audit-remediation.md` mentions `SEO_PROBE_PRODUCT_PATH` or `--product-path`.
    - Final readiness wording does not claim live Shopify Product JSON-LD proof from the default fake fixture path.
    - Existing `performance | FAIL` and owner-gated pending rows remain visible.
  </acceptance_criteria>
  <done>Operators can repeat Product structured-data checks without accidentally probing a missing fixture product.</done>
</task>

</tasks>

<verification>
- [ ] `node --test scripts/seo/probe-launch-seo.test.mjs`
- [ ] `node scripts/seo/probe-launch-seo.mjs --mode enabled --base-url http://127.0.0.1:3000`
- [ ] `node scripts/seo/probe-crawlable-html.mjs --start-server --base-url http://127.0.0.1:4173`
- [ ] `pnpm exec prettier --check docs/launch/seo-audit-remediation.md docs/launch/final-production-readiness-report.md`
</verification>

<success_criteria>
- Product JSON-LD checks still fail for an existing product page missing Product schema.
- Default fake fixture path against a real-data server produces an actionable warning instead of a misleading schema failure.
- Fake-provider production evidence for `/products/test-standard-tea` still proves Product JSON-LD.
- Docs and final evidence explain when to provide a real product path.
</success_criteria>

<output>
After execution, create `.planning/phases/18-seo-audit-remediation/18-06-SUMMARY.md`.
</output>
