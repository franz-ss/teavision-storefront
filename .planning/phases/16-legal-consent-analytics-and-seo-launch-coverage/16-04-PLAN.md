---
phase: 16-legal-consent-analytics-and-seo-launch-coverage
plan: 04
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/lib/seo/launch-route-matrix.ts
  - src/lib/seo/launch-route-matrix.test.ts
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/lib/seo/noindex.ts
  - scripts/seo/probe-launch-seo.mjs
  - scripts/component-contracts/noindex-mode.test.mjs
  - docs/launch/analytics-and-indexing-runbook.md
  - docs/launch/seo-route-evidence.md
autonomous: true
requirements:
  - SEO-01
  - LEGAL-01
  - LEGAL-02
  - ANALYTICS-03
must_haves:
  truths:
    - "SEO-01: site owner can flip launch indexing safely with robots, sitemap, canonical, structured-data, and noindex behavior verified"
    - "LEGAL-01: legal pages are represented in launch route evidence"
    - "LEGAL-02: policy redirects are represented in launch route evidence"
    - "ANALYTICS-03: runbook includes indexing and analytics destination verification before and after cutover"
    - "D-19: DISABLE_INDEXING remains the explicit launch indexing gate"
    - "D-20: automated SEO route matrix verifies robots, sitemap, canonicals, noindex behavior, structured data, and policy redirects"
    - "D-21: sitemap coverage expands to legal pages and key owner-authored static landing/service pages intended for launch discovery"
    - "D-22: Search Console submission/access is owner-gated and documented rather than treated as a code blocker"
  artifacts:
    - path: "src/lib/seo/launch-route-matrix.ts"
      provides: "Canonical route expectations for disabled and enabled indexing modes"
    - path: "scripts/seo/probe-launch-seo.mjs"
      provides: "Deterministic launch SEO evidence probe"
    - path: "docs/launch/seo-route-evidence.md"
      provides: "Recorded route matrix and owner-gated Search Console evidence state"
  key_links:
    - from: "src/lib/legal/policies.ts"
      to: "src/app/sitemap.ts"
      via: "legal page sitemap inclusion"
    - from: "src/lib/seo/launch-route-matrix.ts"
      to: "scripts/seo/probe-launch-seo.mjs"
      via: "route expectation fixtures"
---

<objective>
Make the launch indexing flip testable. This plan expands sitemap coverage, preserves `DISABLE_INDEXING`, adds a deterministic SEO route matrix and probe for disabled/enabled states, verifies policy redirects and no 404s, and documents Search Console as owner-gated evidence.
</objective>

<execution_context>
@AGENTS.md
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/robots.md
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/sitemap.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/redirects.md
@.planning/REQUIREMENTS.md
@.planning/phases/teavision-16-legal-consent-analytics-and-seo-launch-coverage/16-CONTEXT.md
@.planning/phases/teavision-16-legal-consent-analytics-and-seo-launch-coverage/16-RESEARCH.md
@.planning/phases/teavision-16-legal-consent-analytics-and-seo-launch-coverage/16-VALIDATION.md
@.planning/phases/teavision-16-legal-consent-analytics-and-seo-launch-coverage/16-PATTERNS.md
@src/app/robots.ts
@src/app/sitemap.ts
@src/lib/seo/noindex.ts
@src/lib/seo/site-url.ts
@src/lib/seo/serialize-inline-json.ts
@scripts/component-contracts/noindex-mode.test.mjs
@src/lib/legal/policies.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Define launch route expectations for legal, static, product, search, and redirect surfaces</name>
  <files>src/lib/seo/launch-route-matrix.ts, src/lib/seo/launch-route-matrix.test.ts</files>
  <read_first>
    - src/lib/legal/policies.ts
    - src/app/sitemap.ts
    - src/app/robots.ts
    - src/lib/seo/noindex.ts
    - src/lib/seo/site-url.ts
    - .planning/phases/teavision-16-legal-consent-analytics-and-seo-launch-coverage/16-CONTEXT.md
  </read_first>
  <action>
    Create `src/lib/seo/launch-route-matrix.ts` with named exports:

    - `LaunchSeoRouteExpectation` type containing `path`, `expectedStatus`, `canonicalPath`, `shouldIndexWhenEnabled`, `shouldAppearInSitemap`, and `checks`.
    - `LEGAL_ROUTE_EXPECTATIONS` derived from `LEGAL_POLICIES`.
    - `STATIC_LAUNCH_ROUTE_EXPECTATIONS` for `/`, `/search`, `/pages/wholesale`, `/pages/wholesale-account-request`, `/pages/our-story`, `/pages/contact`, `/pages/bulk-wholesale-supply`, `/pages/private-label-packing`, `/pages/tea-bag-manufacturer`, and `/pages/new-product-development-order-form`.
    - `REDIRECT_ROUTE_EXPECTATIONS` for policy redirects from Plan 16-01.
    - `getLaunchSeoRouteExpectations()`.

    Mark `/search` as `shouldIndexWhenEnabled: false` unless current SEO decisions say otherwise; it can appear in the existing sitemap only if the phase explicitly decides it is launch-indexable. Add `launch-route-matrix.test.ts` to assert all five legal routes and all policy redirects are represented.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/lib/seo/launch-route-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Matrix contains `/pages/privacy-policy`, `/pages/shipping-policy`, `/pages/refund-policy`, `/pages/terms-of-service`, and `/pages/cookie-preferences`.
    - Matrix contains `/pages/bulk-wholesale-supply`, `/pages/private-label-packing`, `/pages/tea-bag-manufacturer`, and `/pages/new-product-development-order-form`.
    - Redirect expectations include `/policies/privacy-policy`.
    - Test asserts every legal registry entry appears in route expectations.
  </acceptance_criteria>
  <done>Launch SEO expectations are centralized before sitemap and probe updates.</done>
</task>

<task type="auto">
  <name>Task 2: Expand sitemap and preserve disabled-indexing behavior</name>
  <files>src/app/sitemap.ts, src/app/robots.ts, src/lib/seo/noindex.ts, scripts/component-contracts/noindex-mode.test.mjs</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/robots.md
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/sitemap.md
    - src/app/sitemap.ts
    - src/app/robots.ts
    - src/lib/seo/noindex.ts
    - src/lib/seo/launch-route-matrix.ts
    - src/lib/legal/policies.ts
  </read_first>
  <action>
    Update `src/app/sitemap.ts` so enabled-indexing sitemap output includes launch-indexable legal pages and key static landing/service pages from `getLaunchSeoRouteExpectations()`. Preserve current behavior where `isNoindexModeEnabled()` returns an empty sitemap when `DISABLE_INDEXING` is true.

    Keep `src/app/robots.ts` using `isNoindexModeEnabled()` and `SITE_URL`. If no code change is needed in robots/noindex, leave those files unchanged and update only tests/probe expectations. Extend `scripts/component-contracts/noindex-mode.test.mjs` so it asserts sitemap imports or route expectations include the legal/static route matrix and still returns no sitemap URLs in disabled mode.
  </action>
  <verify>
    <automated>node --test scripts/component-contracts/noindex-mode.test.mjs</automated>
    <automated>pnpm test:unit -- src/lib/seo/launch-route-matrix.test.ts</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/sitemap.ts` includes launch-indexable legal pages and key static landing pages when indexing is enabled.
    - Disabled indexing still returns an empty sitemap.
    - `src/app/robots.ts` still omits sitemap when disabled and includes `${SITE_URL}/sitemap.xml` when enabled.
    - Existing product, collection, blog, article, and tag sitemap behavior is preserved.
  </acceptance_criteria>
  <done>Sitemap launch coverage expands without weakening DISABLE_INDEXING.</done>
</task>

<task type="auto">
  <name>Task 3: Add launch SEO probe for disabled and enabled modes</name>
  <files>scripts/seo/probe-launch-seo.mjs, docs/launch/seo-route-evidence.md</files>
  <read_first>
    - scripts/component-contracts/noindex-mode.test.mjs
    - scripts/security/probe-production-security.mjs
    - src/lib/seo/launch-route-matrix.ts
    - src/lib/seo/serialize-inline-json.ts
    - src/app/(storefront)/products/[handle]/page.tsx
  </read_first>
  <action>
    Create `scripts/seo/probe-launch-seo.mjs` as a Node script that can run against a local production server URL supplied by env or CLI, defaulting to `http://127.0.0.1:3000`. Support modes:

    - `--mode redirects`: checks policy redirect expectations only.
    - `--mode disabled`: checks disabled-indexing behavior: robots has no sitemap, sitemap is empty or unavailable by project convention, representative pages expose noindex metadata where server-rendered output allows it.
    - `--mode enabled`: checks robots sitemap line, sitemap includes expected launch-indexable pages, policy redirects resolve, canonical links match expectations, and representative product page structured data can be parsed safely.
    - `--mode runbook`: checks docs/runbook evidence headings exist.

    The script must not require live Shopify hosted checkout, payment, tax, shipping, order, success redirect, Search Console, GA4, or marketing destination access. It may skip product structured-data checks with a clear warning if Shopify credentials are absent and a product page cannot be rendered locally.

    Create `docs/launch/seo-route-evidence.md` with sections for command, date, disabled-mode result, enabled-mode result, redirects result, structured-data result, and owner-gated Search Console evidence.
  </action>
  <verify>
    <automated>node scripts/seo/probe-launch-seo.mjs --mode runbook</automated>
    <automated>node scripts/seo/probe-launch-seo.mjs --mode redirects</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seo/probe-launch-seo.mjs` supports `--mode disabled`, `--mode enabled`, `--mode redirects`, and `--mode runbook`.
    - Script contains no real Shopify checkout/payment/order URLs.
    - `docs/launch/seo-route-evidence.md` contains `Owner-gated Search Console evidence`.
    - Redirect mode checks `/policies/privacy-policy` and `/policies/terms-of-service`.
  </acceptance_criteria>
  <done>SEO launch evidence can be generated locally without owner-gated external access.</done>
</task>

<task type="auto">
  <name>Task 4: Complete launch indexing and Search Console runbook coverage</name>
  <files>docs/launch/analytics-and-indexing-runbook.md, docs/launch/seo-route-evidence.md</files>
  <read_first>
    - docs/launch/analytics-and-indexing-runbook.md
    - docs/launch/seo-route-evidence.md
    - .planning/phases/teavision-16-legal-consent-analytics-and-seo-launch-coverage/16-CONTEXT.md
    - .planning/phases/teavision-16-legal-consent-analytics-and-seo-launch-coverage/16-VALIDATION.md
  </read_first>
  <action>
    Update `docs/launch/analytics-and-indexing-runbook.md` so indexing launch steps are adjacent to analytics verification. Include:

    - Pre-cutover: confirm `DISABLE_INDEXING=true`, run `node scripts/seo/probe-launch-seo.mjs --mode disabled`, verify legal redirects, and record owner/legal approval state.
    - Cutover: set `DISABLE_INDEXING=false`, deploy, and run enabled-mode route matrix against the launch host.
    - Post-cutover: submit `/sitemap.xml` in Google Search Console, inspect representative legal/product/collection/static URLs, and record results.
    - Owner-gated: Search Console property access and submission proof are pending until owner access exists.

    Update `docs/launch/seo-route-evidence.md` with a table containing `Check`, `Command`, `Expected`, `Status`, and `Proof` columns. Initial Search Console status must be owner-gated or pending unless real proof exists.
  </action>
  <verify>
    <automated>node scripts/seo/probe-launch-seo.mjs --mode runbook</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('docs/launch/analytics-and-indexing-runbook.md','utf8');for(const n of ['DISABLE_INDEXING=true','DISABLE_INDEXING=false','Google Search Console','node scripts/seo/probe-launch-seo.mjs --mode disabled','node scripts/seo/probe-launch-seo.mjs --mode enabled']){if(!s.includes(n)){throw new Error('missing '+n)}}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Runbook includes both disabled and enabled indexing commands.
    - Runbook states Search Console access/submission is owner-gated.
    - SEO evidence doc has `Check`, `Command`, `Expected`, `Status`, and `Proof` columns.
    - No runbook step asks CI to submit Search Console, GA4, or real checkout/order data.
  </acceptance_criteria>
  <done>SEO-01 launch flip and owner-gated Search Console evidence are ready for final verification.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `DISABLE_INDEXING` env to public crawler output | Robots, sitemap, and metadata change launch visibility |
| Legacy URLs to canonical URLs | Redirects and canonicals consolidate duplicate policy content |
| Local launch probes to owner-gated external tools | Search Console cannot be automated without owner access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Repudiation / SEO integrity | launch indexing flip | mitigate | Disabled and enabled mode probes record command output and evidence docs |
| T-16-12 | Spoofing / SEO integrity | duplicate policy URLs | mitigate | Redirect and canonical checks for policy aliases |
| T-16-13 | Repudiation | Search Console proof | avoid | Document owner-gated access and do not claim submission without proof |

</threat_model>

<verification>

```bash
pnpm test:unit -- src/lib/seo/launch-route-matrix.test.ts
node --test scripts/component-contracts/noindex-mode.test.mjs
node scripts/seo/probe-launch-seo.mjs --mode runbook
node scripts/seo/probe-launch-seo.mjs --mode redirects
pnpm lint -- src/lib/seo src/app/sitemap.ts src/app/robots.ts scripts/seo/probe-launch-seo.mjs
pnpm typecheck
```

</verification>

<success_criteria>

- `DISABLE_INDEXING` remains the explicit launch indexing gate.
- Enabled-mode sitemap includes legal pages and intended launch static landing/service pages.
- Disabled-mode behavior keeps the site out of indexes using existing noindex/robots controls.
- SEO probe covers robots, sitemap, canonicals, noindex, structured-data safety, policy redirects, and no policy 404s.
- Search Console steps are documented as owner-gated until proof exists.

</success_criteria>

<output>

- SEO launch route matrix and tests.
- Expanded sitemap launch coverage.
- Launch SEO probe script.
- SEO route evidence and indexing runbook sections.

</output>

