---
phase: 15-security-dependency-and-runtime-header-hardening
plan: 05
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04]
files_modified:
  - scripts/security/probe-production-security.mjs
  - package.json
  - .planning/phases/15-security-dependency-and-runtime-header-hardening/15-RUNTIME-SECURITY-EVIDENCE.md
  - .planning/phases/15-security-dependency-and-runtime-header-hardening/15-DEPENDENCY-AUDIT.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
must_haves:
  truths:
    - "SEC-01: final evidence records no critical/high audit findings and residual moderate rationale"
    - "SEC-02: production response probes prove required security headers and no x-powered-by leakage"
    - "SEC-03: CSP report-only allowlist evidence is captured before Phase 16 adds analytics hosts"
    - "SEC-04: account OAuth local/fake proof and live owner-gated status are recorded"
    - "SEC-05: form/search abuse-control production posture is recorded"
    - "D-03: dependency audit evidence is updated with final command results"
    - "D-08: built local production server probes representative storefront/account/API routes"
    - "D-10: callback/logout origin evidence is recorded without requiring live Shopify admin access"
    - "D-11: live Shopify Customer Account OAuth remains owner-gated unless approval is recorded"
    - "D-12: account entry/exit local evidence is included"
    - "D-13: production abuse protection explicit stance is included"
  artifacts:
    - path: "scripts/security/probe-production-security.mjs"
      provides: "Repeatable production-response header probe"
    - path: ".planning/phases/15-security-dependency-and-runtime-header-hardening/15-RUNTIME-SECURITY-EVIDENCE.md"
      provides: "Final Phase 15 runtime security evidence"
    - path: ".planning/phases/15-security-dependency-and-runtime-header-hardening/15-DEPENDENCY-AUDIT.md"
      provides: "Final dependency audit evidence"
  key_links:
    - from: "scripts/security/probe-production-security.mjs"
      to: "15-RUNTIME-SECURITY-EVIDENCE.md"
      via: "recorded route/header results"
---

<objective>
Create the final Phase 15 evidence layer: a repeatable production-response probe and a concise evidence report proving dependency, headers/CSP, account OAuth-start, and abuse-control requirements are ready for Phase 16/17 without overstating owner-gated live Shopify proof.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-security-dependency-and-runtime-header-hardening/15-CONTEXT.md
@.planning/phases/15-security-dependency-and-runtime-header-hardening/15-RESEARCH.md
@.planning/phases/15-security-dependency-and-runtime-header-hardening/15-VALIDATION.md
@.planning/phases/15-security-dependency-and-runtime-header-hardening/15-DEPENDENCY-AUDIT.md
@docs/testing/customer-accounts-setup.md
@docs/conventions.md
@playwright.config.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add a production security probe script</name>
  <files>scripts/security/probe-production-security.mjs, package.json</files>
  <read_first>
    - scripts/component-contracts/production-boundaries.test.mjs
    - scripts/component-contracts/security-headers.test.mjs
    - package.json
    - next.config.ts
    - src/lib/security/headers.ts
  </read_first>
  <action>
    Create `scripts/security/probe-production-security.mjs` as a Node script. It accepts a required base URL argument, for example:

    `node scripts/security/probe-production-security.mjs http://127.0.0.1:3000`

    The script must fetch this route list:

    - `/`
    - `/collections/all`
    - `/cart`
    - `/account`
    - `/account/login`
    - `/pages/contact`
    - `/api/search/suggestions?q=tea`

    The script should allow `PHASE15_PROBE_PRODUCT_PATH` to add a product route such as `/products/test-standard-tea` when fake/local data supports it. For every fetched route, assert:

    - `x-powered-by` is absent
    - `strict-transport-security` is present
    - `x-content-type-options` equals `nosniff`
    - `referrer-policy` equals `strict-origin-when-cross-origin`
    - `permissions-policy` is present
    - `x-frame-options` equals `SAMEORIGIN`
    - `content-security-policy-report-only` is present
    - `content-security-policy` is absent unless the executor intentionally documents enforcement in the evidence file

    Print a markdown table with columns `Route`, `Status`, `Headers`, `Result`. Exit non-zero on any missing header or unexpected `x-powered-by`.

    Add a `test:security` package script:

    `node scripts/security/probe-production-security.mjs`

    The script may require a base URL argument and should print usage with a non-zero exit if omitted.
  </action>
  <verify>
    <automated>node scripts/security/probe-production-security.mjs 2>$null; if ($LASTEXITCODE -eq 0) { throw 'probe should require base URL' } else { 'OK: usage failure is expected' }</automated>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/security/probe-production-security.mjs` exists.
    - The script checks all required header names above.
    - The script fails when no base URL is provided.
    - `package.json` contains `test:security`.
  </acceptance_criteria>
  <done>Production security headers can be probed repeatably from a running server.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 15 verification against a built local production server</name>
  <files>.planning/phases/15-security-dependency-and-runtime-header-hardening/15-RUNTIME-SECURITY-EVIDENCE.md, .planning/phases/15-security-dependency-and-runtime-header-hardening/15-DEPENDENCY-AUDIT.md</files>
  <read_first>
    - .planning/phases/15-security-dependency-and-runtime-header-hardening/15-DEPENDENCY-AUDIT.md
    - scripts/security/probe-production-security.mjs
    - playwright.config.ts
    - docs/testing/customer-accounts-setup.md
  </read_first>
  <action>
    Run the final command set and collect exact outputs into `15-RUNTIME-SECURITY-EVIDENCE.md`:

    ```bash
    pnpm audit --audit-level moderate
    pnpm lint
    pnpm typecheck
    pnpm build
    pnpm test:unit
    pnpm test:integration
    pnpm test:contracts
    pnpm test:e2e
    ```

    Then start a built local production server with safe local/fake env values. Use a port that is free. Example environment values:

    - `SHOPIFY_STORE_DOMAIN=fake-shopify.test`
    - `SHOPIFY_STOREFRONT_ACCESS_TOKEN=test-token`
    - `SHOPIFY_STOREFRONT_TEST_MODE=true`
    - `SHOPIFY_STOREFRONT_TEST_URL=http://127.0.0.1:4517/graphql`
    - `SHOPIFY_CUSTOMER_ACCOUNT_TEST_MODE=true`
    - `SHOPIFY_CUSTOMER_ACCOUNT_TEST_URL=http://127.0.0.1:4518`
    - `SHOPIFY_CUSTOMER_ACCOUNT_API_CLIENT_ID=test-client-id`
    - `SHOPIFY_CUSTOMER_ACCOUNT_SESSION_SECRET=test-session-secret-with-at-least-32-characters`
    - `SHOPIFY_CUSTOMER_ACCOUNT_REDIRECT_URI=http://127.0.0.1:{PORT}/account/callback`
    - `SHOPIFY_CUSTOMER_ACCOUNT_LOGOUT_REDIRECT_URI=http://127.0.0.1:{PORT}/account/login`
    - `RATE_LIMIT_ALLOW_MEMORY_FALLBACK=true`
    - `RATE_LIMIT_TRUSTED_IP_HEADER=x-forwarded-for`

    If a local fake Customer Account server is not available for `pnpm start`, still run the header probe against routes that do not require account data, record the exact blocker, and keep the account proof from integration/e2e tests.

    Run:

    `node scripts/security/probe-production-security.mjs http://127.0.0.1:{PORT}`

    Paste or summarize the markdown table output in `15-RUNTIME-SECURITY-EVIDENCE.md`.
  </action>
  <verify>
    <automated>pnpm audit --audit-level moderate</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
    <automated>pnpm test:unit</automated>
    <automated>pnpm test:integration</automated>
    <automated>pnpm test:contracts</automated>
    <automated>pnpm test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `15-RUNTIME-SECURITY-EVIDENCE.md` contains `## Automated Verification`.
    - `15-RUNTIME-SECURITY-EVIDENCE.md` contains `## Production Response Probe`.
    - The response probe table shows no `x-powered-by` leakage.
    - Any command that cannot run due to missing owner/admin/live credentials is recorded with exact output and owner-gated status.
    - `15-DEPENDENCY-AUDIT.md` final audit section is updated after all dependency work.
  </acceptance_criteria>
  <done>Automated Phase 15 security evidence is captured.</done>
</task>

<task type="auto">
  <name>Task 3: Write final evidence summary and owner-gated launch notes</name>
  <files>.planning/phases/15-security-dependency-and-runtime-header-hardening/15-RUNTIME-SECURITY-EVIDENCE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/15-security-dependency-and-runtime-header-hardening/15-CONTEXT.md
    - .planning/phases/15-security-dependency-and-runtime-header-hardening/15-DEPENDENCY-AUDIT.md
    - docs/testing/customer-accounts-setup.md
    - docs/conventions.md
  </read_first>
  <action>
    Finalize `15-RUNTIME-SECURITY-EVIDENCE.md` with these exact sections:

    - `# Phase 15 Runtime Security Evidence`
    - `## Requirement Coverage` with rows for `SEC-01` through `SEC-05`
    - `## Dependency Audit` linking to `15-DEPENDENCY-AUDIT.md` and summarizing final counts
    - `## Security Headers and CSP` with header names, CSP mode `report-only`, and current allowlist domains
    - `## Account OAuth Evidence` separating automated local/fake proof from owner-gated live Shopify OAuth
    - `## Abuse-Control Evidence` listing contact, wholesale, custom blend, NPD order, newsletter, and `/api/search/suggestions`
    - `## Owner-Gated Items` with live Customer Account OAuth approval status and a note that hosted checkout/payment/tax/shipping/order tests remain Phase 17 owner-gated
    - `## Commands Run` with exact command statuses

    Do not claim live Shopify Customer Account OAuth passed unless owner/admin approval and configured callback/logout URLs were actually recorded during execution.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/15-security-dependency-and-runtime-header-hardening/15-RUNTIME-SECURITY-EVIDENCE.md';const s=fs.readFileSync(p,'utf8');for(const needle of ['# Phase 15 Runtime Security Evidence','SEC-01','SEC-02','SEC-03','SEC-04','SEC-05','report-only','Owner-Gated Items','/api/search/suggestions']){if(!s.includes(needle)){console.error('Missing '+needle);process.exit(1)}}console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Evidence file covers all five Phase 15 requirement IDs.
    - Evidence file says CSP mode is `report-only`.
    - Evidence file explicitly separates fake/local account proof from live owner-gated OAuth.
    - Evidence file lists all public abuse-control surfaces from D-16.
  </acceptance_criteria>
  <done>Phase 15 has a concise evidence artifact ready for verification and Phase 17 final audit.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local/fake automation to production-readiness evidence | Evidence must not overstate fake/local tests as live Shopify proof |
| Running server to header probe | Probe validates actual response headers rather than constants only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Repudiation | launch evidence | mitigate | Record commands, counts, route probes, and owner-gated status in phase evidence |
| T-15-11 | Information Disclosure | response headers | mitigate | Probe live local production responses for missing headers and `x-powered-by` leakage |
| T-15-EVID-SC | Tampering | evidence quality | mitigate | Script exits non-zero on missing headers; docs separate automated proof from manual gates |

</threat_model>

<verification>

```bash
pnpm audit --audit-level moderate
pnpm lint
pnpm typecheck
pnpm build
pnpm test:unit
pnpm test:integration
pnpm test:contracts
pnpm test:e2e
node scripts/security/probe-production-security.mjs http://127.0.0.1:{PORT}
```

</verification>

<success_criteria>

- Final evidence covers `SEC-01` through `SEC-05`.
- Production response probe verifies headers and no `x-powered-by`.
- Dependency audit evidence is updated after remediation.
- Owner-gated live Shopify OAuth/checkout boundaries are not overstated.
- Phase 15 is ready for `$gsd-verify-work` and for Phase 16 to build on the report-only CSP baseline.

</success_criteria>

<output>

- `scripts/security/probe-production-security.mjs`
- `package.json` `test:security` script
- `.planning/phases/15-security-dependency-and-runtime-header-hardening/15-RUNTIME-SECURITY-EVIDENCE.md`
- Updated `.planning/phases/15-security-dependency-and-runtime-header-hardening/15-DEPENDENCY-AUDIT.md`

</output>

