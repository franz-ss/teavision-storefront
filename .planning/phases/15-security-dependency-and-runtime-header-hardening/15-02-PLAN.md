---
phase: 15-security-dependency-and-runtime-header-hardening
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - next.config.ts
  - src/lib/security/headers.ts
  - src/lib/security/headers.test.ts
  - scripts/component-contracts/security-headers.test.mjs
  - package.json
autonomous: true
requirements:
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "SEC-02: representative production responses include required security headers and omit x-powered-by"
    - "SEC-03: CSP is staged report-only with explicit current-host allowlists"
    - "D-05: static CSP ships in report-only mode first, not enforced CSP"
    - "D-06: header set includes HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, frame protection, no x-powered-by, and staged CSP"
    - "D-07: CSP allowlists current required hosts only; GA4, GTM, Meta, Klaviyo, and Shopify pixels are not pre-allowed in Phase 15"
    - "D-08: verification uses a built local production server and representative route probes"
  artifacts:
    - path: "src/lib/security/headers.ts"
      provides: "Testable security header and report-only CSP builder"
    - path: "next.config.ts"
      provides: "Next headers() integration and poweredByHeader false"
    - path: "scripts/component-contracts/security-headers.test.mjs"
      provides: "Source contract for required headers"
  key_links:
    - from: "src/lib/security/headers.ts"
      to: "next.config.ts"
      via: "securityHeaders exported into headers()"
---

<objective>
Add the Phase 15 production security header baseline through `next.config.ts`, with a testable static `Content-Security-Policy-Report-Only` helper and no `x-powered-by` leakage. This plan stages CSP report-only and avoids nonce CSP so Cache Components/PPR behavior is not intentionally sacrificed.
</objective>

<execution_context>
@.planning/phases/15-security-dependency-and-runtime-header-hardening/15-CONTEXT.md
@.planning/phases/15-security-dependency-and-runtime-header-hardening/15-RESEARCH.md
@.planning/phases/15-security-dependency-and-runtime-header-hardening/15-PATTERNS.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md
@node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md
@next.config.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add a pure security header and report-only CSP helper</name>
  <files>src/lib/security/headers.ts, src/lib/security/headers.test.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md
    - node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md
    - next.config.ts
    - src/lib/seo/noindex.ts
    - src/lib/env/server.ts
  </read_first>
  <action>
    Create `src/lib/security/headers.ts` with named exports only:

    - `export type SecurityHeader = { key: string; value: string }`
    - `export const contentSecurityPolicyReportOnlyHeaderName = 'Content-Security-Policy-Report-Only'`
    - `export const securityHeaders: SecurityHeader[]`
    - `export function buildContentSecurityPolicy(): string`

    `securityHeaders` must include exactly these header keys:

    - `Strict-Transport-Security` with value `max-age=63072000; includeSubDomains; preload`
    - `X-Content-Type-Options` with value `nosniff`
    - `Referrer-Policy` with value `strict-origin-when-cross-origin`
    - `Permissions-Policy` with value `camera=(), microphone=(), geolocation=(), browsing-topics=()`
    - `X-Frame-Options` with value `SAMEORIGIN`
    - `Content-Security-Policy-Report-Only` with value from `buildContentSecurityPolicy()`

    `buildContentSecurityPolicy()` must output a single-line semicolon-separated policy containing these directives:

    - `default-src 'self'`
    - `base-uri 'self'`
    - `object-src 'none'`
    - `frame-ancestors 'self'`
    - `form-action 'self'`
    - `script-src 'self' 'unsafe-inline' https://searchserverapi.com`
    - `style-src 'self' 'unsafe-inline'`
    - `img-src 'self' blob: data: https://cdn.shopify.com https://www.teavision.com.au https://cdn.sanity.io https://searchserverapi.com https://searchserverapi1.com`
    - `font-src 'self' data:`
    - `connect-src 'self' https://searchserverapi1.com https://api.trustoo.io https://*.myshopify.com https://*.shopify.com`
    - `frame-src 'self' https://maps.google.com`
    - `media-src 'self'`
    - `manifest-src 'self'`
    - `upgrade-insecure-requests`

    Create `src/lib/security/headers.test.ts` asserting all required header names/values are present, the CSP is report-only, the CSP does not contain `googletagmanager.com`, `google-analytics.com`, `facebook.net`, `klaviyo`, or `cdn.shopify.com/s/shopify-pixels`, and the policy string contains no newline characters.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/lib/security/headers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/security/headers.ts` has no default export and no `'use client'` directive.
    - `securityHeaders` contains all six required header names.
    - CSP uses `Content-Security-Policy-Report-Only`, not enforced `Content-Security-Policy`.
    - CSP contains Searchanise, Shopify, Sanity, Trustoo, and maps hosts listed above.
    - CSP does not contain GA4/GTM/Meta/Klaviyo speculative hosts.
  </acceptance_criteria>
  <done>Security header policy is centralized and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire headers into Next config and disable x-powered-by</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts
    - src/lib/security/headers.ts
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/headers.md
  </read_first>
  <action>
    Import `securityHeaders` into `next.config.ts` using a relative import from the repo root. Add `poweredByHeader: false` at the top level of `nextConfig`. Add an async `headers()` function that returns one rule:

    ```ts
    {
      source: '/:path*',
      headers: securityHeaders,
    }
    ```

    Preserve the existing `allowedDevOrigins`, `cacheComponents`, `env`, `redirects()`, and `images` config exactly. Do not remove redirects or image remotePatterns. Do not introduce middleware/proxy or nonce CSP.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains `poweredByHeader: false`.
    - `next.config.ts` exports `headers()` with `source: '/:path*'`.
    - Existing redirect from `/collections/:handle/products/:productHandle` remains.
    - Existing image remotePatterns for `cdn.shopify.com`, `www.teavision.com.au`, and `cdn.sanity.io` remain.
    - No `proxy.ts` or middleware nonce implementation is added.
  </acceptance_criteria>
  <done>Next config applies the security header baseline globally.</done>
</task>

<task type="auto">
  <name>Task 3: Add source contract coverage for security headers</name>
  <files>scripts/component-contracts/security-headers.test.mjs, package.json</files>
  <read_first>
    - scripts/component-contracts/rate-limit.test.mjs
    - scripts/component-contracts/production-boundaries.test.mjs
    - package.json
    - next.config.ts
    - src/lib/security/headers.ts
  </read_first>
  <action>
    Create `scripts/component-contracts/security-headers.test.mjs` using the existing Node test runner pattern from `scripts/component-contracts/rate-limit.test.mjs`. It must read `next.config.ts` and `src/lib/security/headers.ts` as text and assert:

    - `next.config.ts` contains `poweredByHeader: false`
    - `next.config.ts` contains `headers()`
    - `src/lib/security/headers.ts` contains `Strict-Transport-Security`
    - `src/lib/security/headers.ts` contains `Content-Security-Policy-Report-Only`
    - `src/lib/security/headers.ts` contains `X-Content-Type-Options`
    - `src/lib/security/headers.ts` contains `Permissions-Policy`
    - `src/lib/security/headers.ts` does not contain `googletagmanager.com`, `google-analytics.com`, `facebook.net`, or `klaviyo`

    No package script change is required if `pnpm test:contracts` already runs `scripts/component-contracts/*.test.mjs`. If it does not, update only the existing `test:contracts` glob so this file runs.
  </action>
  <verify>
    <automated>pnpm test:contracts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:contracts` executes the new security header test.
    - The contract test fails if `poweredByHeader: false` is removed.
    - The contract test fails if speculative analytics hosts are added before Phase 16.
  </acceptance_criteria>
  <done>Source-level regression coverage protects the Phase 15 header baseline.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to storefront responses | Security headers constrain browser behavior for every public route |
| Third-party host allowlist to storefront | CSP report-only policy inventories allowed browser fetch/script/frame/image destinations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure / Clickjacking | missing security headers | mitigate | Add HSTS, content-type, referrer, permissions, frame protection, and no powered-by leakage |
| T-15-04 | XSS / Injection | CSP policy | mitigate | Stage explicit report-only CSP with current required hosts only; defer enforcement until smoke evidence |
| T-15-HDR-SC | Denial of Service | nonce CSP dynamic rendering trade-off | avoid | Do not add nonce CSP or Proxy in Phase 15 |

</threat_model>

<verification>

```bash
pnpm test:unit -- src/lib/security/headers.test.ts
pnpm test:contracts
pnpm typecheck
pnpm build
```

Plan 05 owns production response probing across representative routes.

</verification>

<success_criteria>

- `next.config.ts` disables `x-powered-by` with `poweredByHeader: false`.
- `headers()` applies required security headers to `/:path*`.
- CSP is report-only and explicit.
- No speculative analytics/advertising hosts are pre-allowed.
- Unit and contract tests cover the header baseline.

</success_criteria>

<output>

- `src/lib/security/headers.ts`
- `src/lib/security/headers.test.ts`
- `scripts/component-contracts/security-headers.test.mjs`
- Updated `next.config.ts`

</output>

