---
phase: 08-optimized-collection-quick-add
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shopify/queries/collection.graphql
  - src/lib/shopify/types/index.ts
  - src/lib/shopify/operations/collection.ts
  - src/lib/searchanise/search.ts
  - src/components/collection/quick-add-button/quick-add-button.tsx
  - src/components/collection/quick-add-button/quick-add-button.stories.tsx
  - src/components/collection/quick-add-button/index.ts
  - src/components/collection/product-card/product-card.tsx
  - src/components/collection/product-card/product-card.stories.tsx
  - src/components/collection/index.ts
autonomous: false
requirements:
  - CQA-01
  - CQA-02
  - CQA-03
  - CQA-04
  - CQA-05
  - CQA-06
user_setup: []

must_haves:
  truths:
    - "Collection/search listing cards restore a visible add-to-cart affordance only when a product has a safe single-variant quick-add target."
    - "Multi-variant products never quick-add a guessed/default variant; they keep Quick View and PDP routes for selection."
    - "Collection product data remains bounded: add only minimal quick-add variant fields and keep Phase 5 cursor/page-size behavior."
    - "ProductCard remains a Server Component; only the new quick-add button is a small client leaf."
    - "Quick-add uses the existing useAddToCart/addToCartAction behavior and accessible pending/success/error feedback."
    - "The old ProductPurchaseForm is not reattached to every listing card."
  artifacts:
    - path: "src/components/collection/quick-add-button/quick-add-button.tsx"
      provides: "Small collection-domain client leaf for direct add-to-cart"
      contains: "export function QuickAddButton"
    - path: "src/lib/shopify/types/index.ts"
      provides: "ProductQuickAdd type and CollectionProductSummary.quickAdd"
      contains: "ProductQuickAdd"
    - path: "src/lib/shopify/queries/collection.graphql"
      provides: "Minimal variants(first: 2) query fields for quick-add eligibility"
      contains: "variants(first: 2)"
    - path: "src/components/collection/product-card/product-card.tsx"
      provides: "Card action area showing quick-add for eligible products and Quick View fallback for others"
      contains: "QuickAddButton"
  key_links:
    - from: "src/components/collection/product-card/product-card.tsx"
      to: "src/components/collection/quick-add-button/quick-add-button.tsx"
      via: "eligible product.quickAdd renders QuickAddButton"
      pattern: "QuickAddButton"
    - from: "src/components/collection/quick-add-button/quick-add-button.tsx"
      to: "src/components/product/use-add-to-cart.ts"
      via: "shared cart mutation feedback controller"
      pattern: "useAddToCart"
---

<objective>
Restore optimized listing-card add-to-cart for collection/search results. The implementation adds a visible direct-add button only for safe single-variant products, keeps multi-variant products on Quick View/PDP selection, and preserves the Phase 5 PLP performance constraints that removed full per-card purchase forms.

Output:
- Minimal quick-add data contract in Shopify collection/Searchanise summaries.
- New `QuickAddButton` collection-domain client leaf with Storybook interaction coverage.
- Updated `ProductCard` action layout that shows quick-add where safe and keeps Quick View fallback where required.
- Verification evidence that full `ProductPurchaseForm` was not restored to every card.
</objective>

<execution_context>
@.planning/phases/08-optimized-collection-quick-add/08-CONTEXT.md
@.planning/phases/08-optimized-collection-quick-add/08-RESEARCH.md
@CODEBASE_REVIEW.md
@.planning/phases/05-codebase-review-remediation/05-03-PLAN.md
@.planning/phases/05-codebase-review-remediation/05-03-SUMMARY.md
@docs/conventions.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add a minimal quick-add data contract to collection/search summaries</name>
  <read_first>
    - src/lib/shopify/types/index.ts
    - src/lib/shopify/queries/collection.graphql
    - src/lib/shopify/operations/collection.ts
    - src/lib/searchanise/search.ts
    - .planning/phases/08-optimized-collection-quick-add/08-CONTEXT.md
  </read_first>
  <action>
Add a `ProductQuickAdd` type to `src/lib/shopify/types/index.ts`:

```ts
export type ProductQuickAdd = {
  variantId: string
  variantTitle: string
  availableForSale: boolean
}
```

Add `quickAdd: ProductQuickAdd | null` to `CollectionProductSummary`.

In `src/lib/shopify/queries/collection.graphql`, add a bounded variants probe to `GetCollectionProducts`:

```graphql
variants(first: 2) {
  edges {
    node {
      id
      title
      availableForSale
    }
  }
}
```

Run `pnpm codegen`.

In `src/lib/shopify/operations/collection.ts`, compute quick-add eligibility in `reshapeCollectionProductSummary()` from the two-variant probe:
- if there is exactly one variant and it is available, return its id/title as `quickAdd`
- otherwise return `quickAdd: null`

Do not fetch images, prices, quantity price breaks, quantity rules, or all variants in the collection query.

In `src/lib/searchanise/search.ts`, map a defensive `quickAdd` only when Searchanise data clearly identifies one available variant/add-to-cart id; otherwise return `quickAdd: null`. Keep all unknown narrowing; no `any`.
  </action>
  <verify>
    - `pnpm codegen`
    - `pnpm typecheck`
    - `rg -n "ProductQuickAdd|quickAdd" src/lib/shopify/types/index.ts src/lib/shopify/operations/collection.ts src/lib/searchanise/search.ts`
    - `rg -n "variants\\(first: 2\\)" src/lib/shopify/queries/collection.graphql`
  </verify>
  <acceptance_criteria>
    - `CollectionProductSummary` has `quickAdd: ProductQuickAdd | null`
    - `GetCollectionProducts` uses `variants(first: 2)` and only includes `id`, `title`, and `availableForSale`
    - No full variant array or product purchase form payload is restored to collection summaries
    - Searchanise mapping sets `quickAdd` defensively and falls back to `null` for ambiguous data
    - `pnpm codegen` and `pnpm typecheck` pass
  </acceptance_criteria>
  <done>Collection/Searchanise summaries expose a minimal quick-add eligibility contract without restoring full per-card variant payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Create the QuickAddButton client leaf with Storybook interaction states</name>
  <read_first>
    - src/components/product/use-add-to-cart.ts
    - src/components/collection/product-card/product-purchase-form.tsx
    - src/components/ui/button/button.tsx
    - docs/conventions.md
  </read_first>
  <action>
Scaffold `src/components/collection/quick-add-button/` with:
- `quick-add-button.tsx`
- `quick-add-button.stories.tsx`
- `index.ts`

The component is a small client leaf:
- includes `'use client'`
- imports `ShoppingCart` from `lucide-react`
- imports `Button` from `@/components/ui`
- imports `type AddToCart, useAddToCart` from `@/components/product/use-add-to-cart`
- accepts primitive props only:

```ts
type QuickAddButtonProps = {
  addToCart?: AddToCart
  disabled?: boolean
  productTitle: string
  variantId: string
}
```

Behavior:
- `onClick` calls `addItem(variantId, 1)`
- disabled when `disabled || isPending`
- button label is `Add to cart` / loading state via `Button isLoading`
- render `message` as `role="status"` and `error` as `role="alert"` using existing type/color utilities
- success copy should be short, e.g. `Added to cart`

Add Storybook coverage for:
- Default
- Success
- Error
- Pending/disabled
- Disabled sold-out

Mock `addToCart`; do not call Shopify.

Update `src/components/collection/index.ts` to export the new folder.
  </action>
  <verify>
    - `pnpm typecheck`
    - `pnpm lint`
    - `pnpm build-storybook`
    - `rg -n "export function QuickAddButton|useAddToCart|role=\"status\"|role=\"alert\"" src/components/collection/quick-add-button`
  </verify>
  <acceptance_criteria>
    - `QuickAddButton` is the only new Client Component in this phase
    - It reuses `useAddToCart` and does not import `addToCartAction` directly
    - It has accessible success and error feedback
    - Stories cover success/error/pending/disabled states with mocked actions
    - No default exports, no `any`, no inline styles, no raw hex classes
    - Typecheck, lint, and Storybook build pass
  </acceptance_criteria>
  <done>QuickAddButton exists as a tiny reusable collection client leaf with mocked Storybook interaction coverage and shared cart behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Render quick-add on ProductCard without restoring the old purchase form</name>
  <read_first>
    - src/components/collection/product-card/product-card.tsx
    - src/components/collection/product-card/product-card.stories.tsx
    - src/components/collection/quick-add-button/quick-add-button.tsx
    - src/components/product/product-quick-view/product-quick-view.tsx
  </read_first>
  <action>
Update `ProductCard` to import and render `QuickAddButton` when `product.quickAdd` is non-null and `product.availableForSale` is true.

Action layout intent:
- Keep the `From <Price>` display.
- Keep `More info` for all products.
- Render `QuickAddButton` for quick-add eligible products.
- Render `ProductQuickView` for non-eligible available products so multi-variant products still have a purchase path.
- Do not import or render `ProductPurchaseForm`.

If the card action row becomes too crowded, prefer a stable responsive wrap:
- price takes `mr-auto`
- buttons use `size="sm"`
- quick-add button must not cause title/text overflow on mobile

Update `product-card.stories.tsx` with fixtures:
- single-variant quick-add eligible product
- multi-variant product with `quickAdd: null`
- sold-out product
- long-title mobile layout

Where ProductCard stories need to avoid real cart mutation, pass the mocked add-to-cart into `QuickAddButton` through a prop chain only if necessary. If that would pollute the server component API, keep interaction coverage on `QuickAddButton` stories and use ProductCard stories for visual state coverage.
  </action>
  <verify>
    - `pnpm typecheck`
    - `pnpm lint`
    - `pnpm build`
    - `pnpm build-storybook`
    - `rg -n "ProductPurchaseForm" src/components/collection/product-card/product-card.tsx` returns no matches
    - `rg -n "QuickAddButton|ProductQuickView" src/components/collection/product-card/product-card.tsx`
  </verify>
  <acceptance_criteria>
    - Eligible products show `Add to cart` on the card
    - Multi-variant products still show `Quick View`
    - Sold-out products do not render an enabled quick-add button
    - `ProductCard` remains a Server Component with no `'use client'`
    - `ProductCard` does not import `ProductPurchaseForm`
    - Typecheck, lint, build, and Storybook build pass
  </acceptance_criteria>
  <done>Product cards show optimized direct add only where safe, keep Quick View fallback for multi-variant products, and do not reattach the heavy purchase form.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Collection/search product cards now show a visible `Add to cart` action for safe single-variant products. Multi-variant products keep Quick View and More info. The data contract uses a minimal quick-add variant probe, and the old full ProductPurchaseForm is not mounted on every card.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open `http://localhost:3000/collections/all`. Find a single-variant available product and confirm the card shows `Add to cart`. Click it and confirm the button enters a pending state, then shows success, and the cart count refreshes.
    2. Find a multi-variant product and confirm it does not quick-add a guessed variant. It should keep `Quick View`; open Quick View and confirm variant selection still controls add-to-cart.
    3. Open `http://localhost:3000/search?q=tea` and confirm search result cards follow the same quick-add/fallback rules.
    4. Check mobile width: title, price, More info, Quick View/Add to cart, and status messages must not overlap or resize the card awkwardly.
    5. Confirm no performance regression: initial PLP is still paginated/bounded, and source checks show `ProductPurchaseForm` is not imported by `ProductCard`.
  </how-to-verify>
  <resume-signal>Type "approved" once collection and search cards restore safe direct add without wrong-variant adds or layout/performance regressions, or describe what needs revision.</resume-signal>
</task>

</tasks>

<verification>

- `pnpm codegen`
- `pnpm typecheck`
- `pnpm lint`
- `pnpm build`
- `pnpm build-storybook`
- Targeted source checks:
  - `rg -n "variants\\(first: 2\\)" src/lib/shopify/queries/collection.graphql`
  - `rg -n "ProductPurchaseForm" src/components/collection/product-card/product-card.tsx` returns no matches
  - `rg -n "'use client'" src/components/collection/product-card/product-card.tsx` returns no matches
  - `rg -n "useAddToCart" src/components/collection/quick-add-button/quick-add-button.tsx`
- Browser checks for `/collections/all`, at least one category PLP if available, `/search?q=tea`, Quick View, and cart badge refresh.

</verification>

<success_criteria>

1. Single-variant available products can be added to cart directly from listing cards. (CQA-01)
2. Multi-variant products require explicit variant selection through Quick View/PDP. (CQA-02)
3. Listing payload remains lightweight and bounded; only minimal quick-add variant fields are added. (CQA-03)
4. Quick-add uses the existing Shopify cart action path with accessible feedback. (CQA-04)
5. Storybook coverage proves quick-add states and product-card visual fallbacks. (CQA-05)
6. Verification confirms no full per-card `ProductPurchaseForm` hydration regression. (CQA-06)

</success_criteria>

<output>
After completion, create `.planning/phases/08-optimized-collection-quick-add/08-01-SUMMARY.md` recording:
- exact quick-add eligibility rule implemented
- verification command results
- whether collection/search manual checks passed
- any residual performance risk or follow-up for a shared list-level Quick View controller
</output>
