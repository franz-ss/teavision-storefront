---
phase: 05-codebase-review-remediation
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/lib/shopify/operations/collection.ts
  - src/lib/shopify/queries/collection.graphql
  - src/lib/shopify/types/index.ts
  - src/app/(storefront)/collections/[handle]/_components/page-content.tsx
  - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
  - src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts
  - src/components/collection/product-card/product-card.tsx
  - src/components/collection/product-card/product-purchase-form.tsx
  - src/components/layout/header/header.tsx
  - src/components/layout/header/mega-nav.tsx
  - src/components/layout/header/mobile-mega-nav.tsx
  - src/components/product/product-quick-view/product-quick-view.tsx
  - src/app/api/products/[handle]/quick-view/route.ts
  - src/components/product/product-gallery/product-gallery.tsx
  - src/lib/shopify/image-url.ts
autonomous: false
requirements:
  - AUDIT-06
  - AUDIT-10
must_haves:
  truths:
    - D-13: PLP performance work must reduce initial fetch/render/hydration cost, not merely hide it behind Suspense.
    - D-15: Shared image URL handling should be centralized when multiple callers duplicate it.
  artifacts:
    - src/lib/shopify/operations/collection.ts
    - src/app/(storefront)/collections/[handle]/_components/page-content.tsx
    - src/lib/shopify/image-url.ts
  key_links:
    - CODEBASE_REVIEW.md -> critical performance risk -> PLP fetch/render redesign
---

<objective>
Reduce the storefront performance risks identified in the review: PLP overfetching/rendering, per-card client hydration, heavy nav/quick-view islands, gallery/image handling, and broad Suspense placement.
</objective>

<execution_context>
@CODEBASE_REVIEW.md
@.planning/phases/05-codebase-review-remediation/05-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@docs/conventions.md
@node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign collection product fetching for bounded initial work</name>
  <files>src/lib/shopify/operations/collection.ts, src/lib/shopify/queries/collection.graphql, src/app/(storefront)/collections/[handle]/_components/page-content.tsx, src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts</files>
  <read_first>src/lib/shopify/operations/collection.ts, src/app/(storefront)/collections/[handle]/_components/page-content.tsx, src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts</read_first>
  <action>Stop fetching every Shopify page for initial PLP render. Add a bounded collection product operation that respects a page size and cursor/page parameter. Push category tag filters into Shopify product filters when possible rather than fetching all products and filtering locally. Keep existing URL semantics stable.</action>
  <verify>Run codegen if GraphQL changes. Compare `/collections/[handle]` and `/collections/[handle]/[category]` product counts, filters, empty states, and pagination.</verify>
  <acceptance_criteria>
    - Initial collection render fetches a bounded product count.
    - Category pages do not require fetching the entire collection when Shopify can filter by tag.
    - Existing filter/sort URL behavior remains stable.
    - Empty and invalid category behavior remains correct.
  </acceptance_criteria>
  <done>PLP data fetching is bounded and closer to the query layer.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce product-list hydration and quick-buy cost</name>
  <files>src/app/(storefront)/collections/[handle]/_components/product-list.tsx, src/components/collection/product-card/product-card.tsx, src/components/collection/product-card/product-purchase-form.tsx</files>
  <read_first>src/components/collection/product-card/product-card.tsx, src/components/collection/product-card/product-purchase-form.tsx, src/components/search/search-results-view/product-results.tsx</read_first>
  <action>Render lightweight server product cards by default. Move add-to-cart controls into a lazy quick-buy island, quick-view, or user-intent-loaded client component. Avoid serializing large variant arrays for every card when only display data is needed.</action>
  <verify>Inspect RSC/client bundle impact manually and confirm add-to-cart still works where rendered.</verify>
  <acceptance_criteria>
    - Product cards do not hydrate a purchase form for every product by default.
    - Quick-buy remains available through an explicit user action or constrained island.
    - Product cards keep image, price, title, vendor, and rating display.
  </acceptance_criteria>
  <done>PLP and search result cards are lighter at initial render.</done>
</task>

<task type="auto">
  <name>Task 3: Lazy-load expensive shared storefront islands</name>
  <files>src/components/layout/header/header.tsx, src/components/layout/header/mega-nav.tsx, src/components/layout/header/mobile-mega-nav.tsx, src/components/product/product-quick-view/product-quick-view.tsx, src/app/api/products/[handle]/quick-view/route.ts</files>
  <read_first>src/components/layout/header/header.tsx, src/components/layout/header/mega-nav.tsx, src/components/layout/header/mobile-mega-nav.tsx, src/components/product/product-quick-view/product-quick-view.tsx</read_first>
  <action>Reduce sitewide header hydration by code-splitting desktop/mobile mega panels or hydrating only active disclosure controls. Replace per-card quick-view modal state with one shared lazily mounted modal. Change quick-view API to return a minimal DTO needed by the modal rather than the full product payload.</action>
  <verify>Check desktop and mobile header menus, keyboard behavior, and quick-view open/add-to-cart behavior.</verify>
  <acceptance_criteria>
    - Header no longer eagerly hydrates both desktop and mobile heavy menu trees where avoidable.
    - Quick-view state is centralized and lazy.
    - Quick-view API response shape is smaller and runtime-guarded.
  </acceptance_criteria>
  <done>Shared client islands are smaller and more user-intent driven.</done>
</task>

<task type="auto">
  <name>Task 4: Standardize image URL and LCP handling</name>
  <files>src/lib/shopify/image-url.ts, src/components/product/product-gallery/product-gallery.tsx, src/components/collection/product-card/product-card.tsx, src/app/(storefront)/cart/page.tsx, src/components/layout/header/header.tsx, src/app/(storefront)/collections/[handle]/_components/hero.tsx, src/app/(storefront)/pages/custom-tea-blends/_components/blend-image.tsx</files>
  <read_first>node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md, src/components/product/product-gallery/product-gallery.tsx, src/components/collection/product-card/product-card.tsx</read_first>
  <action>Create one Shopify image URL helper that safely adds width/height/crop params whether or not the source URL already has a query string. Replace direct `&width=` appends. Replace deprecated `priority` usage with current Next 16 `preload` only for true LCP images. Keep logo preloading only if measured or justified. Avoid rendering every gallery full-size slide eagerly if the selected image can be server-rendered first.</action>
  <verify>Run lint/build and browser-check PDP, PLP, cart, collection hero, and custom blend images.</verify>
  <acceptance_criteria>
    - No direct ad hoc Shopify `&width=` URL mutations remain.
    - Next Image usage follows local Next 16 docs.
    - PDP primary image remains visible and stable at first paint.
  </acceptance_criteria>
  <done>Image handling is safer and less wasteful.</done>
</task>

</tasks>

<testing_steps>

1. `pnpm codegen` if GraphQL query shape changes.
2. `pnpm typecheck`
3. `pnpm lint`
4. `pnpm build`
5. Browser checks for PLP default, category PLP, filtered PLP, search results, PDP gallery, header desktop/mobile menus, quick-view.
6. Compare route payload/client bundle qualitatively before and after with Next build output or analyzer if available.

</testing_steps>

<risks_and_edge_cases>

- Shopify Storefront API filter syntax may not support every current category convention; preserve fallback behavior if needed.
- PLP URL compatibility matters; avoid breaking shared/filter links.
- Do not remove add-to-cart affordances without replacing them with an explicit quick-buy or quick-view path.
- Header code-splitting must not break keyboard access or mobile menu disclosure behavior.

</risks_and_edge_cases>

<success_criteria>

- `AUDIT-06` is implemented.
- Initial PLP fetch/render work is bounded.
- Product-list hydration is reduced.
- Header, quick-view, gallery, and image handling are measurably lighter or structurally prepared for measurement.
- Lint/build pass.

</success_criteria>
