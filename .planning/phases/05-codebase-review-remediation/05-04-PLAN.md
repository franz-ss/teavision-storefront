---
phase: 05-codebase-review-remediation
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - src/app/(storefront)/products/[handle]/page.tsx
  - src/app/(storefront)/products/[handle]/_components/*
  - src/app/(storefront)/products/[handle]/_lib/*
  - src/app/(storefront)/collections/page.tsx
  - src/app/(storefront)/collections/_components/*
  - src/app/(storefront)/pages/wholesale/page.tsx
  - src/app/(storefront)/pages/wholesale/_components/*
  - src/components/ui/product-card/product-card.tsx
  - src/components/product/recommendation-product-card/*
  - src/components/ui/accordion/accordion.tsx
  - src/components/contact/contact-form/contact-form.tsx
  - src/components/homepage/contact-form/contact-form.tsx
  - src/components/layout/footer/newsletter-form/newsletter-form.tsx
  - src/components/product/use-add-to-cart.ts
  - src/lib/shopify/operations/product.ts
  - src/lib/shopify/operations/collection.ts
  - src/lib/shopify/operations/cart.ts
  - src/lib/shopify/operations/mappers.ts
  - src/components/blog/portable-text/portable-text.tsx
  - scripts/create-component.mjs
autonomous: false
requirements:
  - AUDIT-07
  - AUDIT-08
  - AUDIT-10
must_haves:
  truths:
    - D-03: Application code stays under `src/` and follows `docs/conventions.md`.
    - D-14: Domain components should not live in `src/components/ui`.
    - D-15: Shared add-to-cart behavior, Shopify reshapers, and runtime JSON guards should be centralized when duplication already exists.
  artifacts:
    - src/app/(storefront)/products/[handle]/_components
    - src/lib/shopify/operations/mappers.ts
    - src/components/product/use-add-to-cart.ts
  key_links:
    - CODEBASE_REVIEW.md -> component/code quality findings -> boundary refactors
---

<objective>
Resolve the review's maintainability concerns by tightening component, route, and TypeScript boundaries after the launch-critical and performance changes are stable.
</objective>

<execution_context>
@CODEBASE_REVIEW.md
@.planning/phases/05-codebase-review-remediation/05-CONTEXT.md
@docs/conventions.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Split broad route files into route-owned components and helpers</name>
  <files>src/app/(storefront)/products/[handle]/page.tsx, src/app/(storefront)/products/[handle]/_components/*, src/app/(storefront)/products/[handle]/_lib/*, src/app/(storefront)/collections/page.tsx, src/app/(storefront)/collections/_components/*, src/app/(storefront)/pages/wholesale/page.tsx, src/app/(storefront)/pages/wholesale/_components/*</files>
  <read_first>src/app/(storefront)/products/[handle]/page.tsx, src/app/(storefront)/collections/page.tsx, src/app/(storefront)/pages/wholesale/page.tsx, docs/conventions.md</read_first>
  <action>Move non-trivial local React components and script/JSON-LD helpers out of broad route files into `_components` and `_lib` folders. Keep Next special files as default exports. Preserve route behavior, metadata, Suspense boundaries, and generated structured data.</action>
  <verify>Run lint/build and compare rendered PDP, collections index, and wholesale page.</verify>
  <acceptance_criteria>
    - Product route no longer defines multiple extracted React components in `page.tsx`.
    - Collections index and wholesale route have clearer section ownership.
    - No tiny one-off helper components are extracted unnecessarily.
  </acceptance_criteria>
  <done>Route files return to orchestration rather than mixed UI/data/script ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Move domain components out of ui and fill API/story gaps</name>
  <files>src/components/ui/product-card/product-card.tsx, src/components/product/recommendation-product-card/*, src/components/ui/index.ts, src/components/product/index.ts, src/components/layout/header/header.stories.tsx, src/components/collection/sort-select/sort-select.stories.tsx</files>
  <read_first>src/components/ui/product-card/product-card.tsx, src/components/collection/product-card/product-card.tsx, src/components/layout/header/header.tsx, src/components/collection/sort-select/sort-select.tsx</read_first>
  <action>Move the Shopify-aware `ui/product-card` into a product-domain component with a clearer name. Keep `ui` primitive-only. Add missing stories for public header shell and sort select. Export prop types for public components where consumers reasonably wrap them.</action>
  <verify>Run lint/storybook build; inspect imports to ensure no broken barrels.</verify>
  <acceptance_criteria>
    - `src/components/ui` contains primitives, not Shopify-domain cards.
    - Duplicate `ProductCard` naming ambiguity is reduced.
    - Header and sort select have co-located stories.
  </acceptance_criteria>
  <done>Component ownership and public API coverage are clearer.</done>
</task>

<task type="auto">
  <name>Task 3: Centralize add-to-cart behavior and form primitive usage</name>
  <files>src/components/product/use-add-to-cart.ts, src/components/product/product-form/product-form.tsx, src/components/collection/product-card/product-purchase-form.tsx, src/components/product/product-quick-view/product-quick-view.tsx, src/components/contact/contact-form/contact-form.tsx, src/components/homepage/contact-form/contact-form.tsx, src/components/layout/footer/newsletter-form/newsletter-form.tsx</files>
  <read_first>src/components/product/product-form/product-form.tsx, src/components/collection/product-card/product-purchase-form.tsx, src/components/product/product-quick-view/product-quick-view.tsx, src/components/ui/text-input/text-input.tsx, src/components/ui/form-label/form-label.tsx</read_first>
  <action>Extract shared add-to-cart state/error/success/refresh behavior into a small client hook or controller. Refactor forms that currently define local raw field styling to compose existing `FormLabel`, `TextInput`, `Textarea`, `Select`, and checkbox primitives where practical.</action>
  <verify>Run interaction stories from Plan 02 and manually check form layouts on mobile/desktop.</verify>
  <acceptance_criteria>
    - Add-to-cart behavior cannot diverge between PDP, cards, and quick-view.
    - Form field markup uses existing primitives unless a documented exception exists.
    - Accessibility feedback remains intact.
  </acceptance_criteria>
  <done>Interactive behavior and form presentation are less duplicated.</done>
</task>

<task type="auto">
  <name>Task 4: Extract Shopify mappers and runtime guards</name>
  <files>src/lib/shopify/operations/product.ts, src/lib/shopify/operations/collection.ts, src/lib/shopify/operations/cart.ts, src/lib/shopify/operations/mappers.ts, src/lib/shopify/runtime-guards.ts, src/components/blog/portable-text/portable-text.tsx</files>
  <read_first>src/lib/shopify/operations/product.ts, src/lib/shopify/operations/collection.ts, src/lib/shopify/operations/cart.ts, src/components/blog/portable-text/portable-text.tsx</read_first>
  <action>Extract duplicated `reshapeMoney`, `reshapeImage`, and rating parsing into shared Shopify mapper helpers. Add narrow runtime guards for owned API JSON payloads and replace broad `as Product` or shape assertions where feasible. Replace Portable Text weak casts with focused type guards and split complex renderers if needed.</action>
  <verify>Run typecheck/lint/build. Use `rg` to confirm duplicate mappers are removed and unsafe assertions are reduced.</verify>
  <acceptance_criteria>
    - Shared money/image/rating mappers have one source of truth.
    - Owned API client JSON parsing narrows `unknown` before use.
    - Portable Text rendering handles malformed CMS blocks safely.
  </acceptance_criteria>
  <done>Runtime boundary and mapper maintainability risks are reduced.</done>
</task>

<task type="auto">
  <name>Task 5: Fix scaffold and import-boundary drift</name>
  <files>scripts/create-component.mjs, src/components/ui/index.ts, src/components/homepage/index.ts, src/components/product/index.ts</files>
  <read_first>scripts/create-component.mjs, docs/conventions.md, eslint.config.mjs</read_first>
  <action>Update the component scaffolder so generated components import and use `cn()` when accepting `className`, avoid convention-breaking examples, and do not encourage unnecessary barrel exports. Prefer direct imports in client components where broad barrels obscure bundle boundaries.</action>
  <verify>Run scaffolder dry run in a temporary location if supported; run lint.</verify>
  <acceptance_criteria>
    - New scaffolded components start compliant with repo rules.
    - Client components do not rely on broad barrels where direct imports are clearer.
    - Existing public barrels remain only where they serve a clear API.
  </acceptance_criteria>
  <done>Future component work starts from the same standards the repo enforces.</done>
</task>

</tasks>

<testing_steps>

1. `pnpm typecheck`
2. `pnpm lint`
3. `pnpm build`
4. `pnpm build-storybook`
5. Run interaction tests added in Plan 02.
6. Browser check PDP, collections index, wholesale, contact/newsletter forms, quick-view, product cards.

</testing_steps>

<risks_and_edge_cases>

- Refactors after performance work must not undo PLP data/hydration improvements.
- Moving components can create import churn; keep changes scoped and preserve named exports.
- Avoid broad formatting of unrelated files, especially files with existing user edits.
- Runtime guard extraction should not replace generated GraphQL type safety with loose hand-written types.

</risks_and_edge_cases>

<success_criteria>

- `AUDIT-07` and `AUDIT-08` are implemented.
- Route files and component folders follow documented ownership boundaries.
- Shared mappers and add-to-cart behavior reduce meaningful duplication.
- Typecheck, lint, build, and Storybook build pass.

</success_criteria>
