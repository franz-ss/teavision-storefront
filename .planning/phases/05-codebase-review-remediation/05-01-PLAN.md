---
phase: 05-codebase-review-remediation
plan: 01
type: execute
wave: 1
depends_on:
  - 04-footer-1-1-parity
files_modified:
  - src/app/(storefront)/page.tsx
  - src/app/(storefront)/layout.tsx
  - src/app/(storefront)/collections/page.tsx
  - src/app/error.tsx
  - src/lib/contact/actions.ts
  - src/components/homepage/contact-form/contact-form.tsx
  - src/components/homepage/newsletter/newsletter.tsx
  - src/components/product/product-form/product-form.tsx
  - src/components/layout/header/cart-badge.tsx
  - src/components/product/related-products-carousel/related-products-carousel.tsx
  - src/lib/seo/serialize-inline-json.ts
  - src/app/(storefront)/collections/[handle]/_components/json-ld.tsx
  - src/app/(storefront)/pages/[...slug]/_components/json-ld.tsx
  - src/app/(storefront)/pages/custom-tea-blends/_components/json-ld.tsx
autonomous: false
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-10
must_haves:
  truths:
    - D-01: `CODEBASE_REVIEW.md` is the remediation source of truth.
    - D-02: User-visible conversion and accessibility blockers come first.
    - D-05: Homepage forms must provide accessible pending, success, and error feedback.
    - D-06: PDP add-to-cart must refresh visible cart state and announce success.
    - D-09: Inline script data must use one script-safe serializer.
  artifacts:
    - src/lib/seo/serialize-inline-json.ts
    - src/app/(storefront)/page.tsx
    - src/app/(storefront)/layout.tsx
    - src/components/product/product-form/product-form.tsx
  key_links:
    - CODEBASE_REVIEW.md -> quick wins -> visible conversion and accessibility fixes
---

<objective>
Fix the highest-priority launch blockers from the review: broken homepage routing, silent homepage forms, stale PDP add-to-cart feedback, unsafe JSON-LD serialization, missing skip navigation, nested landmarks, root error recovery, and carousel motion control.
</objective>

<execution_context>
@CODEBASE_REVIEW.md
@.planning/phases/05-codebase-review-remediation/05-CONTEXT.md
@docs/conventions.md
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Fix homepage CTA and shared inline JSON serialization</name>
  <files>src/app/(storefront)/page.tsx, src/lib/seo/serialize-inline-json.ts, src/app/(storefront)/collections/page.tsx, src/app/(storefront)/blogs/[blog]/[article]/page.tsx, src/app/(storefront)/collections/[handle]/_components/json-ld.tsx, src/app/(storefront)/pages/[...slug]/_components/json-ld.tsx, src/app/(storefront)/pages/custom-tea-blends/_components/json-ld.tsx</files>
  <read_first>CODEBASE_REVIEW.md, src/app/(storefront)/products/[handle]/page.tsx, src/app/(storefront)/page.tsx</read_first>
  <action>Create one named `serializeInlineJson(value: unknown): string` helper under `src/lib/seo/`. It must return JSON and replace `<` with `\\u003c`. Replace all route-local JSON-LD serializers and raw `JSON.stringify()` script usage with the helper. Fix the homepage CTA href from `/contact` to `/pages/contact`.</action>
  <verify>Run `pnpm lint`; inspect all `dangerouslySetInnerHTML` JSON-LD call sites with `rg` and confirm none use raw `JSON.stringify()` for JSON-LD.</verify>
  <acceptance_criteria>
    - Homepage contact CTA points to `/pages/contact`.
    - All JSON-LD script payloads use the shared safe serializer.
    - No new default exports are introduced.
    - No app code imports directly from generated Shopify types.
  </acceptance_criteria>
  <done>Structured data serialization and homepage routing are safe and consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Convert homepage contact and newsletter forms to observable submissions</name>
  <files>src/lib/contact/actions.ts, src/components/homepage/contact-form/contact-form.tsx, src/components/homepage/newsletter/newsletter.tsx, src/app/(storefront)/page.tsx</files>
  <read_first>src/components/contact/contact-form/contact-form.tsx, src/components/ui/newsletter-signup/newsletter-signup.tsx, src/lib/contact/actions.ts</read_first>
  <action>Change homepage form action props from `Promise<void>` to result-returning actions or reuse the existing richer client form patterns. Add pending, success, and error states using `role=\"status\"` and `role=\"alert\"` where appropriate. Preserve honeypot fields, current validation, Resend behavior, and server action boundaries. Keep client components as interactive leaves only.</action>
  <verify>Exercise empty, invalid, honeypot-filled, and valid-looking submissions locally. Verify screen-reader status text exists in the DOM.</verify>
  <acceptance_criteria>
    - Contact form exposes visible and accessible failure feedback.
    - Newsletter form exposes visible and accessible success/error feedback.
    - Honeypot behavior is preserved.
    - Missing `RESEND_API_KEY` returns user-safe errors rather than silent completion.
  </acceptance_criteria>
  <done>Homepage lead capture cannot silently fail.</done>
</task>

<task type="auto">
  <name>Task 3: Harmonize PDP add-to-cart feedback</name>
  <files>src/components/product/product-form/product-form.tsx, src/components/layout/header/cart-badge.tsx</files>
  <read_first>src/components/collection/product-card/product-purchase-form.tsx, src/components/product/product-quick-view/product-quick-view.tsx, src/lib/cart/actions.ts</read_first>
  <action>After successful PDP `addToCartAction`, call `router.refresh()` and set a success message announced with `role=\"status\"`. Keep existing error messages and quantity validation. Make the header cart badge update announcement-friendly if it changes after refresh.</action>
  <verify>Add an item from the PDP in a local browser and confirm the cart badge updates and success text is announced/rendered.</verify>
  <acceptance_criteria>
    - PDP add-to-cart refreshes the route after success.
    - PDP add-to-cart renders a success status.
    - Existing quantity cap and error behavior remain intact.
  </acceptance_criteria>
  <done>PDP add-to-cart provides the same reliability level as quick-view and product-card add-to-cart.</done>
</task>

<task type="auto">
  <name>Task 4: Fix global landmarks and error recovery</name>
  <files>src/app/(storefront)/layout.tsx, src/app/(storefront)/collections/page.tsx, src/app/error.tsx, src/app/global-error.tsx</files>
  <read_first>node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md, node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md, src/app/(storefront)/cart/error.tsx</read_first>
  <action>Add a token-styled skip link before `Header` and set the storefront `main` id to `main-content`. Replace the nested `/collections` `<main>` with a non-landmark wrapper. Update root `error.tsx` to the current Next 16 recovery prop pattern and add `global-error.tsx` if the local Next docs require it for root-level failures.</action>
  <verify>Keyboard-test the skip link; inspect landmarks; trigger/review error boundary shape by typechecking/building.</verify>
  <acceptance_criteria>
    - Keyboard users can skip to `#main-content`.
    - `/collections` has one `main` landmark.
    - Root error recovery follows local Next 16 docs.
    - Error logging remains user-safe.
  </acceptance_criteria>
  <done>Core App Router and landmark issues are remediated.</done>
</task>

<task type="auto">
  <name>Task 5: Address carousel motion control</name>
  <files>src/components/product/related-products-carousel/related-products-carousel.tsx, src/components/product/related-products-carousel/related-products-carousel.stories.tsx</files>
  <read_first>src/components/product/related-products-carousel/related-products-carousel.tsx, docs/conventions.md</read_first>
  <action>Remove autoplay by default or add a persistent pause/play control using existing button/icon primitives. If autoplay remains, ensure it pauses on focus/hover and can be stopped by keyboard and pointer users. Add or update Storybook coverage for paused/running states.</action>
  <verify>Keyboard-test the control and confirm no timed movement occurs after pause. Run Storybook for the carousel story.</verify>
  <acceptance_criteria>
    - Users can persistently stop carousel motion.
    - Control has an accessible name.
    - Motion behavior does not regress layout or focus order.
  </acceptance_criteria>
  <done>Autoplay motion risk is closed.</done>
</task>

</tasks>

<testing_steps>

1. `pnpm lint`
2. `pnpm build`
3. Browser check `/`, `/collections`, one PDP, and one article page.
4. Keyboard check skip link and carousel pause/play.
5. Submission checks for homepage contact/newsletter forms.
6. Inspect JSON-LD scripts for escaped `<` and valid JSON.

</testing_steps>

<risks_and_edge_cases>

- Homepage form changes must not move server-only Resend logic into client components.
- `global-error.tsx` may require an `<html>`/`<body>` shell depending on local Next docs.
- Shared JSON serializer should not over-escape data in a way that breaks structured-data parsing.
- Carousel control should not introduce a card-within-card or off-brand visual pattern.

</risks_and_edge_cases>

<success_criteria>

- `AUDIT-01`, launch-critical pieces of `AUDIT-02`, and JSON-LD pieces of `AUDIT-03` are implemented.
- `pnpm lint` and `pnpm build` pass.
- Homepage and PDP conversion flows provide visible and accessible feedback.
- Core landmarks and error recovery are corrected.

</success_criteria>
