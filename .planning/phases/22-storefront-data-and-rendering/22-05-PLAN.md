---
phase: "22-storefront-data-and-rendering"
plan: "05"
type: execute
wave: 5
depends_on: ["22-04"]
files_modified:
  - "src/components/homepage/testimonials/testimonials.tsx"
  - "src/components/homepage/testimonials/testimonials.stories.tsx"
  - "src/components/homepage/tea-journal/tea-journal.tsx"
  - "src/components/homepage/tea-journal/tea-journal.stories.tsx"
  - "src/lib/blog/operations.ts"
  - "src/lib/blog/operations.test.ts"
autonomous: true
requirements: [DATA-02, RENDER-01]
user_setup: []
must_haves:
  truths:
    - "D-09/D-11: Testimonials and Tea Journal consume modeled CMS fields through existing components."
    - "D-13: Tea Journal uses CMS section configuration while article data still comes from live blog operations."
    - "D-10: Testimonials carousel behavior remains isolated in the existing client leaf."
  artifacts:
    - path: "src/components/homepage/testimonials/testimonials.tsx"
      provides: "CMS intro and testimonial items with existing slider handoff."
    - path: "src/components/homepage/tea-journal/tea-journal.tsx"
      provides: "CMS intro/config with live blog article rendering."
    - path: "src/lib/blog/operations.ts"
      provides: "Homepage article fetch with configurable max post count."
  key_links:
    - from: "src/components/homepage/tea-journal/tea-journal.tsx"
      to: "src/lib/blog/operations.ts"
      via: "getHomepageArticles(blogHandle, maxPosts)"
      pattern: "getHomepageArticles"
---

<objective>
Prop-enable testimonials and Tea Journal configuration while preserving live article ownership.

Purpose: Tea Journal is the main cross-domain component in the homepage cutover because CMS controls section configuration but live article data stays in the existing blog operation layer.
Output: CMS-driven testimonial and Tea Journal props plus tested configurable homepage article fetching.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evang/.codex/get-shit-done/templates/summary.md
@.agents/skills/building-components/SKILL.md
@.agents/skills/vercel-react-best-practices/SKILL.md
@docs/conventions.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/22-storefront-data-and-rendering/22-CONTEXT.md
@.planning/phases/22-storefront-data-and-rendering/22-PATTERNS.md
@.planning/phases/22-storefront-data-and-rendering/22-UI-SPEC.md
@.planning/phases/22-storefront-data-and-rendering/22-01-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-04-SUMMARY.md
@src/lib/sanity/home-page.ts
@src/lib/blog/operations.ts
@src/components/homepage/testimonials/testimonials.tsx
@src/components/homepage/testimonials/testimonials-slider.tsx
@src/components/homepage/tea-journal/tea-journal.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prop-enable testimonials and Tea Journal config</name>
  <files>src/components/homepage/testimonials/testimonials.tsx, src/components/homepage/testimonials/testimonials.stories.tsx, src/components/homepage/tea-journal/tea-journal.tsx, src/components/homepage/tea-journal/tea-journal.stories.tsx, src/lib/blog/operations.ts, src/lib/blog/operations.test.ts</files>
  <read_first>
    @.planning/phases/22-storefront-data-and-rendering/22-CONTEXT.md
    @.planning/phases/22-storefront-data-and-rendering/22-PATTERNS.md
    @.planning/phases/22-storefront-data-and-rendering/22-UI-SPEC.md
    @.planning/phases/22-storefront-data-and-rendering/22-01-SUMMARY.md
    @.planning/phases/22-storefront-data-and-rendering/22-04-SUMMARY.md
    @src/lib/sanity/home-page.ts
    @src/lib/blog/operations.ts
    @src/lib/blog/operations.test.ts
    @src/components/homepage/testimonials/testimonials.tsx
    @src/components/homepage/testimonials/testimonials-slider.tsx
    @src/components/homepage/tea-journal/tea-journal.tsx
  </read_first>
  <behavior>
    - Testimonials receive CMS intro and testimonial items while `TestimonialsSlider` remains the client leaf.
    - Tea Journal receives CMS intro, blog handle, link label, and max posts.
    - Article cards still come from `getHomepageArticles()` and live empty-article behavior remains unchanged.
    - `getHomepageArticles('teavision-blogs', 2)` returns at most 2 articles while default behavior remains 3.
  </behavior>
  <action>Update testimonials to accept required CMS content per D-09 and D-11 while keeping `TestimonialsSlider` as the only carousel client leaf. Extend `getHomepageArticles(blogHandle = DEFAULT_BLOG_HANDLE, maxPosts = 3)` with tests; keep Sanity article data ownership in `src/lib/blog/operations.ts`, not the homepage CMS boundary, per D-13. Update `TeaJournal` to accept CMS section intro, blog handle, link label, and maxPosts from `HomepageContent`; do not hardcode `DEFAULT_BLOG_HANDLE`, `View all`, or the count in the component path when CMS data is supplied. Preserve the existing behavior where an empty live article result omits the section. Existing story files already exist, so update args in place.</action>
  <verify>
    <automated>pnpm test:unit -- src/lib/blog/operations.test.ts</automated>
    <automated>pnpm test:stories -- --project chromium --testNamePattern "Testimonials|TeaJournal"</automated>
    <automated>pnpm lint -- --quiet</automated>
  </verify>
  <acceptance_criteria>Testimonials and Tea Journal render CMS configuration, `getHomepageArticles()` supports bounded max posts with default behavior preserved, and live article ownership remains in blog operations.</acceptance_criteria>
  <done>Testimonials and Tea Journal render CMS configuration while live blog article ownership remains in blog operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CMS config -> live blog query | CMS blog handle and maxPosts influence a Sanity article read. |
| HomepageContent -> carousel wrapper | CMS testimonial content renders through a server wrapper around a client carousel leaf. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-22-14 | Tampering | `tea-journal.tsx` / `operations.ts` | mitigate | Use CMS config only for handle/label/count; keep article source in existing blog operation and validate required config in Plan 22-01. |
| T-22-15 | Elevation of Privilege | `testimonials.tsx` | mitigate | Keep carousel state in `TestimonialsSlider`; do not add `'use client'` to the server wrapper. |
| T-22-16 | Denial of Service | `getHomepageArticles()` | mitigate | Bound fetched article count through the tested `maxPosts` argument and keep existing empty-article behavior. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned in this phase. |
</threat_model>

<verification>
Run after this plan:
- `pnpm test:unit -- src/lib/blog/operations.test.ts`
- `pnpm test:stories -- --project chromium --testNamePattern "Testimonials|TeaJournal"`
- `pnpm lint -- --quiet`
</verification>

<success_criteria>
- Testimonials and Tea Journal consume explicit CMS props.
- Tea Journal uses CMS config while live article data still comes from `src/lib/blog/operations.ts`.
- Storybook stories and blog operation tests cover the updated behavior.
</success_criteria>

<output>
Create `.planning/phases/22-storefront-data-and-rendering/22-05-SUMMARY.md` when done.
</output>
