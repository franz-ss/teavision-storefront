---
phase: "22-storefront-data-and-rendering"
plan: "07"
type: execute
wave: 7
depends_on: ["22-06"]
files_modified:
  - "src/app/(storefront)/page.tsx"
  - "src/app/(storefront)/page.test.tsx"
  - "src/components/homepage/content.ts"
autonomous: true
requirements: [DATA-01, DATA-02, RENDER-01, RENDER-02, QUALITY-01]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-04: `/` renders from `getHomepage()` and does not import static homepage fallback content."
    - "D-05/D-06/D-07/D-08: `generateMetadata()` uses required CMS SEO fields, canonical '/', and `withNoindexRobots()`."
    - "D-10/D-12/D-13: Route composition preserves code-owned actions, fixed section order, and live Tea Journal article ownership."
    - "QUALITY-01: Route tests assert one H1, JSON-LD, exact section order, and no static SEO fallback."
  artifacts:
    - path: "src/app/(storefront)/page.tsx"
      provides: "CMS-backed homepage route render and metadata."
      exports: ["generateMetadata", "default"]
    - path: "src/app/(storefront)/page.test.tsx"
      provides: "Route/metadata tests, including exact section-order assertions."
    - path: "src/components/homepage/content.ts"
      provides: "Fixture/story data only after route cutover."
  key_links:
    - from: "src/app/(storefront)/page.tsx"
      to: "src/lib/sanity/home-page.ts"
      via: "getHomepage() in page and metadata"
      pattern: "getHomepage"
    - from: "src/app/(storefront)/page.tsx"
      to: "src/lib/seo/noindex.ts"
      via: "withNoindexRobots()"
      pattern: "withNoindexRobots"
---

<objective>
Cut over the storefront `/` route and metadata to the typed Sanity homepage boundary.

Purpose: This is the runtime switchover from static homepage constants to published Sanity content, with route-level tests proving the high-risk wiring.
Output: Async homepage route, CMS-backed metadata, route tests, and fixture-only static content.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evang/.codex/get-shit-done/templates/summary.md
@.agents/skills/next-cache-components/SKILL.md
@.agents/skills/vercel-react-best-practices/SKILL.md
@docs/conventions.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/22-storefront-data-and-rendering/22-CONTEXT.md
@.planning/phases/22-storefront-data-and-rendering/22-RESEARCH.md
@.planning/phases/22-storefront-data-and-rendering/22-PATTERNS.md
@.planning/phases/22-storefront-data-and-rendering/22-VALIDATION.md
@.planning/phases/22-storefront-data-and-rendering/22-UI-SPEC.md
@.planning/phases/22-storefront-data-and-rendering/22-01-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-02-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-03-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-04-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-05-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-06-SUMMARY.md
@node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-metadata.md
@node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md
@node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md
@src/app/(storefront)/page.tsx
@src/lib/sanity/home-page.ts
@src/lib/seo/noindex.ts
@src/lib/seo/serialize-inline-json.ts
@src/lib/seo/homepage-json-ld.ts
@src/lib/contact/actions.ts
@src/components/homepage/index.ts
@src/components/homepage/content.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route and metadata tests with exact section order</name>
  <files>src/app/(storefront)/page.test.tsx</files>
  <read_first>
    @.planning/phases/22-storefront-data-and-rendering/22-CONTEXT.md
    @.planning/phases/22-storefront-data-and-rendering/22-PATTERNS.md
    @.planning/phases/22-storefront-data-and-rendering/22-UI-SPEC.md
    @.planning/phases/22-storefront-data-and-rendering/22-VALIDATION.md
    @src/app/(storefront)/page.tsx
    @src/lib/sanity/home-page.ts
    @src/lib/seo/noindex.ts
    @src/lib/seo/homepage-json-ld.ts
  </read_first>
  <behavior>
    - Tests mock `getHomepage()` with CMS content and render the route to static markup.
    - Tests assert exact section order: `HomepageHero`, `ProductRange`, `HomepageNewsletter`, `PrivateLabel`, `OrganicHerbs`, `SupplyChain`, `CertificationCoverage`, `SupplyChainProtection`, `Testimonials`, `TeaJournal`, `ContactSection`, `Cta`, `Faq`.
    - Tests assert one visible `<h1`, Organization/WebSite JSON-LD nodes, CMS hero/product content, and absence of old static SEO fallback strings.
    - Metadata tests assert CMS noIndex yields `robots.index === false` after `withNoindexRobots()` is applied.
  </behavior>
  <action>Create `src/app/(storefront)/page.test.tsx` before route implementation. This file is scaffold-exempt because it is a co-located test for an existing Next special route file; the project scaffolds do not generate tests for existing route files. Mock homepage section components to emit stable `data-section` markers and assert their rendered index order exactly matches the UI-SPEC section order listed in the behavior block. Mock `getHomepage()`, `withNoindexRobots()`, and form actions at their external boundaries; do not mock the route function under test. Cover D-01, D-02, D-04, D-05, D-06, D-08, D-10, D-12, and D-13.</action>
  <verify>
    <automated>pnpm test:unit -- "src/app/(storefront)/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>Route tests exist and fail until `/` consumes `getHomepage()`, CMS metadata, JSON-LD constants, form actions, fixture-only static content, and the exact section order.</acceptance_criteria>
  <done>Route tests exist and fail until `/` consumes `getHomepage()`, CMS metadata, JSON-LD constants, and the exact section order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut over `/` render and metadata</name>
  <files>src/app/(storefront)/page.tsx, src/app/(storefront)/page.test.tsx, src/components/homepage/content.ts</files>
  <read_first>
    @src/app/(storefront)/page.test.tsx
    @src/app/(storefront)/page.tsx
    @src/lib/sanity/home-page.ts
    @src/lib/seo/noindex.ts
    @src/lib/seo/serialize-inline-json.ts
    @src/lib/seo/homepage-json-ld.ts
    @src/lib/contact/actions.ts
    @src/components/homepage/index.ts
    @src/components/homepage/content.ts
  </read_first>
  <behavior>
    - `page.tsx` exports `generateMetadata()` and no longer exports a static `metadata` object.
    - Route render calls `getHomepage()` and passes every section slice to existing components in exact fixed order.
    - Route render emits code-owned Organization/WebSite JSON-LD via `serializeInlineJson()`.
    - Static homepage content is not imported by the live route.
  </behavior>
  <action>Replace the static `metadata` export in `src/app/(storefront)/page.tsx` with `generateMetadata()` that awaits `getHomepage()` and maps CMS `seo.title`, description, canonical `/`, noIndex, and optional OG image into Next `Metadata`. Use `title: { absolute: homepage.seo.title }`, `alternates: { canonical: '/' }`, Open Graph URL `/`, and pass the final object through `withNoindexRobots()` per D-05, D-06, D-07, and D-08. Make the default route async, call `getHomepage()`, render JSON-LD from `src/lib/seo/homepage-json-ld.ts`, and pass content slices in this exact order: `HomepageHero`, `ProductRange`, `HomepageNewsletter`, `PrivateLabel`, `OrganicHerbs`, `SupplyChain`, `CertificationCoverage`, `SupplyChainProtection`, `Testimonials`, `TeaJournal`, `ContactSection`, `Cta`, `Faq`. Preserve `sendNewsletterSignupAction` and `submitContactFormAction` per D-10. Do not call `notFound()` for invalid homepage content; allow Plan 22-01 errors to fail rendering per D-01 and D-02. Remove live route imports from `src/components/homepage/content.ts` and leave that file as fixture/story/test data per D-04.</action>
  <verify>
    <automated>pnpm test:unit -- "src/app/(storefront)/page.test.tsx" src/lib/sanity/home-page.test.ts src/lib/blog/operations.test.ts</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>`/` renders and generates metadata from typed Sanity homepage content while preserving JSON-LD, actions, fixed section order, noindex controls, one-H1 behavior, and no static fallback import path.</acceptance_criteria>
  <done>`/` renders and generates metadata from typed Sanity homepage content while preserving JSON-LD, actions, fixed section order, noindex controls, one-H1 behavior, and no static fallback import path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Sanity homepage operation -> route render | Typed CMS content becomes public homepage HTML. |
| CMS SEO fields -> crawler metadata | CMS title, description, canonical, and noIndex influence metadata. |
| Route render -> contact/newsletter actions | CMS content renders around code-owned form actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-22-20 | Tampering | `page.tsx` section order | mitigate | Add exact route section-order assertions for all 13 sections. |
| T-22-21 | Information Disclosure | `generateMetadata()` | mitigate | Always pass CMS metadata through `withNoindexRobots()`. |
| T-22-22 | Repudiation | Static route fallback | mitigate | Test that route imports no static homepage content and excludes old static SEO fallback strings. |
| T-22-23 | Elevation of Privilege | Form action handoff | mitigate | Preserve code-owned `sendNewsletterSignupAction` and `submitContactFormAction`. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned in this phase. |
</threat_model>

<verification>
Run after this plan:
- `pnpm test:unit -- "src/app/(storefront)/page.test.tsx" src/lib/sanity/home-page.test.ts src/lib/blog/operations.test.ts`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm build`
</verification>

<success_criteria>
- `/` calls `getHomepage()` in page render and `generateMetadata()`.
- `page.tsx` no longer imports from `@/components/homepage/content`.
- Route tests assert the exact 13-section order, one H1, JSON-LD, CMS content, no static SEO fallback, and noindex handling.
- Build, lint, and typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/22-storefront-data-and-rendering/22-07-SUMMARY.md` when done.
</output>
