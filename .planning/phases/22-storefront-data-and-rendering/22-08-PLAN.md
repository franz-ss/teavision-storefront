---
phase: "22-storefront-data-and-rendering"
plan: "08"
type: execute
wave: 8
depends_on: ["22-07"]
files_modified: []
autonomous: false
requirements: [RENDER-01, RENDER-02, QUALITY-01]
user_setup: []
must_haves:
  truths:
    - "Final automated gates pass across Sanity data, route, Storybook, lint, typecheck, and build."
    - "Manual `/` homepage visual parity is checked against seeded content and the Phase 20 homepage baseline."
    - "Exact section order remains verified by automated tests and human browser review."
  artifacts: []
  key_links:
    - from: "22-VALIDATION.md"
      to: "22-08-PLAN.md"
      via: "final sampling and manual parity contract"
      pattern: "Visual homepage parity"
---

<objective>
Run final guards and capture the required manual homepage visual parity check.

Purpose: The implementation changes are complete only when automated checks and a browser parity sweep both confirm that the CMS-rendered homepage still matches the approved storefront experience.
Output: Final verification evidence in the plan summary and a human-approved `/` parity checkpoint.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evang/.codex/get-shit-done/templates/summary.md
@.planning/phases/22-storefront-data-and-rendering/22-VALIDATION.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/22-storefront-data-and-rendering/22-CONTEXT.md
@.planning/phases/22-storefront-data-and-rendering/22-VALIDATION.md
@.planning/phases/22-storefront-data-and-rendering/22-UI-SPEC.md
@.planning/phases/22-storefront-data-and-rendering/22-01-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-02-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-03-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-04-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-05-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-06-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated final guard suite</name>
  <files>None - verification-only plan</files>
  <read_first>
    @.planning/phases/22-storefront-data-and-rendering/22-VALIDATION.md
    @.planning/phases/22-storefront-data-and-rendering/22-07-SUMMARY.md
    @AGENTS.md
  </read_first>
  <action>Run the full Phase 22 validation commands from `22-VALIDATION.md`. Confirm route tests still include exact section-order assertions for `HomepageHero`, `ProductRange`, `HomepageNewsletter`, `PrivateLabel`, `OrganicHerbs`, `SupplyChain`, `CertificationCoverage`, `SupplyChainProtection`, `Testimonials`, `TeaJournal`, `ContactSection`, `Cta`, and `Faq`. Confirm no real Shopify hosted checkout, payment, shipping-rate, tax, order-creation, or success-redirect tests are run.</action>
  <verify>
    <automated>pnpm test:unit -- src/lib/sanity/queries/home-page.test.ts src/lib/sanity/home-page.test.ts "src/app/(storefront)/page.test.tsx" src/lib/blog/operations.test.ts</automated>
    <automated>pnpm test:stories</automated>
    <automated>pnpm lint</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>All final automated checks pass, no forbidden real Shopify checkout/payment/order tests run, and the summary records command outputs.</acceptance_criteria>
  <done>All final automated checks pass and the summary records the command outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual `/` homepage visual parity check</name>
  <files>None - human verification checkpoint</files>
  <read_first>
    @.planning/phases/22-storefront-data-and-rendering/22-VALIDATION.md
    @.planning/phases/22-storefront-data-and-rendering/22-UI-SPEC.md
    @.planning/phases/22-storefront-data-and-rendering/22-07-SUMMARY.md
  </read_first>
  <what-built>The storefront `/` route now renders seeded Sanity homepage content through the existing homepage component tree with CMS images, CMS metadata, and code-owned commerce/form/decorative boundaries.</what-built>
  <action>Pause execution after Task 1 automated guards pass and request human browser verification. Do not mark the phase ready for verification until the user approves the `/` homepage visual parity check or reports issues for a follow-up fix.</action>
  <how-to-verify>
    1. Run `pnpm dev`.
    2. Open `http://localhost:3000/`.
    3. Compare the page against the Phase 20 homepage baseline and the seeded Phase 21 content.
    4. Confirm the visible section order is exactly: HomepageHero, ProductRange, HomepageNewsletter, PrivateLabel, OrganicHerbs, SupplyChain, CertificationCoverage, SupplyChainProtection, Testimonials, TeaJournal, ContactSection, Cta, Faq.
    5. Confirm hero LCP image framing, product cards, service cards, organic image, certification marks, testimonials carousel, Tea Journal, contact form, catalogue CTA, FAQ, focus states, and mobile/desktop layout parity.
    6. Confirm newsletter/contact forms are present and styled, but do not run real external checkout/payment/order tests.
  </how-to-verify>
  <verify>
    <automated>pnpm test:unit -- "src/app/(storefront)/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>Human reviewer approves `/` visual parity, or reported section/viewport issues are captured before phase verification proceeds.</acceptance_criteria>
  <done>Human reviewer approves `/` visual parity, or reported issues are captured before phase verification proceeds.</done>
  <resume-signal>Type "approved" if visual parity passes, or describe the exact section/viewport issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Completed source -> release confidence | Automated and human checks determine whether the CMS route is ready for phase verification. |
| Browser-rendered homepage -> human parity approval | Human inspection catches layout/visual regressions not covered by unit/story tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-22-24 | Repudiation | Final evidence | mitigate | Record exact automated command outputs and human parity result in `22-08-SUMMARY.md`. |
| T-22-25 | Tampering | Section order parity | mitigate | Require both automated section-order assertion and manual browser confirmation. |
| T-22-26 | Denial of Service | Build/runtime readiness | mitigate | Run `pnpm build` after lint, typecheck, unit, and Storybook checks. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned in this phase. |
</threat_model>

<verification>
Final phase checks:
- `pnpm test:unit -- src/lib/sanity/queries/home-page.test.ts src/lib/sanity/home-page.test.ts "src/app/(storefront)/page.test.tsx" src/lib/blog/operations.test.ts`
- `pnpm test:stories`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm build`
- Human browser parity check at `http://localhost:3000/`
</verification>

<success_criteria>
- Full automated suite passes.
- Route section-order assertions are present and passing.
- Human `/` visual parity check is approved or issues are captured for revision.
- Summary records that formal preview, webhook revalidation, and release PageSpeed/SEO proof remain Phase 23 requirements, not Phase 22 runtime behavior.
</success_criteria>

<output>
Create `.planning/phases/22-storefront-data-and-rendering/22-08-SUMMARY.md` when done.
</output>
