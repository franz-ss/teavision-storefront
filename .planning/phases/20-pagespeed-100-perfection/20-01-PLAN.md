---
phase: 20-pagespeed-100-perfection
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [PSI-03, PSI-05, PSI-06, PSI-07, PSI-08]
files_modified:
  - src/components/homepage/hero/hero.tsx
  - next.config.ts
  - instrumentation-client.ts
  - scripts/component-contracts/launch-image-performance.test.mjs
  - docs/launch/homepage-performance-fixes.md

must_haves:
  truths:
    - "The homepage hero LCP image carries fetchPriority=high so the real-PSI 'LCP request discovery' audit passes and the 2,040ms resource-load-delay (20-PSI-EVIDENCE.md) is eliminated"
    - "The Next image optimizer emits AVIF (then WebP) so the 24KB hero AVIF is no longer transcoded to a larger WebP for AVIF-capable browsers"
    - "When NEXT_PUBLIC_SENTRY_DSN is unset, the ~142KB Sentry browser SDK is NOT in the route's cold first-load JS (the PSI 'reduce unused JS' + 'legacy JS' flags on / clear), and onRouterTransitionStart is still exported"
    - "Render-blocking CSS on / is reduced OR an explicit, measured decision to skip experimental.inlineCss is recorded (kept only if it improves PSI without a TTFB regression)"
    - "No regressions: lint, typecheck, build, all component-contract tests, the Phase 18 crawlable-HTML probe, and the Phase 19 single-visible-H1 test stay green; CLS on / stays 0"
  artifacts:
    - path: "src/components/homepage/hero/hero.tsx"
      provides: "hero Image with fetchPriority=high (distinct from the banned deprecated priority)"
      contains: "fetchPriority"
    - path: "next.config.ts"
      provides: "images.formats AVIF+WebP (+ experimental.inlineCss only if measured-beneficial)"
      contains: "image/avif"
    - path: "instrumentation-client.ts"
      provides: "client Sentry SDK lazy-loaded only when DSN is set; no top-level static @sentry/nextjs import"
    - path: "docs/launch/homepage-performance-fixes.md"
      provides: "before/after real-PSI / numbers for the four screenshot fixes + the inlineCss decision"
  key_links:
    - from: "src/components/homepage/hero/hero.tsx"
      to: "the rendered LCP image preload request"
      via: "fetchPriority=high"
      pattern: "fetchPriority=\"high\""
    - from: "instrumentation-client.ts"
      to: "@sentry/nextjs"
      via: "dynamic import inside the DSN guard (no top-level static import)"
      pattern: "import\\('@sentry/nextjs'\\)"
---

<objective>
Resolve EXACTLY the homepage `/` issues shown in the 2026-06-30 Google PSI screenshots (`20-PSI-EVIDENCE.md`) — nothing more. Lean, minimal, production-ready, architecture-preserving, no regressions. The four findings and their one-to-one fixes:

1. **LCP 3.4s / 2,040ms resource-load-delay** ("fetchpriority=high should be applied to the image preload request") → add `fetchPriority="high"` to the hero Image.
2. **Improve image delivery 9 KiB** (AVIF source transcoded to a larger WebP because `images.formats` is unset) → enable AVIF output.
3. **Reduce unused JS 88 KiB + Legacy JS 14 KiB** (the always-on ~142KB Sentry client chunk) → lazy-load the client Sentry SDK so it ships only when `NEXT_PUBLIC_SENTRY_DSN` is set.
4. **Render-blocking requests 540ms** (two CSS files) → enable `experimental.inlineCss` ONLY if, after fixes 1-3 are re-measured, render-blocking CSS still materially hurts AND inlineCss improves real PSI without a TTFB regression; otherwise record the explicit skip.

OUT OF SCOPE (deferred — do NOT touch): `/account` CLS, Accessibility/Best-Practices/SEO→100, the OKLCH/scrim/heading-order a11y work, the unused Caveat font, collection-card preload count, PDP gallery cap, minimumCacheTTL, resource hints, the tags-live third-party ceiling, the full PSI harness, and superseding PERF-01. Those remain in the deferred plans (`deferred/20-02..05-PLAN.md`, recoverable from commit e0e42881).

Output: hero `fetchPriority="high"`; `images.formats: ['image/avif','image/webp']`; lazy-loaded client Sentry; a measured inlineCss decision; an updated image contract test; a short before/after evidence note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pagespeed-100-perfection/20-CONTEXT.md
@.planning/phases/20-pagespeed-100-perfection/20-PSI-EVIDENCE.md
@src/components/homepage/hero/hero.tsx
@next.config.ts
@instrumentation-client.ts
@src/instrumentation.ts
@scripts/component-contracts/launch-image-performance.test.mjs
@CLAUDE.md
@AGENTS.md
@docs/conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fetchPriority="high" to the homepage hero LCP image (fixes the 2,040ms resource-load-delay)</name>
  <read_first>
    - src/components/homepage/hero/hero.tsx (the hero Image, line 27-34: currently `fill sizes="100vw" preload`, no fetchPriority, no priority)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md (fetchPriority ~L287-289 — recommended for LCP; `priority` deprecated ~L293; `preload` inserts head <link> ~L274 — read to confirm the exact prop combination that makes the emitted preload <link> carry fetchpriority=high)
    - scripts/component-contracts/launch-image-performance.test.mjs (test "launch image components avoid deprecated priority and invalid preload combinations" — confirm whether it forbids fetchPriority on the hero; if so it MUST be updated to allow fetchPriority="high" on the LCP hero while still forbidding the deprecated `priority`)
    - .planning/phases/20-pagespeed-100-perfection/20-PSI-EVIDENCE.md (the failing "LCP request discovery" audit)
  </read_first>
  <action>In src/components/homepage/hero/hero.tsx add fetchPriority="high" to the hero `<Image>` (the one with `fill sizes="100vw" preload` at lines 27-34). Keep `preload`, `fill`, `sizes="100vw"`, `alt=""`, and the className unchanged. fetchPriority is the distinct, current next/image prop (image.md:287-289) — do NOT add the deprecated `priority` prop and do NOT add `loading="eager"`. The goal is that the emitted `<link rel=preload as=image>` for the hero carries fetchpriority=high; confirm the exact emitted markup against image.md. Then read scripts/component-contracts/launch-image-performance.test.mjs: if it asserts the hero has no fetchPriority (or forbids fetchPriority globally), update only that assertion so fetchPriority="high" is ALLOWED specifically on the launch LCP hero, while it still fails on the deprecated `priority` prop and on `loading="eager"`+preload combinations. Do not weaken the deprecated-`priority` guard.</action>
  <verify>
    <automated>node --test scripts/component-contracts/launch-image-performance.test.mjs && node -e "const s=require('fs').readFileSync('src/components/homepage/hero/hero.tsx','utf8');if(!/fetchPriority=\"high\"/.test(s)){console.error('hero missing fetchPriority=high');process.exit(1)};if(/<Image[^>]*\spriority\b/.test(s)){console.error('deprecated priority prop present');process.exit(1)}"</automated>
    <human-check>On the next real PSI run of /, the "LCP request discovery" audit passes all three checks (not lazy, discoverable, fetchpriority=high).</human-check>
  </verify>
  <acceptance_criteria>
    - src/components/homepage/hero/hero.tsx hero Image has fetchPriority="high" and keeps preload/fill/sizes/alt=""; the deprecated `priority` prop and `loading="eager"` are NOT used
    - scripts/component-contracts/launch-image-performance.test.mjs passes and still fails on a deprecated `priority` prop / eager+preload combo (the deprecated-priority guard is intact)
  </acceptance_criteria>
  <done>The hero LCP image is prioritized via fetchPriority=high, eliminating the 2,040ms resource-load-delay, with the deprecated-priority guard preserved. (PSI-03)</done>
</task>

<task type="auto">
  <name>Task 2: Enable AVIF image output (fixes "Improve image delivery")</name>
  <read_first>
    - next.config.ts (images block lines 33-51: `qualities: [68, 75]` + remotePatterns; NO `formats` key → fork default is WebP-only, so the 24KB hero AVIF is re-encoded to a larger WebP)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md (formats default ['image/webp'] ~L738; AVIF encode note ~L770 — first cold encode is slower, warm/field is smaller)
    - scripts/component-contracts/launch-image-performance.test.mjs (AVIF byte-budget assertions — confirm enabling AVIF output does not break the source-asset budget checks)
  </read_first>
  <action>In next.config.ts add `formats: ['image/avif', 'image/webp']` to the `images` block (AVIF first so AVIF-capable browsers receive AVIF instead of a transcoded WebP). Keep `qualities: [68, 75]` and `remotePatterns` unchanged. Do NOT add deviceSizes/imageSizes/minimumCacheTTL (out of scope — those are deferred). Note: the first cold optimize after enabling AVIF is ~50% slower to encode; warm before measuring.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('next.config.ts','utf8');if(!/image\/avif/.test(s)||!/image\/webp/.test(s)){console.error('AVIF formats not enabled');process.exit(1)}" && node --test scripts/component-contracts/launch-image-performance.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts images config contains `formats: ['image/avif','image/webp']`
    - No other images-config keys are added (no deviceSizes/imageSizes/minimumCacheTTL — scope discipline)
    - launch-image-performance.test.mjs stays green
  </acceptance_criteria>
  <done>AVIF output is enabled so the hero AVIF is served as AVIF, recovering the flagged image-delivery bytes. (PSI-08)</done>
</task>

<task type="auto">
  <name>Task 3: Lazy-load the client Sentry SDK (fixes "Reduce unused JS" + "Legacy JS")</name>
  <read_first>
    - instrumentation-client.ts (top-level `import * as Sentry from '@sentry/nextjs'` at line 1 — this bundles the full ~142KB SDK on every route regardless of DSN; conditional init at lines 5-13; `export const onRouterTransitionStart = Sentry.captureRouterTransitionStart` at line 16)
    - src/instrumentation.ts (the existing runtime-gated `await import(...)` pattern to mirror — only imports when needed)
    - .planning/phases/20-pagespeed-100-perfection/20-PSI-EVIDENCE.md (chunk 3cagti3ou0q1b.js = 141.9KB transfer, 87.6KB unused + 13.9KB legacy polyfills)
  </read_first>
  <action>Rewrite instrumentation-client.ts so the Sentry browser SDK is NOT statically imported at the top (remove `import * as Sentry from '@sentry/nextjs'`). Instead, only `import('@sentry/nextjs')` dynamically INSIDE the `if (clientDsn)` guard, then call `Sentry.init({...})` with the existing options (dsn, environment, release, sendDefaultPii:false, tracesSampleRate:0, enableLogs:false) and capture its `captureRouterTransitionStart`. Still export a working `onRouterTransitionStart`: export a stable function that delegates to the loaded Sentry transition handler once the dynamic import resolves, and is a safe no-op when DSN is unset or before the import resolves. Use a properly typed function (no `any`; derive the type from `typeof import('@sentry/nextjs').captureRouterTransitionStart` or an explicit parameter type). Net effect: when NEXT_PUBLIC_SENTRY_DSN is unset, `@sentry/nextjs` is not in the cold first-load chunk; when set, Sentry loads lazily and router-transition capture still works. Do NOT change next.config.ts Sentry build options (the `withSentryConfig` 2nd arg is a build option, not a runtime/perf issue) and do NOT touch src/instrumentation.ts (server) or sentry.server/edge configs. Record in the Task-5 evidence note the accepted micro-tradeoff: router transitions firing in the brief window before the dynamic import resolves are not captured (acceptable vs. shipping the ~142KB SDK eagerly).</action>
  <verify>
    <automated>pnpm typecheck && node -e "const s=require('fs').readFileSync('instrumentation-client.ts','utf8');if(/^import \* as Sentry from '@sentry\/nextjs'/m.test(s)){console.error('top-level static Sentry import still present');process.exit(1)};if(!/import\('@sentry\/nextjs'\)/.test(s)){console.error('no dynamic Sentry import');process.exit(1)};if(!/onRouterTransitionStart/.test(s)){console.error('onRouterTransitionStart export missing');process.exit(1)};if(/\bany\b/.test(s)){console.error('any type introduced');process.exit(1)}"</automated>
    <human-check>After build, a cold PSI run of / no longer flags the ~142KB Sentry chunk under "Reduce unused JavaScript"/"Legacy JavaScript" (DSN unset on preview). If DSN is set, error monitoring + router-transition capture still function.</human-check>
  </verify>
  <acceptance_criteria>
    - instrumentation-client.ts has NO top-level static `import ... from '@sentry/nextjs'`; the SDK is imported only via `import('@sentry/nextjs')` inside the DSN guard
    - `onRouterTransitionStart` is still exported and is a typed function (no `any`) that no-ops safely when Sentry is not loaded
    - `pnpm typecheck` passes; next.config.ts and server instrumentation are unchanged
  </acceptance_criteria>
  <done>The ~142KB client Sentry SDK no longer ships on cold loads when DSN is unset, clearing the unused/legacy-JS flags, with error capture intact when DSN is set. (PSI-05, PSI-06)</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Re-measure / on real PSI, then decide on experimental.inlineCss (render-blocking CSS)</name>
  <read_first>
    - next.config.ts (no `experimental` block today; cacheComponents is a top-level key, not under experimental)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/inlineCss.md (inlineCss inlines CSS into a <style> tag — removes the render-blocking <link>, but re-downloads CSS per HTML response: a TTFB/bandwidth cost for repeat visitors)
    - .planning/phases/20-pagespeed-100-perfection/20-PSI-EVIDENCE.md (render-blocking CSS = 540ms, two stylesheets)
  </read_first>
  <decision>After Tasks 1-3 are built and deployed to the preview, re-run real PSI on `/`. Does render-blocking CSS still materially hurt LCP/Speed-Index, and does enabling `experimental.inlineCss` improve PSI on `/` WITHOUT regressing TTFB / repeat-visit performance?</decision>
  <context>inlineCss is a global rendering change with a real tradeoff (no CSS caching across navigations). Per "no unintended side effects", it must be measured, not enabled blind. Fixes 1-3 (esp. freeing the Slow-4G pipe by removing the 142KB Sentry chunk) may already collapse most of the LCP/SI gap, making inlineCss unnecessary.</context>
  <options>
    <option id="enable-inlinecss">
      <name>Enable experimental.inlineCss</name>
      <pros>Removes the 540ms render-blocking CSS; improves FCP/LCP/SI on /</pros>
      <cons>CSS re-inlined per HTML response (no cross-navigation cache) — verify no TTFB/repeat-visit regression before keeping</cons>
    </option>
    <option id="skip-inlinecss">
      <name>Skip inlineCss</name>
      <pros>Zero architectural risk; leanest; keeps CSS cacheable</pros>
      <cons>The 540ms render-blocking CSS remains (acceptable if Tasks 1-3 already cleared the LCP/SI gap)</cons>
    </option>
  </options>
  <resume-signal>Provide the post-Tasks-1-3 real PSI / numbers and choose enable-inlinecss or skip-inlinecss; then type "approved".</resume-signal>
  <acceptance_criteria>
    - If enable-inlinecss: next.config.ts has `experimental: { inlineCss: true }` AND docs/launch/homepage-performance-fixes.md records the before/after PSI showing improvement with no TTFB regression
    - If skip-inlinecss: next.config.ts has NO experimental.inlineCss AND the skip decision + the rationale (Tasks 1-3 cleared the gap, or the TTFB tradeoff) is recorded in docs/launch/homepage-performance-fixes.md
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const cfg=fs.readFileSync('next.config.ts','utf8');const doc=fs.readFileSync('docs/launch/homepage-performance-fixes.md','utf8');const has=/inlineCss/.test(cfg);if(has&&!/inlineCss/i.test(doc)){console.error('inlineCss enabled but decision not documented');process.exit(1)};if(!has&&!/inlineCss/i.test(doc)){console.error('inlineCss skip not documented');process.exit(1)}"</automated>
    <human-check>The inlineCss decision is backed by before/after real PSI numbers on /.</human-check>
  </verify>
  <done>The render-blocking-CSS finding is resolved by a measured decision (enable only if it helps without a TTFB regression), documented with PSI numbers. (PSI-07)</done>
</task>

<task type="auto">
  <name>Task 5: Regression sweep + before/after evidence note</name>
  <read_first>
    - .planning/phases/20-pagespeed-100-perfection/20-PSI-EVIDENCE.md (the before baseline: / Perf ~86, LCP 3.4s, SI 5.1s, CLS 0)
    - scripts/seo/probe-crawlable-html.mjs (Phase 18 crawlable-HTML guard)
    - tests/e2e/h1-correctness.spec.ts (Phase 19 single-visible-H1 guard)
  </read_first>
  <action>Run the full no-regression sweep and confirm all green: `pnpm lint`, `pnpm typecheck`, `pnpm build`, `node --test scripts/component-contracts/*.test.mjs` (all contract tests), `node scripts/seo/probe-crawlable-html.mjs` (Phase 18 — the static shell still contains the crawlable H1/content), and `pnpm exec playwright test tests/e2e/h1-correctness.spec.ts` (Phase 19 single visible H1). Then author docs/launch/homepage-performance-fixes.md: a short note recording the four screenshot findings, the exact fix applied to each, the before (`/` Perf ~86, LCP 3.4s/2,040ms resource-load-delay, SI 5.1s, CLS 0 — from 20-PSI-EVIDENCE.md) and the after real-PSI `/` numbers (owner-supplied), the inlineCss decision from Task 4, and an explicit "no regressions" line citing the green guards. Keep it lean — homepage `/` only; do NOT expand to other routes or categories (deferred).</action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm build && node --test scripts/component-contracts/*.test.mjs && node scripts/seo/probe-crawlable-html.mjs && pnpm exec playwright test tests/e2e/h1-correctness.spec.ts</automated>
    <human-check>docs/launch/homepage-performance-fixes.md shows before/after real-PSI / numbers; CLS stayed 0; LCP dropped materially; no new console errors.</human-check>
  </verify>
  <acceptance_criteria>
    - lint, typecheck, build, all component-contract tests, the crawlable-HTML probe, and the single-H1 e2e all pass
    - docs/launch/homepage-performance-fixes.md records the four findings, their fixes, before/after `/` PSI, the inlineCss decision, and a no-regressions statement
    - Scope discipline: the note covers homepage `/` Performance only; nothing about a11y/BP/SEO/other routes
  </acceptance_criteria>
  <done>The four screenshot fixes are verified regression-free and evidenced with before/after real PSI on /. (PSI-03, PSI-05, PSI-06, PSI-07, PSI-08)</done>
</task>

</tasks>

<verification>
- Hero renders `fetchPriority="high"` (no deprecated `priority`); PSI "LCP request discovery" passes on /.
- next.config.ts emits AVIF; client Sentry SDK is lazy (no top-level static import).
- experimental.inlineCss is enabled only if measured-beneficial, else explicitly skipped — both documented.
- NO REGRESSIONS: lint, typecheck, build, all component-contract tests, `scripts/seo/probe-crawlable-html.mjs`, and `tests/e2e/h1-correctness.spec.ts` pass; CLS on / stays 0; cacheComponents stays enabled; no out-of-scope files changed.
- docs/launch/homepage-performance-fixes.md records before/after real-PSI / numbers.
</verification>

<success_criteria>
- The four homepage `/` PSI screenshot findings (LCP/fetchpriority, image delivery, unused+legacy JS, render-blocking CSS) are each resolved with the minimal targeted fix.
- The change set is lean and architecture-preserving (one prop, one images-config key, one lazy import, one measured config flag, one contract-test reconciliation) with no regressions.
- Deferred scope (a11y/BP/SEO 100, CLS, fonts, caching, third-parties, full harness, PERF-01 supersession) is untouched.
</success_criteria>

<output>
Create `.planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md` when done. Record the before/after real-PSI `/` numbers, the inlineCss decision, and confirm the no-regression guards stayed green.
</output>
