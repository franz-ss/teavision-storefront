---
phase: 20-pagespeed-100-perfection
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [PSI-01, PSI-02]
files_modified:
  - scripts/performance/probe-psi.mjs
  - scripts/performance/probe-lighthouse.mjs
  - package.json
  - docs/launch/psi-evidence.md
  - docs/launch/lcp-root-cause.md
  - .env.example
user_setup:
  - service: preview-host
    why: "A public, fetchable, noindexed preview deployment is required so Google PageSpeed Insights servers can reach the storefront. PSI's crawler cannot supply Vercel bypass tokens, so Deployment Protection / auth walls must be OFF for the preview URL while app-level DISABLE_INDEXING stays true."
    env_vars:
      - name: GSD_PREVIEW_URL
        source: "The public preview deployment base URL (e.g. https://teavision-storefront-<hash>.vercel.app) — confirm the host in the Wave-1 decision checkpoint."
      - name: PAGESPEED_API_KEY
        source: "Optional Google Cloud PageSpeed Insights API key (Google Cloud Console -> APIs & Services -> Credentials). Raises the anonymous PSI rate limit; the script must also run keyless."
    dashboard_config:
      - task: "Disable Deployment Protection / password auth on the preview deployment so PSI can fetch it; confirm app robots noindex stays active (DISABLE_INDEXING=true)."
        location: "Hosting provider dashboard (Vercel: Project Settings -> Deployment Protection)"

must_haves:
  truths:
    - "Running the PSI probe writes real Google PSI mobile and desktop category scores for all 9 representative routes into docs/launch/psi-evidence.md"
    - "summarizeLhr records Performance, Best-Practices, and SEO category scores (not only Accessibility) and enumerates each failing accessibility audit by id"
    - "The /account/login CLS-0.128 shifting element is identified and recorded with the cause"
    - "A lab-vs-PSI reconciliation per route states whether each prior LCP failure is a measurement artifact or a real defect"
  artifacts:
    - path: "scripts/performance/probe-psi.mjs"
      provides: "Scripted Google PageSpeed Insights REST probe (mobile + desktop) writing machine-readable JSON + markdown evidence"
      min_lines: 80
    - path: "docs/launch/psi-evidence.md"
      provides: "Real PSI score-of-record per route per category per device"
    - path: "docs/launch/lcp-root-cause.md"
      provides: "Per-route LCP root-cause table (artifact vs real defect) + /account CLS diagnosis + lab/PSI reconciliation"
  key_links:
    - from: "scripts/performance/probe-psi.mjs"
      to: "pagespeedonline.googleapis.com runPagespeed REST endpoint"
      via: "fetch with strategy=mobile|desktop and category params"
      pattern: "runPagespeed|pagespeedonline"
    - from: "scripts/performance/probe-lighthouse.mjs summarizeLhr"
      to: "lhr.categories.{performance,best-practices,seo}.score"
      via: "categoryScore extraction"
      pattern: "best-practices|categories\\.seo|categories\\.performance"
---

<objective>
Establish a trustworthy measurement baseline and root-cause the Phase 17 LCP gap BEFORE any remediation (D-03). This is the gating wave: stand up / confirm a public, auth-disabled, noindexed preview reachable by Google PSI; script real PSI as the score of record (D-01); extend the local Lighthouse harness so it records all four category scores and enumerates failing a11y audits; pinpoint the `/account/login` CLS-0.128 shifting element; and reconcile local lab numbers against real PSI per route.

Purpose: Phase 17 burned effort remediating an unverified local lab LCP number. The strongest verified hypothesis is that the local "all routes FAIL LCP" evidence is a Lighthouse simulate-throttle artifact (text-LCP routes with no image post the same ~3.9s LCP). This wave proves it on real PSI so later waves remediate real defects, not lab noise.

Output: `scripts/performance/probe-psi.mjs`, extended `summarizeLhr`, `docs/launch/psi-evidence.md`, `docs/launch/lcp-root-cause.md`, a new `test:psi` package script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pagespeed-100-perfection/20-CONTEXT.md
@.planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md
@docs/launch/performance-evidence.md
@scripts/performance/probe-lighthouse.mjs
@CLAUDE.md
@AGENTS.md
@docs/conventions.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm preview host + PSI reachability (or encode the documented fallback)</name>
  <read_first>
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Open Questions 1, 2, 3, 9; "Recommended FIRST deliverable")
    - .planning/phases/20-pagespeed-100-perfection/20-CONTEXT.md (D-01, D-07)
    - docs/launch/seo-audit-pdf-compliance-2026-06-30.md (the only in-repo mention of teavision-storefront.vercel.app)
  </read_first>
  <decision>Is a public, auth-disabled, noindexed preview deployment reachable by Google PSI available now, and which host serves it? Also confirm: desktop in scope (D-07 says yes — capture both), the real product/collection/blog slugs to score, and whether NEXT_PUBLIC_SENTRY_DSN is set on the preview.</decision>
  <context>Research Open Question 1/2: there is no vercel.json/.vercel/ in-repo; the host is unconfirmed. PSI's crawler cannot supply auth-bypass tokens, so the preview must be publicly fetchable with auth OFF but app-level DISABLE_INDEXING=true (noindex). The decision boundary D-01 makes real PSI the score of record; the documented fallback (lab-as-proxy now, real PSI deferred to launch) must be an EXPLICIT recorded decision, never a silent assumption.</context>
  <options>
    <option id="real-psi-now">
      <name>Public noindexed preview is available now</name>
      <pros>Real PSI is captured this wave; full D-01 compliance; later waves verify against the true score of record</pros>
      <cons>Requires the owner to provision/confirm the preview URL and disable deployment protection</cons>
    </option>
    <option id="lab-proxy-fallback">
      <name>No public preview can be stood up pre-launch — use reconciled lab-as-proxy, defer real PSI to launch</name>
      <pros>Phase 20 proceeds without blocking on hosting; honest documentation of the deferral</pros>
      <cons>The literal "Google PSI 100/100" claim cannot be proven pre-launch; recorded as an explicit D-04 limitation in psi-evidence.md and lcp-root-cause.md</cons>
    </option>
  </options>
  <resume-signal>Provide: GSD_PREVIEW_URL (or "fallback"), desktop in/out of scope, real product/collection/blog slugs, and whether NEXT_PUBLIC_SENTRY_DSN is set on preview. Then type "approved".</resume-signal>
  <acceptance_criteria>
    - The chosen option (real-psi-now or lab-proxy-fallback) is recorded in the docs/launch/psi-evidence.md preamble
    - GSD_PREVIEW_URL (or an explicit "fallback" decision) is captured
    - Device scope (mobile + desktop per D-07), the real product/collection/blog slugs, and the NEXT_PUBLIC_SENTRY_DSN-on-preview status are recorded for downstream tasks
  </acceptance_criteria>
  <verify>
    <automated>MISSING — decision checkpoint; no automated gate. Record the chosen option and answers in docs/launch/psi-evidence.md preamble.</automated>
    <human-check>Owner has confirmed the preview URL (or fallback) and the route slugs.</human-check>
  </verify>
  <done>The preview host (or documented fallback), device scope, real route slugs, and Sentry-DSN-on-preview status are recorded and approved; subsequent tasks consume these answers.</done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/performance/probe-psi.mjs (real PSI score of record) + test:psi script</name>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs (DEFAULT_ROUTES lines 8-14; summarizeLhr line 670; markdown evidence writer ~line 1041)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md ("Recommended FIRST deliverable", "Validation Architecture" -> Primary, "Open Questions" 5/6/7)
    - package.json (scripts block; test:performance entry)
  </read_first>
  <action>Create scripts/performance/probe-psi.mjs that calls the Google PageSpeed Insights REST endpoint (pagespeedonline.googleapis.com/pagespeedonline/v5/runPagespeed) for each representative route — the 9 routes are the 7 existing DEFAULT_ROUTES plus the Tea Journal blog listing (/blogs/teavision-blogs) and one real blog article (slug from Task 1), using the real product/collection slugs from Task 1 against GSD_PREVIEW_URL. For each route, request strategy=mobile AND strategy=desktop and category=performance,accessibility,best-practices,seo. Read PAGESPEED_API_KEY from env (also run keyless). Run each route cold then warm twice to populate the edge image cache; record which run each number came from. Write machine-readable JSON (lighthouseResult.categories[*].score and audits for lcp/cls/lcp-element) and render docs/launch/psi-evidence.md with a per-route x per-category x per-device table, a preamble recording the Task-1 decision (host or fallback, device scope, slugs, Sentry-DSN status), and an explicit "scenario" note (cold PSI always sees consent default-deny — state that this is the cold/default-deny basis, not tags-live). Add a "test:psi" entry to package.json running this script. No default exports; follow the existing .mjs style of probe-lighthouse.mjs. If Task 1 chose the fallback, probe-psi.mjs must still exist and run, but psi-evidence.md records the deferral and points at reconciled local lab numbers as the interim proxy (D-04 honest documentation).</action>
  <verify>
    <automated>node scripts/performance/probe-psi.mjs --help 2>&1 | head -5 && node -e "const fs=require('fs');const s=fs.readFileSync('package.json','utf8');if(!/\"test:psi\"/.test(s))process.exit(1)"</automated>
    <human-check>If a live preview exists, docs/launch/psi-evidence.md contains real PSI mobile + desktop scores for all 9 routes; if fallback, the deferral and reconciliation basis are documented.</human-check>
  </verify>
  <acceptance_criteria>
    - scripts/performance/probe-psi.mjs exists and references "runPagespeed" and both "strategy=mobile" and "strategy=desktop" (or equivalent param construction)
    - probe-psi.mjs requests all four categories (performance, accessibility, best-practices, seo)
    - package.json scripts contains a "test:psi" key invoking node scripts/performance/probe-psi.mjs
    - docs/launch/psi-evidence.md exists and enumerates all 9 representative routes (the 7 DEFAULT_ROUTES + /blogs/teavision-blogs + a blog article) and labels each number cold/default-deny vs warm
    - psi-evidence.md preamble records the Task-1 answers (host/fallback, device scope, real slugs, Sentry-DSN-on-preview)
  </acceptance_criteria>
  <done>A scripted, repeatable PSI probe exists; real PSI (or the documented fallback) is captured as committed evidence for all 9 routes mobile + desktop. (PSI-01, PSI-02)</done>
</task>

<task type="auto">
  <name>Task 3: Extend summarizeLhr to record all category scores + enumerate failing a11y audits; document LCP root cause + /account CLS</name>
  <read_first>
    - scripts/performance/probe-lighthouse.mjs (summarizeLhr lines 670-696; categoryScore helper used at line 672; auditNumericValue; the markdown writer that consumes summarizeLhr output)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md ("Root-Cause Analysis: The LCP Gap" table; Per-Dimension Finding 1 + 7; "Research Coverage Note" — /account exact shifting element is unassigned and must be pinpointed here)
    - docs/launch/performance-evidence.md (prior local LCP/FCP/CLS per route; /account CLS 0.128)
  </read_first>
  <action>Extend summarizeLhr in scripts/performance/probe-lighthouse.mjs to also extract bestPracticesScore via categoryScore(lhr, 'best-practices'), seoScore via categoryScore(lhr, 'seo'), and performanceScore via categoryScore(lhr, 'performance'), plus a failingA11yAudits array enumerating every accessibility audit whose score is below 1 (id + title), not just the numeric a11yScore. Surface these new fields in the returned object and in the markdown evidence table the harness writes. Do NOT change the existing simulate/mobile throttling, warmup logic, or the LCP/CLS/TBT regression thresholds (treat them as a tripwire only). Then run the local harness (pnpm test:performance) to populate fresh local rows, run probe-psi.mjs (or use Task-1 fallback proxy), and author docs/launch/lcp-root-cause.md with: (a) a per-route table mirroring the research "Root-Cause Analysis: The LCP Gap" — LCP type, local LCP, PSI LCP, verdict (artifact vs real defect), confirming probe; (b) a concrete diagnosis of the /account/login?returnTo=%2Faccount CLS-0.128 shifting element (inspect Lighthouse layout-shift sources / the login-bridge geometry — name the element/component responsible); (c) a lab<->PSI reconciliation statement declaring PSI the score of record where they diverge. Use real diagnostic data; do not assert the artifact conclusion without the per-route numbers backing it.</action>
  <verify>
    <automated>pnpm test:performance && node -e "const s=require('fs').readFileSync('scripts/performance/probe-lighthouse.mjs','utf8');for(const t of ['best-practices','seo','performance','failingA11yAudits']){if(!s.includes(t)){console.error('missing '+t);process.exit(1)}}"</automated>
    <human-check>docs/launch/lcp-root-cause.md names the /account CLS element and gives a per-route artifact/defect verdict backed by numbers.</human-check>
  </verify>
  <acceptance_criteria>
    - summarizeLhr returns bestPracticesScore, seoScore, performanceScore, and a failingA11yAudits array (audit id + title for each scored-below-1 a11y audit)
    - The harness markdown evidence output includes Best-Practices and SEO category scores per route (not only Accessibility)
    - scripts/performance/probe-lighthouse.mjs still passes no --throttling-method (simulate retained) and still applies the warmup runs
    - docs/launch/lcp-root-cause.md exists with a per-route LCP root-cause verdict (artifact vs real defect) and the named /account/login CLS-0.128 shifting element
    - docs/launch/lcp-root-cause.md contains an explicit lab<->PSI reconciliation statement
  </acceptance_criteria>
  <done>The harness records all four category scores and per-audit a11y failures; the LCP gap and /account CLS are root-caused per route; lab is reconciled against PSI. The gate for Waves 2-5 is satisfied. (PSI-01, PSI-02)</done>
</task>

</tasks>

<verification>
- GATE: PSI numbers (or the documented fallback proxy) exist for all 9 routes mobile + desktop; do not start Wave 2 blind.
- `node scripts/performance/probe-psi.mjs` runs and writes docs/launch/psi-evidence.md.
- `pnpm test:performance` runs and the harness now emits Performance/Best-Practices/SEO scores plus failing-a11y-audit ids.
- docs/launch/lcp-root-cause.md root-causes every prior LCP failure and the /account CLS-0.128 element, with a lab<->PSI reconciliation.
- Regression guard (no code render path changed this wave): confirm `node scripts/seo/probe-launch-seo.mjs` and `pnpm exec playwright test tests/e2e/h1-correctness.spec.ts` still pass if any harness env/build flag was altered.
</verification>

<success_criteria>
- A public, fetchable, noindexed preview is confirmed (or the fallback is explicitly recorded) and real PSI mobile + desktop scores are committed for all 9 representative routes.
- The local Lighthouse harness is reconciled against PSI and records all four category scores + per-audit a11y enumeration.
- The Phase 17 LCP gap and the /account CLS-0.128 are root-caused per route before any remediation begins.
</success_criteria>

<output>
Create `.planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md` when done. Record: chosen preview option, real route slugs, NEXT_PUBLIC_SENTRY_DSN-on-preview status, the per-route artifact-vs-defect verdicts, and the named /account CLS element — Waves 2-4 depend on these.
</output>
