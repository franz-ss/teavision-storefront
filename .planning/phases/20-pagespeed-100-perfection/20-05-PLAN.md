---
phase: 20-pagespeed-100-perfection
plan: 05
type: execute
wave: 5
depends_on: ["20-01", "20-02", "20-03", "20-04"]
autonomous: true
requirements: [PSI-15]
files_modified:
  - docs/launch/psi-evidence.md
  - docs/launch/performance-acceptance.md
  - docs/launch/final-production-readiness-report.md
  - .planning/REQUIREMENTS.md
  - public

must_haves:
  truths:
    - "A consolidated evidence pack records final per-route, per-category, per-device scores with cold/default-deny vs tags-live deltas"
    - "Where a perfect 100 is not physically achievable, the limitation and best-achievable alternative are explicitly documented (no fabricated pass)"
    - "The PERF-01 non-blocking acceptance is superseded/updated with the real PSI result"
    - "Oversized unused /public PNGs are pruned/downscaled only after verifying no references"
  artifacts:
    - path: "docs/launch/psi-evidence.md"
      provides: "Final consolidated PSI evidence pack with the impact x effort roadmap and honest waivers"
    - path: "docs/launch/performance-acceptance.md"
      provides: "Superseded/updated PERF-01 acceptance reflecting the real result"
    - path: "docs/launch/final-production-readiness-report.md"
      provides: "Readiness report updated with the genuine performance outcome"
  key_links:
    - from: "docs/launch/performance-acceptance.md"
      to: "docs/launch/psi-evidence.md"
      via: "real PSI result supersedes the prior lab-failure acceptance"
      pattern: "psi-evidence|supersede|PERF-01"
    - from: "docs/launch/final-production-readiness-report.md"
      to: "the real performance outcome"
      via: "performance status reflects genuine PSI, not pass-ratio"
      pattern: "performance"
---

<objective>
Consolidate Waves 1-4 into a single honest evidence pack and supersede the Phase 17 PERF-01 non-blocking acceptance with the real result. Record final per-route/per-category/per-device scores, the cold/default-deny vs tags-live deltas, the prioritized (impact x effort) remediation roadmap, and explicit waivers where a perfect 100 is not physically achievable. Update performance-acceptance.md and final-production-readiness-report.md to reflect the genuine PSI outcome. Optionally prune oversized unused /public PNGs.

Purpose: D-06/PSI-15 require the final evidence to record the REAL PSI outcome (genuine pass, or an honest documented ceiling with the best alternative) and update the prior acceptance — no fabricated pass. The current "100/100" is an automated pass-ratio, not a Lighthouse Performance score; this wave replaces that framing with the truth.

Output: consolidated psi-evidence.md with the roadmap + waivers; superseded performance-acceptance.md; updated final-production-readiness-report.md; REQUIREMENTS.md PSI rows marked Complete; optional /public PNG cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pagespeed-100-perfection/20-CONTEXT.md
@.planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md
@.planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md
@.planning/phases/20-pagespeed-100-perfection/20-02-SUMMARY.md
@.planning/phases/20-pagespeed-100-perfection/20-03-SUMMARY.md
@.planning/phases/20-pagespeed-100-perfection/20-04-SUMMARY.md
@docs/launch/performance-acceptance.md
@docs/launch/final-production-readiness-report.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@AGENTS.md
@docs/conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate the final evidence pack + impact x effort roadmap + honest waivers</name>
  <read_first>
    - docs/launch/psi-evidence.md (Wave 1 baseline + Wave 2 warm re-measurement)
    - docs/launch/lcp-root-cause.md (per-route artifact-vs-defect verdicts)
    - docs/launch/third-party-ceiling.md, docs/launch/js-budget-evidence.md (Wave 3)
    - docs/launch/accessibility-100-evidence.md, docs/launch/seo-100-evidence.md, docs/launch/best-practices-ceiling.md (Wave 4)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md ("Impact x Effort Prioritization" table; Achievability Assessment table; "Wave 5"; D-04/D-05/D-06 honesty requirements)
  </read_first>
  <action>Consolidate the per-wave evidence into the canonical docs/launch/psi-evidence.md final section: a table of FINAL scores per representative route x category (Performance, Accessibility, Best Practices, SEO) x device (mobile + desktop), labelling each Performance number cold/default-deny vs tags-live; include the prioritized impact x effort remediation roadmap (mirroring the research table, marking what shipped); and, per D-04, an explicit "Limitations & Best Alternative" section stating where a perfect 100 is NOT physically achievable and why — at minimum: (a) SEO ~69 on the noindexed preview is intentional (is-crawlable weight), with SEO 100 proven on the indexable build; (b) the tags-live Performance/Best-Practices ceiling (with the cold-vs-tags-live delta); (c) any image-LCP route that lands in a documented high-90s on throttled mobile with a root-caused reason (throttle modeling of a correctly-sized hero, not a fixable defect). No fabricated pass — every claimed number must trace to a committed PSI/Lighthouse run.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/launch/psi-evidence.md','utf8');for(const t of ['Best Practices','SEO','Accessibility','Performance','desktop','Limitations']){if(!s.includes(t)){console.error('missing '+t);process.exit(1)}}"</automated>
    <human-check>psi-evidence.md final section has the per-route/category/device score matrix, the impact x effort roadmap, and explicit honest waivers where 100 is not achievable.</human-check>
  </verify>
  <acceptance_criteria>
    - docs/launch/psi-evidence.md final section records per-route x per-category x per-device final scores with cold/default-deny vs tags-live labelling
    - The prioritized impact x effort remediation roadmap is present and marks what shipped
    - A "Limitations & Best Alternative" section explicitly documents the SEO ~69 preview ceiling, the tags-live ceiling, and any image-LCP high-90s with root cause
    - Every claimed score traces to a committed PSI/Lighthouse run (no fabricated pass)
  </acceptance_criteria>
  <done>A single consolidated, honest evidence pack documents the achieved scores, the roadmap, and the best alternative wherever 100 is not physically reachable. (PSI-15)</done>
</task>

<task type="auto">
  <name>Task 2: Supersede PERF-01 acceptance + update the readiness report; mark PSI requirements complete</name>
  <read_first>
    - docs/launch/performance-acceptance.md (the dated PERF-01 non-blocking acceptance to supersede)
    - docs/launch/final-production-readiness-report.md (records automated 100/100 with performance accepted; reframe to the genuine result)
    - scripts/launch/run-final-readiness-audit.mjs (applyPerformanceAcceptance ~L351-381 — understand how the acceptance flows so the update is consistent)
    - .planning/REQUIREMENTS.md (PSI-01..PSI-15 traceability rows ~L143-157; flip Planning -> Complete)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (D-06; "Wave 5"; Risks "Don't confuse readiness 100/100 with a Lighthouse category score")
  </read_first>
  <action>Update docs/launch/performance-acceptance.md to supersede the Phase 17 PERF-01 lab-failure acceptance with the real Phase 20 PSI result: reference docs/launch/psi-evidence.md, state the genuine outcome (real PSI pass, or the honestly documented ceiling + best alternative), and keep it dated/attributable. Update docs/launch/final-production-readiness-report.md so the performance status reflects the genuine PSI outcome rather than the prior automated-pass-ratio framing — explicitly distinguish the Lighthouse/PSI category scores from the readiness pass-ratio. In .planning/REQUIREMENTS.md flip the PSI-01..PSI-15 traceability rows from Planning to Complete (only those that the Wave 1-5 evidence genuinely supports; any that remain owner-gated or documented-as-ceiling are recorded as such, not falsely passed). Do not fabricate a pass anywhere; where the result is a documented ceiling, say so.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/launch/performance-acceptance.md','utf8');if(!/psi-evidence|PSI|Phase 20/i.test(s)){process.exit(1)}" && node -e "const s=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');const m=s.match(/PSI-01[^\n]*/);if(m&&/Planning/.test(m[0])){console.error('PSI-01 still Planning');process.exit(1)}"</automated>
    <human-check>performance-acceptance.md references the real PSI result and supersedes PERF-01; the readiness report distinguishes category scores from the pass-ratio; REQUIREMENTS.md PSI rows reflect genuine status.</human-check>
  </verify>
  <acceptance_criteria>
    - docs/launch/performance-acceptance.md supersedes the PERF-01 lab-failure acceptance and references the real Phase 20 PSI result (dated/attributable)
    - docs/launch/final-production-readiness-report.md performance status reflects the genuine PSI outcome and distinguishes category scores from the readiness pass-ratio
    - .planning/REQUIREMENTS.md PSI-01..PSI-15 rows are updated to genuine status (Complete where evidence supports; ceiling/owner-gated noted otherwise) — none falsely passed
  </acceptance_criteria>
  <done>PERF-01 is superseded by the real result, the readiness report is honest about category scores vs pass-ratio, and the PSI requirements reflect genuine status. (PSI-15)</done>
</task>

<task type="auto">
  <name>Task 3: Prune/downscale oversized unused /public PNGs (reference-checked)</name>
  <read_first>
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 2 "Oversized source PNGs"; Impact x Effort row 25; Risks "Verify references before deleting")
    - public/ (organic-herbs-and-spices.png ~9.9MB; 33 PNGs >1MB, ~99MB total)
  </read_first>
  <action>Identify oversized PNGs in /public (>1MB) and, for each, search the codebase for references (src/, public/, .graphql, content.ts files) BEFORE any change. For genuinely unused files, remove them. For referenced-but-oversized files that are re-optimized through /_next/image and sit below the fold (off the LCP path), downscale the source to a sane max dimension/weight while keeping the same path so references stay valid. Do NOT touch any image on the LCP path or referenced by the hero/first-card preload. This is repo-hygiene / latent-regression-risk reduction, not an LCP fix (these are below-fold + re-optimized). Record the before/after total /public image weight in the SUMMARY.</action>
  <verify>
    <automated>node -e "const{execSync}=require('child_process');const out=execSync('git status --porcelain public',{encoding:'utf8'});console.log(out||'no /public changes');" </automated>
    <human-check>Each removed/downscaled PNG was confirmed unused or below-fold-and-re-optimized; no LCP-path image was touched; the build still succeeds.</human-check>
  </verify>
  <acceptance_criteria>
    - Every removed PNG was verified to have zero code references before deletion
    - Every downscaled-but-kept PNG retains its original path so existing references stay valid
    - No image on the LCP path (hero, first collection/PDP card preload) was modified
    - The production build still succeeds after the cleanup
  </acceptance_criteria>
  <done>Oversized unused /public PNGs are pruned/downscaled safely, reducing repo bloat without touching any LCP-path image. (PSI-15)</done>
</task>

</tasks>

<verification>
- docs/launch/psi-evidence.md has the final per-route/category/device matrix, the impact x effort roadmap, and explicit honest waivers.
- docs/launch/performance-acceptance.md supersedes PERF-01 with the real result; final-production-readiness-report.md is honest about category scores vs pass-ratio.
- .planning/REQUIREMENTS.md PSI-01..PSI-15 reflect genuine status.
- /public cleanup touched no LCP-path image and the build still succeeds.
- FINAL REGRESSION SWEEP (D-09): `node scripts/seo/probe-launch-seo.mjs`, `node scripts/seo/probe-crawlable-html.mjs`, `pnpm exec playwright test tests/e2e/h1-correctness.spec.ts`, `pnpm test:performance`, and `node --test scripts/component-contracts/performance-fonts.test.mjs` all pass.
</verification>

<success_criteria>
- A prioritized (impact x effort) roadmap and a final evidence pack document achieved scores per route/category/device, explicitly state where a perfect 100 is and is not realistically achievable plus the best alternative, and supersede/update the PERF-01 acceptance with the real result — no fabricated pass.
</success_criteria>

<output>
Create `.planning/phases/20-pagespeed-100-perfection/20-05-SUMMARY.md` when done. Record the final headline scores per category/device, the documented ceilings, the PERF-01 supersession, and the /public weight before/after.
</output>
