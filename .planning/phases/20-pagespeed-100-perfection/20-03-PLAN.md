---
phase: 20-pagespeed-100-perfection
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
autonomous: true
requirements: [PSI-05, PSI-06, PSI-07, PSI-11]
files_modified:
  - instrumentation-client.ts
  - scripts/performance/probe-lighthouse.mjs
  - scripts/performance/probe-psi.mjs
  - scripts/launch/probe-readiness.mjs
  - src/app/(storefront)/products/[handle]/page.tsx
  - next.config.ts
  - docs/launch/js-budget-evidence.md
  - docs/launch/third-party-ceiling.md
  - package.json

must_haves:
  truths:
    - "When NEXT_PUBLIC_SENTRY_DSN is unset, the ~138KB client Sentry SDK is not shipped in every route's first-load JS while onRouterTransitionStart is still exported"
    - "A per-route JS-byte budget assertion exists so a Sentry/bundle regression is caught automatically"
    - "The harness can measure a 'tags-live' worst case (analytics + Searchanise enabled, consent granted) so the production-tags ceiling is quantified, not hidden"
    - "The inert PDP Shopify inline analytics scripts are consent-gated rather than firing unconditionally"
  artifacts:
    - path: "instrumentation-client.ts"
      provides: "DSN-gated / lazy client Sentry init that still exports onRouterTransitionStart"
      contains: "onRouterTransitionStart"
    - path: "docs/launch/third-party-ceiling.md"
      provides: "Documented cold/default-deny vs tags-live performance ceiling with the best-achievable alternative"
    - path: "docs/launch/js-budget-evidence.md"
      provides: "next experimental-analyze before/after Sentry + per-route JS-byte budget table"
  key_links:
    - from: "instrumentation-client.ts"
      to: "the route bundle first-load chunks"
      via: "DSN-gated import so Sentry is not unconditionally bundled"
      pattern: "NEXT_PUBLIC_SENTRY_DSN"
    - from: "scripts/performance/probe-lighthouse.mjs tags-live mode"
      to: "GA4/Searchanise + seeded consent"
      via: "env toggle measuring the consented worst case"
      pattern: "tags-live|tagsLive|ANALYTICS_MODE"
---

<objective>
Reduce always-on JavaScript weight and quantify the third-party ceiling. Gate/lazy-init/no-op the client Sentry SDK (the single largest always-on JS payload, ~138KB gz on all 46 routes) behind a DSN check; baseline before/after with next experimental-analyze and add a per-route JS-byte budget assertion; add a "tags-live" harness mode and measure the consented worst case; consent-gate the inert PDP Shopify inline scripts; evaluate experimental.inlineCss via A/B PSI; and close the font Link-header harness blind spot.

Purpose: Sentry is the one third-party-adjacent JS a cold PSI run executes and is ~52% of every route's client JS, bundled unconditionally because instrumentation-client.ts top-level `import * as Sentry` runs regardless of DSN. The tags-live Performance/Best-Practices ceiling is currently UNMEASURED — all prior evidence ran analytics fake + Searchanise disabled. Per D-05 the production-tags ceiling must be measured and documented honestly, not gamed.

Output: DSN-gated client Sentry; JS-byte budget assertion + experimental-analyze evidence; tags-live harness mode + third-party-ceiling.md; consent-gated PDP inline scripts; inlineCss A/B note; font Link-header warmup/assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pagespeed-100-perfection/20-CONTEXT.md
@.planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md
@.planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md
@instrumentation-client.ts
@next.config.ts
@scripts/performance/probe-lighthouse.mjs
@src/components/product/searchanise-recommendations/searchanise-script-loader.tsx
@CLAUDE.md
@AGENTS.md
@docs/conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate/no-op the client Sentry SDK behind a DSN check; preserve onRouterTransitionStart</name>
  <read_first>
    - instrumentation-client.ts (top-level `import * as Sentry` line 1 runs regardless of DSN; onRouterTransitionStart export line 16 — MUST be preserved)
    - src/instrumentation.ts (already uses the runtime-gated `await import` pattern — mirror this lower-risk approach)
    - next.config.ts (withSentryConfig wrapper lines 55-75; the `webpack` opt at 72-74 is a Sentry build option, NOT a Next webpack config — do NOT open a build-risk workstream)
    - node_modules/next/dist/docs/01-app/... (read current instrumentation-client / Sentry integration docs before changing — this fork differs from training data)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 4; Impact x Effort row 8; Risks "Sentry is the one tag a cold run executes"; Open Question 6 — confirm DSN-on-preview from 20-01-SUMMARY)
    - .planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md (NEXT_PUBLIC_SENTRY_DSN-on-preview status)
  </read_first>
  <action>Rewrite instrumentation-client.ts so the heavy `@sentry/nextjs` client SDK is NOT imported at module top level when NEXT_PUBLIC_SENTRY_DSN is unset. Replace the unconditional `import * as Sentry` with a DSN-gated dynamic import (mirroring the runtime-gated `await import` pattern already used in src/instrumentation.ts) so the SDK only enters the client bundle/executes when a DSN is present. CRITICAL: still export onRouterTransitionStart — when no DSN is set it must be a safe no-op function (the Next router-transition hook requires the export to exist); when a DSN is set it must delegate to Sentry.captureRouterTransitionStart. Keep sendDefaultPii:false, tracesSampleRate:0, enableLogs:false for the DSN-set path. Do NOT touch next.config.ts withSentryConfig (that controls build-time release/sourcemaps and is correct). After the change, run a production build and confirm via .next/diagnostics/route-bundle-stats.json that the Sentry client chunk is no longer in firstLoadChunkPaths for routes when DSN is unset.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('instrumentation-client.ts','utf8');if(!/onRouterTransitionStart/.test(s)){process.exit(1)};if(!/NEXT_PUBLIC_SENTRY_DSN/.test(s)){process.exit(1)};if(/^import \* as Sentry/m.test(s)){console.error('top-level unconditional Sentry import remains');process.exit(1)}"</automated>
    <human-check>A no-DSN production build's route-bundle-stats.json no longer lists the ~138KB Sentry chunk in firstLoad; onRouterTransitionStart still functions.</human-check>
  </verify>
  <acceptance_criteria>
    - instrumentation-client.ts has no unconditional top-level `import * as Sentry`; the SDK is imported only behind a NEXT_PUBLIC_SENTRY_DSN check
    - instrumentation-client.ts still exports onRouterTransitionStart (no-op when DSN unset; delegates to Sentry.captureRouterTransitionStart when set)
    - A no-DSN production build does not include the Sentry client chunk in route first-load JS (verified against route-bundle-stats.json)
    - next.config.ts withSentryConfig wrapper is unchanged
  </acceptance_criteria>
  <done>The largest always-on client JS payload is removed when no DSN is configured, lowering TBT/INP and unused JS, with the router-transition hook preserved. (PSI-06)</done>
</task>

<task type="auto">
  <name>Task 2: JS-byte budget assertion + experimental-analyze evidence; evaluate experimental.inlineCss; font Link-header warmup</name>
  <read_first>
    - scripts/launch/probe-readiness.mjs (existing readiness check structure — add a per-route JS-byte budget assertion here)
    - scripts/performance/probe-lighthouse.mjs (warmup/asset discovery ~L1053-1142 parses only in-HTML preload tags — fonts ship as Link response headers, a blind spot to close)
    - .next/diagnostics/route-bundle-stats.json (firstLoad chunk sizes per route — the budget source of truth)
    - node_modules/next/dist/docs/01-app/02-guides/package-bundling.md (next experimental-analyze, Turbopack-only; @next/bundle-analyzer does NOT apply)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/inlineCss.md (production-only, global, re-downloads CSS per HTML response — A/B, do not adopt blind)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 4; Impact x Effort rows 17, 18, 22, 24; Cross-cutting "experimental.inlineCss")
  </read_first>
  <action>Add a per-route JS-byte budget assertion to scripts/launch/probe-readiness.mjs that reads .next/diagnostics/route-bundle-stats.json firstLoad sizes and fails if any route exceeds a budget set just above the post-Sentry-trim baseline (so the Sentry win cannot silently regress). Run `next experimental-analyze` before and after the Task-1 Sentry trim and capture the firstLoad deltas in docs/launch/js-budget-evidence.md (note this is Turbopack-only; do not introduce @next/bundle-analyzer). Add a Link-header font warmup/assertion to scripts/performance/probe-lighthouse.mjs: parse the `Link: rel=preload; as="font"` response header (curl -D / fetch headers) and warm those font URLs + assert the expected font preloads are present (closes the blind spot where header-only font preloads are never warmed and a dropped preload goes undetected). Evaluate experimental.inlineCss via an A/B PSI run (inlineCss on vs off) and record the FCP/LCP/returning-visitor TTFB tradeoff in js-budget-evidence.md; only add the flag to next.config.ts if the A/B shows a net win (it re-downloads CSS per HTML response, hurting returning visitors). Do NOT enable productionBrowserSourceMaps (weight 0; leaks source).</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/launch/probe-readiness.mjs','utf8');if(!/firstLoad|route-bundle-stats|js.?budget/i.test(s)){process.exit(1)}" && node -e "const s=require('fs').readFileSync('scripts/performance/probe-lighthouse.mjs','utf8');if(!/Link|as=\"font\"|as=font/i.test(s)){process.exit(1)}"</automated>
    <human-check>docs/launch/js-budget-evidence.md shows before/after firstLoad deltas and the inlineCss A/B result with a clear adopt/skip recommendation.</human-check>
  </verify>
  <acceptance_criteria>
    - scripts/launch/probe-readiness.mjs contains a per-route JS-byte budget assertion sourced from route-bundle-stats.json firstLoad
    - docs/launch/js-budget-evidence.md records next experimental-analyze before/after Sentry firstLoad deltas
    - scripts/performance/probe-lighthouse.mjs parses the font Link response header and asserts/warms the expected font preloads
    - experimental.inlineCss A/B result is recorded in js-budget-evidence.md; the flag is added to next.config.ts only if the A/B shows a net win
    - productionBrowserSourceMaps is NOT enabled
  </acceptance_criteria>
  <done>JS weight is bounded by an automated budget, the Sentry trim is quantified, the font preload blind spot is closed, and inlineCss is evaluated on real PSI evidence. (PSI-05, PSI-06, PSI-07)</done>
</task>

<task type="auto">
  <name>Task 3: Consent-gate inert PDP inline scripts + measure & document the tags-live ceiling</name>
  <read_first>
    - src/app/(storefront)/products/[handle]/page.tsx (Shopify analytics inline Script blocks lines 251-271 — fire unconditionally, gated only by product id; set window.ShopifyAnalytics.meta / __st only, no tracker JS loads)
    - src/components/product/searchanise-recommendations/searchanise-script-loader.tsx (lazyOnload init.js from searchserverapi.com — the only client third party; IntersectionObserver-gated)
    - src/lib/consent/adapter.ts (DEFAULT_CONSENT analytics:false — cold runs load zero analytics)
    - scripts/performance/probe-lighthouse.mjs (env fake/disabled lines 167-169 — add a tags-live mode that flips these + seeds consent)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 5; Impact x Effort rows 14, 20, 21; Risks "Preconnecting consent-gated hosts"; Open Questions 5, 7; Achievability table BP "tags-live" row)
    - .planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md (which production tags ship at launch, per owner confirmation)
  </read_first>
  <action>Consent-gate the inert PDP Shopify inline scripts in src/app/(storefront)/products/[handle]/page.tsx so they only render when analytics consent is granted (compliance hygiene; ~zero perf value but removes an always-firing script) — verify checkout does not read __st pre-consent before gating. Add a "tags-live" mode to scripts/performance/probe-lighthouse.mjs (and a corresponding scenario in probe-psi.mjs) that builds/runs with a throwaway GA4 id + NEXT_PUBLIC_SEARCHANISE_ENABLED=true and seeds teavision_consent=granted, so the consented worst case is measurable. Run both the cold/default-deny scenario and the tags-live scenario and author docs/launch/third-party-ceiling.md recording, side by side, the cold vs tags-live Performance and Best-Practices deltas, an itemized list of vendor third-party cookies / console errors / deprecated APIs surfaced when tags load, and — per D-04/D-05 — an honest statement of the tags-live ceiling plus the best-achievable alternative (e.g. consent-default-deny + lazyOnload as primary; document that a literal Performance/BP 100 with all production tags live may be unattainable). Note the caveat: a headless PSI crawl may not fire consent-gated tags, so the tags-live PSI number can understate real cost — CrUX is the only true post-launch signal. Keep consent-default-deny + lazyOnload as the primary strategy; do not preconnect consent-gated hosts.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/app/(storefront)/products/[handle]/page.tsx','utf8');if(!/consent/i.test(s)){console.error('PDP inline scripts not consent-gated');process.exit(1)}" && node -e "const s=require('fs').readFileSync('scripts/performance/probe-lighthouse.mjs','utf8');if(!/tags-live|tagsLive/i.test(s)){process.exit(1)}"</automated>
    <human-check>docs/launch/third-party-ceiling.md shows cold vs tags-live deltas, an itemized vendor cookie/console/deprecation list, and an honest best-alternative ceiling statement.</human-check>
  </verify>
  <acceptance_criteria>
    - src/app/(storefront)/products/[handle]/page.tsx renders the Shopify analytics inline scripts only behind an analytics-consent gate
    - scripts/performance/probe-lighthouse.mjs has a tags-live mode (GA4 + Searchanise enabled + seeded consent) distinct from the cold/default-deny default
    - docs/launch/third-party-ceiling.md records cold vs tags-live Performance + Best-Practices deltas and itemizes vendor third-party cookies / console errors / deprecated APIs
    - docs/launch/third-party-ceiling.md states the honest tags-live ceiling and the best-achievable alternative (D-04/D-05)
  </acceptance_criteria>
  <done>Third-party scripts stay off the critical path, the inert PDP scripts are consent-gated, and the production-tags-live ceiling is measured and documented honestly. (PSI-11)</done>
</task>

</tasks>

<verification>
- No-DSN production build excludes the Sentry client chunk from route first-load JS; onRouterTransitionStart still works.
- `node scripts/launch/probe-readiness.mjs` enforces the per-route JS-byte budget.
- docs/launch/third-party-ceiling.md and docs/launch/js-budget-evidence.md exist with the required deltas.
- REGRESSION GUARDS (D-09): after the Sentry gating, PDP consent-gating, and any inlineCss change, re-run `node scripts/seo/probe-launch-seo.mjs`, `node scripts/seo/probe-crawlable-html.mjs`, and `pnpm exec playwright test tests/e2e/h1-correctness.spec.ts` — the static shell must still contain crawlable H1/content and PDP JSON-LD must stay server-rendered.
- `pnpm test:performance` re-run confirms TBT stays minimal after the Sentry trim.
</verification>

<success_criteria>
- Unused/duplicate JS is minimized (Sentry DSN-gated), bounded by an automated per-route budget, with no render-blocking scripts in the critical path.
- Render-blocking/unused CSS is evaluated via inlineCss A/B on real PSI evidence.
- Third-party scripts are consent-gated/lazy/deferred off the critical path and the tags-live ceiling is measured and documented with the best alternative.
</success_criteria>

<output>
Create `.planning/phases/20-pagespeed-100-perfection/20-03-SUMMARY.md` when done. Record the Sentry firstLoad delta, the JS budget value chosen, the cold-vs-tags-live ceiling numbers, and confirm the Phase 18/19 regression guards stayed green.
</output>
