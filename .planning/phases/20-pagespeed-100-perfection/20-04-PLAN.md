---
phase: 20-pagespeed-100-perfection
plan: 04
type: execute
wave: 4
depends_on: ["20-01", "20-02", "20-03"]
autonomous: false
requirements: [PSI-04, PSI-12, PSI-13, PSI-14]
files_modified:
  - src/app/globals.css
  - src/components/layout/footer/link-list/link-list.tsx
  - src/components/layout/footer/quality-column/quality-column.tsx
  - src/components/layout/footer/newsletter-column/newsletter-column.tsx
  - src/components/search/search-results-view/search-results-view.tsx
  - src/components/homepage/hero/hero.tsx
  - src/app/(storefront)/account/login
  - scripts/performance/check-contrast.mjs
  - docs/launch/accessibility-100-evidence.md
  - docs/launch/seo-100-evidence.md
  - docs/launch/best-practices-ceiling.md

must_haves:
  truths:
    - "axe reports zero scored color-contrast failures on /, /cart, /search, /account, /pages/privacy-policy after the OKLCH token nudges"
    - "axe reports zero heading-order failures on /account and /search"
    - "/account (login bridge) CLS is <= 0.1 on real PSI / reconciled lab (was 0.128)"
    - "Lighthouse Accessibility scores 100 on the representative routes"
    - "Lighthouse SEO scores 100 on a temporary indexable build, with the noindexed preview's ~69 documented as intentional"
    - "Lighthouse Best Practices scores 100 tags-off on the real HTTPS preview, with the tags-live ceiling documented"
  artifacts:
    - path: "src/app/globals.css"
      provides: "darkened ink-faint + gold-deep OKLCH tokens that clear 4.5:1 at small sizes"
      contains: "--color-ink-faint"
    - path: "scripts/performance/check-contrast.mjs"
      provides: "OKLCH->WCAG contrast verifier confirming ink-faint/gold-deep clear 4.5:1 at 11px on paper/paper-2/card/gold-tint before commit"
    - path: "docs/launch/accessibility-100-evidence.md"
      provides: "Per-route zero-scored-failure a11y evidence"
    - path: "docs/launch/seo-100-evidence.md"
      provides: "SEO 100 on indexable build + documented preview ~69"
  key_links:
    - from: "src/app/globals.css --color-ink-faint / --color-gold-deep"
      to: "every text-ink-faint / text-gold-deep usage"
      via: "darkened OKLCH lightness clearing WCAG AA"
      pattern: "--color-ink-faint|--color-gold-deep"
    - from: "footer h3 columns"
      to: "the page heading outline"
      via: "h3 -> h2 to remove the h1->h3 skip"
      pattern: "<h2"
---

<objective>
Take Accessibility, Best Practices, and SEO to 100 on the representative routes and fix the /account CLS-0.128 — all without regressing the warm/botanical design system or Phase 18/19 SEO. Nudge the two failing OKLCH tokens, restrict text-gold to dark backgrounds, add an opaque hero scrim (owner-approved preview first), fix heading order via footer h3->h2 + an sr-only h2 on /search, fix the /account login-bridge CLS, score SEO on a temporary indexable build, and measure Best Practices tags-off on the real HTTPS preview.

Purpose: a11y is binary/deterministic (not lab noise) — every route fails exactly one audit (color-contrast) and two routes additionally fail heading-order, so 100 is genuinely achievable. SEO is physically capped at ~69 on the noindexed preview by design (is-crawlable is ~31% of the category), so the only honest path to SEO 100 is a throwaway indexable build. Best Practices 100 is achievable tags-off; the tags-live ceiling (from Wave 3) is documented, not gamed.

Output: darkened ink-faint + gold-deep tokens (verified by an OKLCH->WCAG script); restricted text-gold; owner-approved hero scrim; footer heading-order fix + sr-only h2; /account CLS fix; SEO-100 on indexable build + documented preview ~69; Best-Practices tags-off 100 + documented ceiling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pagespeed-100-perfection/20-CONTEXT.md
@.planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md
@.planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md
@src/app/globals.css
@src/components/layout/footer/link-list/link-list.tsx
@src/components/layout/footer/quality-column/quality-column.tsx
@src/components/layout/footer/newsletter-column/newsletter-column.tsx
@CLAUDE.md
@AGENTS.md
@docs/conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Darken ink-faint + gold-deep OKLCH tokens (WCAG-verified) and restrict text-gold to dark backgrounds</name>
  <read_first>
    - src/app/globals.css (--color-ink-faint line 21 = oklch(56% ...); --color-gold line 37 = oklch(74% ...); --color-gold-deep line 38 = oklch(60% ...); --color-gold-tint line 39; the @theme token block)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 7 with raw axe nodes; Impact x Effort rows 6, 10; "Validation Architecture" -> Accessibility — verify OKLCH->WCAG BEFORE committing token values; Risks "Lighthouse a11y is binary per audit")
    - .planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md (the enumerated failing color-contrast nodes from the extended summarizeLhr)
  </read_first>
  <action>First create scripts/performance/check-contrast.mjs: an OKLCH->sRGB->WCAG-contrast verifier that, given a foreground token and the background tokens it sits on (paper, paper-2, card, gold-tint), computes the contrast ratio and asserts >= 4.5:1 at small text (11px). Use it to choose new lightness values BEFORE editing globals.css. Then in src/app/globals.css darken --color-ink-faint from L56% to ~L50% and --color-gold-deep from L60% to ~L52% (keep hue + chroma unchanged so the warm/botanical palette holds — D-08/D-14). Do NOT change --color-ink, --color-ink-soft, or --color-gold-tint. Audit all text-gold usages (the gold token ~2.14:1 on light fails): where text-gold sits on a light background (e.g. gold eyebrows on paper), switch those usages to text-gold-deep; keep text-gold only on dark/inverse surfaces where it already clears AA. Use cn() / token classes only — never raw hex. Re-run the contrast script and an accessibility-only Lighthouse pass to confirm zero scored color-contrast failures on /, /cart, /search, /account, /pages/privacy-policy (binary — every node must clear).</action>
  <verify>
    <automated>node scripts/performance/check-contrast.mjs && node -e "const s=require('fs').readFileSync('src/app/globals.css','utf8');const m=s.match(/--color-ink-faint:\s*oklch\(([0-9.]+)%/);if(!m||parseFloat(m[1])>52){console.error('ink-faint not darkened enough');process.exit(1)}"</automated>
    <human-check>An accessibility-only Lighthouse/axe run reports zero scored color-contrast failures on all representative routes.</human-check>
  </verify>
  <acceptance_criteria>
    - scripts/performance/check-contrast.mjs exists and asserts ink-faint + gold-deep clear 4.5:1 at 11px on paper/paper-2/card/gold-tint
    - src/app/globals.css --color-ink-faint lightness is ~50% (down from 56%) and --color-gold-deep ~52% (down from 60%), hue/chroma unchanged
    - text-gold on light backgrounds is replaced with text-gold-deep; text-gold remains only on dark surfaces
    - axe reports zero scored color-contrast failures on /, /cart, /search, /account, /pages/privacy-policy
  </acceptance_criteria>
  <done>The two failing contrast tokens clear WCAG AA at small sizes and text-gold is restricted to dark backgrounds, with the warm palette preserved. (PSI-12)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Add opaque hero scrim / denser trust-strip band (owner visual approval)</name>
  <read_first>
    - src/components/homepage/hero/hero.tsx (hero over-image title text + bg-ink/40 trust strip — axe samples the image background #9da09a, so no text-color change alone clears it; needs an opaque scrim)
    - src/app/globals.css (existing hero-scrim utility if present; prefers-reduced-motion block ~L304-313)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 7 "over-image hero text"; Impact x Effort row 9; Risks "Over-image text cannot be fixed by text color"; Open Question 10 — owner accepts the denser scrim visually?)
    - MEMORY: preview designs before implementing
  </read_first>
  <what-built>An opaque/denser scrim behind the hero over-image title text (densify the existing `hero-scrim` utility in globals.css) and a denser bg-ink band behind the trust strip (raise `bg-ink/40` to `bg-ink/60`+ in hero.tsx) so axe's image-sampled contrast clears AA. CSS/token classes only — do NOT alter the hero Image's fetchPriority="high" (set in 20-02), preload, fill, sizes, or blur (no LCP regression). Build a quick before/after preview of the hero with the denser scrim for owner review.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open the homepage; compare the hero title legibility and trust strip with the prior lighter overlay.
    2. Run an accessibility-only Lighthouse/axe pass on / and confirm the over-image color-contrast node now clears.
    3. Confirm the warm/botanical look is preserved and the hero still feels balanced (per D-08; per project memory the owner reviews UI surfaces before sign-off).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the scrim density, or describe the desired adjustment.</resume-signal>
  <acceptance_criteria>
    - src/components/homepage/hero/hero.tsx (and/or the `hero-scrim` utility in globals.css) applies a denser/opaque scrim behind the over-image title and raises the trust-strip band from `bg-ink/40` to `bg-ink/60`+ (CSS/token classes only)
    - The hero Image keeps `fetchPriority="high"` (set in 20-02) and its preload/fill/sizes/blur posture; the deprecated `priority` prop is still not used (D-10 refined / D-15)
    - An accessibility-only axe/Lighthouse pass on / shows the over-image color-contrast node cleared
    - The owner has approved the scrim density (warm/botanical look preserved per D-08)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/homepage/hero/hero.tsx','utf8');if(/bg-ink\/40\b/.test(s)){console.error('trust strip still bg-ink/40 - not densified');process.exit(1)};if(!/bg-ink\/(5[0-9]|[6-9][0-9]|100)\b/.test(s)){console.error('no denser bg-ink band (>=50% opacity) applied');process.exit(1)};if(/\bpriority\b/.test(s)){console.error('deprecated priority prop present');process.exit(1)};if(!/fetchPriority/.test(s)){console.error('hero fetchPriority=high from 20-02 was regressed');process.exit(1)};if(/loading=\"eager\"/.test(s)){console.error('eager loading regressed');process.exit(1)}"</automated>
    <human-check>Owner approves the hero scrim density and axe shows the over-image contrast node cleared.</human-check>
  </verify>
  <done>The over-image hero text clears contrast via an owner-approved opaque scrim, with the LCP preload posture and warm palette intact. (PSI-12)</done>
</task>

<task type="auto">
  <name>Task 3: Fix heading-order (footer h3->h2 + sr-only h2 on /search) and the /account login-bridge CLS</name>
  <read_first>
    - src/components/layout/footer/link-list/link-list.tsx (h3 line 7), quality-column/quality-column.tsx (h3 line 16), newsletter-column/newsletter-column.tsx (h3 line 18) — the global footer h1->h3 skip
    - src/components/search/search-results-view/search-results-view.tsx (the real /search heading owner — it composes the search hero h1 and the product-card h3 grid via product-results.tsx; the h1->h3 skip occurs here, NOT in the thin src/app/(storefront)/search/_components/results.tsx wrapper). Add the sr-only h2 "Search results" here, immediately before the product-results grid.
    - src/app/(storefront)/account/login (the login bridge — the named CLS-0.128 shifting element from 20-01-SUMMARY)
    - tests/e2e/h1-correctness.spec.ts (single-visible-H1 invariant — must stay green; fixes use sr-only h2 only, NO second visible h1)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 7 heading-order; Impact x Effort row 7; "Validation Architecture" -> Regression guards; Risks "Footer h3 rides every page")
    - .planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md (the named /account CLS element + verdict)
  </read_first>
  <action>Fix heading order centrally: change the footer column headings from h3 to h2 in link-list.tsx, quality-column.tsx, and newsletter-column.tsx (keep the exact existing class strings — only the tag changes), removing the h1->h3 skip that fails /account and /search. On /search add an sr-only h2 "Search results" in src/components/search/search-results-view/search-results-view.tsx, immediately before the product-results grid (product-results.tsx renders the product-card h3 titles), so the h1->h3 skip is bridged WITHOUT adding a second visible h1 (D-09 / Phase 19 single-visible-H1 invariant). Do NOT add it to the thin src/app/(storefront)/search/_components/results.tsx wrapper — the heading outline lives in search-results-view.tsx. Fix the /account login-bridge CLS-0.128 by reserving stable geometry for the element identified in 20-01-SUMMARY (e.g. reserve min-height / fixed dimensions for the shifting login-bridge block so it does not reflow after paint) — use token classes / cn(), warm palette, no raw hex. Re-run the accessibility-only pass to confirm zero heading-order failures on /account and /search, and re-measure /account CLS on real PSI / reconciled lab to confirm <= 0.1. Then run the Phase 18/19 regression guards.</action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/h1-correctness.spec.ts && node -e "for(const f of ['link-list/link-list.tsx','quality-column/quality-column.tsx','newsletter-column/newsletter-column.tsx']){const s=require('fs').readFileSync('src/components/layout/footer/'+f,'utf8');if(/<h3/.test(s)){console.error(f+' still has h3');process.exit(1)}}"</automated>
    <human-check>axe shows zero heading-order failures on /account and /search; /account CLS <= 0.1 on PSI/reconciled lab; no second visible h1 introduced.</human-check>
  </verify>
  <acceptance_criteria>
    - Footer link-list, quality-column, and newsletter-column render h2 (not h3) with their existing class strings unchanged
    - search-results-view.tsx renders an sr-only h2 "Search results" before the product-results h3 grid; no second visible h1 anywhere
    - The /account login-bridge shifting element reserves stable geometry; /account CLS is <= 0.1 on PSI/reconciled lab
    - axe reports zero heading-order failures on /account and /search
    - tests/e2e/h1-correctness.spec.ts passes (single visible H1 invariant intact)
  </acceptance_criteria>
  <done>Heading order is correct site-wide via sr-only h2 (no second visible h1) and the /account CLS drops to good. (PSI-04, PSI-12)</done>
</task>

<task type="auto">
  <name>Task 4: Score SEO 100 on a temporary indexable build; document preview ~69; measure Best Practices tags-off 100 + ceiling</name>
  <read_first>
    - src/lib/seo/noindex.ts, src/app/robots.ts, src/lib/env/server.ts (DISABLE_INDEXING ~L27-29 — flip to false ONLY for a throwaway local build, never the public preview)
    - scripts/performance/probe-psi.mjs (the score-of-record probe from 20-01)
    - docs/launch/third-party-ceiling.md (the Wave-3 tags-live ceiling — Best Practices tags-on numbers feed best-practices-ceiling.md)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Achievability table SEO + BP rows; Per-Dimension Finding 8; Impact x Effort rows 19, 20; Risks "SEO 100 impossible on noindexed preview", "Enforcing CSP yields no BP gain")
  </read_first>
  <action>Score SEO on a temporary indexable build: run a short-lived build with DISABLE_INDEXING=false (locally or a throwaway indexable target that is NEVER linked or submitted to Search Console — owner-gated boundary), capture the SEO category = 100 via PSI/Lighthouse, then tear it down. Author docs/launch/seo-100-evidence.md recording the indexable-build SEO 100 AND explicitly documenting that the public preview's ~69 SEO is INTENTIONAL (is-crawlable is ~31% of the SEO category, by design so noindex fails it — verified math 9/13.043 = 69.0%); the public preview stays noindexed for launch safety. Confirm titles/descriptions/canonical/robots/structured-data are unchanged from Phase 16/18/19 (do not regress them). For Best Practices, run the real HTTPS preview tags-OFF and confirm BP = 100 (HTTPS/HSTS representative on the real edge, not loopback); author docs/launch/best-practices-ceiling.md recording the tags-off 100 and the tags-live ceiling from third-party-ceiling.md (itemized vendor cookies/console/deprecations). Do NOT enforce CSP for a score (csp-xss weight 0) and do NOT enable public source maps (valid-source-maps weight 0).</action>
  <verify>
    <automated>node -e "const fs=require('fs');for(const f of ['docs/launch/seo-100-evidence.md','docs/launch/best-practices-ceiling.md']){if(!fs.existsSync(f)){console.error('missing '+f);process.exit(1)}}" && node -e "const s=require('fs').readFileSync('docs/launch/seo-100-evidence.md','utf8');if(!/69/.test(s)||!/indexable/i.test(s)){process.exit(1)}"</automated>
    <human-check>seo-100-evidence.md shows SEO 100 on the indexable build and documents the intentional ~69 on the noindexed preview; best-practices-ceiling.md shows tags-off 100 + the documented tags-live ceiling.</human-check>
  </verify>
  <acceptance_criteria>
    - docs/launch/seo-100-evidence.md records SEO = 100 on a temporary indexable build (DISABLE_INDEXING=false) and documents the intentional ~69 on the noindexed preview, never submitting the indexable build to Search Console
    - Phase 16/18/19 SEO signals (titles/descriptions/canonical/robots/structured-data) are confirmed unregressed
    - docs/launch/best-practices-ceiling.md records Best Practices = 100 tags-off on the real HTTPS preview and the documented tags-live ceiling
    - CSP is not enforced for a score and productionBrowserSourceMaps is not enabled
  </acceptance_criteria>
  <done>SEO scores 100 on an indexable build with the noindexed preview's ~69 honestly documented, and Best Practices scores 100 tags-off with the tags-live ceiling documented. (PSI-13, PSI-14)</done>
</task>

</tasks>

<verification>
- axe/Lighthouse accessibility-only run reports zero scored color-contrast and zero heading-order failures on all representative routes; Accessibility = 100.
- /account CLS <= 0.1 on real PSI / reconciled lab.
- SEO = 100 on the temporary indexable build; preview ~69 documented as intentional.
- Best Practices = 100 tags-off on the real HTTPS preview; tags-live ceiling documented.
- REGRESSION GUARDS (D-09): `node scripts/seo/probe-launch-seo.mjs`, `node scripts/seo/probe-crawlable-html.mjs`, and `pnpm exec playwright test tests/e2e/h1-correctness.spec.ts` all pass — heading-order fix uses footer h3->h2 + sr-only h2 only, NO second visible h1; cacheComponents stays enabled.
</verification>

<success_criteria>
- Accessibility, Best Practices, and SEO each score 100 on the representative routes (Accessibility gap 95-97 -> 100) without regressing the warm/botanical design system or Phase 16/18/19 SEO.
- /account CLS is <= 0.1.
- Where SEO 100 cannot exist on the noindexed preview, the limitation is honestly documented with the indexable-build alternative.
</success_criteria>

<output>
Create `.planning/phases/20-pagespeed-100-perfection/20-04-SUMMARY.md` when done. Record the final per-route a11y/BP/SEO scores, the chosen token lightness values, the /account CLS result, and confirm the Phase 18/19 regression guards stayed green.
</output>
