---
phase: 20-pagespeed-100-perfection
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
autonomous: true
requirements: [PSI-03, PSI-08, PSI-09, PSI-10]
files_modified:
  - next.config.ts
  - src/app/(storefront)/layout.tsx
  - src/components/homepage/content.ts
  - src/components/homepage/hero/hero.tsx
  - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
  - src/components/product/product-gallery/product-gallery.tsx
  - src/app/layout.tsx
  - scripts/component-contracts/performance-fonts.test.mjs

must_haves:
  truths:
    - "The Next image optimizer emits AVIF (then WebP) so the 24KB hero AVIF is no longer transcoded to a larger WebP for AVIF-capable browsers"
    - "The storefront head contains a preconnect to cdn.shopify.com (the image-LCP origin) and dns-prefetch for consent-gated third-party hosts"
    - "The homepage hero declares its true intrinsic dimensions (1440x650) and renders an LQIP blur placeholder"
    - "Collection pages preload exactly one above-the-fold card image, not three"
    - "The unused Caveat font is removed from the bundle and its contract test"
    - "Warm real PSI (or reconciled lab) LCP on image routes improves or holds vs the Wave-1 baseline with CLS staying at 0"
  artifacts:
    - path: "next.config.ts"
      provides: "images.formats AVIF+WebP and a raised minimumCacheTTL"
      contains: "image/avif"
    - path: "src/app/(storefront)/layout.tsx"
      provides: "resource hints (preconnect cdn.shopify.com, dns-prefetch consent hosts) in the server-rendered head"
  key_links:
    - from: "next.config.ts images.formats"
      to: "the Next image optimizer output negotiation"
      via: "['image/avif','image/webp']"
      pattern: "image/avif.*image/webp"
    - from: "src/app/(storefront)/layout.tsx head"
      to: "cdn.shopify.com"
      via: "<link rel=preconnect>"
      pattern: "rel=\"preconnect\""
    - from: "src/app/(storefront)/collections/[handle]/_components/product-list.tsx"
      to: "ProductCard preload prop"
      via: "priority for index 0 only"
      pattern: "index < 1|index === 0"
---

<objective>
Apply the verified, low-risk, high-leverage image / font / Core Web Vitals config wins identified in research: enable AVIF output (silently disabled today), add resource hints to the image-LCP origin, correct the stale hero dimensions, add a hero LQIP blur, reduce collection LCP-candidate contention from 3 preloads to 1, raise the PDP Shopify image cap, raise minimumCacheTTL, and remove the dead Caveat font. Re-measure warm PSI per route against the Wave-1 baseline.

Purpose: These are config-level, not churn. `images.formats` is unset (fork default WebP-only) so AVIF is off and the 24KB hero AVIF is re-encoded to a larger WebP — a bytes regression on the most important LCP image. There are zero resource hints despite dependence on cdn.shopify.com for PDP/collection LCP images. Per D-03 these only proceed AFTER Wave-1 root cause; they are real-world LCP levers, not lab-LCP chasing.

Output: AVIF formats + raised cache TTL in next.config.ts; resource hints in the storefront head; corrected hero dims + LQIP; collection preload 3->1; PDP cap raise; Caveat removed (+ contract test updated); warm PSI re-measurement appended to evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pagespeed-100-perfection/20-CONTEXT.md
@.planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md
@.planning/phases/20-pagespeed-100-perfection/20-01-SUMMARY.md
@next.config.ts
@src/app/(storefront)/layout.tsx
@src/components/homepage/content.ts
@src/components/collection/product-card/product-card.tsx
@src/components/collection/product-card/product-card.test.tsx
@src/app/layout.tsx
@scripts/component-contracts/performance-fonts.test.mjs
@CLAUDE.md
@AGENTS.md
@docs/conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable AVIF output, raise minimumCacheTTL, add resource hints</name>
  <read_first>
    - next.config.ts (images block lines 33-51; formats/minimumCacheTTL are unset)
    - src/app/(storefront)/layout.tsx (server component; head injection point — preconnect/dns-prefetch must land in head)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md (formats default ['image/webp'] ~L738; minimumCacheTTL 14400 ~L783; AVIF encode note ~L770 — read before changing)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Findings 2 + 6; Impact x Effort rows 3, 4, 12; Risks "AVIF cold-transform paradox", "Preconnecting consent-gated hosts")
  </read_first>
  <action>In next.config.ts images block, add formats: ['image/avif', 'image/webp'] (AVIF first so AVIF-capable browsers get AVIF, not a transcoded WebP) and set minimumCacheTTL to ~31 days in seconds (2678400). Keep qualities: [68, 75] and remotePatterns unchanged. Do NOT add deviceSizes/imageSizes unless diagnostics justify it. In src/app/(storefront)/layout.tsx add resource hints that render into the document head: a link rel="preconnect" to https://cdn.shopify.com (the PDP/collection LCP image origin) and, for consent-gated third-party hosts (searchserverapi.com, googletagmanager, api.trustoo.io), use rel="dns-prefetch" ONLY (NOT preconnect — preconnecting a consent-gated host opens a connection the consent gate is meant to prevent). The storefront layout is a Server Component — keep it server-first, no 'use client', static link markup only. Note: brotli does NOT compress images (the host excludes image formats) — do not add compression config expecting an image LCP win. The first cold run after enabling AVIF may look slower (~50% longer encode); always warm before measuring.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('next.config.ts','utf8');if(!/image\/avif/.test(s)||!/image\/webp/.test(s)){process.exit(1)};if(!/minimumCacheTTL/.test(s)){process.exit(1)}" && node -e "const s=require('fs').readFileSync('src/app/(storefront)/layout.tsx','utf8');if(!/preconnect/.test(s)||!/cdn\.shopify\.com/.test(s)){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts images config contains formats: ['image/avif','image/webp'] and a minimumCacheTTL of 2678400
    - src/app/(storefront)/layout.tsx renders a link with rel="preconnect" and href="https://cdn.shopify.com"
    - Consent-gated hosts (searchserverapi.com / googletagmanager / api.trustoo.io) appear only with rel="dns-prefetch", never rel="preconnect"
    - src/app/(storefront)/layout.tsx remains a Server Component (no 'use client' directive added)
  </acceptance_criteria>
  <done>AVIF delivery is enabled, static assets carry a long cache TTL, and the image-LCP origin is preconnected while consent hosts are only dns-prefetched. (PSI-08, PSI-10)</done>
</task>

<task type="auto">
  <name>Task 2: Fix hero dims, add hero LQIP, reduce collection preloads 3->1, raise PDP gallery cap</name>
  <read_first>
    - src/components/homepage/content.ts (HOMEPAGE_HERO image block lines 111-116; declared height 810 is stale — file is 1440x650)
    - src/components/homepage/hero/hero.tsx (the hero Image; fill + sizes="100vw" + preload, no placeholder today)
    - src/app/(storefront)/collections/[handle]/_components/product-list.tsx (lines 53-57: priority={index < FIRST_VISIBLE_PRODUCT_ROW_COUNT} — reduce to 1 LCP candidate)
    - src/components/product/product-gallery/product-gallery.tsx (Shopify ?width=800 pin then /_next/image re-optimize — under-serves 2x DPR; raise/remove the 800px cap)
    - src/components/collection/product-card/product-card.test.tsx (guards exactly one rel=preload as=image and no eager/fetchPriority — must stay green)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 2; Impact x Effort rows 5, 11, 13, 15; Risks "Only one LCP candidate per viewport")
  </read_first>
  <action>In src/components/homepage/content.ts fix HOMEPAGE_HERO.image.height from 810 to 650 (true intrinsic is 1440x650) and add a hand-made blurDataURL for the LQIP (a string src blocks Next auto-blur, so supply a tiny base64 blur for the hero AVIF). In src/components/homepage/hero/hero.tsx wire the blurDataURL through with placeholder="blur" while keeping fill + sizes + preload + alt="" intact (D-10: keep the rendered preload posture; never re-add deprecated priority/eager/fetchPriority). In product-list.tsx reduce the collection preload set to exactly the first card — make priority true only for index 0 (one LCP candidate; multiple preloads cause bandwidth contention and can worsen LCP); verify the first DOM card is the visual first. In product-gallery.tsx raise the Shopify ?width=800 cap to ~1280 so 2x-DPR of the declared sizes (up to ~38rem) is served; keep quality=68. Do not change product-card.tsx's preload API; the existing product-card.test.tsx assertions (exactly one image preload, no eager, no fetchPriority="high") must remain green.</action>
  <verify>
    <automated>pnpm exec vitest run src/components/collection/product-card/product-card.test.tsx && node -e "const s=require('fs').readFileSync('src/components/homepage/content.ts','utf8');if(/height:\s*810/.test(s)){process.exit(1)};if(!/blurDataURL/.test(s)){process.exit(1)}" && node -e "const s=require('fs').readFileSync('src/app/(storefront)/collections/[handle]/_components/product-list.tsx','utf8');if(/index < 3/.test(s)){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/homepage/content.ts HOMEPAGE_HERO.image.height is 650 (not 810) and a blurDataURL is present
    - src/components/homepage/hero/hero.tsx uses placeholder="blur" and still renders fill + sizes + preload + alt="" with no priority/eager/fetchPriority added
    - product-list.tsx preloads exactly the first card (index 0), not the first three
    - product-gallery.tsx Shopify image width cap is raised above 800 (to ~1280) with quality kept at 68
    - product-card.test.tsx passes (exactly one rel=preload as=image; no loading="eager"; no fetchPriority="high")
  </acceptance_criteria>
  <done>The hero declares correct dims + LQIP, collection LCP contention drops to one preload, PDP serves 2x-DPR images, with the rendered preload posture preserved. (PSI-03, PSI-08)</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused Caveat font + update contract test; re-measure warm PSI</name>
  <read_first>
    - src/app/layout.tsx (Caveat import line 2, caveat = Caveat({...}) lines 37-42, caveat.variable in body className line 74)
    - src/app/globals.css (line ~9 references --font-caveat — remove the dead reference if present)
    - scripts/component-contracts/performance-fonts.test.mjs (asserts a caveat block exists, lines 30, 36, 61-63 — these will fail once Caveat is removed and MUST be updated in the same change)
    - .planning/phases/20-pagespeed-100-perfection/20-RESEARCH.md (Per-Dimension Finding 3 "Caveat is dead weight"; Impact x Effort row 16; "Validation Architecture" -> Contract/unit tests)
  </read_first>
  <action>Remove the Caveat font entirely: delete the Caveat import and the caveat = Caveat({...}) declaration in src/app/layout.tsx, remove caveat.variable from the body className composition (keep cn() usage for the remaining three font variables), and remove any --font-caveat reference in src/app/globals.css. In the SAME change update scripts/component-contracts/performance-fonts.test.mjs to drop the caveat assertions (the loop entry, the fontConfig('caveat') call, and the caveat preload:false assertion at lines 61-63) so the contract test reflects three fonts, not four. Do NOT change Spectral/Hanken Grotesk/Space Mono — Space Mono must keep preload: true and all three must keep display: 'optional' (zero font CLS by construction; do not switch to display:swap or add font preconnect). After all Wave-2 changes, build and run the local harness warm, then run probe-psi.mjs warm, and append the new warm scores per route to docs/launch/psi-evidence.md labelled "Wave 2 (image/font config)".</action>
  <verify>
    <automated>node --test scripts/component-contracts/performance-fonts.test.mjs && node -e "const s=require('fs').readFileSync('src/app/layout.tsx','utf8');if(/Caveat|caveat/.test(s)){console.error('caveat still referenced');process.exit(1)};if(!/preload:\s*true/.test(s)){console.error('space mono preload regressed');process.exit(1)}"</automated>
    <human-check>docs/launch/psi-evidence.md has a "Wave 2" warm re-measurement showing image-route LCP improved or held and CLS still 0.</human-check>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains no Caveat import, no caveat font declaration, and no caveat.variable in the body className
    - scripts/component-contracts/performance-fonts.test.mjs passes and no longer references caveat
    - Space Mono keeps preload: true; all remaining fonts keep display: 'optional'
    - docs/launch/psi-evidence.md contains a Wave-2 warm re-measurement row set per route
  </acceptance_criteria>
  <done>Dead font weight is removed, the fonts contract test is current, and a warm PSI re-baseline confirms the image/font wins. (PSI-09)</done>
</task>

</tasks>

<verification>
- `node --test scripts/component-contracts/performance-fonts.test.mjs` passes (three fonts).
- `pnpm exec vitest run src/components/collection/product-card/product-card.test.tsx` passes (preload posture intact, D-10).
- next.config.ts emits AVIF and a 31-day minimumCacheTTL; the storefront head preconnects cdn.shopify.com.
- REGRESSION GUARDS (D-09): `node scripts/seo/probe-launch-seo.mjs`, `node scripts/seo/probe-crawlable-html.mjs`, and `pnpm exec playwright test tests/e2e/h1-correctness.spec.ts` still pass after the hero/head changes (preconnect + LQIP must not alter the crawlable static shell or the single-visible-H1 invariant).
- Warm PSI re-measurement appended to docs/launch/psi-evidence.md labelled "Wave 2".
</verification>

<success_criteria>
- AVIF/WebP delivery, responsive sizes, correct hero dimensions, LQIP, and one-preload LCP discipline are in place with zero CLS regression.
- Fonts load non-render-blocking with the critical Space Mono preload retained and the dead Caveat font removed.
- Static/immutable assets carry a long cache TTL and the image-LCP origin is preconnected.
- A warm PSI re-baseline confirms image-route LCP improved or held against Wave 1.
</success_criteria>

<output>
Create `.planning/phases/20-pagespeed-100-perfection/20-02-SUMMARY.md` when done. Record the Wave-2 warm PSI deltas per route and confirm the Phase 18/19 regression guards stayed green.
</output>
