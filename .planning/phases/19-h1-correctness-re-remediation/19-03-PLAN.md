---
phase: 19-h1-correctness-re-remediation
plan: 03
type: execute
wave: 2
depends_on: ['19-01']
files_modified:
  - src/app/(storefront)/collections/[handle]/_components/hero.tsx
  - src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx
  - .planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md
autonomous: false
requirements: [SEO-H1-02, SEO-H1-03]
user_setup: []

must_haves:
  truths:
    - "A design preview of the banner-mode visible-H1 treatment is captured and signed off before implementation."
    - "Banner-mode collection pages render one visible page-level <h1> below the banner, at heading prominence consistent with the green-band variant."
    - "The breadcrumb current-page element in banner mode is a non-H1 <span aria-current=\"page\">, not an <h1>."
    - "Exactly one <h1> remains on a banner-mode collection page."
    - "page-content.test.tsx asserts the visible display-class H1 and the single-H1 invariant, and no longer requires the 11px type-mono-meta H1."
  artifacts:
    - path: "src/app/(storefront)/collections/[handle]/_components/hero.tsx"
      provides: "Banner-mode visible H1 below the banner + breadcrumb reverted to span"
      contains: "font-display text-ink"
    - path: "src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx"
      provides: "Updated locking test for the visible banner H1"
      contains: "<h1"
    - path: ".planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md"
      provides: "Design preview + sign-off of the banner-mode H1 treatment"
  key_links:
    - from: "src/app/(storefront)/collections/[handle]/_components/hero.tsx"
      to: "page-content.test.tsx"
      via: "single visible H1 invariant locked by test"
      pattern: "font-display text-ink"
---

<objective>
Restore a single, visible, page-level H1 on banner-image collection pages (item #2, SEO-H1-02). Revert the breadcrumb current-page element from an 11px `type-mono-meta` `<h1>` crumb to a non-H1 `<span aria-current="page">`, add a visible display-class `<h1>` below the banner, and update the locking test in the same change — gated behind a design preview/sign-off because it reverses the shipped "compact banner heading" decision (D-08).

Purpose: Banner-mode collections currently have no visually meaningful page heading (the only `<h1>` is a tiny gold crumb), which the auditor flagged as a hidden-H1 / cloaking-like pattern. This finishes the unfinished half of Phase 18 D-09.
Output: Updated `hero.tsx`, updated `page-content.test.tsx`, and a `19-03-BANNER-H1-DESIGN-PREVIEW.md` sign-off record.

Independence note: This plan touches only `hero.tsx` and `page-content.test.tsx` — no file overlap with 19-02 — so it runs in parallel within Wave 2. It is sequenced after 19-01 only so the phase's reproduce-and-decide gate lands first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-h1-correctness-re-remediation/19-CONTEXT.md
@.planning/phases/19-h1-correctness-re-remediation/19-RESEARCH.md
@src/app/(storefront)/collections/[handle]/_components/hero.tsx
@src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx

<interfaces>
<!-- The good reference H1 markup to match, and the current bad banner H1 to replace. -->

GOOD reference — green-band visible H1 (hero.tsx:127-134):
  font-display text-paper mt-4 text-balance text-[clamp(2.4rem,5vw,4rem)] leading-[1.04]
  (uses text-paper because it sits on the brand band; banner mode is on paper background → use text-ink.)

GOOD reference — rich-hero visible H1 (collection-rich-hero.tsx:22).

BAD current — banner-mode breadcrumb crumb that is the page's only <h1> (hero.tsx:65-70):
  <h1 aria-current="page" className="type-mono-meta text-gold-deep m-0 inline">{collectionTitle}</h1>
  This sits inside the breadcrumb <nav> between "/" separators.

Token facts (src/app/globals.css): type-mono-meta ≈ 11px uppercase mono; type-display ≈ large display heading.

Current locking test assertions to UPDATE (page-content.test.tsx:500-508), inside the
"renders banner H1 visibly and places read-more story after product grid" test:
  - expect(html.match(/<h1\b/g)).toHaveLength(1)                 // KEEP (single-H1 invariant)
  - expect(html).not.toContain('type-display')                  // REMOVE (the visible H1 should carry a display/heading class)
  - expect(html).toContain('<h1 aria-current="page" class="type-mono-meta text-gold-deep m-0 inline">Wholesale Bulk Tea</h1>')  // REPLACE: assert a <span aria-current="page"> crumb instead, and a visible <h1> with the display class
  - the read-more-after-grid assertions (id="product-grid" before "Read more about ...") — KEEP unchanged.

Styling rules (CLAUDE.md / conventions): use cn() for conditional className composition, design token classes, warm/botanical palette (no cool grays), no hidden text, no raw hex. text-ink (paper background), NOT text-paper, for the banner H1.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture banner-mode visible-H1 design preview and sign-off</name>
  <files>.planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md</files>
  <read_first>
    - src/app/(storefront)/collections/[handle]/_components/hero.tsx (banner branch ~24-75; green-band H1 reference ~127-134)
    - src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx (rich-hero H1 reference, line 22)
    - 19-CONTEXT.md (D-06, D-08: visible H1 below banner, text-ink, design-gate)
    - docs/launch/seo-audit-staging-analysis.md (Action 3 + page 5/6: desired above-grid structure = banner image, breadcrumb, visible H1, brief intro)
  </read_first>
  <action>
    Produce a design preview of the proposed banner-mode H1 treatment and capture sign-off in `.planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md` BEFORE writing component code (per the project preview-designs-first rule and D-08, since this reverses commit 27b518d4 "compact banner heading"). The preview must show: the banner image first, then the breadcrumb (Home / Collections / current-page as a small crumb), then a visible display-class H1 below the `</nav>` at heading prominence consistent with the green-band H1, using `text-ink` on the paper background — e.g. `font-display text-ink mt-4 text-balance text-[clamp(2rem,4vw,3.1rem)] leading-[1.06]`. Note the open design call: whether to also render the brief intro paragraph in banner mode (currently dropped) — present it as an option for the owner to accept or decline. Provide the preview via a Storybook story render or a screenshot/markup mock referenced in the doc. Do NOT modify hero.tsx in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md && grep -q "text-ink" .planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `19-03-BANNER-H1-DESIGN-PREVIEW.md` exists, shows the proposed banner-mode layout (banner → breadcrumb → visible H1 [→ optional intro]), and specifies the visible H1 uses `text-ink` and a display/heading class.
    - The brief-intro-in-banner-mode option is presented as an explicit accept/decline decision.
    - Owner/developer sign-off on the H1 treatment (and the intro decision) is recorded. hero.tsx is unchanged in this task.
  </acceptance_criteria>
  <done>The banner-mode visible-H1 design is previewed and signed off, with the intro-in-banner decision recorded.</done>
  <what-built>A design preview of the banner-mode visible-H1 treatment (Storybook render or mock) plus a sign-off doc.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md` (and the referenced Storybook story / screenshot).
    2. Confirm the visible H1 prominence below the banner looks right on the paper background and matches the warm/botanical system.
    3. Decide whether banner mode should also show the brief intro paragraph.
  </how-to-verify>
  <resume-signal>Reply "approved" (and "with intro" / "without intro"), or describe changes to the H1 treatment.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the visible banner H1 and update the locking test</name>
  <files>src/app/(storefront)/collections/[handle]/_components/hero.tsx, src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx</files>
  <read_first>
    - .planning/phases/19-h1-correctness-re-remediation/19-03-BANNER-H1-DESIGN-PREVIEW.md (the signed-off treatment + intro decision — authoritative)
    - src/app/(storefront)/collections/[handle]/_components/hero.tsx (banner branch ~24-75)
    - src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx (~471-516, the locking test)
    - src/app/(storefront)/collections/[handle]/_components/collection-rich-hero.tsx (H1 styling reference)
    - docs/conventions.md (cn() usage, token classes, no hidden text, no raw hex)
  </read_first>
  <behavior>
    - Banner-mode collection page renders exactly one <h1>.
    - That <h1> is a visible display-class heading below the breadcrumb <nav> (font-display, text-ink, display-size clamp), NOT the 11px type-mono-meta crumb.
    - The breadcrumb current-page element is a <span aria-current="page"> (non-H1), styled as the small gold crumb it was.
    - If the design sign-off chose "with intro", banner mode also renders the brief intro paragraph; otherwise it does not.
    - Read-more story content still renders after the product grid (id="product-grid" precedes "Read more about ...").
  </behavior>
  <action>
    In hero.tsx banner branch: change the current-page `<h1 aria-current="page" className="type-mono-meta text-gold-deep m-0 inline">` back to `<span aria-current="page" className="type-mono-meta text-gold-deep m-0 inline">` so the crumb is no longer the page H1. After the `</nav>` (still inside the banner-mode Section.Container), add a visible `<h1>` using the signed-off treatment, e.g. `font-display text-ink mt-4 text-balance text-[clamp(2rem,4vw,3.1rem)] leading-[1.06]` with `{collectionTitle}` — use `text-ink` (paper background), never `text-paper`. Compose any conditional classes with `cn()`. If the sign-off chose "with intro", render the `heroDescription` paragraph below the H1 in banner mode (token classes, no raw hex); otherwise leave it out. Keep exactly one `<h1>` on the page. Then update page-content.test.tsx (~471-516) in the SAME change: keep `expect(html.match(/<h1\b/g)).toHaveLength(1)`; remove the `not.toContain('type-display')` assertion; replace the `toContain('<h1 ... type-mono-meta ...>')` assertion with one that asserts the breadcrumb crumb is now a `<span aria-current="page" ...>` and that a visible `<h1 ... font-display ...>` (display/heading class) is present; keep the read-more-after-grid assertions. Adjust the intro assertion to match the signed-off intro decision.
  </action>
  <verify>
    <automated>pnpm test:unit -- page-content && pnpm typecheck && pnpm lint:tailwind</automated>
  </verify>
  <acceptance_criteria>
    - hero.tsx banner branch renders a `<span aria-current="page">` crumb and a separate visible `<h1>` carrying `font-display text-ink` and a display-size class; exactly one `<h1>` is produced.
    - page-content.test.tsx no longer requires the `type-mono-meta` H1 or forbids `type-display`; it asserts the single-H1 invariant, a `<span aria-current="page">` breadcrumb, and a visible display-class `<h1>`. The read-more-after-grid assertions remain.
    - `pnpm test:unit -- page-content` passes; `pnpm typecheck` passes; `pnpm lint:tailwind` passes (no raw hex, token classes only).
    - The intro paragraph presence in banner mode matches the signed-off decision.
  </acceptance_criteria>
  <done>Banner-mode collection pages show one visible page-level H1 below the banner with a non-H1 breadcrumb crumb, and the locking test enforces it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered HTML heading hierarchy | The H1 markup change must not introduce a second H1 or hidden text; the locking test is the guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Spoofing (cloaking-like) | banner-mode H1 visibility | mitigate | Replace the 11px crumb H1 with a visible display-class H1 (text-ink), no sr-only/hidden; test asserts visible class. |
| T-19-05 | Tampering | single-H1 invariant | mitigate | Keep `expect(html.match(/<h1\b/g)).toHaveLength(1)` in the locking test. |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `pnpm test:unit -- page-content` passes with the updated assertions.
- `pnpm typecheck` and `pnpm lint:tailwind` pass.
- Banner-mode HTML contains exactly one `<h1>` (display class, text-ink) and a `<span aria-current="page">` crumb.
</verification>

<success_criteria>
- Every banner-image collection page renders a single visible page-level H1 at heading prominence consistent with the green-band/rich-hero variants (SEO-H1-02).
- The locking test enforces the visible H1 and single-H1 invariant; no Phase 18 single-route H1 guarantee is regressed (SEO-H1-03).
</success_criteria>

<output>
Create `.planning/phases/19-h1-correctness-re-remediation/19-03-SUMMARY.md` when done. Record the signed-off H1 treatment, the intro-in-banner decision, and the exact test assertions changed.
</output>
