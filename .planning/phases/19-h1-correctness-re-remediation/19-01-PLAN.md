---
phase: 19-h1-correctness-re-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/h1-correctness.spec.ts
  - .planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md
autonomous: false
requirements: [SEO-H1-01]
user_setup: []

must_haves:
  truths:
    - "A committed in-browser test navigates Home → ProductA → ProductB (plus a collection) and reads the raw accumulated DOM, not the a11y tree."
    - "On the current (pre-fix) build the test FAILS: it observes more than one raw <h1> and/or the leaked homepage H1 in the accumulated DOM."
    - "The Activity-DOM root cause is confirmed in writing (cacheComponents keeps prior routes mounted in hidden <Activity>)."
    - "A written recommendation chooses ONE remediation approach (A/B/C) with measured cost and risk, gated on owner/developer sign-off."
  artifacts:
    - path: "tests/e2e/h1-correctness.spec.ts"
      provides: "Multi-route raw-DOM H1 regression test (currently failing)"
      contains: "locator('h1')"
    - path: ".planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md"
      provides: "Root-cause confirmation + approach A/B/C evaluation + recommendation"
      contains: "MAX_BF_CACHE_ENTRIES"
  key_links:
    - from: "tests/e2e/h1-correctness.spec.ts"
      to: "raw accumulated browser DOM"
      via: "await page.locator('h1').count()"
      pattern: "locator\\('h1'\\)\\.count"
---

<objective>
Reproduce the multiple / per-visit-changing H1 defect with a committed, FAILING in-browser regression test, confirm the Cache Components Activity-DOM root cause, and produce a written remediation recommendation (approach A/B/C with measured cost/risk) for owner/developer sign-off before any architectural change lands.

Purpose: The defect (audit item #1, SEO-H1-01) only appears in the accumulated multi-route browser DOM, never in single-route HTML — so the Phase 18 single-route probe method structurally cannot reproduce it. We must reproduce-first, then choose the remediation deliberately because it touches core caching architecture (D-03, D-04).
Output: A failing `tests/e2e/h1-correctness.spec.ts` and a `19-01-REMEDIATION-RECOMMENDATION.md` recommendation doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-h1-correctness-re-remediation/19-CONTEXT.md
@.planning/phases/19-h1-correctness-re-remediation/19-RESEARCH.md
@docs/launch/seo-audit-staging-analysis.md

<interfaces>
<!-- Existing fake-Shopify e2e harness the new test must reuse. Do not stand up a new harness. -->

From playwright.config.ts:
- testDir: './tests/e2e'; project 'chromium' (Desktop Chrome); workers: 1; fullyParallel: false.
- webServer boots the fake-Shopify GraphQL server (run-fake-shopify-server.ts) AND `next dev` with SHOPIFY_STOREFRONT_TEST_MODE='true' pointing at the fake store. `cacheComponents: true` is active in this dev server.
- baseURL is http://localhost:${PLAYWRIGHT_PORT ?? 4173}; navigate with relative paths via page.goto('/...').

From tests/e2e/cart-checkout.spec.ts (pattern to copy):
- import { expect, test } from '@playwright/test'
- import { blockThirdPartyRequests } from '../mocks/third-party-network'
- test.beforeEach blocks third-party requests; product route used in the suite is '/products/test-standard-tea'.

Known fake-Shopify product/collection handles available in the harness:
- Product: '/products/test-standard-tea' (used by cart-checkout.spec.ts). Discover a SECOND distinct product handle and one collection handle from tests/mocks/run-fake-shopify-server.ts / shopify-graphql-server.ts / tests/fixtures before writing the test.

Homepage H1 (the leaked heading to assert absent on inner routes) — from src/components/homepage/content.ts:
- HOMEPAGE_HERO.title === "Australia's #1 tea company" (rendered as the homepage <h1> in src/components/homepage/hero/hero.tsx:42-44).

Root-cause facts (from 19-RESEARCH.md, verified against node_modules):
- next.config.ts:9 sets cacheComponents: true.
- node_modules/next/dist/client/components/bfcache-state-manager.js: MAX_BF_CACHE_ENTRIES = process.env.__NEXT_CACHE_COMPONENTS ? 3 : 1.
- node_modules/next/dist/docs/01-app/02-guides/preserving-ui-state.md: hidden <Activity> DOM stays in the document; "Opt-out strategies are being considered" (no per-route opt-out in 16.2.9).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the failing multi-route raw-DOM H1 regression test</name>
  <files>tests/e2e/h1-correctness.spec.ts</files>
  <read_first>
    - tests/e2e/cart-checkout.spec.ts (harness/import pattern, beforeEach blockThirdPartyRequests, real fake-Shopify product handles)
    - tests/e2e/consent.spec.ts (additional spec pattern reference)
    - playwright.config.ts (testDir, webServer, baseURL, single-worker config)
    - tests/mocks/run-fake-shopify-server.ts and tests/mocks/shopify-graphql-server.ts and tests/fixtures/shopify/product.ts (to confirm a SECOND distinct product handle and a real collection handle available in the fake store)
    - src/components/homepage/content.ts (HOMEPAGE_HERO.title literal to assert absent)
    - node_modules/next/dist/docs/01-app/02-guides/preserving-ui-state.md (why getByRole('heading') falsely passes on hidden Activity nodes)
  </read_first>
  <action>
    Create tests/e2e/h1-correctness.spec.ts in the existing fake-Shopify e2e suite. Reuse the cart-checkout.spec.ts import shape (`import { expect, test } from '@playwright/test'`) and a `test.beforeEach` that calls `blockThirdPartyRequests(page)`. Write a single test that performs CLIENT-SIDE (soft) navigation across ≥3 SEO-critical routes in one browser session: start at '/', then navigate to a first product, then a second DISTINCT product, then a collection — using in-page link clicks (e.g. `page.getByRole('link', ...).click()` or `page.click('a[href="..."]')`) so that Next soft-navigation retains prior routes in Activity, NOT `page.goto` for the inner hops (goto triggers a hard navigation that defeats the reproduction). Use the real fake-store handles discovered from the mocks; do not invent handles. After the final navigation, assert on the RAW DOM: `expect(await page.locator('h1').count()).toBe(1)` and `expect(await page.locator('h1', { hasText: "Australia's #1 tea company" }).count()).toBe(0)`. Add an inline comment stating the test MUST use raw `locator('h1')` and MUST NOT use `getByRole('heading')` because Activity-hidden nodes are removed from the a11y tree but remain in the DOM (false pass). Do NOT add any production code or config change in this task — this test is expected to FAIL on the current build, which is the deliverable (proof of the defect). Wait for the visible route content (e.g. the product title) before asserting so the soft navigation has settled.
  </action>
  <verify>
    <automated>pnpm test:e2e -- h1-correctness 2>&1 | tee /tmp/h1-prefix.txt; grep -q "failed" /tmp/h1-prefix.txt && echo "EXPECTED-FAIL-CONFIRMED"</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/h1-correctness.spec.ts exists and contains the literal substring `locator('h1')` and does NOT contain `getByRole('heading')`.
    - The test uses soft (link-click) navigation for the inner route hops, not `page.goto` for each hop.
    - Running `pnpm test:e2e -- h1-correctness` on the current pre-fix build FAILS (the assertion `locator('h1').count()).toBe(1)` does not hold and/or the leaked homepage H1 is present), proving the accumulated-DOM defect. A non-zero exit / "failed" in the reporter output is the success signal for THIS task.
  </acceptance_criteria>
  <done>The committed regression test reproduces the defect: it fails on the current build by observing more than one raw &lt;h1&gt; (and/or the leaked homepage H1) in the accumulated multi-route DOM.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm root cause and choose the item #1 remediation approach (owner/developer sign-off)</name>
  <files>.planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md</files>
  <read_first>
    - 19-RESEARCH.md (Finding 1: root cause + remediation options A/B/C with measured cost)
    - 19-CONTEXT.md (D-02, D-04: locked root cause + execution-gated remediation choice)
    - next.config.ts:9 (cacheComponents toggle)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/cacheComponents.md
    - node_modules/next/dist/docs/01-app/02-guides/preserving-ui-state.md
    - node_modules/next/dist/docs/01-app/02-guides/caching-without-cache-components.md
    - The 5 'use cache' modules to scope approach-A cost: src/lib/blog/operations.ts, src/lib/reviews/trustoo.ts, src/lib/shopify/operations/collection.ts, src/lib/shopify/operations/product.ts, src/lib/shopify/operations/storefront-page.ts
  </read_first>
  <action>
    First, in the recommendation doc, record the DevTools/test confirmation of the root cause: that the failing test's extra h1(s) originate from prior routes retained in hidden React `<Activity mode="hidden">` boundaries because `cacheComponents: true` sets `MAX_BF_CACHE_ENTRIES` to 3 (vs 1 when off). Then write `.planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md` evaluating all three approaches against measured cost: (A) disable cacheComponents — migrate the 5 'use cache' modules / 41 cacheLife|cacheTag calls to `unstable_cache` / route-segment `revalidate` per caching-without-cache-components.md, re-prove crawlable-HTML + CWV; the ONLY approach that guarantees one H1 in the live DOM. (B) keep Cache Components but force hard (full-document) navigation for SEO-critical product/collection links — note `prefetch={false}` alone does NOT prevent Activity retention; weaker guarantee + SPA UX cost. (C) await an upstream per-route Activity opt-out — none exists in 16.2.9; document-only, do not ship alone. State a single RECOMMENDATION with rationale (per 19-RESEARCH: prefer A if the 5-module migration re-proves cleanly within budget; fall back to B if A risks Phase 18 streaming evidence). This is a decision checkpoint: present the recommendation and the cost/risk for owner/developer sign-off BEFORE 19-02 implements. Do NOT make any code or next.config.ts change in this plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md && grep -q "MAX_BF_CACHE_ENTRIES" .planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md && grep -Eq "Recommendation|RECOMMENDATION" .planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 19-01-REMEDIATION-RECOMMENDATION.md exists, names the root cause (`MAX_BF_CACHE_ENTRIES` / hidden `<Activity>` / `cacheComponents`), and evaluates all of approaches A, B, and C with the measured approach-A cost (5 modules, 41 cacheLife/cacheTag calls).
    - The doc states exactly one recommended approach with explicit cost/risk rationale.
    - The owner/developer has signed off on the chosen approach (recorded resume signal). No code/config change was made in this plan.
  </acceptance_criteria>
  <done>Root cause is confirmed in writing and one remediation approach is selected with owner/developer sign-off, ready for 19-02 to implement.</done>
  <what-built>A failing multi-route raw-DOM H1 regression test (Task 1) and a written A/B/C remediation recommendation with measured cost/risk (this task's doc).</what-built>
  <how-to-verify>
    1. Read `.planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md` — confirm the root-cause statement and the single recommended approach.
    2. Confirm the recommended approach (A disable cacheComponents + migrate 5 modules, B hard-navigation, or C wait/document) and the cost/risk are acceptable for execution in 19-02.
  </how-to-verify>
  <resume-signal>Reply "approved: A", "approved: B", or "approved: C" (or describe a different choice). 19-02 implements the approved approach.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test:e2e -- h1-correctness` FAILS on the current build (proves the defect).
- `tests/e2e/h1-correctness.spec.ts` uses raw `locator('h1')`, never `getByRole('heading')`, and soft-navigates ≥3 routes.
- `19-01-REMEDIATION-RECOMMENDATION.md` confirms the Activity-DOM root cause and recommends one approach with measured cost/risk.
</verification>

<success_criteria>
- A committed failing in-browser regression test reproduces the multiple-H1 accumulated-DOM defect (SEO-H1-01 reproduction).
- The root cause is confirmed and a remediation approach is chosen with owner/developer sign-off, unblocking 19-02.
</success_criteria>

<output>
Create `.planning/phases/19-h1-correctness-re-remediation/19-01-SUMMARY.md` when done. Record the chosen approach (A/B/C) and the exact fake-store product/collection handles the regression test navigates, so 19-02 can reuse them.
</output>
