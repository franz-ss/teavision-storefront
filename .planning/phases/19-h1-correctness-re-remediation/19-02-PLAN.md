---
phase: 19-h1-correctness-re-remediation
plan: 02
type: execute
wave: 2
depends_on: ['19-01']
files_modified:
  - next.config.ts
  - src/lib/shopify/operations/collection.ts
  - src/lib/shopify/operations/product.ts
  - src/lib/shopify/operations/storefront-page.ts
  - src/lib/blog/operations.ts
  - src/lib/reviews/trustoo.ts
  - .planning/phases/19-h1-correctness-re-remediation/19-02-CACHE-MIGRATION-EVIDENCE.md
autonomous: false
requirements: [SEO-H1-01, SEO-H1-03]
user_setup: []

must_haves:
  truths:
    - "After the fix, the multi-route accumulated DOM contains exactly one raw <h1>; the leaked homepage H1 is absent."
    - "tests/e2e/h1-correctness.spec.ts (from 19-01) now PASSES."
    - "Phase 18 crawlable-HTML evidence still holds: crawl-critical content precedes any skeleton marker for collection and product routes."
    - "Phase 18 launch SEO evidence (lang, robots, sitemap, canonical, structured data) still holds."
    - "Data caching still works after migration: collection/product/page/blog/reviews fetches are cached without 'use cache' regressions or fail-fast breakage."
  artifacts:
    - path: "next.config.ts"
      provides: "Item #1 remediation (approach-dependent config change)"
    - path: ".planning/phases/19-h1-correctness-re-remediation/19-02-CACHE-MIGRATION-EVIDENCE.md"
      provides: "Before/after probe output proving regression test passes and Phase 18 evidence is intact"
      contains: "probe-crawlable-html"
  key_links:
    - from: "tests/e2e/h1-correctness.spec.ts"
      to: "post-fix accumulated DOM"
      via: "single visible H1 after multi-route nav"
      pattern: "locator\\('h1'\\)\\.count"
    - from: "src/lib/shopify/operations/collection.ts"
      to: "cached Shopify fetch"
      via: "unstable_cache or route revalidate (approach A) / unchanged (approach B)"
      pattern: "unstable_cache|use cache"
---

> ⚠ **SUPERSEDED / NOT EXECUTED AS WRITTEN (2026-06-29).** This plan assumed
> Approach A (disable `cacheComponents`). Approach A was implemented in a prior
> session (`9267da5f`) then **reverted** (`efed85b3`) at the owner's direction —
> `cacheComponents` stays enabled. SEO-H1-01 was instead resolved via
> **accept + document** (`docs/launch/seo-audit-pages-2-9-response.md`), and
> SEO-H1-02 via the visible-H1 fix (`e1c4204c`). Retained for history only — do
> not execute this plan.

<objective>
Implement the item #1 remediation approach chosen and signed off in 19-01, make the `h1-correctness` regression test pass (exactly one raw &lt;h1&gt; in the accumulated multi-route DOM), and re-prove Phase 18's crawlable-HTML and launch-SEO evidence is not regressed.

Purpose: Eliminate the multiple / per-visit-changing H1 leak in the live browser DOM (SEO-H1-01) without breaking Phase 18's server-rendered crawlable HTML, metadata, indexation, or streaming guarantees (SEO-H1-03).
Output: The approach-dependent code change (config + cache migration for approach A, or hard-navigation links for approach B), plus `19-02-CACHE-MIGRATION-EVIDENCE.md`.

IMPORTANT — approach-gated scope: This plan is written for the recommended/likely approach (A) and lists the 5 cache modules + next.config.ts in `files_modified`. If 19-01 sign-off selected approach (B), the executor MUST NOT migrate cache modules: instead convert SEO-critical product/collection `<Link>`s to hard-navigation (plain `<a>` / full-document nav) in the relevant link components and leave `next.config.ts`/cache modules unchanged. If approach (C) was selected, this plan does not ship a code fix — escalate to the planner. Read the 19-01 SUMMARY for the recorded decision BEFORE writing code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-h1-correctness-re-remediation/19-CONTEXT.md
@.planning/phases/19-h1-correctness-re-remediation/19-RESEARCH.md
@.planning/phases/19-h1-correctness-re-remediation/19-01-SUMMARY.md
@.planning/phases/19-h1-correctness-re-remediation/19-01-REMEDIATION-RECOMMENDATION.md

<interfaces>
<!-- Migration targets and evidence scripts. Read the Next docs before changing caching. -->

The 5 'use cache' modules to migrate IF approach A (all cacheLife/cacheTag calls in these modules, 41 total):
- src/lib/shopify/operations/collection.ts (uses 'use cache' / cacheLife / cacheTag)
- src/lib/shopify/operations/product.ts
- src/lib/shopify/operations/storefront-page.ts
- src/lib/blog/operations.ts
- src/lib/reviews/trustoo.ts
Each has a co-located *.test.ts that may assert caching behavior — re-run them after migration.

Migration model (approach A) per node_modules/next/dist/docs/01-app/02-guides/caching-without-cache-components.md:
- Replace 'use cache' + cacheLife()/cacheTag() with `unstable_cache(fn, keyParts, { revalidate, tags })` and/or route-segment `export const revalidate = N`.
- Preserve fail-fast behavior on missing Shopify credentials (no stub fallback).

next.config.ts:9 — `cacheComponents: true`. Approach A flips this to false (or removes it). Re-run build after.

Evidence scripts (re-run after the change, approach A specifically):
- scripts/seo/probe-crawlable-html.mjs (asserts crawl-critical HTML precedes any skeleton marker; supports --start-server --collection-route --product-route). Use the harness-realizable routes /collections/all and /products/test-standard-tea (the only fake-store collection and product — see 19-01 harness notes).
- scripts/seo/probe-launch-seo.mjs (htmlLang/robots/sitemap/canonical/structured-data)
- docs/launch/performance-evidence.md (CWV/LCP evidence to reconcile)

Regression test from 19-01: tests/e2e/h1-correctness.spec.ts — must now PASS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the chosen item #1 remediation</name>
  <files>next.config.ts, src/lib/shopify/operations/collection.ts, src/lib/shopify/operations/product.ts, src/lib/shopify/operations/storefront-page.ts, src/lib/blog/operations.ts, src/lib/reviews/trustoo.ts</files>
  <read_first>
    - .planning/phases/19-h1-correctness-re-remediation/19-01-SUMMARY.md and 19-01-REMEDIATION-RECOMMENDATION.md (the approved approach A/B/C — authoritative)
    - node_modules/next/dist/docs/01-app/02-guides/caching-without-cache-components.md (approach A migration model)
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/cacheComponents.md
    - node_modules/next/dist/docs/01-app/02-guides/streaming.md (re-validate Suspense behavior after change)
    - next.config.ts (cacheComponents toggle, line 9)
    - For approach A: src/lib/shopify/operations/collection.ts, product.ts, storefront-page.ts, src/lib/blog/operations.ts, src/lib/reviews/trustoo.ts (+ their co-located *.test.ts)
    - For approach B: the SEO-critical collection/product link components and the existing prefetch precedent src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.tsx:41
  </read_first>
  <action>
    Apply ONLY the approach approved in 19-01.
    APPROACH A (disable Cache Components): set `cacheComponents: false` (or remove the key) in next.config.ts so prior routes unmount on navigation (MAX_BF_CACHE_ENTRIES → 1). Migrate every `'use cache'` directive in the 5 modules to the non-cache-components model from caching-without-cache-components.md: wrap each data-fetch in `unstable_cache(fn, keyParts, { revalidate, tags })` mapping each prior `cacheLife()` to a `revalidate` window and each `cacheTag()` to a `tags` entry, and/or set route-segment `export const revalidate` where a route owns the fetch. Preserve fail-fast-on-missing-credentials behavior — do not introduce stub/fallback data. Keep the exported function names and signatures unchanged so callers do not break. Do not touch any H1 markup.
    APPROACH B (keep Cache Components, hard navigation): convert SEO-critical product and collection links to full-document navigation (plain `<a href>` instead of next/link `<Link>`, or an equivalent hard-nav) so no Activity DOM accumulates across those hops. Note in code comments that `prefetch={false}` alone does NOT prevent Activity retention. Leave next.config.ts and the 5 cache modules unchanged; record the true file set in the SUMMARY.
    Do not change collection/product H1 markup in this plan (item #2 is 19-03).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:e2e -- h1-correctness</automated>
  </verify>
  <acceptance_criteria>
    - The exact code change matches the approved approach from 19-01 (config + cache migration for A; hard-nav links for B; escalation note for C).
    - `pnpm typecheck` passes.
    - `pnpm test:e2e -- h1-correctness` PASSES (exit 0): `locator('h1').count()` is 1 and the leaked homepage H1 is absent in the accumulated DOM — the same test that FAILED in 19-01.
    - For approach A: no `'use cache'` directive remains in the 5 migrated modules (verified by `grep -rL "use cache"` or a filtered `grep -c` returning 0), and Shopify fetches still fail fast on missing credentials.
  </acceptance_criteria>
  <done>The 19-01 regression test passes: exactly one visible H1 in the accumulated multi-route DOM, using the approved remediation approach.</done>
</task>

<task type="auto">
  <name>Task 2: Re-prove Phase 18 evidence and write migration evidence note</name>
  <files>.planning/phases/19-h1-correctness-re-remediation/19-02-CACHE-MIGRATION-EVIDENCE.md</files>
  <read_first>
    - scripts/seo/probe-crawlable-html.mjs (crawl-critical HTML before skeleton marker; --start-server --collection-route --product-route flags)
    - scripts/seo/probe-launch-seo.mjs (lang/robots/sitemap/canonical/structured-data)
    - src/lib/shopify/operations/collection.test.ts, src/lib/shopify/operations/product.test.ts, src/lib/blog/operations.test.ts (cache-behavior contract tests to re-run)
    - docs/launch/performance-evidence.md (CWV baseline to reconcile if approach A)
    - .planning/phases/18-seo-audit-remediation/18-VERIFICATION.md (Phase 18 outcomes not to regress)
  </read_first>
  <action>
    Re-run the Phase 18 evidence and capture results in `.planning/phases/19-h1-correctness-re-remediation/19-02-CACHE-MIGRATION-EVIDENCE.md`. Run `pnpm build`, then run the crawlable-HTML probe against the harness-realizable routes: `node scripts/seo/probe-crawlable-html.mjs --start-server --collection-route /collections/all --product-route /products/test-standard-tea` and confirm crawl-critical content (H1, product grid, buy section, JSON-LD) still precedes any skeleton marker. The probe MUST exit non-zero on any skeleton-before-content regression (this is the machine-enforced approach-A streaming guard — see verify block). Run `node scripts/seo/probe-launch-seo.mjs` and confirm lang=en-AU, robots account/login disallows, sitemap host, canonical, and structured data are unchanged. Re-run the co-located cache contract tests (`pnpm test:unit` scoped to the collection/product/blog operation tests). For approach A specifically, re-measure CWV via `pnpm test:performance` (or the documented probe) and reconcile against docs/launch/performance-evidence.md, recording any LCP/streaming delta as evidence — do NOT silently accept a new performance regression; record it and flag if it crosses the Phase 17 PERF gate. Record before/after notes: the h1-correctness test now passes; single-route HTML one-visible-H1 guarantees and crawlable HTML remain intact. Clearly separate local code evidence from owner/operator-gated production proof.
  </action>
  <verify>
    <automated>node scripts/seo/probe-crawlable-html.mjs --start-server --collection-route /collections/all --product-route /products/test-standard-tea && node scripts/seo/probe-launch-seo.mjs && test -f .planning/phases/19-h1-correctness-re-remediation/19-02-CACHE-MIGRATION-EVIDENCE.md && grep -q "probe-crawlable-html" .planning/phases/19-h1-correctness-re-remediation/19-02-CACHE-MIGRATION-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/seo/probe-crawlable-html.mjs --start-server --collection-route /collections/all --product-route /products/test-standard-tea` EXITS 0 — crawl-critical content precedes any skeleton marker for the collection and product routes (an approach-A streaming regression that reintroduces a skeleton-before-content shell makes this command exit non-zero and fails the gate, not just human review).
    - `node scripts/seo/probe-launch-seo.mjs` passes (lang=en-AU, robots, sitemap host, canonical, structured data unchanged from Phase 18).
    - `19-02-CACHE-MIGRATION-EVIDENCE.md` exists and contains the `probe-crawlable-html` evidence output and the `probe-launch-seo` result.
    - The co-located cache contract tests pass after migration (approach A); any CWV/LCP delta vs docs/launch/performance-evidence.md is recorded, not silently accepted.
  </acceptance_criteria>
  <done>Phase 18 crawlable-HTML, launch-SEO, and caching evidence are re-proven intact after the item #1 fix — with the crawlable-HTML guard machine-enforced in the verify gate; deltas are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/runtime caching config | Changing cacheComponents / cache strategy alters how SEO-critical HTML is produced and streamed; a wrong migration could silently re-introduce a skeleton shell or stale data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | cache migration (5 'use cache' modules) | mitigate | Machine-enforced: probe-crawlable-html (exits non-zero on skeleton-before-content) + probe-launch-seo + co-located cache contract tests in the verify gate; preserve fail-fast on missing credentials. |
| T-19-02 | Information Disclosure | stale-cache after revalidate mapping | mitigate | Map each cacheLife window to an equivalent revalidate and each cacheTag to a tags entry; re-run cache contract tests. |
| T-19-03 | Denial of Service (perf regression) | disabled Cache Components streaming | accept-with-evidence | Re-measure CWV; record LCP/streaming delta in evidence note; flag if it crosses the Phase 17 PERF gate. |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `pnpm test:e2e -- h1-correctness` PASSES (was failing in 19-01).
- `node scripts/seo/probe-crawlable-html.mjs --start-server --collection-route /collections/all --product-route /products/test-standard-tea` exits 0 (crawl-critical content before any skeleton marker); non-zero on regression fails the gate.
- `node scripts/seo/probe-launch-seo.mjs` passes unchanged.
- For approach A: no residual `'use cache'` in the 5 modules; cache contract tests pass; CWV delta recorded.
</verification>

<success_criteria>
- Exactly one visible H1 in the accumulated multi-route browser DOM (SEO-H1-01 closed at the runtime-DOM level).
- Phase 18 crawlable-HTML, metadata/canonical/robots/sitemap, structured data, and caching behavior are re-proven not regressed (SEO-H1-03), with the crawlable-HTML guard machine-enforced.
</success_criteria>

<output>
Create `.planning/phases/19-h1-correctness-re-remediation/19-02-SUMMARY.md` when done. Record the approach actually implemented, the true `files_modified` set (especially if approach B differed from this plan's list), and the CWV delta if approach A.
</output>
