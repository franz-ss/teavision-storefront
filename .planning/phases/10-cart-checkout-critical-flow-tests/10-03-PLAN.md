---
phase: 10-cart-checkout-critical-flow-tests
roadmap_status: standalone_phase_5_follow_up
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/(storefront)/cart/page.tsx
  - src/app/(storefront)/cart/_components/cart-view.tsx
  - src/app/(storefront)/cart/_components/cart-view.stories.tsx
  - src/app/(storefront)/cart/_components/cart-line-actions.stories.tsx
  - src/components/product/product-form/product-form.stories.tsx
  - src/components/collection/product-card/product-purchase-form.stories.tsx
  - src/components/product/product-quick-view/product-quick-view.stories.tsx
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-10
must_haves:
  truths:
    - 'Cart rendering is extracted into a fixture-driven route-only view without changing /cart behavior'
    - 'Cart view stories cover empty, populated, discounted, missing-image, long-title, and checkout-handoff states'
    - 'CartLineActions stories cover update/remove payloads, pending, minimum quantity, and error states'
    - 'Product add-to-cart stories cover PDP, listing purchase form, and quick-view behavior'
  artifacts:
    - path: 'src/app/(storefront)/cart/_components/cart-view.tsx'
      provides: 'Fixture-driven cart rendering boundary'
    - path: 'src/app/(storefront)/cart/_components/cart-view.stories.tsx'
      provides: 'Cart render state stories'
    - path: 'src/app/(storefront)/cart/_components/cart-line-actions.stories.tsx'
      provides: 'Cart line interaction stories'
    - path: 'src/components/product/product-form/product-form.stories.tsx'
      provides: 'PDP add-to-cart behavior stories'
  key_links:
    - from: 'src/app/(storefront)/cart/page.tsx'
      to: 'src/app/(storefront)/cart/_components/cart-view.tsx'
      via: 'CartView extraction'
    - from: 'src/app/(storefront)/cart/_components/cart-view.stories.tsx'
      to: 'tests/fixtures/shopify/cart.ts'
      via: 'typed cart fixtures'
---

<objective>
Add behavior-driven Storybook coverage for cart render states, cart line interactions, and product add-to-cart UI without mocking implementation internals or calling Shopify.
</objective>

<context>
@.planning/phases/10-cart-checkout-critical-flow-tests/10-00-STRATEGY.md
@.planning/phases/10-cart-checkout-critical-flow-tests/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CartView for fixture-driven rendering</name>
  <files>src/app/(storefront)/cart/page.tsx, src/app/(storefront)/cart/_components/cart-view.tsx</files>
  <action>
Move presentational cart rendering from the async page into a route-only `CartView` component that accepts a `Cart | null`. Keep data fetching in `CartContent` and preserve current page behavior.
  </action>
  <verify>
```bash
pnpm typecheck
pnpm build
```
  </verify>
  <acceptance_criteria>
    - `/cart` page output is behaviorally unchanged.
    - `CartView` has no data fetching and no direct cookie access.
    - Checkout link still uses `cart.checkoutUrl`.
  </acceptance_criteria>
  <done>Cart rendering is testable with typed fixtures while route behavior remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add CartView stories</name>
  <files>src/app/(storefront)/cart/_components/cart-view.stories.tsx</files>
  <action>
Add stories for empty cart, single item, multiple items, discounts, null discount title, missing image, long product/variant names, and checkout handoff.
  </action>
  <verify>
```bash
pnpm test:stories
```
  </verify>
  <acceptance_criteria>
    - Empty cart has no checkout link.
    - Populated cart checkout link matches fixture `checkoutUrl`.
    - Shipping/tax copy is visible.
    - Discount rows render title or `Discount` fallback.
  </acceptance_criteria>
  <done>Cart page visual and handoff states are covered through Storybook fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Extend CartLineActions stories</name>
  <files>src/app/(storefront)/cart/_components/cart-line-actions.stories.tsx</files>
  <action>
Add behavior stories for decrease payload, increase payload, remove payload, update pending, remove pending, minimum quantity, and action error using the injected `action` prop.
  </action>
  <verify>
```bash
pnpm test:stories
```
  </verify>
  <acceptance_criteria>
    - Assertions use roles/names and captured action results.
    - Pending state disables relevant controls.
    - Error state renders `role="alert"`.
  </acceptance_criteria>
  <done>Cart line update/remove UI behavior is covered without relying on hidden-input implementation details.</done>
</task>

<task type="auto">
  <name>Task 4: Extend product add-to-cart stories</name>
  <files>src/components/product/product-form/product-form.stories.tsx, src/components/collection/product-card/product-purchase-form.stories.tsx, src/components/product/product-quick-view/product-quick-view.stories.tsx</files>
  <action>
Add or extend stories for selected variant payload, selected quantity payload, bulk-tier add payload, sold-out disabled state, max quantity feedback, pending, success, error, feedback reset on variant/quantity changes, quick-view fetch failure, invalid response, retry success, and add failure recovery.
  </action>
  <verify>
```bash
pnpm test:stories
pnpm build-storybook
```
  </verify>
  <acceptance_criteria>
    - Stories use injected `addToCart` and `onCartChanged` functions.
    - No story calls Shopify.
    - Global fetch mocks in quick-view stories are restored.
    - Assertions use visible behavior and accessible roles/names.
  </acceptance_criteria>
  <done>Product add-to-cart UI coverage spans PDP, listing purchase form, and quick view.</done>
</task>

</tasks>

<verification>
```bash
pnpm lint
pnpm typecheck
pnpm test:stories
pnpm build-storybook
pnpm build
```
</verification>

<success_criteria>

- Cart UI is fixture-testable.
- Storybook covers critical cart and add-to-cart interaction states.
- Tests remain behavior-driven and avoid snapshots/classes.
  </success_criteria>
