---
phase: 10-cart-checkout-critical-flow-tests
roadmap_status: standalone_phase_5_follow_up
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/shopify/client.test.ts
  - src/lib/shopify/operations/cart.test.ts
  - src/lib/cart/actions.test.ts
  - src/app/api/products/[handle]/quick-view/route.test.ts
  - src/lib/cart/actions.ts
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-10
must_haves:
  truths:
    - 'shopifyFetch transport behavior is tested without live Shopify'
    - 'Cart operations are tested for variables, no-store behavior, userErrors, checkoutUrl, costs, and discounts'
    - 'Cart Server Actions are tested for cookie lifecycle, add/update/remove, form intents, quantity policy, stale carts, and safe errors'
    - 'Quick-view route integration coverage exists'
  artifacts:
    - path: 'src/lib/shopify/client.test.ts'
      provides: 'Transport tests'
    - path: 'src/lib/shopify/operations/cart.test.ts'
      provides: 'Cart operation and mapper tests'
    - path: 'src/lib/cart/actions.test.ts'
      provides: 'Cart Server Action tests'
    - path: 'src/app/api/products/[handle]/quick-view/route.test.ts'
      provides: 'Quick-view route tests'
  key_links:
    - from: 'src/lib/cart/actions.test.ts'
      to: 'tests/fixtures/shopify/cart.ts'
      via: 'typed cart fixtures'
    - from: 'src/lib/shopify/operations/cart.test.ts'
      to: 'src/lib/shopify/queries/cart.graphql'
      via: 'cart fragment contract'
---

<objective>
Cover the cart Server Action, Shopify operation, transport, and quick-view route boundaries where most revenue-critical business logic and failure handling live.
</objective>

<context>
@.planning/phases/10-cart-checkout-critical-flow-tests/10-00-STRATEGY.md
@.planning/phases/10-cart-checkout-critical-flow-tests/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test shopifyFetch transport behavior</name>
  <files>src/lib/shopify/client.test.ts, src/lib/shopify/client.ts</files>
  <action>
Add tests for missing credentials, test endpoint override, non-OK HTTP, GraphQL errors, successful data return, variable serialization, and TypedDocumentNode printing.
  </action>
  <verify>
```bash
pnpm test:unit -- src/lib/shopify/client.test.ts
```
  </verify>
  <acceptance_criteria>
    - Missing credentials fail fast without override.
    - Test override routes to fake endpoint.
    - HTTP and GraphQL failures throw safe, useful errors.
  </acceptance_criteria>
  <done>Shopify transport behavior is locked without requiring live Shopify.</done>
</task>

<task type="auto">
  <name>Task 2: Test Shopify cart operations and mapping</name>
  <files>src/lib/shopify/operations/cart.test.ts</files>
  <action>
Add tests for `getCart`, `createCart`, `addCartLines`, `updateCartLines`, and `removeCartLines`. Cover variables, `cache: 'no-store'`, `userErrors`, missing cart, checkout URL, totals, line costs, missing images, and automatic/custom/code/null discount allocations.
  </action>
  <verify>
```bash
pnpm test:unit -- src/lib/shopify/operations/cart.test.ts
```
  </verify>
  <acceptance_criteria>
    - Every cart operation passes expected variables.
    - `cache: 'no-store'` is asserted.
    - Discount allocation variants are covered.
    - The `lines(first: 100)` cap is documented by a contract assertion or comment in the test.
  </acceptance_criteria>
  <done>Shopify cart operation tests cover the mapper and mutation boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Test cart Server Actions</name>
  <files>src/lib/cart/actions.test.ts, src/lib/cart/actions.ts</files>
  <action>
Add tests for cart cookie lifecycle, add, update, remove, form intents, invalid form data, stale null cart recovery, thrown stale cart behavior, Shopify max quantity errors, generic errors, and `/cart` revalidation. Extract pure helpers only if necessary to keep tests maintainable.
  </action>
  <verify>
```bash
pnpm test:integration -- src/lib/cart/actions.test.ts
```
  </verify>
  <acceptance_criteria>
    - Missing cookie add creates cart and sets cookie.
    - Existing cookie add reuses cart.
    - Stale null cart deletes cookie and creates a replacement.
    - Quantity cases cover `0`, negative, `NaN`, decimal, and non-finite values with an explicit documented expectation.
    - Form action returns safe messages for invalid and thrown cases.
  </acceptance_criteria>
  <done>Cart Server Action behavior and failure handling are covered at the boundary users depend on.</done>
</task>

<task type="auto">
  <name>Task 4: Test quick-view route integration</name>
  <files>src/app/api/products/[handle]/quick-view/route.test.ts</files>
  <action>
Add route-handler tests for successful quick-view JSON, product-not-found 404, Shopify/product fetch failure 503, and response shape expectations for client quick-view consumption.
  </action>
  <verify>
```bash
pnpm test:integration -- "src/app/api/products/[handle]/quick-view/route.test.ts"
```
  </verify>
  <acceptance_criteria>
    - Success response contains only `ProductQuickViewDetails` fields.
    - Missing product returns 404.
    - Thrown product fetch returns 503.
  </acceptance_criteria>
  <done>Quick-view route integration coverage matches the strategy matrix.</done>
</task>

</tasks>

<verification>
```bash
pnpm test:unit
pnpm test:integration
pnpm typecheck
```
</verification>

<success_criteria>

- Server and Shopify boundary tests cover P0 cart logic.
- Quick-view route integration promise is fulfilled.
- Tests do not import generated Shopify types directly.
  </success_criteria>
