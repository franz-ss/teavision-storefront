---
phase: 14-shopify-customer-accounts
plan: 05
type: execute
wave: 4
depends_on:
  - "14-01"
  - "14-02"
  - "14-03"
  - "14-04"
files_modified:
  - "src/components/layout/header/header.tsx"
  - "src/components/layout/footer/data.ts"
  - "src/app/(storefront)/account/register/page.tsx"
  - "src/app/(storefront)/account/recover/page.tsx"
  - "src/app/(storefront)/account/reset/[id]/page.tsx"
  - "src/app/(storefront)/account/activate/[id]/page.tsx"
  - "src/app/(storefront)/account/[...legacy]/page.tsx"
  - "src/app/(storefront)/account/_components/legacy-bridge.tsx"
  - "src/app/(storefront)/account/_components/legacy-bridge.stories.tsx"
  - "src/app/(storefront)/account/_lib/legacy-routing.ts"
  - "src/app/(storefront)/account/_lib/legacy-routing.test.ts"
  - "docs/testing/customer-accounts-setup.md"
  - ".env.example"
  - "tests/e2e/cart-checkout.spec.ts"
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - SEC-04
must_haves:
  truths:
    - "MIG-01 is covered by header/footer account links pointing to owned headless account routes."
    - "MIG-02 is covered by classic account bridge/redirect pages with safe parameter handling."
    - "MIG-03 is covered by documented reorder and B2B/customer-pricing parity decisions."
    - "SEC-04 is covered by setup/readiness documentation and explicit real-checkout approval gates."
    - "D-27: Reorder is documented and deferred unless it can safely use authoritative Shopify cart actions for currently available variants without price promises."
    - "D-37 D-38 D-39 D-40 D-41 D-42 D-43 define legacy route preservation, stale mrtea login replacement, classic OAuth bridge, reorder/B2B documentation, stable `/account` entry, and safe context parameters."
    - "D-45 D-46 D-47 D-55 D-56 D-57 define header account icon placement, `/account` link behavior, no duplicate mobile row, classic route bridges, explanatory fallback, and visible account entry when setup is missing."
  artifacts:
    - path: "src/components/layout/header/header.tsx"
      provides: "Header account icon linking to `/account`"
    - path: "src/components/layout/footer/data.ts"
      provides: "Footer Login link replacement"
    - path: "src/app/(storefront)/account/_lib/legacy-routing.ts"
      provides: "Safe legacy account return/context parameter normalization"
    - path: "docs/testing/customer-accounts-setup.md"
      provides: "Launch readiness and manual approval gate"
  key_links:
    - from: "src/components/layout/header/header.tsx"
      to: "src/app/(storefront)/account/page.tsx"
      via: "stable account entry route"
    - from: "docs/testing/customer-accounts-setup.md"
      to: ".planning/REQUIREMENTS.md"
      via: "SEC-04 setup prerequisite coverage"
---

<objective>
Finish Phase 14 migration and launch readiness: header/footer account entry, legacy account bridge routes, safe return/context parameters, reorder/B2B parity documentation, setup checklist, and final fake-only browser coverage.
</objective>

<context>
@.planning/phases/teavision-14-shopify-customer-accounts/14-CONTEXT.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-RESEARCH.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-UI-SPEC.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-VALIDATION.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-PATTERNS.md
</context>

<threat_model>
- T-14-04 Open redirect, severity high: legacy bridge routes preserve only path-only return/context parameters and ignore external URLs, Liquid/template-only values, and unknown parameters.
- T-14-08 Real checkout boundary breach, severity high: readiness docs and tests keep hosted checkout, payment, shipping-rate, tax, order creation, and success redirect manual-only until explicit store-owner approval.
- T-14-02 Token exposure, severity high: setup docs and bridge pages avoid logging or rendering Customer Account credentials, tokens, protected data, or real customer examples.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add header account icon and replace stale footer login link</name>
  <read_first>
    - src/components/layout/header/header.tsx
    - src/components/layout/footer/data.ts
    - src/components/ui/icon-button/icon-button.tsx
    - .planning/phases/teavision-14-shopify-customer-accounts/14-UI-SPEC.md
  </read_first>
  <files>src/components/layout/header/header.tsx, src/components/layout/footer/data.ts</files>
  <action>
Import `CircleUserRound` or `UserRound` from `lucide-react` and add one account link in the existing header right-side icon cluster beside search and cart. The link must:
- href `/account`
- aria-label `Account`
- use 44px hit target matching the cart link
- use icon class `size-4`
- use `strokeWidth={1.8}`
- use hover/focus classes matching search/cart icon treatment

Do not add a duplicate mobile-menu account row.

Update `FOOTER_LINKS` so visible label `Login` has href `/account` instead of `https://mrtea.com.au/account/login`.
  </action>
  <verify>
```bash
pnpm lint
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - `header.tsx` contains `aria-label="Account"`.
    - `header.tsx` contains `href="/account"`.
    - `header.tsx` contains `strokeWidth={1.8}` on the account icon.
    - `footer/data.ts` contains `{ href: '/account', label: 'Login' }`.
    - No mobile mega-nav file is modified to add a second account row.
  </acceptance_criteria>
  <done>Header and footer route customers to the owned headless account entry.</done>
</task>

<task type="auto">
  <name>Task 2: Add legacy account bridge routes and safe parameter helpers</name>
  <read_first>
    - src/app/(storefront)/pages/search-results/page.tsx
    - src/app/(storefront)/pages/search-results-page/page.tsx
    - src/app/(storefront)/account/login/page.tsx
    - src/app/(storefront)/account/_lib/return-path.ts
  </read_first>
  <files>src/app/(storefront)/account/register/page.tsx, src/app/(storefront)/account/recover/page.tsx, src/app/(storefront)/account/reset/[id]/page.tsx, src/app/(storefront)/account/activate/[id]/page.tsx, src/app/(storefront)/account/[...legacy]/page.tsx, src/app/(storefront)/account/_components/legacy-bridge.tsx, src/app/(storefront)/account/_components/legacy-bridge.stories.tsx, src/app/(storefront)/account/_lib/legacy-routing.ts, src/app/(storefront)/account/_lib/legacy-routing.test.ts</files>
  <action>
Create `legacy-routing.ts` with:
- `normalizeLegacyAccountReturn(value: string | null): string`
- `getLegacyAccountBridgeCopy(slug: string[]): { heading: string; body: string; primaryHref: string }`

Allowed preserved parameters are path-only `returnTo`, `checkout_url`, and `redirect`. External, protocol-relative, Liquid/template-looking, and unknown parameters normalize to `/account`.

Create bridge pages for:
- `/account/register`
- `/account/recover`
- `/account/reset/[id]`
- `/account/activate/[id]`
- catch-all `/account/[...legacy]`

Bridge copy must explain that Teavision now uses Shopify-hosted Customer Account sign-in and must not render classic username/password forms. Primary action href starts with `/account/login/start`.
  </action>
  <verify>
```bash
pnpm test:unit -- "src/app/(storefront)/account/_lib/legacy-routing.test.ts"
pnpm test:stories
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - `legacy-routing.test.ts` asserts `https://evil.example` normalizes to `/account`.
    - `legacy-routing.test.ts` asserts `/cart` is preserved.
    - `legacy-bridge.tsx` contains `Shopify-hosted Customer Account sign-in`.
    - Bridge pages do not contain `type="password"`.
    - `legacy-bridge.stories.tsx` exports register, recover, verification-failed, and unknown-route states.
  </acceptance_criteria>
  <done>Legacy account URLs route into modern OAuth with explanatory copy and safe parameters.</done>
</task>

<task type="auto">
  <name>Task 3: Document setup prerequisites, manual approvals, reorder, and B2B parity</name>
  <read_first>
    - .env.example
    - .planning/REQUIREMENTS.md
    - .planning/phases/teavision-14-shopify-customer-accounts/14-RESEARCH.md
    - docs/testing/cart-checkout-refinement-rationale.md
    - docs/teavision-project-reference.md
  </read_first>
  <files>docs/testing/customer-accounts-setup.md, .env.example</files>
  <action>
Create `docs/testing/customer-accounts-setup.md` with sections:
- `## Required Shopify Admin Setup`
- `## Environment Variables`
- `## Local HTTPS OAuth Testing`
- `## Protected Customer Data Access`
- `## Automated Test Boundary`
- `## Manual Approval Gate`
- `## Reorder Parity`
- `## B2B And Customer Pricing Parity`
- `## Launch Checklist`

The manual approval gate must state that real Shopify hosted checkout, payment, shipping-rate, tax, order creation, and success redirect tests are blocked until the store owner explicitly approves checkout testing.

Reorder parity must state it is deferred unless a future implementation uses authoritative cart actions, current variant availability, and no price promises.

B2B/customer pricing parity must state it is Shopify-admin-dependent and checkout/cart-authoritative; the headless UI must not calculate or promise client-side customer-specific prices.
  </action>
  <verify>
```bash
pnpm lint
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - `docs/testing/customer-accounts-setup.md` contains `## Manual Approval Gate`.
    - `docs/testing/customer-accounts-setup.md` contains `hosted checkout, payment, shipping-rate, tax, order creation, and success redirect`.
    - `docs/testing/customer-accounts-setup.md` contains `Reorder Parity`.
    - `docs/testing/customer-accounts-setup.md` contains `B2B And Customer Pricing Parity`.
    - `.env.example` contains every Customer Account env var from Plan 01.
  </acceptance_criteria>
  <done>Operators have explicit setup, parity, and approval documentation for launch readiness.</done>
</task>

<task type="auto">
  <name>Task 4: Add final migration browser coverage and route smoke checks</name>
  <read_first>
    - tests/e2e/cart-checkout.spec.ts
    - src/components/layout/footer/data.ts
    - src/components/layout/header/header.tsx
    - src/app/(storefront)/account/_components/legacy-bridge.tsx
  </read_first>
  <files>tests/e2e/cart-checkout.spec.ts, src/app/(storefront)/account/**/*.test.tsx</files>
  <action>
Extend fake-browser or integration coverage to assert:
- header account link points to `/account`
- footer Login link points to `/account`
- `/account/register` shows bridge copy and primary Shopify sign-in action
- unknown legacy account route shows explanatory fallback instead of generic 404
- account checkout tests still stop at `https://checkout.test/`

Use the existing fake Shopify/Customer Account test infrastructure only.
  </action>
  <verify>
```bash
pnpm test:integration -- "src/app/(storefront)/account/**/*.test.tsx"
pnpm test:e2e
pnpm lint
```
  </verify>
  <acceptance_criteria>
    - Tests assert footer Login href `/account`.
    - Tests assert header account link accessible name `Account`.
    - Tests assert `/account/register` does not render password inputs.
    - Tests assert unknown legacy account route renders the account-focused bridge copy.
    - E2E tests still assert fake checkout URL under `https://checkout.test/`.
  </acceptance_criteria>
  <done>Migration surfaces and browser smoke coverage are ready for final verification.</done>
</task>

</tasks>

<verification>
```bash
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm test:integration
pnpm test:stories
pnpm test:e2e
pnpm build
```
</verification>

<success_criteria>
- Header and footer account links route to `/account`.
- Classic account URLs bridge or redirect to modern OAuth without password forms.
- Unknown legacy account URLs show an account-focused explanatory page, not a generic 404.
- Reorder and B2B/customer-pricing parity are explicitly implemented, deferred, or admin-dependent in docs.
- Launch readiness docs include Customer Account setup and the blocked real-checkout approval gate.
</success_criteria>
