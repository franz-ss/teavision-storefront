---
phase: 14-shopify-customer-accounts
plan: 04
type: execute
wave: 3
depends_on:
  - "14-01"
files_modified:
  - "src/lib/shopify/queries/cart.graphql"
  - "src/lib/shopify/operations/cart.ts"
  - "src/lib/shopify/operations/cart.test.ts"
  - "src/lib/shopify/types/generated/**"
  - "src/lib/cart/actions.ts"
  - "src/lib/cart/actions.test.ts"
  - "src/app/(storefront)/cart/page.tsx"
  - "src/app/(storefront)/cart/checkout/route.ts"
  - "src/app/(storefront)/cart/checkout/route.test.ts"
  - "src/app/(storefront)/cart/_components/checkout-form.tsx"
  - "src/app/(storefront)/cart/_components/account-context.tsx"
  - "src/app/(storefront)/cart/_components/account-context.stories.tsx"
  - "src/app/(storefront)/cart/_components/view.tsx"
  - "src/app/(storefront)/cart/_components/view.stories.tsx"
  - "src/app/(storefront)/account/callback/route.ts"
  - "src/app/(storefront)/account/logout/route.ts"
  - "tests/mocks/shopify-graphql-server.ts"
  - "tests/fixtures/shopify/cart.ts"
  - "tests/e2e/cart-checkout.spec.ts"
autonomous: true
requirements:
  - CART-01
  - CART-02
must_haves:
  truths:
    - "CART-01 is implemented by login-time cart buyer identity sync, signed-in cart creation, and pre-checkout sync."
    - "CART-02 is preserved because automated coverage uses fake-Shopify tests only and stops at fake checkout handoff."
    - "D-29 D-30 D-31 D-32 D-33 D-34 D-35 D-36 define buyer identity sync timing, blocked checkout, subtle cart context, logout behavior, authoritative pricing, fake test boundary, and no empty-cart creation on sign-in."
    - "D-50 D-51 D-52 D-53 D-54 define no silent guest fallback, retry-first recovery, secondary sign-in/support actions, subtle account context, and no manual company selector."
  artifacts:
    - path: "src/lib/shopify/operations/cart.ts"
      provides: "Buyer identity cart operations"
    - path: "src/app/(storefront)/cart/checkout/route.ts"
      provides: "Pre-checkout identity sync boundary"
    - path: "src/app/(storefront)/cart/_components/account-context.tsx"
      provides: "Compact signed-in/blocked checkout UI"
    - path: "tests/e2e/cart-checkout.spec.ts"
      provides: "Fake-Shopify browser coverage"
  key_links:
    - from: "src/app/(storefront)/account/callback/route.ts"
      to: "src/lib/cart/actions.ts"
      via: "sync existing cart after login"
    - from: "src/app/(storefront)/cart/checkout/route.ts"
      to: "src/lib/shopify/operations/cart.ts"
      via: "pre-checkout buyer identity update"
---

<objective>
Connect authenticated customer identity to cart and checkout handoff: Storefront buyer identity mutation, identity-aware cart creation, login-time sync, pre-checkout sync route, blocked checkout recovery UI, logout cart preservation, and fake-Shopify coverage only.
</objective>

<context>
@.planning/phases/teavision-14-shopify-customer-accounts/14-CONTEXT.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-RESEARCH.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-UI-SPEC.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-VALIDATION.md
@.planning/phases/teavision-14-shopify-customer-accounts/14-PATTERNS.md
</context>

<threat_model>
- T-14-06 Cart identity mismatch, severity high: signed-in checkout handoff must not proceed if buyer identity sync fails.
- T-14-08 Real checkout boundary breach, severity high: automated tests use fake Storefront/checkout URLs only and do not run live checkout/payment/tax/shipping/order/success-redirect flows.
- T-14-02 Token exposure, severity high: Customer Account access token is used server-side for Storefront buyer identity and never serialized into cart UI or browser JavaScript.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Storefront buyer identity GraphQL and generated types</name>
  <read_first>
    - src/lib/shopify/queries/cart.graphql
    - src/lib/shopify/operations/cart.ts
    - src/lib/shopify/types/index.ts
    - codegen.ts
    - .env.example
  </read_first>
  <files>src/lib/shopify/queries/cart.graphql, src/lib/shopify/types/generated/**, src/lib/shopify/types/index.ts</files>
  <action>
Update `cart.graphql`:
- Add `buyerIdentity { customer { id emailAddress { emailAddress } } email phone countryCode companyLocationId }` only if supported by the Storefront schema version.
- Change `CartCreate` variables to keep `CartInput!` and allow `buyerIdentity` inside the existing input.
- Add mutation `CartBuyerIdentityUpdate($cartId: ID!, $buyerIdentity: CartBuyerIdentityInput!)` returning `cart { ...CartFields }` and `userErrors { field message }`.

Run `pnpm codegen` after the GraphQL edit. If codegen cannot run because live Shopify credentials are missing, stop the implementation and report the missing credential gate; do not hand-edit generated files.
  </action>
  <verify>
```bash
pnpm codegen
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - `src/lib/shopify/queries/cart.graphql` contains `mutation CartBuyerIdentityUpdate`.
    - `src/lib/shopify/queries/cart.graphql` contains `CartBuyerIdentityInput`.
    - `src/lib/shopify/types/generated/graphql.ts` contains `CartBuyerIdentityUpdate`.
    - `src/lib/shopify/types/index.ts` remains the public import barrel for Storefront types.
  </acceptance_criteria>
  <done>Storefront buyer identity operations are available through generated types.</done>
</task>

<task type="auto">
  <name>Task 2: Extend cart operations and fake Storefront server</name>
  <read_first>
    - src/lib/shopify/operations/cart.ts
    - src/lib/shopify/operations/cart.test.ts
    - tests/mocks/shopify-graphql-server.ts
    - tests/fixtures/shopify/cart.ts
  </read_first>
  <files>src/lib/shopify/operations/cart.ts, src/lib/shopify/operations/cart.test.ts, tests/mocks/shopify-graphql-server.ts, tests/fixtures/shopify/cart.ts</files>
  <action>
Add cart operation types:
- `type CartBuyerIdentity = { customerAccessToken?: string; email?: string; phone?: string; countryCode?: string; companyLocationId?: string }`.
- `createCart(input?: { buyerIdentity?: CartBuyerIdentity }): Promise<Cart>`.
- `syncCartBuyerIdentity(cartId: string, buyerIdentity: CartBuyerIdentity): Promise<Cart>`.
- `tryClearCartBuyerIdentity(cartId: string): Promise<'cleared' | 'unsupported'>`.

`createCart()` must keep existing behavior when called without input and pass `{ input: {} }`.
`createCart({ buyerIdentity })` must pass `{ input: { buyerIdentity } }`.
`syncCartBuyerIdentity()` must call `CartBuyerIdentityUpdateDocument` with `cache: 'no-store'` and use existing `handleUserErrors`.
`tryClearCartBuyerIdentity()` must preserve cart lines and return `unsupported` when Storefront rejects clearing buyer identity, without logging PII.

Extend fake Shopify server to record `buyerIdentity` on `CartCreate` and `CartBuyerIdentityUpdate`, expose a deterministic failure mode for identity sync, and keep fake checkout URLs under `https://checkout.test/`.
  </action>
  <verify>
```bash
pnpm test:unit -- src/lib/shopify/operations/cart.test.ts
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - `cart.ts` exports `syncCartBuyerIdentity`.
    - `cart.ts` exports `tryClearCartBuyerIdentity`.
    - `createCart()` tests still assert variables `{ input: {} }` for anonymous carts.
    - New tests assert identity-aware `createCart` passes `buyerIdentity.customerAccessToken`.
    - Fake server handles operation `CartBuyerIdentityUpdate`.
  </acceptance_criteria>
  <done>Cart operations can associate signed-in customers without regressing anonymous cart behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add account-aware cart actions and OAuth sync hooks</name>
  <read_first>
    - src/lib/cart/actions.ts
    - src/lib/cart/actions.test.ts
    - src/lib/shopify/customer-account/session.ts
    - src/app/(storefront)/account/callback/route.ts
    - src/app/(storefront)/account/logout/route.ts
  </read_first>
  <files>src/lib/cart/actions.ts, src/lib/cart/actions.test.ts, src/app/(storefront)/account/callback/route.ts, src/app/(storefront)/account/logout/route.ts</files>
  <action>
Add server-side helpers in `src/lib/cart/actions.ts`:
- `getCartIdFromCookie(): Promise<string | null>`.
- `syncCartBuyerIdentityForCurrentSession(): Promise<{ synced: boolean; message: string | null }>` that returns `{ synced: false, message: null }` when no cart exists and does not create an empty cart.
- `prepareCheckoutAction(): Promise<never>` or route-shared helper that verifies terms, reads the cart, syncs buyer identity when signed in, blocks on sync failure, and redirects only after success.

Update `getOrCreateCart()` so signed-in customers create carts with buyer identity when a valid session is available; anonymous behavior remains unchanged.

Update OAuth callback route to call `syncCartBuyerIdentityForCurrentSession()` after session creation when a cart cookie exists. Update logout route to preserve cart lines and call `tryClearCartBuyerIdentity(cartId)` when possible.
  </action>
  <verify>
```bash
pnpm test:integration -- src/lib/cart/actions.test.ts "src/app/(storefront)/account/**/*.test.ts"
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - `syncCartBuyerIdentityForCurrentSession` does not call `createCart` when no cart cookie exists.
    - `addToCartAction` tests include a signed-in case where `createCart` receives `buyerIdentity.customerAccessToken`.
    - OAuth callback tests assert an existing cart cookie triggers buyer identity sync after session cookie creation.
    - Logout tests assert cart cookie remains after customer session cookie is cleared.
    - No cart action returns a customer access token.
  </acceptance_criteria>
  <done>Cart state stays server-owned while signed-in customer identity is synced at the right moments.</done>
</task>

<task type="auto">
  <name>Task 4: Replace checkout anchor with pre-checkout sync boundary and blocked state</name>
  <read_first>
    - src/app/(storefront)/cart/page.tsx
    - src/app/(storefront)/cart/_components/checkout-form.tsx
    - src/app/(storefront)/cart/_components/view.tsx
    - src/lib/cart/actions.ts
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md
  </read_first>
  <files>src/app/(storefront)/cart/page.tsx, src/app/(storefront)/cart/checkout/route.ts, src/app/(storefront)/cart/checkout/route.test.ts, src/app/(storefront)/cart/_components/checkout-form.tsx, src/app/(storefront)/cart/_components/account-context.tsx, src/app/(storefront)/cart/_components/account-context.stories.tsx, src/app/(storefront)/cart/_components/view.tsx, src/app/(storefront)/cart/_components/view.stories.tsx</files>
  <action>
Create `POST /cart/checkout` as the checkout handoff boundary. It must:
- read `teavision_cart`
- reject missing cart by redirecting to `/cart?checkout=missing-cart`
- require a submitted terms checkbox value
- call pre-checkout buyer identity sync when a customer session exists
- redirect to `/cart?checkout=identity-sync-failed` when sync fails
- redirect to the fake/live cart checkout URL only when sync succeeds or no session exists

Update `CartCheckoutForm` to submit a form to `/cart/checkout` instead of linking directly to `checkoutUrl`. Keep the terms checkbox client leaf and stable button label `Check Out`.

Add `CartAccountContext` above checkout actions with states:
- signed-in: `Checking out with your Teavision account`
- sync pending: `Confirming your account for checkout`
- sync failed: `We could not confirm your account for checkout. Retry checkout or sign in again before continuing.`

Blocked checkout state must show primary `Retry checkout`, secondary `Sign in again`, and `Contact support`. Do not offer guest checkout fallback.
  </action>
  <verify>
```bash
pnpm test:integration -- "src/app/(storefront)/cart/checkout/route.test.ts"
pnpm test:stories
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - `checkout-form.tsx` no longer renders an anchor with `href={checkoutUrl}` for checkout.
    - `cart/checkout/route.ts` redirects to `/cart?checkout=identity-sync-failed` on sync failure.
    - `account-context.tsx` contains `Checking out with your Teavision account`.
    - `account-context.stories.tsx` exports signed-in, sync-pending, and sync-failed-blocked stories.
    - Blocked checkout UI contains `Retry checkout`, `Sign in again`, and `Contact support`.
  </acceptance_criteria>
  <done>Checkout handoff verifies buyer identity before leaving the headless cart and blocks recoverably on failure.</done>
</task>

<task type="auto">
  <name>Task 5: Extend fake-Shopify E2E coverage without live checkout</name>
  <read_first>
    - tests/e2e/cart-checkout.spec.ts
    - tests/mocks/shopify-graphql-server.ts
    - playwright.config.ts
    - AGENTS.md
  </read_first>
  <files>tests/e2e/cart-checkout.spec.ts, tests/mocks/shopify-graphql-server.ts, tests/fixtures/shopify/cart.ts</files>
  <action>
Extend `tests/e2e/cart-checkout.spec.ts` with fake-only cases:
- signed-in customer reaches `/cart`, sees account context, submits terms, and is handed to `https://checkout.test/cart/fake-cart`.
- buyer identity sync failure keeps the browser on `/cart?checkout=identity-sync-failed`, shows blocked recovery actions, and never visits a real Shopify checkout URL.

Use fake Customer Account session setup helpers from Plan 01 fixtures; do not use live Shopify credentials or hosted checkout.
  </action>
  <verify>
```bash
pnpm test:e2e
pnpm lint
```
  </verify>
  <acceptance_criteria>
    - `cart-checkout.spec.ts` contains `identity-sync-failed`.
    - `cart-checkout.spec.ts` asserts checkout href or navigation starts with `https://checkout.test/`.
    - No E2E test URL contains `myshopify.com/checkouts` or `checkout.shopify.com`.
    - Fake server exposes a deterministic way to force `CartBuyerIdentityUpdate` userErrors.
  </acceptance_criteria>
  <done>Cart-to-checkout account coverage remains fully fake and respects the real-checkout testing ban.</done>
</task>

</tasks>

<verification>
```bash
pnpm codegen
pnpm lint
pnpm typecheck
pnpm test:unit -- src/lib/shopify/operations/cart.test.ts
pnpm test:integration -- src/lib/cart/actions.test.ts "src/app/(storefront)/cart/checkout/route.test.ts" "src/app/(storefront)/account/**/*.test.ts"
pnpm test:stories
pnpm test:e2e
```
</verification>

<success_criteria>
- Existing anonymous cart behavior and cart cookie ownership remain intact.
- Signed-in carts receive buyer identity at creation, after login when a cart exists, and before checkout.
- Checkout handoff blocks when signed-in buyer identity sync fails.
- Logout clears account session and preserves cart lines.
- Automated cart/checkout coverage uses fake Shopify only and never exercises hosted checkout.
</success_criteria>

