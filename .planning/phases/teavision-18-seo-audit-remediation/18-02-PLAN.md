---
phase: 18-seo-audit-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 'src/app/(storefront)/collections/[handle]/_components/hero.tsx'
  - 'src/app/(storefront)/collections/[handle]/_components/page-content.tsx'
  - 'src/app/(storefront)/collections/[handle]/_components/product-list.tsx'
  - 'src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx'
  - 'src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts'
  - 'src/app/(storefront)/collections/[handle]/_lib/page-helpers.test.ts'
  - 'src/lib/shopify/html-content.ts'
  - 'src/lib/shopify/html-content.test.ts'
  - 'src/app/(storefront)/products/[handle]/page.tsx'
  - 'src/app/(storefront)/products/[handle]/page.test.tsx'
autonomous: true
requirements:
  - SEO-AUDIT-02
  - SEO-AUDIT-03
  - SEO-AUDIT-06
user_setup: []
must_haves:
  truths:
    - 'SEO-AUDIT-02: representative collection and product pages render exactly one visible h1 in crawlable HTML.'
    - 'SEO-AUDIT-03: long collection read-more/story content renders after the product grid in DOM order.'
    - 'SEO-AUDIT-06: collection and product routes keep SEO-critical title, intro/description, product/buy context, metadata, canonical, and JSON-LD server-rendered.'
    - 'D-09 D-10: banner collection pages show visible breadcrumb, H1, brief intro, product grid, then long story content.'
    - 'D-11 D-12: imported Shopify rich content cannot emit h1 or h2 inside collection read-more or PDP description content.'
    - 'D-20 D-21: broad skeleton shells do not mask crawl-critical collection/PDP content in first-response HTML.'
  artifacts:
    - path: 'src/app/(storefront)/collections/[handle]/_components/hero.tsx'
      provides: 'Visible banner collection H1 and brief intro'
      contains: '<h1'
    - path: 'src/app/(storefront)/collections/[handle]/_components/page-content.tsx'
      provides: 'Collection story content below the product grid'
      contains: 'StoryDisclosure'
    - path: 'src/lib/shopify/html-content.ts'
      provides: 'Imported rich-content heading demotion'
      contains: 'buildHeadingTransforms'
    - path: 'src/app/(storefront)/products/[handle]/page.test.tsx'
      provides: 'Representative PDP one-H1 and description-heading regression coverage'
  key_links:
    - from: 'src/app/(storefront)/collections/[handle]/_components/page-content.tsx'
      to: 'src/app/(storefront)/collections/[handle]/_components/product-list.tsx'
      via: 'story disclosure renders after ProductList'
      pattern: "ProductList[\\s\\S]*StoryDisclosure"
    - from: 'src/app/(storefront)/products/[handle]/page.tsx'
      to: 'src/lib/shopify/html-content.ts'
      via: 'PDP description uses compact sanitizer'
      pattern: 'sanitizeShopifyCompactHtml'
---

<objective>
Fix visible heading hierarchy and collection story placement for collection and product pages.

Purpose: The audit flagged hidden/multiple H1s and above-grid read-more content. This plan makes the crawler-visible document hierarchy match the approved Phase 18 UI contract without converting SEO-critical routes to client-rendered shells.

Output: Collection banner H1s become visible, long collection story content moves below the product grid, imported rich content heading levels are safe, and focused tests prove collection/PDP one-H1 behavior.
</objective>

<execution_context>
@AGENTS.md
@node_modules/next/dist/docs/01-app/02-guides/streaming.md
@node_modules/next/dist/docs/01-app/02-guides/migrating-to-cache-components.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/cacheComponents.md
@.planning/phases/teavision-18-seo-audit-remediation/18-CONTEXT.md
@.planning/phases/teavision-18-seo-audit-remediation/18-RESEARCH.md
@.planning/phases/teavision-18-seo-audit-remediation/18-UI-SPEC.md
@.planning/phases/teavision-18-seo-audit-remediation/18-VALIDATION.md
@.planning/phases/teavision-18-seo-audit-remediation/18-PATTERNS.md
@src/app/(storefront)/collections/[handle]/\_components/page-content.tsx
@src/app/(storefront)/collections/[handle]/\_components/hero.tsx
@src/app/(storefront)/products/[handle]/page.tsx
@src/lib/shopify/html-content.ts
</execution_context>

<threat_model>
Trust boundaries:

- Shopify/Sanity rich HTML crosses into storefront rendering through sanitizers.
- Server-rendered collection/PDP HTML crosses into crawler and customer browsers before hydration.

STRIDE register:

- T-18-03 tampering: imported rich content can inject competing headings. Mitigation: demote or strip H1/H2 through `normalizeHtml()` and compact rich-text sanitizer tests.
- T-18-04 spoofing: hidden H1s can present a different page topic to crawlers than users see. Mitigation: visible H1 tests count rendered HTML and reject `sr-only` as the only H1 presentation.
- T-18-09 denial-of-service: moving content can break product-grid/pagination anchors. Mitigation: keep `#product-grid`, pagination hrefs, filters, and sidebar scan order covered by existing collection tests.
  </threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make banner collection H1 visible and move long story content below the grid</name>
  <files>src/app/(storefront)/collections/[handle]/_components/hero.tsx, src/app/(storefront)/collections/[handle]/_components/page-content.tsx, src/app/(storefront)/collections/[handle]/_components/product-list.tsx, src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx</files>
  <read_first>
    - src/app/(storefront)/collections/[handle]/_components/hero.tsx
    - src/app/(storefront)/collections/[handle]/_components/page-content.tsx
    - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
    - src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx
    - .planning/phases/teavision-18-seo-audit-remediation/18-UI-SPEC.md
  </read_first>
  <action>Update `Hero` so banner mode no longer renders the collection title only as `<h1 className="sr-only">`. Banner mode must render the image first when present, then breadcrumb, then a visible `<h1>` using existing display-heading utilities and the collection title, followed by the brief `heroDescription` paragraph when non-empty. Remove the `storyDisclosure` prop from `Hero` and render `StoryDisclosure title={"Read more about " + collection.title}` in `PageContent` after the `ProductList` grid inside a transparent `Section.Root` with spacing that follows the UI-SPEC (`mt-10` or section spacing, no raw colors). Keep `id="product-grid"` on the product list and preserve pagination anchors.</action>
  <verify>pnpm test:unit -- "src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx"</verify>
  <acceptance_criteria>
    - `hero.tsx` does not contain `<h1 className="sr-only">{collectionTitle}</h1>`.
    - `hero.tsx` contains a visible `<h1` in the banner branch.
    - `page-content.tsx` renders `ProductList` before `StoryDisclosure` in source order.
    - `page-content.test.tsx` contains an assertion that `product-grid` appears before `Read more about Wholesale Bulk Tea` in rendered HTML.
  </acceptance_criteria>
  <done>Banner collection pages expose a visible H1 and long story/read-more content follows the grid in DOM order.</done>
</task>

<task type="auto">
  <name>Task 2: Prevent imported rich content from creating H1/H2 conflicts</name>
  <files>src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts, src/app/(storefront)/collections/[handle]/_lib/page-helpers.test.ts, src/lib/shopify/html-content.ts, src/lib/shopify/html-content.test.ts, src/app/(storefront)/products/[handle]/page.tsx, src/app/(storefront)/products/[handle]/page.test.tsx</files>
  <read_first>
    - src/app/(storefront)/collections/[handle]/_lib/page-helpers.ts
    - src/app/(storefront)/collections/[handle]/_lib/page-helpers.test.ts
    - src/lib/shopify/html-content.ts
    - src/app/(storefront)/products/[handle]/page.tsx
    - src/app/(storefront)/products/[handle]/_components/related-products.tsx
    - src/app/(storefront)/products/[handle]/_components/customers-also-bought.tsx
  </read_first>
  <action>Tighten compact rich HTML heading transforms so `sanitizeShopifyCompactHtml()` demotes both `<h1>` and `<h2>` to `<h3>` with the compact H3 class. Preserve existing page/article behavior unless tests require a specific safe demotion. Add tests showing collection `normalizeHtml()` converts H1/H2 to H3 and compact Shopify HTML converts both H1 and H2 to H3. Add a PDP server-render test that mocks `getProduct()` with description HTML containing `<h1>` and `<h2>`, renders `ProductPage`/product content to static markup, and proves the product title is the only `<h1>` while imported description headings are `<h3>` or lower.</action>
  <verify>pnpm test:unit -- "src/app/(storefront)/collections/[handle]/_lib/page-helpers.test.ts" "src/lib/shopify/html-content.test.ts" "src/app/(storefront)/products/[handle]/page.test.tsx"</verify>
  <acceptance_criteria>
    - `src/lib/shopify/html-content.test.ts` contains `sanitizeShopifyCompactHtml`.
    - `src/lib/shopify/html-content.test.ts` asserts `<h1` and `<h2` are absent from compact sanitized output.
    - `src/app/(storefront)/products/[handle]/page.test.tsx` counts exactly one `<h1`.
    - `src/app/(storefront)/products/[handle]/page.test.tsx` contains `Only Product Title`.
  </acceptance_criteria>
  <done>Imported rich text cannot introduce extra H1/H2 content on collection or PDP surfaces.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm test:unit -- "src/app/(storefront)/collections/[handle]/_components/page-content.test.tsx" "src/app/(storefront)/collections/[handle]/_lib/page-helpers.test.ts" "src/lib/shopify/html-content.test.ts" "src/app/(storefront)/products/[handle]/page.test.tsx"`
- [ ] `pnpm lint`
</verification>

<success_criteria>

- Banner collection pages have a visible collection H1.
- Collection read-more/story content follows the product grid in DOM order.
- PDP and collection imported content cannot create competing H1/H2 headings.
- Server-rendered SEO-critical collection/PDP content remains in Server Components.
  </success_criteria>

<output>
After completion, create `.planning/phases/teavision-18-seo-audit-remediation/18-02-SUMMARY.md`.
</output>
