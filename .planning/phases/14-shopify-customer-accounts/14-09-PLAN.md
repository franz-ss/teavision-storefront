---
phase: 14-shopify-customer-accounts
plan: 09
type: execute
wave: 5
depends_on:
  - "14-05"
autonomous: true
gap_closure: true
files_modified:
  - "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.tsx"
  - "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.stories.tsx"
  - "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.test.tsx"
requirements:
  - MIG-02
  - MIG-03
  - SEC-04
must_haves:
  truths:
    - "Legacy account bridge pages use compact-card heading typography instead of oversized page-heading typography."
    - "Register/recover headings do not wrap awkwardly inside the compact bridge card."
    - "Register/recover bridge stories prove heading fit at desktop and mobile Storybook viewports."
    - "Legacy bridge pages still explain Shopify Customer Accounts, preserve safe sign-in paths, and never show password inputs."
    - "Storybook covers register, recover, verification-failed, and unknown-route bridge states after visual polish."
  artifacts:
    - path: "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.tsx"
      provides: "Polished account bridge surface"
    - path: "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.stories.tsx"
      provides: "Bridge state visual coverage"
    - path: "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.test.tsx"
      provides: "No-password and sign-in-link regression coverage"
  key_links:
    - from: "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.tsx"
      to: "src/app/(storefront)/account/_lib/legacy-routing.ts"
      via: "heading/body/primaryHref props"
      pattern: "heading|body|primaryHref"
---

<objective>
Close the legacy account bridge visual UAT gap by redesigning the compact bridge surface so headings fit cleanly.

Purpose: UAT screenshots showed long register/recover headings wrapping heavily in a compact card because `LegacyBridge` used page-heading typography inside a constrained surface. The bridge must stay account-focused and no-password while using typography and layout appropriate for a compact route-local card.

Output: Polished `LegacyBridge` component, updated stories, and regression coverage preserving no-password behavior.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shopify-customer-accounts/14-UAT.md
@.planning/phases/14-shopify-customer-accounts/14-CONTEXT.md
@.planning/phases/14-shopify-customer-accounts/14-UI-SPEC.md
@.planning/phases/14-shopify-customer-accounts/14-05-SUMMARY.md

<interfaces>
Existing `LegacyBridge` public props:

```ts
type LegacyBridgeProps = {
  body: string
  heading: string
  primaryHref: string
}

export function LegacyBridge({ body, heading, primaryHref }: LegacyBridgeProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle LegacyBridge for compact account-card typography</name>
  <files>src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.tsx, src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.stories.tsx, src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.test.tsx</files>
  <behavior>
    - Bridge heading uses compact card heading typography such as `type-heading-03`, `type-heading-04`, or `type-heading-05`, not `type-heading-01`.
    - Register and Recover stories include browser-level visual assertions for desktop and mobile viewports proving the heading and card do not overflow.
    - Register and Recover heading line counts stay professional: no more than 2 rendered lines at desktop width and no more than 3 rendered lines at the Storybook `mobile1` viewport.
    - The surface keeps warm token classes, existing `Section`, `Card`, and `Button` primitives, and no raw color classes.
    - Primary action remains `Sign in with Shopify`; secondary action remains support/contact.
    - Focused regression coverage confirms `type-heading-01` is absent from `LegacyBridge`.
  </behavior>
  <action>
Redesign `LegacyBridge` as a calmer compact account bridge surface per D-14, D-39, D-55, and D-56. Keep the existing props contract. Replace oversized page-heading typography inside the card with compact heading typography and add appropriate text balancing or max-width treatment using Tailwind utilities available in the project.

Use existing local primitives and Tailwind token classes only. Do not modify `src/app/globals.css` unless a local utility is truly unavailable; the preferred fix is local component typography/layout. Do not add password inputs or classic account form controls.

In `legacy-bridge.stories.tsx`, add a shared play/assertion helper for the Register and Recover bridge headings. The helper must measure rendered DOM, not only class names:
- assert `canvasElement.scrollWidth <= canvasElement.clientWidth`
- find the rendered heading by its text and assert its `scrollWidth <= clientWidth`
- derive rendered line count from `getBoundingClientRect().height / getComputedStyle(heading).lineHeight`
- assert desktop Register/Recover headings render in no more than 2 lines
- add mobile Storybook coverage for Register and Recover using `parameters.viewport.defaultViewport = 'mobile1'`
- assert mobile Register/Recover headings render in no more than 3 lines and keep the primary `Sign in with Shopify` link visible

Add or extend a focused source/assertion test named `does not use type-heading-01` in `legacy-bridge.test.tsx` that reads or renders `LegacyBridge` and fails if the component still uses `type-heading-01`. Keep this typography regression in Task 1 so the task-level verify proves the compact bridge typography change directly.
  </action>
  <verify>
    <automated>pnpm test:integration -- "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.test.tsx" -t "does not use type-heading-01" && pnpm test:stories -- --testNamePattern="Account/LegacyBridge.*(Register|Recover)"</automated>
  </verify>
  <done>`LegacyBridge` no longer uses oversized page-heading typography, and Register/Recover bridge headings fit cleanly without awkward wrapping or horizontal overflow at desktop and Storybook `mobile1` widths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update bridge stories and regression test for polished states</name>
  <files>src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.stories.tsx, src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.test.tsx</files>
  <behavior>
    - Register, recover, verification-failed, and unknown-route stories still render.
    - Story play checks the Shopify-hosted explanation and primary sign-in href.
    - Regression test confirms no password input is rendered.
  </behavior>
  <action>
Update Storybook coverage so all existing bridge states render the polished component. Keep literal story args so Storybook does not import server-only legacy routing helpers.

Extend the existing jsdom regression test to prove `LegacyBridge` does not render password inputs. Keep the test focused on the bridge component; do not require live Shopify OAuth. The `type-heading-01` regression assertion belongs to Task 1 and should remain covered there.
  </action>
  <verify>
    <automated>pnpm test:integration -- "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.test.tsx" && pnpm test:stories -- --testNamePattern="Account/LegacyBridge"</automated>
  </verify>
  <done>Bridge visual states stay covered while no-password guarantees remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy URL -> bridge UI | Customers arrive from old account links and need safe modern sign-in context. |
| bridge UI -> OAuth start | Primary CTA forwards only safe route-local sign-in hrefs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09-01 | Spoofing | LegacyBridge copy | mitigate | Keep explicit Shopify-hosted Customer Account sign-in explanation and owned `/account/login/start` CTA. |
| T-14-09-02 | Information Disclosure | LegacyBridge UI | accept | Bridge displays only static account migration copy and no customer data. |
| T-14-09-03 | Elevation of Privilege | LegacyBridge UI | mitigate | Preserve no-password-input guarantee; do not recreate classic credential forms. |
| T-14-09-04 | Denial of Service | LegacyBridge layout | mitigate | Use stable compact typography and dimensions so headings do not overflow or obscure actions. |
</threat_model>

<verification>
```bash
pnpm test:integration -- "src/app/(storefront)/account/_components/legacy-bridge/legacy-bridge.test.tsx"
pnpm test:stories -- --testNamePattern="Account/LegacyBridge.*(Register|Recover)"
pnpm test:stories -- --testNamePattern="Account/LegacyBridge"
pnpm lint
pnpm typecheck
```
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Legacy account migration surfaces preserve Tea Vision account expectations. | Covered by Tasks 1-2. |
| REQ | MIG-02, MIG-03 | Covered by polished bridge pages and preserved explanatory OAuth migration copy. |
| REQ | SEC-04 | Covered by no classic password inputs and static safe copy. |
| RESEARCH | Legacy account links bridge into Customer Account OAuth; no classic password forms. | Covered by Tasks 1-2. |
| CONTEXT | D-14, D-37, D-39, D-43, D-55, D-56 | Covered by compact account UI and preserved safe bridge behavior. |
| UI-SPEC | Login/bridge states use compact card/panel and no marketing hero. | Covered by Task 1. |
| UAT | Test 9 legacy bridge visual polish issue. | Covered by Tasks 1-2. |
</source_audit>

<success_criteria>
- `LegacyBridge` uses compact account-card heading typography and no `type-heading-01`.
- Long register/recover headings fit professionally inside the compact card, with Storybook browser assertions proving no horizontal overflow and acceptable line counts at desktop and mobile widths.
- Bridge pages still show Shopify-hosted account explanation, `Sign in with Shopify`, and support fallback.
- No password input appears in bridge markup or tests.
</success_criteria>

<output>
After completion, create `.planning/phases/14-shopify-customer-accounts/14-09-SUMMARY.md`.
</output>
