---
phase: 14-shopify-customer-accounts
plan: 08
type: execute
wave: 5
depends_on:
  - "14-05"
autonomous: true
gap_closure: true
files_modified:
  - "src/components/layout/header/header.tsx"
  - "src/components/layout/footer/data.ts"
  - "src/components/layout/footer/link/link-item.stories.tsx"
  - "tests/e2e/cart-checkout.spec.ts"
requirements:
  - MIG-01
  - MIG-02
  - SEC-04
must_haves:
  truths:
    - "Header account icon anchor href is verified directly as `/account` before protected-route navigation."
    - "Footer Login anchor href is verified directly as `/account`."
    - "No Storybook footer fixture still suggests the production footer Login link points at `mrtea.com.au`."
    - "Protected `/account` redirect to `/account/login?returnTo=%2Faccount` is documented as expected auth behavior, not link-target evidence."
  artifacts:
    - path: "src/components/layout/header/header.tsx"
      provides: "Header account link target and accessible name"
    - path: "src/components/layout/footer/data.ts"
      provides: "Production footer Login link target"
    - path: "src/components/layout/footer/link/link-item.stories.tsx"
      provides: "Non-confusing isolated footer link stories"
    - path: "tests/e2e/cart-checkout.spec.ts"
      provides: "Direct account-link href evidence before navigation"
  key_links:
    - from: "tests/e2e/cart-checkout.spec.ts"
      to: "src/components/layout/header/header.tsx"
      via: "accessible link named Account"
      pattern: "getByRole\\('link'.*Account"
    - from: "tests/e2e/cart-checkout.spec.ts"
      to: "src/components/layout/footer/data.ts"
      via: "footer Login href assertion"
      pattern: "Login.*\\/account"
---

<objective>
Close the account link UAT evidence gap by verifying link targets directly and removing stale fixture confusion.

Purpose: The UAT screenshot captured the expected unauthenticated protected-route redirect after clicking `/account`, not the actual header link target. The plan must create direct anchor evidence for header and footer links and remove the isolated Storybook fixture that still points to `mrtea.com.au`.

Output: Direct link-target test coverage and updated footer link story fixture.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shopify-customer-accounts/14-UAT.md
@.planning/phases/14-shopify-customer-accounts/14-CONTEXT.md
@.planning/phases/14-shopify-customer-accounts/14-UI-SPEC.md
@.planning/phases/14-shopify-customer-accounts/14-05-SUMMARY.md

<interfaces>
Expected account link contracts:

```tsx
// Header
<Link href="/account" aria-label="Account">...</Link>

// Footer production data
{ href: '/account', label: 'Login' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct header and footer account href assertions</name>
  <files>tests/e2e/cart-checkout.spec.ts, src/components/layout/header/header.tsx, src/components/layout/footer/data.ts</files>
  <behavior>
    - Browser test reads the header account link `href` attribute before clicking it.
    - Browser test reads the footer Login link `href` attribute before clicking or navigating.
    - Test accepts `/account/login?returnTo=%2Faccount` only as the expected protected-route redirect after navigation, not as link-target proof.
  </behavior>
  <action>
Extend existing fake-Shopify E2E or migration smoke coverage to assert account link targets directly. Use role/name selectors where possible:
- header: `getByRole('link', { name: 'Account' })` has href ending in `/account`
- footer: visible `Login` link has href ending in `/account`

If the test also navigates to `/account`, assert the unauthenticated redirect to `/account/login?returnTo=%2Faccount` as protected-route behavior. Do not treat that redirect URL as the account link href.

Do not add live Shopify OAuth requirements or real checkout navigation to this test. Keep fake checkout boundaries intact.
  </action>
  <verify>
    <automated>pnpm test:e2e -- tests/e2e/cart-checkout.spec.ts --grep "account migration links and legacy routes use the modern bridge"</automated>
  </verify>
  <done>UAT can distinguish correct account link targets from expected unauthenticated route protection redirects.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale mrtea account fixture from footer Storybook story</name>
  <files>src/components/layout/footer/link/link-item.stories.tsx</files>
  <action>
Update the isolated `External` footer link story so it no longer uses `https://mrtea.com.au/account/login` for a Login example. Use a clearly non-account external example, or replace the account example with `/account` if the story is meant to show Login. Preserve the story's purpose and avoid changing production footer data unless Task 1 finds it incorrect.
  </action>
  <verify>
    <automated>pnpm test:stories -- --testNamePattern="Layout/Footer/Text Link"</automated>
  </verify>
  <done>Storybook no longer presents a stale external customer login fixture that can confuse account-link verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public navigation UI -> protected account route | Anonymous customers follow account links into route protection. |
| Storybook fixture -> verifier interpretation | Non-production fixtures can be mistaken for production account link behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08-01 | Spoofing | footer story fixture | mitigate | Remove stale `mrtea.com.au/account/login` fixture so evidence reflects owned account routing. |
| T-14-08-02 | Repudiation | UAT evidence | mitigate | Add direct href assertions before navigation so protected-route redirects cannot be confused with link targets. |
| T-14-08-03 | Information Disclosure | account route redirect | accept | Redirect contains only a path-only returnTo value already required by D-02. |
</threat_model>

<verification>
```bash
pnpm test:e2e
pnpm test:stories -- --testNamePattern="Layout/Footer/Text Link"
pnpm lint
pnpm typecheck
```
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Account entry links preserve Tea Vision account expectations. | Covered by Tasks 1-2. |
| REQ | MIG-01, MIG-02 | Covered by direct header/footer href evidence. |
| REQ | SEC-04 | Covered by preserving protected-route auth behavior and fake-only testing. |
| RESEARCH | Header/footer account links point into owned headless routes. | Covered by Task 1. |
| CONTEXT | D-08, D-38, D-42, D-45, D-46, D-47, D-57 | Covered by direct link assertions and no duplicate mobile row changes. |
| UAT | Test 8 account link verification ambiguity. | Covered by Tasks 1-2. |
</source_audit>

<success_criteria>
- Header account link has accessible name `Account` and href `/account`.
- Footer Login link has href `/account`.
- Tests capture direct href evidence before navigation.
- Storybook no longer includes `mrtea.com.au/account/login` as a Login/account fixture.
</success_criteria>

<output>
After completion, create `.planning/phases/14-shopify-customer-accounts/14-08-SUMMARY.md`.
</output>
