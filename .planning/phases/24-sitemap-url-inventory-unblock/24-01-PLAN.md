---
phase: '24-sitemap-url-inventory-unblock'
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - '.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt'
  - 'src/lib/seo/url-inventory.ts'
  - 'src/lib/seo/url-inventory.test.ts'
  - 'src/app/api/seo/url-inventory/route.ts'
  - 'src/app/api/seo/url-inventory/route.test.ts'
  - 'src/lib/env/server.ts'
  - 'src/lib/observability/logger.ts'
  - 'src/lib/observability/logger.test.ts'
  - '.env.example'
  - 'package.json'
autonomous: true
requirements: [SEO-01, SEO-02]
user_setup:
  - 'For an approved consultant handoff window only, set SEO_URL_EXPORT_ENABLED=true and set SEO_URL_EXPORT_SECRET to a rotated random value of at least 32 characters. Keep DISABLE_INDEXING=true on staging, send the secret outside URLs, disable the flag after delivery, and do not commit the downloaded CSV or secret.'
must_haves:
  truths:
    - 'SEO-01: The export is a finite, test-locked inventory of canonical public content destinations: eligible launch-matrix rows, four audited route-owned gaps, non-colliding Shopify pages, all products, all collections, the canonical blog listing, and eligible canonical articles.'
    - 'SEO-01: Every emitted URL has the exact origin https://www.teavision.com.au, rows are unique and lexically sorted, dates are normalized without invented freshness, and CSV serialization is deterministic.'
    - 'SEO-02: GET /api/seo/url-inventory is concealed unless SEO_URL_EXPORT_ENABLED is the literal true value and accepts only one header-based Authorization: Bearer credential matching a configured 32+ character SEO_URL_EXPORT_SECRET via a same-length timing-safe comparison.'
    - 'SEO-02: Every route response, including 404/500/401/502 failures, is private/no-store and carries X-Robots-Tag: noindex, nofollow; authorized upstream failures never return a partial CSV.'
    - 'SEO-02: The export is independent of DISABLE_INDEXING, while src/app/sitemap.ts, src/app/robots.ts, src/lib/seo/noindex.ts, scripts/component-contracts/noindex-mode.test.mjs, and the sitemap-consumed launch route matrix remain unchanged from the durable phase-start commit and the exact noindex contract stays green.'
    - 'Security: logs contain outcome/source/count metadata only; request URLs, authorization headers, configured or caller secrets, CSV bodies, individual URLs/handles, and upstream payloads never enter log calls.'
  artifacts:
    - path: '.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt'
      provides: 'Durable 40-character Git commit baseline captured before Task 1 edits for cross-task frozen-file verification.'
      exports: []
    - path: 'src/lib/seo/url-inventory.ts'
      provides: 'Pure finite inventory assembly, canonical-host normalization, conflict-aware deduplication, sorting, and exact CSV serialization.'
      exports:
        [
          'UrlInventoryRow',
          'UrlInventorySources',
          'buildUrlInventoryRows',
          'serializeUrlInventoryCsv',
        ]
    - path: 'src/app/api/seo/url-inventory/route.ts'
      provides: 'Flag- and bearer-secret-gated request-time CSV Route Handler with fail-closed source orchestration and response-wide noindex/no-store headers.'
      exports: ['GET']
    - path: 'src/lib/seo/url-inventory.test.ts'
      provides: 'Executable completeness contract for included sources, exclusions, canonical host, row counts, duplicates, dates, ordering, and CSV format.'
      exports: []
    - path: 'src/app/api/seo/url-inventory/route.test.ts'
      provides: 'HTTP-boundary coverage for gates, timing-safe auth outcomes, headers, source failures, canonical payloads, and metadata-only logging.'
      exports: []
  key_links:
    - from: 'src/app/api/seo/url-inventory/route.ts'
      to: 'src/lib/env/server.ts'
      via: 'isSeoUrlExportEnabledFromEnv() is checked before getSeoUrlExportSecret(); both are server-only named env readers.'
      pattern: 'isSeoUrlExportEnabledFromEnv|getSeoUrlExportSecret'
    - from: 'src/app/api/seo/url-inventory/route.ts'
      to: 'src/lib/shopify/operations/product.ts, src/lib/shopify/operations/collection.ts, src/lib/shopify/operations/storefront-page.ts, src/lib/blog/operations.ts'
      via: 'Authorized Promise.all orchestration calls getAllProducts(), getCollectionSummaries(250), getPages(), and getBlog(DEFAULT_BLOG_HANDLE) exactly once and fails the response if any source is incomplete.'
      pattern: 'getAllProducts|getCollectionSummaries|getPages|getBlog'
    - from: 'src/lib/seo/url-inventory.ts'
      to: 'src/lib/seo/launch-route-matrix.ts, src/lib/legal/policies.ts, src/lib/seo/site-url.ts'
      via: "The helper reuses route policy and canonical URL helpers while keeping the export-specific four-path gap registry outside the frozen sitemap's shared matrix."
      pattern: 'getLaunchSeoRouteExpectations|LEGAL_POLICIES|getSiteUrl|SITE_URL'
    - from: 'package.json'
      to: 'src/app/api/seo/url-inventory/route.test.ts'
      via: 'The explicit test:integration file list includes the new API Route Handler test, which test:unit intentionally excludes.'
      pattern: 'src/app/api/seo/url-inventory/route.test.ts'
---

<objective>
Provide the SEO consultant with a complete, deterministic canonical-production-host CSV inventory without opening staging to indexing.

Purpose: unblock the external 301 worksheet while preserving the launch-proven noindex boundary.
Output: a pure and fully tested inventory contract, a temporary header-secret-gated GET Route Handler, server-only environment settings, metadata-only observability, integration-suite registration, and frozen sitemap/robots regression proof.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evang/.codex/get-shit-done/templates/summary.md
@.agents/skills/next-cache-components/SKILL.md
@AGENTS.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-sitemap-url-inventory-unblock/24-RESEARCH.md
@.planning/phases/24-sitemap-url-inventory-unblock/24-PATTERNS.md
@.planning/phases/24-sitemap-url-inventory-unblock/24-VALIDATION.md
@docs/conventions.md
@node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md
@node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md
</context>

<interfaces>
- `UrlInventoryRow` fields are exactly `url`, `type`, `lastModified`, `shouldAppearInSitemap`, and `shouldIndexWhenEnabled`; `type` is the closed union `static | legal | page | product | collection | blog | article`.
- `UrlInventorySources` contains readonly `products: ProductSummary[]`, `collections: CollectionSummary[]`, `pages: ShopifyPageSummary[]`, and non-null `blog: BlogIndex` inputs. Import Shopify generated shapes only through `@/lib/shopify/types`; `ShopifyPageSummary` and `BlogIndex` come from their operation modules.
- `buildUrlInventoryRows(sources: UrlInventorySources): UrlInventoryRow[]` is pure: no data fetching, request access, filesystem discovery, logging, or cache directive. A private `CANONICAL_URL_INVENTORY_ORIGIN` is exactly `https://www.teavision.com.au`; assembly throws if the configured `SITE_URL`/`getSiteUrl()` result does not use that origin.
- `serializeUrlInventoryCsv(rows: readonly UrlInventoryRow[]): string` emits the exact header `url,type,lastModified,shouldAppearInSitemap,shouldIndexWhenEnabled`, quotes every field, doubles embedded quotes, stringifies booleans as lowercase `true`/`false`, uses CRLF only, and ends with CRLF.
- `isSeoUrlExportEnabledFromEnv(): boolean` delegates to strict `truthyEnv('SEO_URL_EXPORT_ENABLED')`; `getSeoUrlExportSecret(): string | undefined` delegates to trimmed `optionalEnv('SEO_URL_EXPORT_SECRET')`.
- `GET(request: Request): Promise<Response>` reads `request.headers` and stays request-time under Next.js 16 Cache Components. Do not add `dynamic = 'force-dynamic'` and do not put `'use cache'` in the handler; existing source operations retain ownership of their cache policy.
</interfaces>

<inventory_contract>
Included route universe:

1. `getLaunchSeoRouteExpectations()` rows where both `expectedStatus === 200` and `shouldAppearInSitemap === true`, classified as `legal` when the path belongs to `LEGAL_POLICIES` and otherwise `static`; preserve both policy booleans.
2. Export-specific `static` paths `/collections`, `/pages/certifications`, `/pages/download-catalogues`, and `/pages/how-long-does-bulk-tea-last`, each with both booleans true. Do not add them to `launch-route-matrix.ts` in this phase.
3. Every `getPages()` row whose `getPagePath(handle)` does not collide with the full route-owned path set, classified as `page`.
4. Every `getAllProducts()` row and every `getCollectionSummaries(250)` row; the existing operations paginate to exhaustion.
5. `/blogs/teavision-blogs` only when the canonical blog is not noindex, plus articles that are not noindex and pass the existing `isLocalCanonicalPath(canonicalPath, localPath, SITE_URL)` predicate.

Explicit exclusions: `/blog`; `/api/**`; Atom/checkout/OAuth callbacks; `/account/**`; `/cart`; redirects and legacy aliases; `/search`; blog search/tag/page-2+ URLs; collection category/filter/sort/pagination variants; private/parameterized routes; noindex or externally/different-locally-canonical articles. These exclusions are contract-test assertions, not runtime filesystem heuristics.
</inventory_contract>

<source_audit>
| Source | Requirement / finding | Covered by |
| --- | --- | --- |
| GOAL | Complete consultant inventory without weakening staging noindex. | 24-01-01, 24-01-02, 24-01-03 |
| SEO-01 | Complete canonical-host CSV across every locked public content source. | 24-01-01, 24-01-02 |
| SEO-02 | Strict flag + secret, X-Robots-Tag, frozen sitemap/robots behavior. | 24-01-02, 24-01-03 |
| RESEARCH | Current sitemap inputs omit Shopify pages and four audited route-owned canonical gaps. | 24-01-01 |
| RESEARCH | Use complete existing source operations and fail on any rejected source or null canonical blog. | 24-01-02 |
| PATTERNS | Bearer header only, 32-character minimum, same-length timingSafeEqual, flag-first concealment. | 24-01-02 |
| PATTERNS | Use canonical helpers, conflict-aware dedupe, code-point sort, exact quoted CRLF CSV. | 24-01-01 |
| VALIDATION | Unit, integration, contract, static/build, and approved-runtime sampling. | 24-01-01 through 24-01-03 |
</source_audit>

<threat_model>

## Trust boundaries

| Boundary                                           | Untrusted / sensitive input                                                              | Protected output                                                      |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Deployment config and HTTP caller -> export route  | Feature flag, configured secret, Authorization header, caller-controlled request host    | Whether the operational export surface exists and who can retrieve it |
| Shopify and Sanity -> inventory assembler          | Potentially unavailable, partial, malformed, duplicated, or conflicting upstream records | A worksheet presented as complete and authoritative                   |
| Route runtime -> shared caches, crawlers, and logs | Authenticated CSV, errors, secrets, inventory contents                                   | No shared persistence, no indexing, no secret/content disclosure      |
| Phase 24 changes -> existing SEO controls          | New export behavior                                                                      | Exact frozen sitemap/robots/noindex behavior                          |

## Threat register (ASVS L1; HIGH findings block completion)

| ID      | Threat                                                                                                                   | Severity | Mitigation                                                                                                                                                                                                                                                                                                                                                                       | Verification                                                                                                                                                                                                                                                                                       | Residual risk                                                                                                                                                   |
| ------- | ------------------------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-24-01 | Feature-flag or authentication bypass exposes the temporary endpoint.                                                    | High     | Check strict `SEO_URL_EXPORT_ENABLED === 'true'` semantics first; disabled returns 404 without reading the secret. Require a configured 32+ character server-only secret and exactly one bearer credential before any source call. No query-secret compatibility.                                                                                                                | Integration cases for absent/false/`TRUE`, missing/short secret, missing/malformed/extra-part/wrong bearer, source-call counts, and 200 only for a valid bearer.                                                                                                                                   | Low: a valid secret can still be intentionally shared; rotation and a short enablement window remain operational controls.                                      |
| T-24-02 | Bearer secret leaks through URLs/logs or timing differences.                                                             | High     | Header-only transport; never read query secrets or log request URL/headers/caller values. Compare same-length Buffers with `timingSafeEqual`; log fixed metadata enums/aggregates only.                                                                                                                                                                                          | Wrong-length and same-length-wrong tests; serialized logger-call assertions exclude both configured and caller values, Authorization, request URL, and CSV.                                                                                                                                        | Low: platform access logs may record endpoint access, but not the header value under the application contract.                                                  |
| T-24-03 | Authenticated CSV or errors are shared-cached or indexed.                                                                | High     | Reading request headers keeps the handler request-time; every response includes `Cache-Control: private, no-store, max-age=0` and `X-Robots-Tag: noindex, nofollow`; `/api/` remains disallowed.                                                                                                                                                                                 | Assert both headers on 200, 401, 404, 500, and 502; inspect source for no `use cache`/force-static behavior; run noindex contract and build.                                                                                                                                                       | Low: an authorized recipient can retain the downloaded file, which is the intended handoff.                                                                     |
| T-24-04 | Request-host poisoning or misconfigured SITE_URL puts preview/staging URLs in the consultant CSV.                        | High     | Ignore `request.url`, Host, and X-Forwarded-Host for row construction; build only with `getSiteUrl()`/`SITE_URL` and fail assembly unless the origin is exactly the private `CANONICAL_URL_INVENTORY_ORIGIN` value `https://www.teavision.com.au`. Catch assembly failures at the route boundary and return the same secured generic non-CSV 502 used for unavailable inventory. | Use a `phase24-preview.vercel.app` request fixture; assert every parsed row origin is the canonical production value, the preview host is absent, a noncanonical configured site origin throws in the unit suite, and a mocked builder throw returns secured 502 without leaking the thrown value. | Low: the export fails closed until deployment config is corrected.                                                                                              |
| T-24-05 | A rejected/missing upstream or assembly/serialization exception yields a partial export or an unsecured framework error. | High     | Call complete operations only, use fail-closed concurrent orchestration, treat a null blog as incomplete, and catch builder and serializer exceptions separately after authorization. Return generic secured 502 without provider/error details, CSV headers, or partial content.                                                                                                | Reject each source category, return null blog, and make builder and serializer mocks throw; assert 502, both security headers, fixed JSON, no CSV content type/body, no partial serialization, and fixed metadata-only logs. Unit tests assert exact fixture totals by source/type.                | Low: an upstream can return internally incomplete-but-successful data; operation pagination contracts and approved-runtime count sampling reduce this residual. |
| T-24-06 | New functionality weakens the frozen sitemap/robots noindex controls.                                                    | High     | Before Task 1 edits, persist the current commit in `24-PHASE-START-COMMIT.txt`; never overwrite it on resume. Do not modify or abstract `sitemap.ts`, `robots.ts`, `noindex.ts`, or `noindex-mode.test.mjs`; do not make the export depend on `DISABLE_INDEXING`; do not extend the sitemap-consumed launch matrix.                                                              | Validate the stored commit object, compare `${baseline}..HEAD` for all five frozen/shared paths so committed task changes cannot hide, also check the working tree, run unchanged `pnpm test:contracts`, assert the route does not import noindex, and build.                                      | Low: future unrelated changes remain outside this phase and are still guarded by the contract test.                                                             |
| T-24-07 | Duplicate URLs or conflicting metadata silently corrupt the worksheet.                                                   | Medium   | Normalize absolute URLs before Map insertion; collapse only metadata-identical duplicates; throw on any non-URL conflict; sort after dedupe with explicit code-point comparison.                                                                                                                                                                                                 | Unit cases for identical collapse, conflicting throw, exact unique count, type counts, and deterministic lexical order.                                                                                                                                                                            | Low: future source categories require corresponding contract fixtures before inclusion.                                                                         |

No threat may remain High after mitigation. Any failed T-24-01 through T-24-06 assertion blocks plan completion rather than accepting a partial or weakened result.
</threat_model>

<tasks>

<task id="24-01-01" type="auto">
  <name>Task 1: Lock and implement the finite canonical inventory contract</name>
  <files>.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt, src/lib/seo/url-inventory.ts, src/lib/seo/url-inventory.test.ts</files>
  <read_first>
    @.planning/phases/24-sitemap-url-inventory-unblock/24-RESEARCH.md
    @.planning/phases/24-sitemap-url-inventory-unblock/24-PATTERNS.md
    @.planning/phases/24-sitemap-url-inventory-unblock/24-VALIDATION.md
    @docs/conventions.md
    @src/app/sitemap.ts
    @src/lib/seo/launch-route-matrix.ts
    @src/lib/seo/launch-route-matrix.test.ts
    @src/lib/seo/site-url.ts
    @src/lib/seo/site-url.test.ts
    @src/lib/legal/policies.ts
    @src/lib/shopify/types/index.ts
    @src/lib/shopify/operations/product.ts
    @src/lib/shopify/operations/collection.ts
    @src/lib/shopify/operations/storefront-page.ts
    @src/lib/blog/operations.ts
  </read_first>
  <action>Before any implementation edit, create the durable baseline marker `.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt` containing exactly `(git rev-parse HEAD).Trim()` as one 40-character lowercase commit SHA plus a newline. Use the resolved SHA in an `apply_patch` file addition; if the marker already exists during resume, validate that it matches `^[0-9a-f]{40}$` and resolves via `git cat-file -e "$baseline^{commit}"`, reuse it, and never overwrite it. Then create the pure `src/lib/seo/url-inventory.ts` boundary with the four named exports and exact types/signatures from the Interfaces section. Implement the finite universe in the Inventory Contract section: eligible launch-matrix rows, legal classification through `LEGAL_POLICIES`, the export-private `ADDITIONAL_ROUTE_OWNED_PATHS` constant with exactly four audited paths, route-owned collision exclusion for `getPages()` inputs, every supplied product/collection, the non-noindex canonical blog, and only articles accepted by the existing noindex plus `isLocalCanonicalPath()` rules. Build all absolute URLs through `getSiteUrl()`/`SITE_URL`, but define the private `CANONICAL_URL_INVENTORY_ORIGIN = 'https://www.teavision.com.au'` guard and throw before returning rows if the constructed origin differs; do not use a request origin, filesystem discovery, a cache directive, or a data fetch. Normalize every meaningful non-empty date with `new Date(value).toISOString()`, keep unavailable dates as `''`, use the latest eligible article date for the blog listing, and let malformed non-empty dates throw. Normalize URL keys, collapse metadata-identical duplicates, throw on metadata conflicts, and sort with an explicit less-than/greater-than code-point comparator. Implement the exact five-column, quote-every-field, embedded-quote-doubling, lowercase-boolean, CRLF-terminated serializer without a dependency. Add table-driven tests for the minimum representatives and exclusions in `24-PATTERNS.md`, first/last product and collection inputs, one non-colliding and one literal-colliding Shopify page, blog listing plus eligible/path-absolute/noindex/external/different-local-canonical articles, exact totals and counts by type, uniqueness, lexical order, exact origin on every row, preview-host absence, noncanonical SITE_URL failure, ISO/empty/malformed dates, identical/conflicting duplicates, escaping of commas/newlines/quotes, CRLF-only lines, and identical repeated serialization. Do not change `launch-route-matrix.ts` or any frozen file.</action>
  <verify>
    <automated>$baseline = (Get-Content -Raw '.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt').Trim(); if ($baseline -notmatch '^[0-9a-f]{40}$') { throw 'Invalid Phase 24 baseline SHA' }; git cat-file -e "$baseline^{commit}"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
    <automated>pnpm exec vitest run --environment node src/lib/seo/url-inventory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/lib/seo/url-inventory.ts` exports exactly `UrlInventoryRow`, `UrlInventorySources`, `buildUrlInventoryRows`, and `serializeUrlInventoryCsv`; it contains no request access, source operation call, filesystem scan, raw request-host construction, `'use cache'`, `any`, or new dependency.
    - Baseline: `24-PHASE-START-COMMIT.txt` exists before helper edits, contains exactly one valid 40-character lowercase SHA naming a commit object, and is preserved unchanged for Tasks 2 and 3.
    - Behavior: fixtures produce exact asserted totals for `static`, `legal`, `page`, `product`, `collection`, `blog`, and `article`; `/`, `/collections`, `/pages/privacy-policy`, `/pages/certifications`, `/pages/download-catalogues`, and `/pages/how-long-does-bulk-tea-last` are present, while every explicit exclusion in `<inventory_contract>` is absent.
    - Behavior: every emitted URL is unique, sorted by code point, and has `new URL(row.url).origin === 'https://www.teavision.com.au'`; the synthetic staging host is absent, and a noncanonical configured site origin makes assembly throw rather than emit rows.
    - Behavior: identical duplicate metadata collapses to one row, conflicting metadata throws, non-empty dates become ISO strings, missing dates are `''`, and malformed non-empty dates throw.
    - CLI: `pnpm exec vitest run --environment node src/lib/seo/url-inventory.test.ts` exits 0 with the Phase 24 helper suite passing.
    - Response-format contract: serialization begins with the exact five-column header, quotes every field, uses only CRLF row endings, ends with CRLF, escapes comma/newline/quote fixtures, emits lowercase booleans, and is byte-identical on repeated calls.
  </acceptance_criteria>
</task>

<task id="24-01-02" type="auto">
  <name>Task 2: Add the secure fail-closed URL inventory Route Handler</name>
  <files>src/app/api/seo/url-inventory/route.ts, src/app/api/seo/url-inventory/route.test.ts, src/lib/env/server.ts, src/lib/observability/logger.ts, src/lib/observability/logger.test.ts, .env.example, package.json</files>
  <read_first>
    @.planning/phases/24-sitemap-url-inventory-unblock/24-RESEARCH.md
    @.planning/phases/24-sitemap-url-inventory-unblock/24-PATTERNS.md
    @.planning/phases/24-sitemap-url-inventory-unblock/24-VALIDATION.md
    @AGENTS.md
    @docs/conventions.md
    @node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md
    @node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md
    @.agents/skills/next-cache-components/SKILL.md
    @src/app/api/draft/route.ts
    @src/app/api/draft/route.test.ts
    @src/app/api/health/route.ts
    @src/app/api/webhooks/sanity/route.test.ts
    @src/lib/env/read.ts
    @src/lib/env/server.ts
    @src/lib/observability/logger.ts
    @src/lib/observability/logger.test.ts
    @src/lib/seo/url-inventory.ts
    @src/lib/shopify/operations/product.ts
    @src/lib/shopify/operations/collection.ts
    @src/lib/shopify/operations/storefront-page.ts
    @src/lib/blog/operations.ts
    @.env.example
    @package.json
  </read_first>
  <action>Extend the server-only env module with `isSeoUrlExportEnabledFromEnv()` and `getSeoUrlExportSecret()` exactly as specified in the Interfaces section, then document `SEO_URL_EXPORT_ENABLED=false` and a blank `SEO_URL_EXPORT_SECRET=` with 32+ character/temporary-window guidance in `.env.example`; do not introduce `NEXT_PUBLIC_` variants or an example secret. Create `GET(request: Request): Promise<Response>` at `/api/seo/url-inventory` using a shared response-header constant and JSON error helper so every status has `Cache-Control: private, no-store, max-age=0` and `X-Robots-Tag: noindex, nofollow`. Gate in this exact order and lock the generic JSON bodies: strict flag false -> 404 `{ error: 'Not found' }` without reading the secret or calling sources; missing/trimmed-short configured secret -> 500 `{ error: 'URL inventory export unavailable' }`; missing or malformed bearer -> 401 `{ error: 'Unauthorized' }`; otherwise compare candidate/expected same-length Buffers with `timingSafeEqual`. Accept the Bearer scheme case-insensitively but require exactly scheme plus one non-empty token and reject extra whitespace-separated parts; never accept a query/cookie/body secret. After authorization, call `getAllProducts()`, `getCollectionSummaries(250)`, `getPages()`, and `getBlog(DEFAULT_BLOG_HANDLE)` once through fail-closed concurrent orchestration. Label source failures only as `products | collections | pages | blog`, treat a null blog as incomplete, and return 502 `{ error: 'URL inventory unavailable' }` without serialization or CSV headers/body when any source fails. After sources resolve, wrap `buildUrlInventoryRows()` and `serializeUrlInventoryCsv()` in separate route-boundary failure handling: a builder exception logs only fixed `source: 'assembly'`/`reason: 'inventory-build-failed'`, a serializer exception logs only fixed `source: 'serialization'`/`reason: 'csv-serialization-failed'`, and either returns the identical secured generic 502 JSON without `Content-Type: text/csv`, attachment headers, exception text, or partial body. Only after both succeed return status 200 with `text/csv; charset=utf-8` plus `attachment; filename="teavision-url-inventory.csv"`. Add `seo_url_export_completed`, `seo_url_export_failed`, and `seo_url_export_rejected` to `ObservabilityEventName`; log only fixed outcome/reason/source values and aggregate row counts/type counts. Update the logger event-name test, and ensure neither route nor tests pass request URLs, Authorization, either secret, thrown error/message, CSV, individual URLs/handles, or upstream payloads to `logEvent`. Add the quoted route-test path to the explicit `test:integration` script next to other API tests. Build integration tests with `vi.hoisted()` and top-level mocks for env readers, four source operations, helper functions, and logger; use a deliberately noncanonical preview request host, explicitly set `DISABLE_INDEXING=true` in the valid-success case to prove the export remains independent, and cover every gate/header/failure/success/log assertion from `24-PATTERNS.md`. Add explicit builder-throw and serializer-throw cases whose thrown messages contain sensitive sentinel strings; assert the fixed 502 JSON, both security headers, no CSV/attachment headers or body, fixed assembly/serialization metadata, and absence of all sentinel/caller/configured/request/CSV values from serialized log calls. Do not import `isNoindexModeEnabled`, read raw `process.env` in production route code, add `dynamic = 'force-dynamic'`, place `'use cache'` in the handler, add rate limiting/JSON mode/query compatibility, or add a dependency.</action>
  <verify>
    <automated>pnpm exec vitest run --environment node src/app/api/seo/url-inventory/route.test.ts</automated>
    <automated>pnpm exec vitest run --environment node src/lib/observability/logger.test.ts</automated>
    <automated>pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - HTTP: missing, `false`, and `TRUE` flags each return 404 `{ error: 'Not found' }` with both security headers, do not read `SEO_URL_EXPORT_SECRET`, and call no inventory source.
    - HTTP: missing or shorter-than-32 configured secrets return 500 `{ error: 'URL inventory export unavailable' }` with both security headers and no source call; missing/malformed/extra-part/wrong-length/same-length-wrong bearer credentials return 401 `{ error: 'Unauthorized' }` with both headers and no source call.
    - Auth source: the route contains a candidate/expected Buffer length equality guard before `timingSafeEqual`, has no query-secret fallback, and imports only the two named server env readers rather than reading raw `process.env`.
    - HTTP success: with `DISABLE_INDEXING=true`, a valid bearer still calls each complete operation exactly once, calls `getCollectionSummaries(250)`, returns 200 with exact attachment/content-type plus noindex/private-no-store headers, contains canonical-host rows, and contains no preview request host.
    - Fail closed: a rejection from each required source category, a null blog, a thrown `buildUrlInventoryRows()`, and a thrown `serializeUrlInventoryCsv()` each return 502 `{ error: 'URL inventory unavailable' }` with both security headers, no CSV content type, no attachment header/body, and no partial success response.
    - Assembly/serialization tests: the builder-throw case logs only fixed `source: 'assembly'` and `reason: 'inventory-build-failed'`; the serializer-throw case logs only fixed `source: 'serialization'` and `reason: 'csv-serialization-failed'`; neither response or log contains the mocked exception message.
    - Logging: serialized `logEvent` mock calls contain none of the configured secret, caller token, `Authorization`, request URL/preview host, thrown sensitive sentinel, CSV body, individual inventory URL/handle, or raw source payload; calls contain only fixed reason/source metadata and aggregate counts.
    - Environment: `.env.example` contains exactly server-only `SEO_URL_EXPORT_ENABLED=false` and blank `SEO_URL_EXPORT_SECRET=` operational defaults; `package.json` includes the quoted route test in `test:integration` and adds no dependency.
    - CLI: all three `<automated>` commands exit 0; `pnpm test:integration` actually reports the new route test rather than omitting it.
  </acceptance_criteria>
</task>

<task id="24-01-03" type="auto">
  <name>Task 3: Prove frozen noindex behavior and full production readiness</name>
  <files>None (verification-only task; frozen SEO files are read-only)</files>
  <read_first>
    @.planning/phases/24-sitemap-url-inventory-unblock/24-VALIDATION.md
    @.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt
    @src/app/sitemap.ts
    @src/app/robots.ts
    @src/lib/seo/noindex.ts
    @scripts/component-contracts/noindex-mode.test.mjs
    @src/app/api/seo/url-inventory/route.ts
    @src/app/api/seo/url-inventory/route.test.ts
    @package.json
  </read_first>
  <action>Perform the Phase 24 security/regression gate without editing verification targets. Load `$baseline` from the durable `24-PHASE-START-COMMIT.txt`, require the exact 40-character lowercase SHA shape, and require `git cat-file -e "$baseline^{commit}"` to resolve. Compare `git diff --exit-code "$baseline..HEAD" --` across `src/app/sitemap.ts`, `src/app/robots.ts`, `src/lib/seo/noindex.ts`, `scripts/component-contracts/noindex-mode.test.mjs`, and `src/lib/seo/launch-route-matrix.ts` so committed Task 1/2 changes cannot disappear from evidence; then run a separate working-tree `git diff --exit-code --` over the same paths to catch uncommitted Task 3 changes. If either diff fails, stop and report the paths instead of overwriting user work or moving the baseline. Confirm the exact sitemap guard remains `if (isNoindexModeEnabled()) { return [] }`, robots still omits its sitemap line in noindex mode and keeps `/api/` disallowed, the new route does not import/call noindex or use `DISABLE_INDEXING`, and `launch-route-matrix.ts` is unchanged from the phase baseline. Run the unchanged contract test, all static checks, both complete test suites, and the production build. Treat any High threat assertion, contract failure, test failure, lint/type error, or build error as blocking; do not weaken tests, rewrite the baseline, or change frozen behavior to obtain green. Do not run checkout/payment/shipping/tax/order/success-redirect flows, GraphQL codegen, schema pushes, Storybook, or real Shopify hosted checkout. The approved-runtime CSV handoff remains the manual operational step documented after this plan and is not fabricated from mocks.</action>
  <verify>
    <automated>$baseline = (Get-Content -Raw '.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt').Trim(); if ($baseline -notmatch '^[0-9a-f]{40}$') { throw 'Invalid Phase 24 baseline SHA' }; git cat-file -e "$baseline^{commit}"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --exit-code "$baseline..HEAD" -- src/app/sitemap.ts src/app/robots.ts src/lib/seo/noindex.ts scripts/component-contracts/noindex-mode.test.mjs src/lib/seo/launch-route-matrix.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --exit-code -- src/app/sitemap.ts src/app/robots.ts src/lib/seo/noindex.ts scripts/component-contracts/noindex-mode.test.mjs src/lib/seo/launch-route-matrix.ts</automated>
    <automated>pnpm test:contracts</automated>
    <automated>pnpm lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm typecheck; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:unit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:integration; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:contracts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <acceptance_criteria>
    - Frozen source: the marker SHA resolves to a commit, `git diff --exit-code "$baseline..HEAD" -- src/app/sitemap.ts src/app/robots.ts src/lib/seo/noindex.ts scripts/component-contracts/noindex-mode.test.mjs src/lib/seo/launch-route-matrix.ts` exits 0 across committed task history, and the separate working-tree diff over those paths also exits 0.
    - Noindex contract: `pnpm test:contracts` exits 0 without modifying the contract, and the sitemap source still contains the exact noindex early-return shape while robots still disallows `/api/` and omits the sitemap property under noindex.
    - Independence: `rg -n "isNoindexModeEnabled|DISABLE_INDEXING" src/app/api/seo/url-inventory/route.ts src/lib/seo/url-inventory.ts` returns no match; authorized export behavior is controlled only by the two SEO URL export env settings.
    - Full gate: `pnpm lint`, `pnpm typecheck`, `pnpm test:unit`, `pnpm test:integration`, `pnpm test:contracts`, and `pnpm build` all exit 0.
    - Scope: the final diff contains no UI/Storybook/GraphQL/codegen/schema/dependency/checkout changes and no downloaded CSV or secret.
  </acceptance_criteria>
</task>

</tasks>

<validation_sampling>

- After 24-01-01: run the focused helper unit command; maximum intended feedback latency is 120 seconds.
- After 24-01-02: run the focused route integration command, focused logger test, then the registered integration suite.
- At the end of Wave 1 / after 24-01-03: run `pnpm test:unit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:integration; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:contracts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }`, followed by the complete lint/type/build gate in the task.
- Before `$gsd-verify-work`: the full production-build command must be green; mocked counts must be asserted by source/type, not inferred from spot checks.
  </validation_sampling>

<manual_validation>
This plan is autonomous because live Shopify/Sanity and consultant delivery are owner-gated. During the separately approved handoff window, an operator must enable only `SEO_URL_EXPORT_ENABLED`, use a rotated 32+ character bearer secret with `curl.exe -H "Authorization: Bearer ..."`, verify status 200 plus attachment/noindex/private-no-store headers, parse the CSV, assert unique rows and exact `https://www.teavision.com.au` origin for every row, confirm required static paths and plausible counts by type against the approved source systems, then disable the flag and delete the local CSV. Keep `DISABLE_INDEXING=true`; confirm `/sitemap.xml` remains empty and robots omits the sitemap line. Never put the secret in a query string, commit it/the CSV, or exercise checkout.
</manual_validation>

<verification>
- Targeted: `pnpm exec vitest run --environment node src/lib/seo/url-inventory.test.ts` exits 0.
- Targeted: `pnpm exec vitest run --environment node src/app/api/seo/url-inventory/route.test.ts` exits 0.
- Security regression: `$baseline = (Get-Content -Raw '.planning/phases/24-sitemap-url-inventory-unblock/24-PHASE-START-COMMIT.txt').Trim(); if ($baseline -notmatch '^[0-9a-f]{40}$') { throw 'Invalid Phase 24 baseline SHA' }; git cat-file -e "$baseline^{commit}"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --exit-code "$baseline..HEAD" -- src/app/sitemap.ts src/app/robots.ts src/lib/seo/noindex.ts scripts/component-contracts/noindex-mode.test.mjs src/lib/seo/launch-route-matrix.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --exit-code -- src/app/sitemap.ts src/app/robots.ts src/lib/seo/noindex.ts scripts/component-contracts/noindex-mode.test.mjs src/lib/seo/launch-route-matrix.ts` exits 0, then `pnpm test:contracts` exits 0.
- Full: `pnpm lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm typecheck; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:unit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:integration; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm test:contracts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pnpm build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }` exits 0.
- Static scope: no new production dependency, UI, Storybook, GraphQL/codegen, schema, checkout, or frozen-file diff exists.
- Security gate: all T-24-01 through T-24-07 automated assertions pass and no residual High threat remains.
</verification>

<success_criteria>

1. SEO-01: the helper and tests define a finite, complete source universe including Shopify pages and four audited route-owned gaps, and prove exact source/type counts rather than relying on current sitemap sources alone.
2. SEO-01: the exact five-column CSV is deterministic, uniquely deduplicated, conflict-failing, lexically ordered, correctly escaped, and contains only `https://www.teavision.com.au` URLs.
3. SEO-02: disabled export is concealed; enabled export still requires a valid 32+ character header-only bearer secret compared with a same-length timing-safe operation before source access.
4. SEO-02: every response is `noindex, nofollow` and private/no-store; any required upstream failure returns a generic non-CSV 502 instead of a partial worksheet.
5. SEO-02: logs are metadata-only and tests prove configured/caller secrets, headers, request URLs, CSV, individual URLs, and upstream payloads are absent.
6. The export succeeds independently of `DISABLE_INDEXING`, while sitemap, robots, noindex helper, noindex contract, and the sitemap-consumed launch matrix are unchanged.
7. Focused tests, complete suites, contracts, lint, typecheck, and Next.js 16 production build all pass without out-of-scope work or a new dependency.
   </success_criteria>

<output>
After completion, create `.planning/phases/24-sitemap-url-inventory-unblock/24-01-SUMMARY.md`. Record exact automated row counts by type from fixtures, the bearer-only gate/status/header contract, builder/serializer secured-failure proof, the phase-start baseline SHA, the baseline-through-HEAD and working-tree no-diff results for all five frozen/shared SEO sources, full verification results, and that approved-runtime delivery remains owner-gated unless actually performed. Never include a real secret, Authorization value, individual live inventory URLs, source payload, thrown sensitive sentinel, or downloaded CSV in the summary.
</output>
