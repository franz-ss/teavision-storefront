---
milestone: v1.5
audited: 2026-07-01T10:59:15+08:00
status: tech_debt
scores:
  requirements: "5/5 in-scope (lean) satisfied; 10/15 deferred per D-16"
  phases: "1/1 verified"
  integration: "98/100"
  flows: "100/100"
gaps:
  requirements: []
  integration: []
  flows: []
deferred_requirements:
  - ids: ["PSI-01", "PSI-02", "PSI-04", "PSI-09", "PSI-10", "PSI-11", "PSI-12", "PSI-13", "PSI-14", "PSI-15"]
    reason: "Owner directive D-16 (2026-06-30) narrowed Phase 20 to the four homepage `/` PSI screenshot findings. The broad '100/100 across four categories, all representative routes' scope was deliberately deferred, not dropped — the five deferred plans are preserved under .planning/phases/20-pagespeed-100-perfection/deferred/ (20-02..20-05) and recoverable from commit e0e42881."
    disposition: "Track for a future performance milestone. These are an intentional scope narrowing, not verification failures or broken flows."
tech_debt:
  - phase: "20-pagespeed-100-perfection"
    items:
      - "Render-blocking CSS on `/` (~540ms) remains unresolved by design: experimental.inlineCss was enabled, locally measured (LCP/FCP/SI all regressed), and reverted. Documented measured tradeoff (PSI-07), not a silent gap."
      - "Real-PSI LCP on `/` improved 3.4s -> ~3.0s but did not reach the aspirational ~1.5s; the residual gap is diagnosed as a Lantern simulate-throttling lab artifact (hero paints ~150ms observed), accepted with evidence in docs/launch/homepage-performance-fixes.md."
      - "10 of the 15 defined v1.5 (PSI) requirements are deferred per D-16 (see deferred_requirements above); the milestone shipped a lean subset."
      - "REQUIREMENTS.md had all 15 PSI checkboxes marked [x] while its own traceability table listed 10 as 'Deferred (post-lean)' — an internal inconsistency to be corrected at archival."
nyquist:
  compliant_phases: ["20-pagespeed-100-perfection"]
  partial_phases: []
  missing_phases: []
  overall: compliant
---

# Milestone v1.5 Audit — Performance & PageSpeed 100 (lean scope)

**Audited:** 2026-07-01
**Status:** tech_debt
**Scope:** Phase 20 (the only phase in v1.5)

The milestone's single phase is fully verified end-to-end with real owner-run Google PageSpeed Insights evidence, and no cross-phase regression of the prior SEO/H1 milestones (Phase 18/19) was introduced. It is classified `tech_debt` rather than `passed` for one honest reason: v1.5 was defined with 15 PSI requirements, and an owner directive (D-16, 2026-06-30) deliberately narrowed Phase 20 to a lean 5-requirement subset. The other 10 requirements are intentionally deferred (plans preserved), so the milestone did not meet its original definition-of-done in full — that is accumulated, tracked scope, not a critical blocker.

## Scope Note

Per owner directive **D-16**, Phase 20 was replanned on 2026-06-30 from a broad 5-wave "100/100 across Performance, Accessibility, Best Practices, and SEO on all representative routes" plan down to a single lean plan (`20-01`) resolving exactly the four homepage `/` PSI screenshot findings. Requirement IDs actually in scope: **PSI-03, PSI-05, PSI-06, PSI-07, PSI-08**. The deferred plans (`20-02`..`20-05`) are preserved under `deferred/` and recoverable from commit `e0e42881`.

## Phase Verification Summary

| Phase | Plans | VERIFICATION.md | Status | Requirement coverage (in-scope) | Nyquist |
|-------|-------|-----------------|--------|---------------------------------|---------|
| 20 — PageSpeed 100/100 Perfection (lean) | 1/1 | present | verified | 5/5 in-scope (PSI-03/05/06/07/08) satisfied | compliant |

Phase 20's [20-VERIFICATION.md](phases/../phases/20-pagespeed-100-perfection/20-VERIFICATION.md) records `status: verified`, 5/5 lean must-haves verified at the code level, and the one human-gated item (real post-deploy PSI on `/`) **completed and resolved 2026-07-01 (owner-run)**.

## Requirements Coverage (3-Source Cross-Reference)

In-scope lean requirements — cross-referenced across REQUIREMENTS.md traceability, 20-VERIFICATION.md, and 20-01-SUMMARY.md `requirements-completed` frontmatter (`[PSI-03, PSI-05, PSI-06, PSI-07, PSI-08]`):

| REQ-ID | Description (lean scope) | VERIFICATION | SUMMARY frontmatter | Traceability | Final |
|--------|--------------------------|--------------|---------------------|--------------|-------|
| PSI-03 | Hero LCP prioritized via `fetchPriority="high"`; 2,040ms resource-load-delay eliminated | verified (code) + owner-run PSI | listed | present | **satisfied** (LCP 3.4s→3.0s; residual diagnosed as lab artifact) |
| PSI-05 | Cold first-load JS bounded (Sentry chunk removed); TBT minimal | verified | listed | present | **satisfied** (TBT 90ms→30ms real PSI) |
| PSI-06 | Unused/legacy JS minimized (lazy Sentry SDK) | verified | listed | present | **satisfied** (unused JS 88→25 KiB, legacy 14 KiB cleared) |
| PSI-07 | Render-blocking/unused CSS — measured inlineCss decision | verified (measured skip) | listed | present | **satisfied** as measured tradeoff (540ms finding accepted, documented) |
| PSI-08 | Correct image format (AVIF/WebP) | verified | listed | present | **satisfied** (`image/avif` served in production) |

**Deferred requirements (owner directive D-16 — out of scope, not evaluated, not broken):** PSI-01, PSI-02, PSI-04, PSI-09, PSI-10, PSI-11, PSI-12, PSI-13, PSI-14, PSI-15. Present in REQUIREMENTS.md traceability as "Deferred (post-lean)"; plans preserved in `deferred/`. These are treated as **deferred**, not orphaned — they were consciously re-scoped, not assigned-and-dropped.

**Documentation inconsistency (non-blocking):** REQUIREMENTS.md currently shows all 15 PSI checkboxes as `[x]` while its traceability table lists 10 as "Deferred (post-lean)." To be corrected during archival (the 5 in-scope → Validated; the 10 deferred → carried to a future milestone / v2 backlog).

## Integration Report

Integration checker (gsd-integration-checker) result: **98/100 integration**, **100/100 flows**. No cross-phase regressions.

| Integration point | Status | Evidence |
|-------------------|--------|----------|
| Phase 18 crawlable-HTML guard vs. Phase 20 changes | wired | Live `node scripts/seo/probe-crawlable-html.mjs --start-server` — all rows PASS for `/collections/all` and `/products/test-standard-tea` (single-h1, canonical, content-before-skeleton, JSON-LD). |
| Phase 19 single-visible-H1 guard vs. Phase 20 changes | wired | `git show --stat` across all 10 Phase-20-family commits shows zero heading/DOM-structure edits; live `curl /` shows exactly one `<h1>`; 20-VERIFICATION.md records an independent same-day Playwright rerun (3/3 pass). |
| Image-performance contract test integrity | wired | Guard still applies to product-gallery, product-card, both collection heroes; hero exemption is a single narrowly-scoped `if (relativePath === HERO_PATH) continue`. `node --test` → 3/3 pass. |
| `hero.tsx` wiring | wired | `fetchPriority="high"` present; no deprecated `priority` prop; single LCP `fill` Image; preload `<link>` + `<img>` both carry fetchpriority=high in served HTML. |
| `instrumentation-client.ts` wiring | wired | No top-level static Sentry import; dynamic import gated on DSN; `.catch()` present (WR-01 fix); `onRouterTransitionStart` typed, no `any`; no Sentry chunk in cold first-load. |
| `next.config.ts` wiring | wired | `images.formats: ['image/avif','image/webp']`; no `experimental.inlineCss` (measured skip). |
| Build integrity | wired | `pnpm typecheck` clean; eslint clean on 7 touched files; `pnpm build` completed via probe `--start-server`. |
| Homepage `/` E2E render | wired | `GET /` → 200; one `<h1>`; hero preload with fetchpriority=high; only one `as="image"` preload (below-fold Tea Journal preload removed per `e0ba5ec0`); CSP-Report-Only present without inert `upgrade-insecure-requests`. |

## E2E And User Flows

| Flow | Status |
|------|--------|
| Homepage `/` renders hero + above-the-fold content, no new console/CSP errors | complete |
| Real owner-run PSI on deployed preview confirms code fixes live (fetchpriority=high, image/avif, Sentry chunk gated) | complete |
| Prior-milestone crawlable-HTML + single-H1 guards remain green | complete (no regression) |

Real post-deploy PSI (owner-run, 2026-07-01, `docs/launch/homepage-performance-fixes.md` + `20-HUMAN-UAT.md`): **Performance 95–97**, LCP 3.0s (down from 3.4s; residual = Lantern lab artifact), Speed Index 5.1s→1.9s, TBT 90ms→30ms, CLS 0 held, **Accessibility 100**, **Best Practices 100**.

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 20 | exists | true | true | COMPLIANT |

Overall Nyquist status: **compliant**.

## Verdict

**tech_debt** — v1.5's single phase is verified end-to-end with real PSI evidence, has no critical blockers, no broken flows, and no regression of prior milestones. It does not qualify as `passed` only because the milestone was defined with 15 PSI requirements and deliberately shipped a lean 5-requirement subset (owner directive D-16); the 10 deferred requirements are tracked scope for a future performance milestone, with their plans preserved. Safe to complete/archive with the deferral recorded as known-deferred scope.

Recommended closure path:

1. Archive v1.5 (the milestone is verified and unblocked).
2. Record the 10 deferred PSI requirements as carried-forward scope (future performance milestone / v2 backlog).
3. Correct the REQUIREMENTS.md checkbox/traceability inconsistency during archival (5 → Validated, 10 → deferred).

---

_Auditor: Claude (gsd-audit-milestone); Integration checker: gsd-integration-checker (sonnet)_
