---
phase: 11-full-visual-redesign
plan: 20
type: execute
wave: 2
depends_on: ['11-17']
autonomous: true
gap_closure: true
requirements: [RD-03, RD-04]
files_modified:
  - src/app/(storefront)/blogs/[blog]/_components/listing-content.tsx
  - src/app/(storefront)/blogs/[blog]/_components/blog-newsletter-band.tsx
  - src/components/blog/featured-articles/featured-articles.tsx
  - src/components/blog/article-results/article-results.tsx
  - src/components/homepage/legacy-brush-illustration/legacy-brush-illustration.tsx
  - src/components/layout/footer/view/view.tsx
  - src/components/layout/footer/link/link-item.tsx
  - src/components/layout/footer/quality-column/quality-column.tsx
  - src/components/layout/footer/newsletter-column/newsletter-column.tsx
  - src/components/layout/footer/payment-mark/payment-mark.tsx
  - src/components/layout/footer/data.ts

must_haves:
  truths:
    - 'Blog listing includes the newsletter ink band (teapot + stamp) and the contact section, reusing existing components'
    - 'Blog listing section spacing is tightened (no large empty gaps between same-tone bands)'
    - "Footer uses the original site copy/links and spacing/typography copied from the design's .ft section"
  artifacts:
    - path: 'src/app/(storefront)/blogs/[blog]/_components/listing-content.tsx'
      provides: 'newsletter ink band + Contact at page end; collapsed band edges'
    - path: 'src/app/(storefront)/blogs/[blog]/_components/blog-newsletter-band.tsx'
      provides: 'composed blog newsletter ink band (route-only component)'
    - path: 'src/components/layout/footer/view/view.tsx'
      provides: 'footer-local padding clamp + tablet 2-col step + plain copyright'
  key_links:
    - from: 'src/app/(storefront)/blogs/[blog]/_components/listing-content.tsx'
      to: 'Contact section component'
      via: 'renders <Contact action={submitContactFormAction} />'
      pattern: 'Contact'
---

<objective>
Close two parity gaps (UAT tests 14 and 8): blog listing missing the newsletter band + contact section with oversized spacing, and footer using mockup-invented copy with off spacing. Copy exact values from design/extracted-design.html (the `.ft` section for the footer) and restore the original site copy/links.

Purpose: The blog listing is missing two production sections that already exist as components, and the footer diverged from both the original copy and the design's spacing. Both are user-facing surfaces on every page (footer) and the content hub (blog).
Output: Blog listing with reused newsletter ink band + contact section and tightened spacing; footer with original copy/links and design-faithful spacing/typography.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative design source — copy exact values, do not approximate (owner directive)

# Footer: copy from the .ft section of design/extracted-design.html

@design/extracted-design.html

# Original footer copy/links reference (Liquid theme)

# ../teavision-theme/sections/footer.liquid (Popular Searches block lines 76-188)

# Diagnosed evidence

@.planning/debug/blog-sections.md
@.planning/debug/footer-parity.md

<interfaces>
- Blog newsletter band must be COMPOSED — no single component exists. Available pieces: SupplyChain holds the legacy animated artwork components inverse-band layout (copy/illo differ), legacy brush illustration component has an unused `teapot` variant, HomepageNewsletter holds the copy+form, Contact is directly reusable.
- The reusable Contact section is relocated to src/components/contact/ by plan 11-17 (this plan depends on 11-17). Import Contact from src/components/contact and pass `action={submitContactFormAction}`.
- Three stacked Section.Root tone='sunken' bands (FeaturedArticles, ArticleResults, newsletter wrapper) each apply py-section top AND bottom — the homepage already solves this with pt-0 on follow-on bands.
- Images (ArticleCard, blog Hero) already use next/image correctly (lazy, sizes, LCP preload) — NO image change needed; the UAT image note is already satisfied (see blog-sections.md root_cause).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blog listing — newsletter ink band + contact section + spacing</name>
  <files>src/app/(storefront)/blogs/[blog]/_components/listing-content.tsx, src/app/(storefront)/blogs/[blog]/_components/blog-newsletter-band.tsx, src/components/blog/featured-articles/featured-articles.tsx, src/components/blog/article-results/article-results.tsx, src/components/homepage/legacy-brush-illustration/legacy-brush-illustration.tsx</files>
  <action>
    Per .planning/debug/blog-sections.md, matching the design and production page structure:
    - NEWSLETTER BAND (declared home): compose the blog newsletter ink band as a NEW route-only component at src/app/(storefront)/blogs/[blog]/_components/blog-newsletter-band.tsx (per conventions, route-only composed markup belongs in the segment's _components/ — do NOT leave it inline in listing-content.tsx). The band = Section.Root tone='inverse' + legacy brush illustration component `teapot` variant + legacy curved-label component (label "Business"/"Teavision" per the design) + the newsletter form — with the copy "Explore the World of Tea with Monthly Newsletters". Use the SupplyChain band layout as the template (do not reuse it directly; copy/CTA differ). The legacy brush illustration component teapot variant already exists. One component per file, named export, no default export.
    - WIRE-UP: in listing-content.tsx, mount `<BlogNewsletterBand />` followed by `<Contact action={submitContactFormAction} />` at the END of the listing, replacing the small "Tea Journal in your inbox" NewsletterSignup card.
    - DOMAIN DECISION: the motif pieces (legacy animated artwork components) live in homepage/ but certifications already imports cross-route, so a cross-route import from blog-newsletter-band.tsx is acceptable; if you promote legacy animated artwork components to a shared domain instead, update all importers and barrels in lockstep and keep one component per file. Prefer the lighter-touch cross-route import unless promotion is trivial.
    - SPACING: collapse the meeting edges of consecutive same-tone bands using the homepage `pt-0` pattern — featured-articles.tsx and article-results.tsx currently double py-section against adjacent bands (~260px of empty same-background space). Apply pt-0 to follow-on bands so vertical rhythm matches the design.
    - IMAGES: no change — already optimized per the debug session.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories</automated>
    <human-check>Blog listing (e.g. /blogs/tea-journal) ends with the newsletter ink band (teapot + stamp + "Explore the World of Tea with Monthly Newsletters") then the contact section ("Need help? Speak with our Ingredients Experts Today."); no large empty gaps between bands.</human-check>
  </verify>
  <done>Blog listing shows the composed newsletter ink band (extracted to blog-newsletter-band.tsx route component) and the reused contact section, with collapsed same-tone band spacing; contract + stories + lint + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Footer — original copy/links + design spacing/typography</name>
  <files>src/components/layout/footer/view/view.tsx, src/components/layout/footer/link/link-item.tsx, src/components/layout/footer/quality-column/quality-column.tsx, src/components/layout/footer/newsletter-column/newsletter-column.tsx, src/components/layout/footer/payment-mark/payment-mark.tsx, src/components/layout/footer/data.ts</files>
  <action>
    Per .planning/debug/footer-parity.md, copying spacing/typography directly from the `.ft` section of design/extracted-design.html and restoring original site copy/links:
    - SPACING (view.tsx): replace global py-section (clamp 64-130px) with the design's footer-local clamp(50px,7vw,90px); add the 2-col tablet grid step (design drops to 2 cols at <=980px). Make the copyright row plain (de-embellish, remove the full linkification) per the design.
    - LINK ROWS (link-item.tsx): remove the min-h-11 (44px) row inflation and text-[0.95rem]; match the design's ~20px rows with ~11px gaps (currently ~2x the design's column height).
    - QUALITY COLUMN (quality-column.tsx): restore the original HACCP quality text (replace mockup-invented copy). The quality pills are a mockup invention but were an owner decision — keep them; restore the underlying copy.
    - NEWSLETTER COLUMN (newsletter-column.tsx): restore the original blurb "Sign up for exclusive offers, market trends and new product alerts."; size the contact links to the design's mono 12px (currently oversized).
    - PAYMENT MARKS (payment-mark.tsx): fix the tracking/gap/color nits to match the design.
    - DATA (data.ts): reinstate the original Popular Searches collapsible link block from ../teavision-theme/sections/footer.liquid:76-188 (currently replaced by a bare /search link). Grid template, heading typography, link inventories, and payment methods are already verified matching — only restore what's listed.
    - Express all colors through design tokens — no raw hex.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories</automated>
    <human-check>Footer shows original HACCP/newsletter copy and Popular Searches links, plain copyright row, and spacing/typography matching the .ft section of the design at desktop, tablet (2-col at <=980px), and mobile.</human-check>
  </verify>
  <done>Footer uses original copy/links (incl. Popular Searches) with design-faithful footer-local spacing, compact rows, and tablet 2-col step; tokens only; contract + stories + lint + typecheck pass.</done>
</task>

</tasks>

<verification>
- pnpm lint && pnpm typecheck && pnpm test:contracts && pnpm test:stories pass
- Manual: blog listing newsletter band + contact + spacing; footer copy/links/spacing across breakpoints.
</verification>

<success_criteria>
UAT tests 14 and 8 are closed: blog listing reuses the newsletter band + contact section with tight spacing, and the footer restores original copy/links with design-faithful spacing.
</success_criteria>

<output>
Create `.planning/phases/11-full-visual-redesign/11-20-SUMMARY.md` when done.
</output>
