---
phase: 11-full-visual-redesign
plan: 12
type: execute
wave: 4
depends_on: ['11-02', '11-03']
files_modified:
  - src/lib/shopify/html-content.ts
  - src/components/blog/portable-text
  - src/app/(storefront)/blogs/[blog]/[article]/page.tsx
  - src/app/(storefront)/blogs/[blog]/_components/listing-page.tsx
  - src/components/blog/hero
  - src/components/blog/tag-filter-nav
  - src/components/blog/article-results
  - src/components/blog/article-list
  - src/components/blog/pagination
  - src/components/blog/featured-articles
  - src/components/blog/empty-state
autonomous: true
requirements: [RD-07]

must_haves:
  truths:
    - 'Tea Journal listing uses .jcard article cards, a brand hero band, and pill tag filters'
    - 'Articles render editorially: compact prose width, serif headings, mono category/date row, ink-soft body, restyled blockquotes/tables'
    - 'Shopify rich-text HTML (html-content.ts) and Sanity Portable Text map to new token classes with sanitization untouched'
    - 'No legacy-styled article/page bodies remain (Pitfall 8 — hidden non-component consumers covered)'
  artifacts:
    - path: 'src/lib/shopify/html-content.ts'
      provides: 'HTML→new-token class map (93 occurrences swapped)'
      contains: 'text-ink'
    - path: 'src/components/blog/portable-text/portable-text.tsx'
      provides: 'Portable Text new-token class map'
  key_links:
    - from: 'src/lib/shopify/html-content.ts'
      to: 'sanitize-html pipeline'
      via: 'sanitization config byte-identical — classes only'
      pattern: 'sanitize'
    - from: 'src/app/(storefront)/blogs/[blog]/[article]/page.tsx'
      to: 'src/lib/shopify/html-content.ts'
      via: 'article body rendering through the swapped class map'
      pattern: 'html-content|htmlContent'
---

<objective>
Restyle the blog/Tea Journal surface and the two hidden rich-content class mappers — `html-content.ts` (Shopify HTML, 93 occurrences) and `portable-text.tsx` (Sanity, 42) — which RESEARCH flags as the most-missed consumers (Pitfall 8). Sanitization and rendering plumbing stay byte-identical; only class strings change.

Output: redesigned journal per UI-SPEC §5.9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rich-content class maps (html-content.ts + portable-text)</name>
  <files>src/lib/shopify/html-content.ts, src/components/blog/portable-text</files>
  <read_first>src/lib/shopify/html-content.ts (FULL file — identify the sanitize-html config vs the class map; only the class map changes), src/components/blog/portable-text/portable-text.tsx + stories, 11-UI-SPEC.md §3 rename table + §5.9 article body spec</read_first>
  <action>
Swap every old token class per UI-SPEC §3 in both mappers — pure string replacement, mechanical:
- headings → `font-display` scale (`type-heading-02/03/04` per level), `text-ink`
- body/paragraphs → `text-ink-soft text-[1.02rem] leading-[1.6]`
- links → `text-brand hover:text-brand-deep underline underline-offset-2`
- blockquotes → `font-display italic border-l-2 border-gold pl-5 text-ink`
- lists/dividers → `border-hairline`, markers `text-brand`
- tables (.spectable) → rows `border-b border-hairline-2`, header cells `type-mono-meta text-ink-faint`
- captions/meta → `type-mono-meta text-ink-faint`
DO NOT touch: the sanitize-html allowlist/config, dangerouslySetInnerHTML plumbing, tag-to-element mapping logic, or any exported function signatures. Update portable-text stories alongside.
  </action>
  <verify>
    <automated>pnpm test:unit && pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "text-default\|text-muted\|text-subtle\|border-default\|border-subtle\|text-link\|bg-surface\|bg-canvas\|type-display-0" src/lib/shopify/html-content.ts` returns 0
    - `grep -rc "text-default\|text-muted\|text-subtle\|border-default\|text-link" src/components/blog/portable-text` returns 0
    - sanitize-html config unchanged (`git diff src/lib/shopify/html-content.ts` shows only class-string lines changed)
    - `pnpm test:unit` exits 0
  </acceptance_criteria>
  <done>Both hidden mappers fully on new tokens; sanitization byte-identical</done>
</task>

<task type="auto">
  <name>Task 2: Article page + listing page</name>
  <files>src/app/(storefront)/blogs/[blog]/[article]/page.tsx, src/app/(storefront)/blogs/[blog]/_components/listing-page.tsx</files>
  <read_first>both files (49 + 2 occurrences; metadata/JSON-LD/noindex plumbing must not change), 11-UI-SPEC.md §5.9, design/extracted/design-system.css .jrnl block</read_first>
  <action>
**Article page**: body container `Section.Container variant="compact"` (max-w-prose); header — `type-mono-meta` category (`text-brand`) · date row, `font-display text-[clamp(2rem,4vw,3.4rem)]` title, lede `type-lede text-ink-soft`; hero image `rounded-lg`; body via the (now-restyled) html-content/portable-text mappers; author/share rows `border-t border-hairline type-mono-meta`. Keep metadata, JSON-LD via serializeInlineJson, noindex robots, and table keyboard access untouched.

**Listing page**: swap classes per §3; composition delegates to the blog components restyled in Task 3.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `grep -c "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|type-display-0" "src/app/(storefront)/blogs/[blog]/[article]/page.tsx"` returns 0 - `grep -c "serializeInlineJson" "src/app/(storefront)/blogs/[blog]/[article]/page.tsx"` unchanged (no JSON-LD plumbing diff) - `pnpm lint` exits 0
</acceptance_criteria>
<done>Editorial article + listing pages on new tokens; SEO/noindex plumbing untouched</done>
</task>

<task type="auto">
  <name>Task 3: Blog components (hero, tags, results, pagination, featured, empty)</name>
  <files>src/components/blog/hero, src/components/blog/tag-filter-nav, src/components/blog/article-results, src/components/blog/article-list, src/components/blog/pagination, src/components/blog/featured-articles, src/components/blog/empty-state</files>
  <read_first>all seven folders (tag filter URL state, pagination math, article-card usage), 11-UI-SPEC.md §5.9 + §5.4 hero pattern, the restyled src/components/ui/article-card from 11-03</read_first>
  <action>
- **Blog hero**: `.coll__hero` pattern — `Section.Root tone="brand"`, gold Eyebrow "Tea Journal", `font-display text-paper` display title, `text-paper/85` description.
- **Tag filter nav**: pill tags `rounded-full border border-hairline bg-card px-3.5 py-2 type-label hover:bg-brand-tint hover:text-brand`, active `bg-brand text-paper border-brand`; keep URL-param tag state.
- **Article results/list/featured**: compose the restyled ArticleCard (.jcard from 11-03); section heads Eyebrow + `type-heading-02`; class swaps per §3.
- **Pagination**: same pill pattern as 11-09 (min-h-11 targets, active `bg-brand text-paper`); keep aria-current.
- **Empty state**: centered leaf, `font-display` heading, muted copy, ghost button.
Update stories in these folders alongside.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|action-\|brandStrong" src/components/blog` returns no files
    - `grep -rc "bg-brand text-paper" src/components/blog/tag-filter-nav src/components/blog/pagination` total >= 1
    - `pnpm test:unit` exits 0
  </acceptance_criteria>
  <done>All blog components on new tokens; tag/pagination state intact</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                            |
| ---------------------------------- | ---------------------------------------------------------------------- |
| Shopify/Sanity CMS → rendered HTML | third-party-authored rich content flows through sanitizer + class maps |

## STRIDE Threat Register

| Threat ID | Category        | Component               | Disposition | Mitigation Plan                                                                                                                     |
| --------- | --------------- | ----------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-11-28   | Elevation (XSS) | html-content.ts         | mitigate    | sanitize-html config byte-identical; acceptance criterion requires diff to touch only class strings; no new dangerouslySetInnerHTML |
| T-11-29   | Tampering       | portable-text renderers | mitigate    | renderer logic unchanged; class maps only                                                                                           |

</threat_model>

<verification>
- `pnpm test:unit`, `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind` exit 0
- Dev-server check: journal listing + one article (Shopify HTML body) at desktop/375px
</verification>

<success_criteria>

- Journal + both rich-content mappers fully on new tokens; sanitization untouched; zero old tokens in owned files
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-12-SUMMARY.md`
</output>
