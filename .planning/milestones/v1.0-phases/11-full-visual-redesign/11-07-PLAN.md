---
phase: 11-full-visual-redesign
plan: 07
type: execute
wave: 5
depends_on: ['11-06']
files_modified:
  - src/app/globals.css
  - scripts/component-contracts/button-system.test.mjs
  - src/components/homepage/legacy-brush-illustration
  - retired homepage curved-label component folder
  - src/components/homepage/supply-chain
  - src/components/homepage/catalogues
  - src/components/homepage/testimonials
  - src/components/homepage/tea-journal
  - src/components/homepage/newsletter
  - src/components/homepage/contact
  - src/components/homepage/contact-form
  - src/components/homepage/faq
  - src/components/homepage/content.ts
  - src/app/(storefront)/page.tsx
autonomous: true
requirements: [RD-04]

must_haves:
  truths:
    - 'Brand motif bands (supply-chain/catalogues CTAs) render on ink or green-deep with a floating legacy-brush-illustration illustration on one side, a hand-drawn stamp with curved Caveat text on the other, gold eyebrow, and a serif headline with a gold-italic highlight word'
    - 'Testimonials render the mockup .tst layout: selector buttons, gold quote mark, large serif quote, mono attribution'
    - 'Tea Journal renders 3 .jcard articles with hover-zoom media (motion-reduce safe)'
    - 'Newsletter band and homepage contact/help section wear the new system while keeping their Server Actions, honeypot, and feedback states'
    - 'FAQ renders as sunken band, max-w-base, hairline rows, serif questions'
    - 'Homepage section order/composition matches the redesign'
  artifacts:
    - path: 'src/components/homepage/legacy-brush-illustration/legacy-brush-illustration.tsx'
      provides: 'brush illustration motif (next/image over extracted PNGs)'
      exports: ['legacy brush illustration component']
    - path: 'retired homepage curved-label component'
      provides: 'stamp ring PNG + curved Caveat SVG textPath'
      exports: ['Stamp']
    - path: 'src/app/(storefront)/page.tsx'
      provides: 'redesign section composition'
  key_links:
    - from: 'retired homepage curved-label component'
      to: 'public/images/newsletter-label.png'
      via: 'next/image src (asset extracted in 11-01)'
      pattern: 'newsletter-label'
    - from: 'src/components/homepage/content.ts'
      to: 'scripts/component-contracts/button-system.test.mjs'
      via: "contract test asserts variant: 'inverseSecondary' in content.ts — keep it"
      pattern: 'inverseSecondary'
---

<objective>
Finish the homepage: build the legacy brush illustration component and legacy curved-label motif components from brand-motifs.js, restyle the brand bands (supply-chain, catalogues), testimonials, tea-journal, newsletter, contact/help, and FAQ sections, and align the page composition.

Purpose: second half of RD-04; introduces the redesign's signature motif language (brush discs, stamp, Caveat script).
Output: complete redesigned homepage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/brand-motifs.js
@design/extracted/homepage.js
@design/extracted/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: legacy brush illustration component + legacy curved-label motif components</name>
  <files>src/components/homepage/legacy-brush-illustration, retired homepage curved-label component folder</files>
  <read_first>design/extracted/brand-motifs.js (legacy animated artwork components source, float keyframes, textPath radius/viewBox), design/extracted/design-system.css brand-motif block (lines ~780–832), 11-UI-SPEC.md §5.3 New components note, docs/conventions.md (scaffolding + story requirements)</read_first>
  <action>
Scaffold `pnpm create:component -- homepage/legacy-brush-illustration` and `pnpm create:component -- homepage/stamp`. Both are Server Components (no interactivity).

**legacy brush illustration component**: wrapper around `next/image` rendering one of the extracted PNGs (`/images/business-handshake.png`, `/images/catalogue-cup.png`, `/images/newsletter-teapot.png`) — prop `illo: 'handshake' | 'cup' | 'teapot'`; width clamp per mockup (`w-[clamp(184px,18vw,268px)]` or canonical equivalent); decorative (`alt=""`, `aria-hidden`); gentle float animation via a `bc-float` keyframe in globals.css (`animate-[bc-float_7s_ease-in-out_infinite]` style utility defined in CSS, NOT arbitrary) with `motion-reduce:animate-none`.

**legacy curved-label component**: `/images/newsletter-label.png` ring via next/image + absolutely-positioned SVG with two curved `<textPath>` arcs (R=64 on a 200 viewBox, per brand-motifs.js) rendering the stamp copy in `font-script` (Caveat); text fill uses the paper token (`fill-paper` via Tailwind or `className="fill-[--color-paper]"`-equivalent — prefer a `text-paper` + `fill-current` pattern); decorative `aria-hidden`; float keyframe `st-float` with `motion-reduce:animate-none`. Port the SVG path/text geometry from brand-motifs.js verbatim (it is static markup, NOT dangerouslySetInnerHTML — write it as JSX).

Both get stories (legacy-brush-illustration: three illos; stamp: default on dark decorator).
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `src/components/homepage/legacy-brush-illustration/legacy-brush-illustration.tsx` exports `legacy brush illustration component`; `retired homepage curved-label component` exports `legacy curved-label component` and contains `textPath` - `grep -rc "dangerouslySetInnerHTML" src/components/homepage/legacy-brush-illustration retired homepage curved-label component folder` returns 0 - `grep -rc "motion-reduce:animate-none" src/components/homepage/legacy-brush-illustration retired homepage curved-label component folder` returns >= 2 - both folders contain a `.stories.tsx` file; `grep -rc "use client" src/components/homepage/legacy-brush-illustration retired homepage curved-label component folder` returns 0
</acceptance_criteria>
<done>Motif components with stories, decorative a11y handling, reduced-motion-safe float</done>
</task>

<task type="auto">
  <name>Task 2: Brand bands + testimonials + tea-journal</name>
  <files>src/components/homepage/supply-chain, src/components/homepage/catalogues, src/components/homepage/testimonials, src/components/homepage/tea-journal, src/components/homepage/content.ts</files>
  <read_first>all four component folders + content.ts (current CTA variants — contract test asserts `variant: 'inverseSecondary'` in content.ts; testimonials-slider embla wiring), design/extracted/homepage.js GrowBand/CatalogueBand/Testimonials/JournalSection JSX, design/extracted/design-system.css .bband/.tst/.jrnl blocks, 11-UI-SPEC.md §5.3 rows 7–8 + 12</read_first>
  <action>
**Brand bands (.bband)** for supply-chain and catalogues CTA bands: `Section.Root tone="inverse"` (ink) for one and `tone="brand"` (green-deep) for the other (match homepage.js assignment); inner 3-col grid `lg:grid-cols-[minmax(0,260px)_1fr_minmax(0,210px)] items-center gap-10`; left `<legacy brush illustration component illo=.../>`, right `<legacy curved-label component/>`; center: `Eyebrow tone="gold"`, `type-heading-01 text-paper` headline with the highlight word wrapped in `<em className="italic text-gold">`, copy `text-paper/75`, CTA `<Button variant="inverse">`. KEEP `variant: 'inverseSecondary'` in content.ts wherever the contract test asserts it (read the assertion; if the redesigned band uses a different variant visually, satisfy the test by keeping the asserted entry or updating the test in the same commit).

**Testimonials (.tst)**: grid `lg:grid-cols-[0.9fr_1.6fr]`; selector buttons (existing embla/slider state preserved): inactive `text-ink-soft`, active `bg-card shadow-1 rounded-lg`; gold quote glyph; quote `font-display text-[clamp(1.4rem,2.4vw,2.1rem)] leading-[1.32]`; mono attribution `type-mono-meta text-ink-faint`. Keep slider behavior + reduced-motion handling.

**Tea journal (.jcard)**: 3-col grid; media 220px `rounded-lg` with hover zoom — keep exact trio `motion-reduce:transform-none motion-reduce:transition-none motion-reduce:group-hover:scale-100` (contract-tested file); meta row `type-mono-meta` with `text-brand` category; `font-display text-[1.25rem]` titles; section head Eyebrow + heading + link-arrow to the journal.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:contracts</automated>
</verify>
<acceptance_criteria> - `grep -rc "legacy brush illustration component" src/components/homepage/supply-chain src/components/homepage/catalogues` total >= 1; `grep -rc "Stamp" src/components/homepage/supply-chain src/components/homepage/catalogues` total >= 1 - `grep -c "inverseSecondary" src/components/homepage/content.ts` returns >= 1 (or contract test updated in lockstep, both committed together) - `grep -rc "motion-reduce:group-hover:scale-100" src/components/homepage/tea-journal` returns >= 1 - `pnpm test:contracts` exits 0 - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|brandStrong" src/components/homepage/supply-chain src/components/homepage/catalogues src/components/homepage/testimonials src/components/homepage/tea-journal` returns no files
</acceptance_criteria>
<done>Motif bands with brush/stamp, testimonials, journal cards live; contracts green</done>
</task>

<task type="auto">
  <name>Task 3: Newsletter + contact/help + FAQ + page composition</name>
  <files>src/components/homepage/newsletter, src/components/homepage/contact, src/components/homepage/contact-form, src/components/homepage/faq, src/app/(storefront)/page.tsx</files>
  <read_first>all four component folders (Server Action/honeypot/aria wiring in newsletter + contact forms — MUST NOT change), src/app/(storefront)/page.tsx (current section order), design/extracted/homepage.js NewsletterBand/HelpSection/FAQSection JSX + App section order, 11-UI-SPEC.md §5.3 rows 9–11 + 13</read_first>
  <action>
**Newsletter**: green-deep rounded card (`bg-brand-deep rounded-lg` inside a paper section, clamp padding `p-[clamp(28px,5vw,64px)]`) — Eyebrow gold, `type-heading-02 text-paper`, pill input `bg-paper/10 border border-paper/25 rounded-full text-paper placeholder:text-paper/60 focus:border-gold`, submit `<Button variant="inverse">`. KEEP the Server Action, honeypot, pending/success/error + aria-live exactly.

**Contact/help (.help)**: `Section.Root tone="inverse"`; left column: gold Eyebrow, `type-heading-01 text-paper`, contact rows — 44px circle icon `bg-brand-tint text-brand rounded-full`, `type-mono-meta text-paper/60` label, `font-display text-paper` value (keep real tel/mailto links); right: card form `bg-card border border-hairline-2 rounded-lg p-7` using the restyled field primitives. KEEP contact form action/validation/honeypot/feedback.

**FAQ**: `Section.Root tone="sunken"` with `max-w-base` container; rows `border-t border-hairline`; question `font-display text-[clamp(1.1rem,1.8vw,1.4rem)] py-6.5`; indicator: 30px circle `border border-hairline rounded-full`, open state `bg-brand text-paper rotate-45 transition-transform motion-reduce:transition-none`. Keep accordion/disclosure behavior.

**page.tsx**: align section order to homepage.js composition (hero → stats → range → services → organic → certs → testimonials → band → journal → newsletter → help/contact → FAQ — adapt to which sections actually exist; do NOT add mockup-only sections that have no existing component). Section wrappers via Section.Root tones per each component's spec.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:integration && pnpm build</automated>
</verify>
<acceptance_criteria> - `grep -rc "bg-brand-deep" src/components/homepage/newsletter` returns >= 1 - newsletter + contact forms keep their Server Action imports (grep the action identifiers found in read_first — no diff to action wiring) - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|type-display-0\|brandStrong" src/components/homepage/newsletter src/components/homepage/contact src/components/homepage/contact-form src/components/homepage/faq "src/app/(storefront)/page.tsx"` returns no files - `pnpm test:integration` exits 0 (form boundaries intact); `pnpm build` exits 0
</acceptance_criteria>
<done>Homepage complete: newsletter/help/FAQ restyled with actions intact, composition aligned</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                        |
| --------------------------------- | ---------------------------------- |
| user → newsletter + contact forms | untrusted input via restyled forms |

## STRIDE Threat Register

| Threat ID | Category          | Component                           | Disposition | Mitigation Plan                                                           |
| --------- | ----------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------- |
| T-11-15   | Tampering/DoS     | homepage newsletter + contact forms | mitigate    | Server Actions, honeypot, rate limiting untouched; integration tests gate |
| T-11-16   | Elevation (XSS)   | legacy curved-label component SVG port                      | mitigate    | static JSX SVG, no dangerouslySetInnerHTML (acceptance grep)              |
| T-11-17   | DoS (motion/a11y) | float animations, FAQ rotate        | mitigate    | motion-reduce:animate-none / transition-none guards asserted by grep      |

</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind`, `pnpm test:contracts`, `pnpm test:integration`, `pnpm build` exit 0
- Dev-server full homepage pass at desktop + 375px against extracted-design.html
</verification>

<success_criteria>

- Homepage fully matches the redesign composition with motif bands; all form behavior preserved; zero old tokens under src/components/homepage/ and page.tsx
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-07-SUMMARY.md`
</output>
