---
phase: 11-full-visual-redesign
plan: 08
type: execute
wave: 4
depends_on: ['11-02', '11-03']
files_modified:
  - src/components/collection/product-card
  - src/components/collection/quick-add-button
  - src/components/collection/filter-panel
  - src/components/collection/toolbar
  - src/components/collection/sort-select
  - src/components/collection/story-disclosure
  - src/app/(storefront)/collections/[handle]/_components/hero.tsx
  - src/app/(storefront)/collections/[handle]/_components/sidebar.tsx
  - src/app/(storefront)/collections/[handle]/_components/product-list.tsx
  - src/app/(storefront)/collections/[handle]/_components/product-list.test.tsx
  - src/app/(storefront)/collections/[handle]/_components/page-content.tsx
  - src/app/(storefront)/collections/[handle]/page.tsx
  - src/app/(storefront)/collections/page.tsx
  - src/app/(storefront)/collections/_components/collection-card-image.tsx
autonomous: true
requirements: [RD-05]

must_haves:
  truths:
    - 'Product cards match the mockup .pcard: 1/1.12 media on paper-2, badge pills top-left, hover-reveal quick-add (always visible on touch), mono origin/type eyebrow line, serif title as sole PDP link, serif price with mono unit'
    - 'Single-variant available products quick-add via the existing Server Action; multi-variant products keep Quick View/PDP (CQA-02)'
    - 'Cards show a productType eyebrow when non-empty, up to 2 certification badges from tags, no More info button, two-zone identity/controls layout, and no quantity stepper in listing context (Phase 9 intents CARD-02..06 absorbed)'
    - 'Collection hero is a green-deep band with breadcrumb, gold eyebrow, paper serif title, and mono meta stats'
    - 'Filters, toolbar, sort, and the collections index wear the new system with URL/filter behavior unchanged'
  artifacts:
    - path: 'src/components/collection/product-card/product-card.tsx'
      provides: '.pcard redesign absorbing CARD-02..05'
      contains: 'type-mono-meta'
    - path: 'src/components/collection/product-card/product-purchase-form.tsx'
      provides: 'showQuantity prop (default true; card passes false) — CARD-06'
      contains: 'showQuantity'
    - path: 'src/app/(storefront)/collections/[handle]/_components/hero.tsx'
      provides: '.coll__hero green-deep treatment'
  key_links:
    - from: 'src/components/collection/product-card/product-card.tsx'
      to: 'src/components/collection/product-card/product-card.test.tsx'
      via: 'h3 class-string assertion updated in lockstep'
      pattern: 'font-display'
    - from: 'src/app/(storefront)/collections/[handle]/_components/product-list.tsx'
      to: 'product-list.test.tsx'
      via: 'border-subtle → border-hairline-2 assertion updated in lockstep'
      pattern: 'border-hairline-2'
    - from: 'src/components/collection/quick-add-button'
      to: 'src/lib/cart/actions.ts'
      via: 'unchanged Server Action + useAddToCart flow (CQA-04)'
      pattern: 'useAddToCart|addToCart'
---

<objective>
Restyle collection pages and the shared product card to the redesign's listing presentation, absorbing Phase 9's compatible behavioral intents into the new card (productType eyebrow = CARD-02, certification badges from tags = CARD-03, no "More info" = CARD-04, two-zone hierarchy = CARD-05, `showQuantity` prop = CARD-06) while preserving Phase 8 quick-add contracts (CQA-01..06) and bounded PLP payloads.

Note: this plan supersedes the unimplemented Phase 9 plan where compatible with the mockup — the user can mark Phase 9 superseded after this ships. CARD-01 (wide side image column) is NOT absorbed: the mockup card is vertical and supersedes that layout.

Output: redesigned PLP per collection-page.js + product-card-footer.js ProductCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-full-visual-redesign/11-UI-SPEC.md
@design/extracted/collection-page.js
@design/extracted/product-card-footer.js
@design/extracted/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product card redesign (absorb CARD-02..06) + lockstep test</name>
  <files>src/components/collection/product-card, src/components/collection/quick-add-button</files>
  <read_first>src/components/collection/product-card/product-card.tsx + product-purchase-form.tsx + product-card.test.tsx + product-card.stories.tsx (current props, quick-add contract, h3 class assertion), src/components/collection/quick-add-button (Phase 8 QuickAddButton), design/extracted/product-card-footer.js ProductCard JSX, design/extracted/design-system.css .pcard block, 11-UI-SPEC.md §5.5</read_first>
  <action>
**Card (.pcard)** — vertical layout:
- Media: `relative aspect-[1/1.12] rounded-lg bg-paper-2 overflow-hidden`; image `group-hover:scale-[1.06]` WITH the trio `motion-reduce:transform-none motion-reduce:transition-none motion-reduce:group-hover:scale-100`.
- Badges top-left (CARD-03): extract organic/certification signals from product tags (e.g. tags containing "organic", "ACO", "USDA", "certified" — derive the exact tag heuristics from existing tag usage in the codebase if present); render up to 2 `<Badge variant="organic">`/`<Badge variant="gold">` pills.
- Quick-add (Phase 8 contract preserved): full-width `<Button variant="primary" size="sm">` (ink) pinned to media bottom, `opacity-0 group-hover:opacity-100 focus-within:opacity-100 max-lg:opacity-100` (always visible on touch); ONLY for single-variant available products via the existing QuickAddButton/useAddToCart path; multi-variant keeps Quick View/PDP. DO NOT widen the `variants(first: 2)` data contract (CQA-03/06).
- Body (two-zone, CARD-05): identity zone — origin/productType line `type-mono-meta text-ink-faint` rendered when productType is non-empty (CARD-02), title `font-display text-[1.2rem] leading-[1.1] my-1.5` as the SOLE link to the PDP (CARD-04 — remove any "More info" button if present); price row: `font-bold` price + `font-mono text-[11px] text-ink-faint` unit.
- **product-purchase-form.tsx (CARD-06)**: add `showQuantity?: boolean` prop defaulting to `true`; when `false`, omit the quantity stepper (quantity 1). The collection card render passes `showQuantity={false}`. Keep all add-to-cart validation/feedback.

**Lockstep**: update `product-card.test.tsx` h3 class-string assertion to the new classes (e.g. `font-display text-[1.2rem]`). Update `product-card.stories.tsx` to cover: eyebrow present/absent, badges, quick-add states (success/error/pending/sold-out/multi-variant fallback — keep existing CQA-05 stories), and no-quantity-stepper.
</action>
<verify>
<automated>pnpm test:unit && pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `grep -c "showQuantity" src/components/collection/product-card/product-purchase-form.tsx` returns >= 2 (prop + default) - `grep -rc "More info" src/components/collection/product-card/` returns 0 - `grep -c "motion-reduce:group-hover:scale-100" src/components/collection/product-card/product-card.tsx` returns >= 1 - `grep -c "type-mono-meta" src/components/collection/product-card/product-card.tsx` returns >= 1 - `pnpm test:unit` exits 0 (product-card.test.tsx updated in lockstep)
</acceptance_criteria>
<done>.pcard card with CARD-02..06 absorbed, Phase 8 quick-add contract intact, tests/stories lockstepped</done>
</task>

<task type="auto">
  <name>Task 2: Collection hero + sidebar/filters + toolbar/sort + product list</name>
  <files>src/app/(storefront)/collections/[handle]/_components/hero.tsx, src/app/(storefront)/collections/[handle]/_components/sidebar.tsx, src/app/(storefront)/collections/[handle]/_components/product-list.tsx, src/app/(storefront)/collections/[handle]/_components/product-list.test.tsx, src/app/(storefront)/collections/[handle]/_components/page-content.tsx, src/app/(storefront)/collections/[handle]/page.tsx, src/components/collection/filter-panel, src/components/collection/toolbar, src/components/collection/sort-select</files>
  <read_first>all listed files (filter state/URL params, bounded pagination, Shopify hero image usage), design/extracted/collection-page.js JSX, design/extracted/design-system.css .coll__/.filters/.fopt blocks, 11-UI-SPEC.md §5.4</read_first>
  <action>
**Hero (.coll__hero)**: `Section.Root tone="brand"` (green-deep) with `py-[clamp(40px,5vw,70px)]`-equivalent spacing; keep the Shopify collection hero image behind at 35% opacity (`opacity-35 object-cover` absolute fill); breadcrumb `type-mono-meta text-paper/60` with current page `text-gold`; `Eyebrow tone="gold"`; title `text-paper font-display text-[clamp(2.4rem,5vw,4rem)]`; description `text-paper/85 max-w-[52ch]`; meta stats row (`font-display text-[1.6rem] text-paper` + `type-mono-meta text-paper/70` labels).

**Layout**: `lg:grid-cols-[252px_1fr] gap-10`, sidebar `sticky top-32`. **Filter panel**: groups `border-b border-hairline py-5.5`; headings `font-mono text-[11px] tracking-[0.12em] uppercase text-ink-faint`; option rows with restyled checkbox (.fbox from 11-03) + counts `font-mono text-ink-faint` right; "Clear" `type-mono-meta text-gold-deep`. Wholesale upsell card: `bg-brand-tint border border-brand/20 rounded-lg p-5.5` + serif title + `<Button variant="brand" size="sm">` full-width. KEEP all filter state handling and URL params.

**Toolbar**: result count `type-mono-meta text-ink-faint`; sort pill `border border-hairline bg-card rounded-full px-4 py-2.25 type-label`; active filter chips `bg-brand-tint text-brand rounded-full px-3 py-1.75 text-xs font-semibold` with remove ×.

**Product list/grid**: `lg:grid-cols-3 sm:grid-cols-2 grid-cols-2 gap-y-5.5 gap-x-4.5`; empty state — centered leaf icon, `font-display text-2xl` "No matches" + muted copy + ghost clear button. **Lockstep**: `product-list.test.tsx` asserts `border-subtle` — update the assertion to the new class (`border-hairline-2`). Keep bounded pagination (AUDIT-06).
</action>
<verify>
<automated>pnpm test:unit && pnpm lint && pnpm typecheck && pnpm lint:tailwind</automated>
</verify>
<acceptance_criteria> - `grep -c "border-subtle" src/app/(storefront)/collections/[handle]/_components/product-list.tsx src/app/(storefront)/collections/[handle]/_components/product-list.test.tsx` total returns 0 - `grep -c "tone=\"brand\"" src/app/(storefront)/collections/[handle]/_components/hero.tsx` returns >= 1 (or equivalent Section tone usage) - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|brandStrong" src/components/collection/filter-panel src/components/collection/toolbar src/components/collection/sort-select "src/app/(storefront)/collections/[handle]/_components"` returns no files - `pnpm test:unit` exits 0
</acceptance_criteria>
<done>PLP hero/filters/toolbar/grid match the mockup; URL/filter/pagination behavior unchanged; tests lockstepped</done>
</task>

<task type="auto">
  <name>Task 3: Collections index + story-disclosure + card image</name>
  <files>src/app/(storefront)/collections/page.tsx, src/app/(storefront)/collections/_components/collection-card-image.tsx, src/components/collection/story-disclosure</files>
  <read_first>src/app/(storefront)/collections/page.tsx (48 old-class occurrences — current structure + Shopify collection data usage), collection-card-image.tsx (motion-reduce trio), story-disclosure files, design/extracted/collection-page.js + design-system.css .rtile, 11-UI-SPEC.md §3 + §5.4</read_first>
  <action>
**Collections index (collections/page.tsx)**: restyle to the `.rtile` tile language — collection tiles `relative aspect-[1/1.08] rounded-lg overflow-hidden` with bottom scrim, gold mono tag, `font-display text-paper` title, hover-reveal "Shop now" (keyboard/touch-visible as in 11-06 Task 2); page header: Eyebrow + `type-heading-01`. Keep Shopify collections data source (restored in recent commits) and all links.

**collection-card-image.tsx**: class swap; PRESERVE the motion-reduce trio on its `group-hover:scale` line (contract-tested).

**story-disclosure**: swap classes per UI-SPEC §3 table (`text-muted`→`text-ink-soft`, `border-default`→`border-hairline`, link classes → `text-brand hover:text-brand-deep`); keep the disclosure aria behavior.
</action>
<verify>
<automated>pnpm lint && pnpm typecheck && pnpm lint:tailwind && pnpm test:contracts</automated>
</verify>
<acceptance_criteria> - `grep -c "motion-reduce:group-hover:scale-100" src/app/(storefront)/collections/_components/collection-card-image.tsx` returns >= 1 - `grep -rln "bg-canvas\|bg-surface\|text-default\|text-muted\|text-subtle\|border-default\|type-display-0" "src/app/(storefront)/collections/page.tsx" "src/app/(storefront)/collections/_components" src/components/collection/story-disclosure` returns no files - `pnpm test:contracts` exits 0
</acceptance_criteria>
<done>Collections index on tile language; trio preserved; zero old tokens in owned files</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                       |
| -------------------- | ------------------------------------------------- |
| user → quick-add     | add-to-cart mutation via existing Server Action   |
| user → filter params | URL search params drive server-rendered filtering |

## STRIDE Threat Register

| Threat ID | Category  | Component          | Disposition | Mitigation Plan                                                                                                          |
| --------- | --------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-11-18   | Tampering | quick-add path     | mitigate    | reuse existing Server Action + useAddToCart; no new mutation surface; data contract unchanged (CQA-03 grep + unit tests) |
| T-11-19   | DoS       | PLP payload        | mitigate    | bounded pagination and minimal variant fields preserved (AUDIT-06/CQA-06); no query widening                             |
| T-11-20   | Tampering | tag-derived badges | accept      | tags are first-party Shopify merchant data rendered as text in pills; no HTML injection vector                           |

</threat_model>

<verification>
- `pnpm test:unit`, `pnpm lint`, `pnpm typecheck`, `pnpm lint:tailwind`, `pnpm test:contracts` exit 0
- Dev-server check: a collection page + collections index at desktop/375px; quick-add on single-variant card; multi-variant fallback
</verification>

<success_criteria>

- PLP + card match the mockup; CARD-02..06 behaviors present; CQA contracts green; zero old tokens in owned files
  </success_criteria>

<output>
After completion, create `.planning/phases/11-full-visual-redesign/11-08-SUMMARY.md`
</output>
