---
phase: 01-bulk-savings-pdp-and-cart-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shopify/queries/product.graphql
  - src/lib/shopify/queries/cart.graphql
  - src/lib/shopify/types/index.ts
  - src/lib/shopify/operations/product.ts
  - src/lib/shopify/operations/cart.ts
  - src/lib/shopify/types/generated/gql.ts
  - src/lib/shopify/types/generated/graphql.ts
  - src/components/product/bulk-savings/bulk-savings.tsx
  - src/components/product/bulk-savings/bulk-savings.stories.tsx
  - src/components/product/bulk-savings/index.ts
  - src/components/product/index.ts
  - src/components/product/product-form/product-form.tsx
  - src/app/(storefront)/products/[handle]/page.tsx
  - src/app/(storefront)/cart/page.tsx
autonomous: true
requirements:
  - BULK-01
  - BULK-02
  - BULK-03
  - BULK-04
  - BULK-05
  - BULK-06
  - BULK-07
  - BULK-08
user_setup: []
must_haves:
  truths:
    - Product variants include quantity price break tiers when Shopify returns them.
    - Product-level fallback tiers parse safely from custom.bulk_pricing_tiers.
    - PDP add-to-cart quantity is selected by the customer and passed to the Server Action.
    - Cart lines expose Shopify line discount allocations and line totals.
  artifacts:
    - src/components/product/bulk-savings/bulk-savings.tsx
    - src/components/product/bulk-savings/bulk-savings.stories.tsx
  key_links:
    - getProduct -> ProductForm -> BulkSavings -> addToCartAction quantity
    - GetCartDocument -> reshapeCart -> cart page discount display
---

<objective>
Implement the bulk savings product and cart parity slice for the headless storefront.

Purpose: replace the live Liquid theme's app-injected "Buy in Bulk and Save" customer flow with first-class, typed Next storefront behavior backed by Shopify data.
Output: GraphQL query updates, domain type updates, PDP quantity and bulk savings UI, cart discount allocation display, generated GraphQL types, and phase summary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bulk-savings-pdp-and-cart-parity/01-CONTEXT.md
@docs/conventions.md
@node_modules/next/dist/docs/01-app/01-getting-started/06-fetching-data.md
@node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
@src/lib/shopify/queries/product.graphql
@src/lib/shopify/queries/cart.graphql
@src/lib/shopify/types/index.ts
@src/lib/shopify/operations/product.ts
@src/lib/shopify/operations/cart.ts
@src/components/product/product-form/product-form.tsx
@src/app/(storefront)/products/[handle]/page.tsx
@src/app/(storefront)/cart/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Shopify product and cart data contracts</name>
  <files>src/lib/shopify/queries/product.graphql, src/lib/shopify/queries/cart.graphql, src/lib/shopify/types/index.ts, src/lib/shopify/operations/product.ts, src/lib/shopify/operations/cart.ts, src/lib/shopify/types/generated/gql.ts, src/lib/shopify/types/generated/graphql.ts</files>
  <read_first>src/lib/shopify/queries/product.graphql, src/lib/shopify/queries/cart.graphql, src/lib/shopify/types/index.ts, src/lib/shopify/operations/product.ts, src/lib/shopify/operations/cart.ts</read_first>
  <action>Add variant `quantityPriceBreaks(first: 10)` and product `bulkPricingTiersMetafield: metafield(namespace: "custom", key: "bulk_pricing_tiers")` to the product GraphQL query. Add cart line `cost` and `discountAllocations` fields to the cart GraphQL fragment. Extend hand-written domain types with `BulkPricingTier`, variant `quantityPriceBreaks`, product `bulkPricingTiers`, cart line `cost`, and cart line `discountAllocations`. Implement safe JSON parsing for fallback tiers in `operations/product.ts` using `unknown` narrowing, no `any`. Reshape native quantity price breaks and cart discounts. Run `pnpm codegen` after query changes.</action>
  <verify>pnpm codegen</verify>
  <acceptance_criteria>
    - `src/lib/shopify/queries/product.graphql` contains `quantityPriceBreaks(first: 10)`.
    - `src/lib/shopify/queries/product.graphql` contains `bulkPricingTiersMetafield`.
    - `src/lib/shopify/queries/cart.graphql` contains `discountAllocations`.
    - `src/lib/shopify/types/index.ts` exports `BulkPricingTier`.
    - `src/lib/shopify/operations/product.ts` contains `parseBulkPricingTiers`.
    - `src/lib/shopify/operations/cart.ts` contains `reshapeDiscountAllocation`.
    - `pnpm codegen` completes successfully or the summary records the exact external schema/auth blocker.
  </acceptance_criteria>
  <done>Product and cart operations return typed tier and discount data without direct generated type imports outside `src/lib/shopify/types/index.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Build PDP bulk savings UI and quantity add-to-cart</name>
  <files>src/components/product/bulk-savings/bulk-savings.tsx, src/components/product/bulk-savings/bulk-savings.stories.tsx, src/components/product/bulk-savings/index.ts, src/components/product/index.ts, src/components/product/product-form/product-form.tsx, src/app/(storefront)/products/[handle]/page.tsx</files>
  <read_first>src/components/product/product-form/product-form.tsx, src/components/ui/quantity-stepper/quantity-stepper.tsx, src/components/ui/price/price.tsx, src/app/(storefront)/products/[handle]/page.tsx</read_first>
  <action>Replace the scaffolded `BulkSavings` component with a presentational tier list/table using the heading `Buy in Bulk and Save`, current quantity highlighting, token classes, and `cn()` for conditional class names. Add Storybook examples for native price-break tiers and fallback percent tiers. Update `ProductForm` to accept optional `bulkPricingTiers`, use `QuantityStepper`, pass selected quantity to `addToCartAction`, and render `BulkSavings` for the selected variant's `quantityPriceBreaks` or product fallback tiers. Update the product page to pass `product.bulkPricingTiers` while preserving pre-existing user layout/analytics edits.</action>
  <verify>pnpm lint</verify>
  <acceptance_criteria>
    - `src/components/product/bulk-savings/bulk-savings.tsx` contains `Buy in Bulk and Save`.
    - `src/components/product/bulk-savings/bulk-savings.stories.tsx` has at least two exported stories.
    - `src/components/product/product-form/product-form.tsx` imports `QuantityStepper`.
    - `src/components/product/product-form/product-form.tsx` calls `addToCartAction(selectedVariant.id, quantity)`.
    - `src/app/(storefront)/products/[handle]/page.tsx` passes `bulkPricingTiers={product.bulkPricingTiers}` to `ProductForm`.
  </acceptance_criteria>
  <done>PDP customers can select a quantity, see configured tiers, and add that quantity to cart.</done>
</task>

<task type="auto">
  <name>Task 3: Show cart discount allocations and complete verification</name>
  <files>src/app/(storefront)/cart/page.tsx, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/01-bulk-savings-pdp-and-cart-parity/01-01-SUMMARY.md</files>
  <read_first>src/app/(storefront)/cart/page.tsx, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Update the cart page to display Shopify-reported line total and a discount row when `line.discountAllocations` is non-empty. Keep the empty cart and checkout flows unchanged. Run final verification with `pnpm lint` and `pnpm build`. Update ROADMAP and STATE to mark Phase 1 complete only after verification passes. Create the SUMMARY file with commits, files, decisions, deviations, and self-check.</action>
  <verify>pnpm lint; pnpm build</verify>
  <acceptance_criteria>
    - `src/app/(storefront)/cart/page.tsx` reads `line.discountAllocations`.
    - `src/app/(storefront)/cart/page.tsx` displays `Price price={line.cost.totalAmount}` for line totals.
    - `.planning/phases/01-bulk-savings-pdp-and-cart-parity/01-01-SUMMARY.md` exists after completion.
    - Final summary records results for `pnpm codegen`, `pnpm lint`, and `pnpm build`.
  </acceptance_criteria>
  <done>Cart reflects Shopify-applied savings and the phase is fully summarized.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm codegen`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Confirm generated query text includes bulk tier and cart discount fields.
- [ ] Confirm new Storybook story file exists for `BulkSavings`.
</verification>

<success_criteria>

- All tasks completed.
- All verification checks pass or any external auth/schema blocker is documented with exact output.
- No errors or warnings introduced by lint/build.
- The PDP can render tier data and submit selected quantity.
- The cart can render Shopify discount allocation data.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-bulk-savings-pdp-and-cart-parity/01-01-SUMMARY.md`.
</output>
