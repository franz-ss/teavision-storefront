---
phase: 10-cart-checkout-critical-flow-tests
roadmap_status: standalone_phase_5_follow_up
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - tests/e2e/cart-checkout.spec.ts
  - tests/mocks/shopify-graphql-server.ts
  - playwright.config.ts
  - docs/testing/cart-checkout-uat.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-10
must_haves:
  truths:
    - 'Playwright covers a deterministic local cart-to-checkout handoff path with fake Shopify'
    - 'E2E does not hit live Shopify or third-party services'
    - 'Hosted Shopify checkout validation is documented as staged UAT but not executed until the dev store is ready'
    - 'Testing documentation reflects the new commands, fake Shopify policy, E2E scope, and residual gaps'
  artifacts:
    - path: 'tests/e2e/cart-checkout.spec.ts'
      provides: 'Thin revenue-path E2E suite'
    - path: 'docs/testing/cart-checkout-uat.md'
      provides: 'Hosted checkout UAT checklist'
    - path: '.planning/codebase/TESTING.md'
      provides: 'Updated project testing documentation'
  key_links:
    - from: 'tests/e2e/cart-checkout.spec.ts'
      to: 'tests/mocks/shopify-graphql-server.ts'
      via: 'fake Storefront backend'
    - from: 'docs/testing/cart-checkout-uat.md'
      to: '.planning/codebase/TESTING.md'
      via: 'hosted checkout UAT reference'
---

<objective>
Add thin E2E coverage for the local revenue path and document hosted Shopify checkout UAT for the external checkout responsibilities.

Real Shopify hosted checkout testing is currently blocked. Do not run payment, order-creation, tax, shipping-rate, or success-redirect tests against Shopify until the dev store is set up and the store owner explicitly confirms checkout testing may begin.
</objective>

<context>
@.planning/phases/10-cart-checkout-critical-flow-tests/10-00-STRATEGY.md
@.planning/phases/10-cart-checkout-critical-flow-tests/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cart-checkout Playwright flow</name>
  <files>tests/e2e/cart-checkout.spec.ts, tests/mocks/shopify-graphql-server.ts, playwright.config.ts</files>
  <action>
Write deterministic Playwright tests against the built app and fake Shopify server. Cover PDP add-to-cart, success status, header badge refresh, `/cart` line/subtotal, quantity update, remove-to-empty, safe add failure/retry, and checkout handoff URL.
  </action>
  <verify>
```bash
pnpm test:e2e
```
  </verify>
  <acceptance_criteria>
    - E2E tests use fake Shopify endpoint, not live Shopify.
    - Third-party hosts are blocked or disabled.
    - Selectors use accessible roles/names.
    - Checkout assertion stops at URL/href handoff and does not pretend to process payment locally.
    - Tests use a fake checkout URL only; they do not navigate into real Shopify checkout.
  </acceptance_criteria>
  <done>One thin, deterministic browser suite protects the local cart-to-checkout handoff path.</done>
</task>

<task type="auto">
  <name>Task 2: Document hosted checkout UAT</name>
  <files>docs/testing/cart-checkout-uat.md</files>
  <action>
Create a repeatable staged Shopify checkout checklist for customer information, shipping address, billing address, shipping method, payment method, order summary, order creation, payment success/failure, tax/shipping calculations, success state, and redirects.

This task documents the checklist only. Do not execute the checklist until the Shopify dev store is configured, test payments are enabled safely, and the store owner gives explicit approval.
</action>
<verify>

```bash
pnpm lint
```

  </verify>
  <acceptance_criteria>
    - Checklist clearly marks Shopify hosted checkout as external to local unit tests.
    - Checklist clearly marks execution as blocked until dev store setup and explicit approval.
    - Checklist includes bulk-discount cart subtotal comparison before shipping/tax.
    - Checklist includes test payment success and failure paths when Shopify test mode is available.
  </acceptance_criteria>
  <done>Hosted checkout validation is documented as repeatable UAT instead of hidden tribal knowledge.</done>
</task>

<task type="auto">
  <name>Task 3: Update testing documentation and residual gaps</name>
  <files>.planning/codebase/TESTING.md</files>
  <action>
Update the codebase testing documentation with new commands, fixture locations, fake Shopify policy, E2E scope, hosted checkout UAT, and residual gaps for discount-code scope and 100-line cart cap.
  </action>
  <verify>
```bash
pnpm lint
pnpm test:contracts
```
  </verify>
  <acceptance_criteria>
    - Docs no longer say cart verification is manual-only.
    - Docs explain when to use unit, integration, Storybook, E2E, and UAT for cart changes.
    - Residual gaps are explicit and traceable.
  </acceptance_criteria>
  <done>Project testing docs reflect the implemented cart/checkout testing strategy and remaining risks.</done>
</task>

</tasks>

<verification>
```bash
pnpm lint
pnpm typecheck
pnpm test:contracts
pnpm test:unit
pnpm test:integration
pnpm test:stories
pnpm test:e2e
pnpm build
```
</verification>

<success_criteria>

- E2E tests cover the local revenue path without live external services.
- Hosted checkout responsibilities are covered by a concrete staged UAT checklist, but execution is gated until the dev store is ready.
- Testing docs are updated with commands, ownership boundaries, and residual risks.
  </success_criteria>
