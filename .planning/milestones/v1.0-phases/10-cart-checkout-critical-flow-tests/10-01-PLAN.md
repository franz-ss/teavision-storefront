---
phase: 10-cart-checkout-critical-flow-tests
roadmap_status: standalone_phase_5_follow_up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - package.json
  - pnpm-lock.yaml
  - vitest.config.mts
  - playwright.config.ts
  - tests/setup/vitest.ts
  - tests/fixtures/shopify/money.ts
  - tests/fixtures/shopify/product.ts
  - tests/fixtures/shopify/cart.ts
  - tests/mocks/shopify-graphql-server.ts
  - tests/mocks/third-party-network.ts
  - src/lib/shopify/client.ts
  - tests/setup/smoke.test.ts
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-10
must_haves:
  truths:
    - 'AGENTS.md documents an explicit approved exception for Vitest and Playwright on revenue-critical cart/checkout coverage'
    - 'package.json and pnpm-lock.yaml include the required test runner dependencies'
    - 'shopifyFetch can use a guarded test-only Storefront GraphQL endpoint override without changing production behavior'
    - 'Fixture and fake Shopify infrastructure exists before downstream plans consume it'
  artifacts:
    - path: 'AGENTS.md'
      provides: 'Testing policy exception'
    - path: 'package.json'
      provides: 'Unit, integration, E2E scripts and devDependencies'
    - path: 'src/lib/shopify/client.ts'
      provides: 'Guarded fake Shopify endpoint override'
    - path: 'tests/mocks/shopify-graphql-server.ts'
      provides: 'Stateful fake Storefront server'
  key_links:
    - from: 'playwright.config.ts'
      to: 'src/lib/shopify/client.ts'
      via: 'test Storefront endpoint env var'
    - from: 'tests/mocks/shopify-graphql-server.ts'
      to: 'tests/fixtures/shopify/cart.ts'
      via: 'fake cart payload factories'
---

<objective>
Create the test infrastructure, policy exception, fixture foundation, fake Shopify boundary, and guarded Storefront endpoint override needed before cart tests can run safely.
</objective>

<context>
@.planning/phases/10-cart-checkout-critical-flow-tests/10-CONTEXT.md
@.planning/phases/10-cart-checkout-critical-flow-tests/10-RESEARCH.md
@.planning/phases/10-cart-checkout-critical-flow-tests/10-00-STRATEGY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile testing policy and add scripts</name>
  <files>AGENTS.md, package.json, vitest.config.mts, playwright.config.ts, tests/setup/vitest.ts</files>
  <action>
Update the testing policy to document the approved exception for Phase 10: Vitest and Playwright are allowed for cart/checkout revenue-critical coverage while Storybook remains the preferred component documentation and interaction surface. Install the required devDependencies and update `pnpm-lock.yaml`. Add scripts for unit, integration, e2e, and CI-friendly execution without removing existing `test:contracts` or `test:stories`.

Recommended dependencies:

- `vitest`
- `@vitejs/plugin-react`
- `vite-tsconfig-paths`
- `jsdom`
- `@testing-library/react`
- `@testing-library/dom`
- `@testing-library/jest-dom`
- `@playwright/test`
  </action>
  <verify>

```bash
pnpm typecheck
pnpm test:contracts
pnpm exec vitest --version
pnpm exec playwright --version
```

  </verify>
  <acceptance_criteria>
    - `AGENTS.md` no longer conflicts with the planned Vitest/Playwright exception.
    - Existing `pnpm test:contracts` remains unchanged and runnable.
    - `package.json` includes explicit `test:unit`, `test:integration`, and `test:e2e` scripts.
    - `package.json` includes required devDependencies for Vitest, React Testing Library, jsdom, and Playwright.
    - `pnpm-lock.yaml` is updated.
    - Vitest config uses the repo TypeScript path aliases.
  </acceptance_criteria>
  <done>Testing policy and scripts are documented, runnable, and compatible with existing contract and Storybook commands.</done>
</task>

<task type="auto">
  <name>Task 2: Add guarded fake Shopify endpoint override</name>
  <files>src/lib/shopify/client.ts, playwright.config.ts</files>
  <action>
Add an explicit test-only Storefront GraphQL URL override for server-side tests and Playwright. The override must be guarded by an explicit test env var such as `SHOPIFY_STOREFRONT_GRAPHQL_URL` and must not silently affect production. Keep existing credential fail-fast behavior for normal runtime.
  </action>
  <verify>
```bash
pnpm typecheck
pnpm build
```
  </verify>
  <acceptance_criteria>
    - Normal runtime still uses `https://${SHOPIFY_STORE_DOMAIN}/api/2026-04/graphql.json`.
    - Test runtime can route `shopifyFetch()` to a local fake endpoint.
    - Missing production credentials still fail fast when no test override is set.
    - Playwright config sets the fake endpoint only for E2E.
  </acceptance_criteria>
  <done>Server-side Shopify calls can be routed to a fake endpoint in tests without changing production behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture factories and fake Shopify server</name>
  <files>tests/fixtures/shopify/money.ts, tests/fixtures/shopify/product.ts, tests/fixtures/shopify/cart.ts, tests/mocks/shopify-graphql-server.ts, tests/mocks/third-party-network.ts, tests/setup/smoke.test.ts</files>
  <action>
Create typed fixtures and a stateful fake Storefront GraphQL server keyed by operation name. Include helpers for money, product variants, cart lines, cart payloads, discounts, missing images, sold-out variants, stale cart IDs, and Shopify `userErrors`. Add a third-party network blocking helper for Playwright. Add a tiny smoke test that imports the fixture and fake-server modules and exercises one fake cart create/read path so Plan 01 is independently verifiable.
  </action>
  <verify>
```bash
pnpm test:unit -- tests/setup/smoke.test.ts
pnpm typecheck
```
  </verify>
  <acceptance_criteria>
    - Fixtures import from `@/lib/shopify/types`, not generated types.
    - Fake server handles `GetProduct`, `CartCreate`, `CartLinesAdd`, `GetCart`, `CartLinesUpdate`, and `CartLinesRemove`.
    - Fake checkout URL is deterministic and tied to the fake cart.
    - Third-party blocking covers analytics, payment-provider, Trustoo, Searchanise, and Resend-like hosts where applicable.
  </acceptance_criteria>
  <done>Reusable fixtures and fake Shopify infrastructure are ready for server and E2E test plans.</done>
</task>

</tasks>

<verification>
```bash
pnpm lint
pnpm typecheck
pnpm test:contracts
pnpm test:unit
```
</verification>

<success_criteria>

- Testing policy conflict is resolved.
- Fake Shopify endpoint override is implemented and guarded.
- Fixture and mock infrastructure is reusable by later plans.
- No live Shopify or payment service is required for local automated tests.
  </success_criteria>
