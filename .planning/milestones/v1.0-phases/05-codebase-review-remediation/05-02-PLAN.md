---
phase: 05-codebase-review-remediation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - .husky/pre-commit
  - .husky/pre-push
  - scripts/component-contracts/button-system.test.mjs
  - src/components/product/product-form/product-form.stories.tsx
  - src/components/collection/product-card/product-purchase-form.stories.tsx
  - src/app/(storefront)/cart/_components/cart-line-actions.stories.tsx
  - src/lib/rate-limit/index.ts
  - src/lib/contact/actions.ts
  - src/app/api/search/suggestions/route.ts
  - src/app/api/webhooks/shopify/route.ts
  - src/app/api/products/[handle]/quick-view/route.ts
  - src/lib/reviews/trustoo.ts
  - src/lib/shopify/operations/product.ts
  - codegen.ts
autonomous: false
requirements:
  - AUDIT-04
  - AUDIT-05
  - AUDIT-10
must_haves:
  truths:
    - D-10: Third-party boundaries must keep using typed narrowing, fail-safe errors, and no secret logging.
    - D-11: Production rate limiting must not rely only on module-local memory.
    - D-16: Verification must be expanded before the phase is considered production-ready.
  artifacts:
    - package.json
    - scripts/component-contracts/button-system.test.mjs
    - src/lib/rate-limit/index.ts
  key_links:
    - CODEBASE_REVIEW.md -> testing/security findings -> executable gates
---

<objective>
Turn the review's reliability/security concerns into executable gates and safer production boundaries for tests, rate limiting, webhooks, quick-view, codegen, and third-party degradation.
</objective>

<execution_context>
@CODEBASE_REVIEW.md
@.planning/phases/05-codebase-review-remediation/05-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@docs/conventions.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add first-class verification scripts</name>
  <files>package.json, .husky/pre-commit, .husky/pre-push, scripts/component-contracts/button-system.test.mjs</files>
  <read_first>package.json, scripts/component-contracts/button-system.test.mjs, .planning/codebase/TESTING.md</read_first>
  <action>Add `typecheck`, `test:contracts`, and `test:stories` scripts. Repair or remove the stale `catalogues.tsx` reference in the button-system contract. Wire lightweight checks into hooks without making local commits painfully slow; put heavier build/storybook checks in pre-push or documented CI commands.</action>
  <verify>Run `pnpm typecheck`, `pnpm test:contracts`, `pnpm lint`, and `pnpm build-storybook`.</verify>
  <acceptance_criteria>
    - Contract tests no longer reference removed files.
    - Typecheck can run without a production build.
    - Storybook test/build command is documented or scripted.
  </acceptance_criteria>
  <done>Testing gates are visible, runnable, and current.</done>
</task>

<task type="auto">
  <name>Task 2: Add executable coverage for risky flows</name>
  <files>src/components/product/product-form/product-form.stories.tsx, src/components/collection/product-card/product-purchase-form.stories.tsx, src/app/(storefront)/cart/_components/cart-line-actions.stories.tsx, src/components/homepage/contact-form/contact-form.stories.tsx, src/components/homepage/newsletter/newsletter.stories.tsx</files>
  <read_first>src/components/product/product-form/product-form.stories.tsx, src/components/collection/product-card/product-purchase-form.tsx, src/app/(storefront)/cart/_components/cart-line-actions.tsx</read_first>
  <action>Add Storybook play tests or interaction stories for PDP add-to-cart success/error, product-card add-to-cart quantity behavior, cart line update/remove states, homepage contact form error/success, and homepage newsletter validation. Mock server actions rather than hitting Shopify or Resend.</action>
  <verify>Run Storybook test/build commands and manually open affected stories if needed.</verify>
  <acceptance_criteria>
    - Core cart/product/form paths have executable interaction coverage.
    - Stories do not require real Shopify, Searchanise, or Resend credentials.
    - Loading, success, and error states are represented.
  </acceptance_criteria>
  <done>Highest-risk user flows are regression-testable.</done>
</task>

<task type="auto">
  <name>Task 3: Replace ad hoc rate limiting with a production-capable boundary</name>
  <files>src/lib/rate-limit/index.ts, src/lib/contact/actions.ts, src/app/api/search/suggestions/route.ts, docs/conventions.md</files>
  <read_first>src/lib/contact/actions.ts, src/app/api/search/suggestions/route.ts, .planning/codebase/CONCERNS.md</read_first>
  <action>Create a small server-only rate-limit helper with a durable-store interface and a development fallback. Integrate it with contact/newsletter actions and search suggestions. If no production store dependency is available, document the required provider-level or KV-backed deployment setting and keep the module-local fallback clearly development-only.</action>
  <verify>Unit/contract-test limiter behavior where possible; manually send repeated contact/newsletter/search requests and confirm throttling behavior.</verify>
  <acceptance_criteria>
    - Contact/newsletter rate limiting no longer depends silently on local module state in production.
    - Search suggestions have server-side abuse protection.
    - No secret values are logged or written to planning docs.
  </acceptance_criteria>
  <done>Public submission/search surfaces have a production-capable abuse boundary.</done>
</task>

<task type="auto">
  <name>Task 4: Harden webhook, quick-view, codegen, and enrichment failure paths</name>
  <files>src/app/api/webhooks/shopify/route.ts, src/app/api/products/[handle]/quick-view/route.ts, src/lib/reviews/trustoo.ts, src/lib/shopify/operations/product.ts, codegen.ts</files>
  <read_first>src/app/api/webhooks/shopify/route.ts, src/app/api/products/[handle]/quick-view/route.ts, src/lib/reviews/trustoo.ts, src/lib/shopify/operations/product.ts, codegen.ts</read_first>
  <action>Add Shopify page webhook topic handling to revalidate `page` and `pages` tags. Wrap quick-view product fetch failures and return stable JSON errors/statuses. Validate codegen Shopify env before constructing the schema URL. Adjust Trustoo/Hulk degradation so empty fallback data is not cached for long periods without telemetry or shorter cache behavior.</action>
  <verify>Run lint/build. Exercise quick-view with a bad handle or mocked Shopify failure. Review webhook topic switch coverage.</verify>
  <acceptance_criteria>
    - Shopify page edits can invalidate page caches.
    - Quick-view API returns stable JSON on external failures.
    - Codegen fails early with actionable missing-env messages.
    - Third-party outages do not silently cache degraded empty enrichment for long durations.
  </acceptance_criteria>
  <done>Production reliability gaps are closed or explicitly documented.</done>
</task>

</tasks>

<testing_steps>

1. `pnpm typecheck`
2. `pnpm test:contracts`
3. `pnpm lint`
4. `pnpm build`
5. `pnpm build-storybook`
6. Targeted manual/API checks for contact/newsletter/search rate limiting and quick-view error output.

</testing_steps>

<risks_and_edge_cases>

- Do not add a new paid rate-limit provider without confirming deployment expectations.
- Storybook interaction tests must not mutate a real Shopify cart.
- Webhook topic handling must keep accepting unknown topics without failing legitimate Shopify retries.
- Degraded enrichment behavior should avoid hiding all ratings/pricing forever after a transient outage.

</risks_and_edge_cases>

<success_criteria>

- `AUDIT-04` and `AUDIT-05` are implemented.
- Contract tests, typecheck, lint, build, and Storybook build pass.
- Public API and Server Action boundaries have safer production behavior.

</success_criteria>
