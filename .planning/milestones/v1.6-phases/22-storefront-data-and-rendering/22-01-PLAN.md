---
phase: "22-storefront-data-and-rendering"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/sanity/queries/home-page.ts"
  - "src/lib/sanity/queries/home-page.test.ts"
  - "src/lib/sanity/types.ts"
  - "src/lib/sanity/home-page.ts"
  - "src/lib/sanity/home-page.test.ts"
  - "src/lib/seo/homepage-json-ld.ts"
autonomous: true
requirements: [DATA-01, DATA-02, RENDER-02, QUALITY-01]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Missing or invalid Sanity homePage content fails loudly at the route data boundary."
    - "D-03/D-07/D-08/D-16: Required document, SEO, canonical '/', link, image URL, and image dimensions are validated before rendering."
    - "D-05/D-06: Homepage SEO fields come from Sanity while global launch noindex controls remain enforceable."
    - "D-14/D-17: Authored homepage images are normalized from Sanity with crop/hotspot-aware URLs and dimensions."
  artifacts:
    - path: "src/lib/sanity/queries/home-page.ts"
      provides: "GROQ query for all Phase 21 modeled homePage fields."
      exports: ["homePageQuery"]
    - path: "src/lib/sanity/home-page.ts"
      provides: "Cached fail-loud Sanity homepage operation and HomepageContent view model."
      exports: ["getHomepage", "HomepageContent"]
    - path: "src/lib/seo/homepage-json-ld.ts"
      provides: "Code-owned homepage Organization/WebSite JSON-LD constants."
      exports: ["organizationJsonLd", "websiteJsonLd"]
  key_links:
    - from: "src/lib/sanity/home-page.ts"
      to: "src/lib/sanity/client.ts"
      via: "sanityFetch() and getSanityImageUrl()"
      pattern: "sanityFetch|getSanityImageUrl"
    - from: "src/lib/sanity/home-page.ts"
      to: "src/lib/sanity/queries/home-page.ts"
      via: "homePageQuery"
      pattern: "homePageQuery"
---

<objective>
Create the typed Sanity homepage data boundary that every rendering plan consumes.

Purpose: Phase 22 cannot safely prop-enable or cut over the homepage until a strict, typed, cached `getHomepage()` contract exists.
Output: GROQ query, raw Sanity types, public `HomepageContent` view model, fail-loud operation tests, and code-owned JSON-LD constants.
</objective>

<execution_context>
@C:/Users/evang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/evang/.codex/get-shit-done/templates/summary.md
@.agents/skills/next-cache-components/SKILL.md
@.agents/skills/vercel-react-best-practices/SKILL.md
@docs/conventions.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-sanity-homepage-model-and-seed/21-01-SUMMARY.md
@.planning/phases/22-storefront-data-and-rendering/22-CONTEXT.md
@.planning/phases/22-storefront-data-and-rendering/22-RESEARCH.md
@.planning/phases/22-storefront-data-and-rendering/22-VALIDATION.md
@.planning/phases/22-storefront-data-and-rendering/22-PATTERNS.md
@.planning/phases/22-storefront-data-and-rendering/22-UI-SPEC.md
@node_modules/next/dist/docs/01-app/01-getting-started/08-caching.md
@node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-cache.md
@node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cacheTag.md
@node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cacheLife.md
@src/lib/sanity/client.ts
@src/lib/sanity/env.ts
@src/lib/sanity/types.ts
@src/lib/sanity/queries/blog.ts
@src/lib/sanity/queries/blog.test.ts
@src/lib/blog/operations.ts
@src/components/homepage/content.ts
@../teavision-cms/schemaTypes/documents/home-page.ts
@../teavision-cms/schemaTypes/objects/homepage/hero.ts
@../teavision-cms/schemaTypes/objects/homepage/image-card.ts
@../teavision-cms/schemaTypes/objects/homepage/link.ts
@../teavision-cms/schemaTypes/objects/homepage/section.ts
@../teavision-cms/schemaTypes/objects/homepage/testimonial.ts
@../teavision-cms/schemaTypes/objects/homepage/faq.ts
@../teavision-cms/scripts/seed-home-page/data.ts
</context>

<interfaces>
- `src/lib/sanity/client.ts` exposes `sanityFetch<T>(query, params)` and `getSanityImageUrl(source, options)`.
- `src/lib/sanity/types.ts` owns raw nullable Sanity result shapes; keep Sanity homepage raw types here.
- `src/lib/sanity/home-page.ts` must export the public app-facing `HomepageContent` type consumed by Plans 22-02 through 22-05.
- Use named exports only for lib modules. Do not import Shopify generated types for Sanity homepage content.
</interfaces>

<dependency_graph>
Needs: Phase 21 `homePage` schema and seed shape.
Creates: `HomepageContent`, `getHomepage()`, `homePageQuery`, and JSON-LD constants.
Enables: Plans 22-02, 22-03, 22-04, and 22-05.
</dependency_graph>

<source_audit>
| SOURCE | ID | Feature/Requirement | Covered By |
| --- | --- | --- | --- |
| GOAL | - | Fetch typed Sanity homepage content and render it through the existing storefront homepage with visual, SEO, and commerce parity. | 22-01, 22-02, 22-03, 22-04, 22-05, 22-06, 22-07, 22-08 |
| REQ | DATA-01 | Typed server-side Sanity storefront data boundary. | 22-01, 22-05 |
| REQ | DATA-02 | Seeded content renders without Sanity owning Shopify commerce data. | 22-01, 22-03, 22-04, 22-05, 22-06, 22-07 |
| REQ | RENDER-01 | Existing homepage design, order, forms, and links preserved with Sanity content. | 22-02, 22-03, 22-04, 22-05, 22-06, 22-07, 22-08 |
| REQ | RENDER-02 | Sanity images render with dimensions, alt, crop/hotspot, and hero LCP discipline. | 22-01, 22-02, 22-03, 22-04, 22-07, 22-08 |
| REQ | QUALITY-01 | Metadata, canonical/indexation, JSON-LD, and one-H1 behavior preserved. | 22-01, 22-07, 22-08 |
| RESEARCH | - | Cached published Sanity read with `use cache`, tags, and `cacheLife('hours')`. | 22-01 |
| RESEARCH | - | Prop-enable existing homepage components instead of CMS-only duplicates. | 22-02, 22-03, 22-04, 22-05, 22-06 |
| RESEARCH | - | CMS Tea Journal config plus live blog posts. | 22-05, 22-07 |
| RESEARCH | - | Replace static metadata with CMS-backed `generateMetadata()` through `withNoindexRobots()`. | 22-07 |
| RESEARCH | - | Preview, webhook homepage revalidation, and release proof are assigned to Phase 23. | excluded |
| CONTEXT | D-01..D-04 | Fail-loud content boundary; fixture data only, no runtime fallback. | 22-01, 22-02, 22-05 |
| CONTEXT | D-05..D-08 | Sanity SEO ownership with launch noindex and canonical `/`. | 22-01, 22-05 |
| CONTEXT | D-09..D-13 | All modeled fields render through existing components, fixed order, live Tea Journal articles. | 22-02, 22-03, 22-04, 22-05, 22-06, 22-07 |
| CONTEXT | D-14..D-18 | Sanity-authored images, hero LCP discipline, dimensions, crop/hotspot, code-owned motifs. | 22-01, 22-02, 22-03, 22-04, 22-06 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the homepage Sanity boundary</name>
  <files>src/lib/sanity/queries/home-page.test.ts, src/lib/sanity/home-page.test.ts</files>
  <read_first>
    @.planning/phases/22-storefront-data-and-rendering/22-CONTEXT.md
    @.planning/phases/22-storefront-data-and-rendering/22-RESEARCH.md
    @src/lib/sanity/client.ts
    @src/lib/sanity/types.ts
    @src/lib/sanity/queries/blog.test.ts
    @src/lib/blog/operations.test.ts
    @../teavision-cms/scripts/seed-home-page/data.ts
  </read_first>
  <behavior>
    - Missing `homePage` result rejects with an explicit error mentioning `homePage`.
    - Missing `seo.metaTitle`, missing `seo.metaDescription`, and `seo.canonicalPath !== "/"` reject.
    - Invalid hrefs reject unless they start with `/`, `https://`, `mailto:`, or `tel:`.
    - Required authored images reject when URL/asset ID or dimensions are missing.
    - Valid seeded content fixture/shape assertions cover 11 product cards, 3 private-label cards, 4 proof points, 6 certification coverage items, 7 supply-chain protection marks, and Tea Journal `blogHandle`, `linkLabel`, and `maxPosts`.
    - `getSanityImageUrl()` receives bounded options: hero `{ width: 1920, quality: 75, fit: "max" }`, card images no wider than 900, mark/logo images no wider than 640.
    - `cacheTag('homePage', 'sanity-homepage')` and `cacheLife('hours')` are called by `getHomepage()`.
  </behavior>
  <action>Add co-located Vitest tests before implementation. These test files are scaffold-exempt because `pnpm create:lib` creates implementation modules, while paired tests are project-standard co-located additions under the same lib/query folders. Use `unknown` plus narrowing in fixtures where needed; do not use `any`. Cover D-01, D-02, D-03, D-07, D-08, D-14, D-16, and D-17 with failing assertions. Per D-03, keep section count/cardinality expectations as seeded fixture/shape assertions only; do not add tests requiring `getHomepage()` to throw solely because a section array has the wrong item count.</action>
  <verify>
    <automated>pnpm test:unit -- src/lib/sanity/queries/home-page.test.ts src/lib/sanity/home-page.test.ts</automated>
  </verify>
  <acceptance_criteria>The new tests exist, fail before implementation, and distinguish runtime fail-loud checks for required render-critical fields from fixture-only seeded cardinality assertions per D-03.</acceptance_criteria>
  <done>The new tests exist and fail for missing `homePageQuery`, `getHomepage()`, and homepage normalization behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement query, types, cached operation, and JSON-LD split</name>
  <files>src/lib/sanity/queries/home-page.ts, src/lib/sanity/types.ts, src/lib/sanity/home-page.ts, src/lib/seo/homepage-json-ld.ts, src/lib/sanity/queries/home-page.test.ts, src/lib/sanity/home-page.test.ts</files>
  <read_first>
    @src/lib/sanity/queries/home-page.test.ts
    @src/lib/sanity/home-page.test.ts
    @src/lib/sanity/client.ts
    @src/lib/sanity/types.ts
    @src/lib/blog/operations.ts
    @../teavision-cms/schemaTypes/documents/home-page.ts
    @../teavision-cms/schemaTypes/objects/homepage/link.ts
    @../teavision-cms/scripts/seed-home-page/data.ts
  </read_first>
  <behavior>
    - `homePageQuery` projects every Phase 21 modeled field in fixed schema order.
    - `getHomepage()` returns `HomepageContent` and throws explicit errors for missing or invalid render-critical content.
    - Sanity images normalize to URL, alt text, width, height, LQIP, and crop/hotspot-aware generated URL.
    - JSON-LD constants move to `src/lib/seo/homepage-json-ld.ts` with named exports.
  </behavior>
  <action>Before adding implementation files, run `pnpm create:lib -- sanity/queries/home-page`, `pnpm create:lib -- sanity/home-page`, and `pnpm create:lib -- seo/homepage-json-ld` where the scaffold produces the expected `src/lib/...` path; if the scaffold refuses an existing parent folder or omits the exact file, manually create only the intended file and record that scaffold limitation in the summary. Implement `homePageQuery` against `*[_type == "homePage" && slug.current == "/"][0]`. Add raw nullable Sanity homepage types to `src/lib/sanity/types.ts`. Implement `HomepageContent` and `getHomepage()` in `src/lib/sanity/home-page.ts` with `'use cache'`, `cacheTag('homePage', 'sanity-homepage')`, `cacheLife('hours')`, `sanityFetch<SanityHomePageResult>()`, fail-loud validation for required document existence, SEO object and required SEO fields, canonical `/`, required links, image URL/asset presence, alt text, dimensions, crop/hotspot-safe image metadata, and fields needed for rendering, plus no commerce authority fields per DATA-02. Do not throw solely for invalid section item counts; trust Sanity schema for detailed count, length, and editor-input rules per D-03, with count/cardinality coverage limited to seeded fixture/shape assertions in tests. Mirror the CMS link allowlist exactly for D-03. Keep Organization/WebSite JSON-LD code-owned in `src/lib/seo/homepage-json-ld.ts` for D-05 and QUALITY-01. Make Task 1 tests pass.</action>
  <verify>
    <automated>pnpm test:unit -- src/lib/sanity/queries/home-page.test.ts src/lib/sanity/home-page.test.ts</automated>
    <automated>pnpm lint -- --quiet</automated>
  </verify>
  <acceptance_criteria>`getHomepage()` is cached, typed, and fail-loud for required document/SEO/canonical/link/image/rendering fields while preserving D-03 by leaving detailed section counts to Sanity schema and fixture tests.</acceptance_criteria>
  <done>`getHomepage()` is a tested, cached, fail-loud homepage data boundary with typed output and no runtime fallback path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Sanity CMS -> storefront server operation | Published CMS content crosses from external Sanity into server rendering and metadata. |
| Sanity image metadata -> Next Image | External image URLs, dimensions, crop, hotspot, LQIP, and alt text become render inputs. |
| CMS links -> storefront anchors/buttons | Editor-authored hrefs become public navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-22-01 | Tampering | `src/lib/sanity/home-page.ts` | mitigate | Validate required document, canonical `/`, SEO fields, links, image URL/asset, alt text, dimensions, crop/hotspot-safe metadata, and render-critical fields; throw on invalid required content. |
| T-22-02 | Information Disclosure | `src/lib/sanity/home-page.ts` SEO mapping | mitigate | Preserve CMS `seo.noIndex` as page intent for Plan 22-05 to pass through `withNoindexRobots()`. |
| T-22-03 | Denial of Service | Sanity image normalization | mitigate | Reject missing dimensions and use bounded image URL options for hero, cards, marks, and logos. |
| T-22-04 | Spoofing | CMS-authored links | mitigate | Accept only `/`, `https://`, `mailto:`, and `tel:` hrefs; reject invalid links. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned in this phase. |
</threat_model>

<verification>
Run after this plan:
- `pnpm test:unit -- src/lib/sanity/queries/home-page.test.ts src/lib/sanity/home-page.test.ts`
- `pnpm lint -- --quiet`
</verification>

<success_criteria>
- `homePageQuery` covers every Phase 21 modeled field.
- `getHomepage()` throws for missing/invalid required CMS document, SEO, canonical `/`, links, images, and render-critical fields while trusting Sanity schema for detailed count rules per D-03.
- `HomepageContent` is exported and usable by later component and route plans.
- JSON-LD constants are isolated from fixture/static homepage content.
</success_criteria>

<output>
Create `.planning/phases/22-storefront-data-and-rendering/22-01-SUMMARY.md` when done.
</output>
