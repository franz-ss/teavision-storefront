---
status: complete
date: 2026-06-22
commit: cd784ab
---

# Quick Task 260622-g38 Summary

## Completed

- Removed the non-editable phone section from the account profile form.
- Updated the profile intro copy so it only references email as Shopify-account-managed.
- Updated the ProfileForm Storybook interaction checks to expect no visible phone label, value, empty-state copy, or phone-change explanatory copy.

## Verification

- Red check: `pnpm vitest run --config vitest.storybook.config.mts "src/app/(storefront)/account/_components/profile-form/profile-form.stories.tsx"` failed before the component change because the `Phone` label was still rendered.
- Green check: `pnpm vitest run --config vitest.storybook.config.mts "src/app/(storefront)/account/_components/profile-form/profile-form.stories.tsx"` passed with 5/5 tests.
- `pnpm lint` passed, including Tailwind class checks.
- `pnpm typecheck` passed.
- Commit hook passed `pnpm lint` and `pnpm test:contracts` with 37/37 tests passing.
