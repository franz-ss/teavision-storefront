---
status: complete
date: 2026-06-22
---

# Quick Task 260622-g38: Remove Non-Editable Account Profile Phone Field

## Goal

Remove the phone field from `/account/profile` because the profile form cannot edit it.

## Tasks

1. Update the existing ProfileForm Storybook interaction to expect no visible phone field or read-only phone copy.
2. Run the focused Storybook test and confirm the updated expectation fails against the current component.
3. Remove the non-editable phone display block from `ProfileForm`.
4. Re-run the focused Storybook test, then run lint.
5. Record the quick-task summary and update `.planning/STATE.md`.

## Acceptance Criteria

- `/account/profile` still shows editable first and last name fields.
- `/account/profile` still shows the non-editable email account-sign-in copy.
- `/account/profile` no longer renders a visible phone label, value, placeholder, or explanatory copy.
- Existing focused Storybook coverage reflects the removed phone UI.
