---
quick_id: 260615-dm4
slug: bespoke-how-long-does-bulk-tea-last-guid
date: 2026-06-15
mode: quick
---

# Quick Task 260615-dm4: Bespoke /pages/how-long-does-bulk-tea-last

## Goal

Replace the generic `[...slug]` catch-all treatment of `/pages/how-long-does-bulk-tea-last`
(plain brand hero + raw Shopify rich-text body + boilerplate support CTA) with a
bespoke, scannable buyer guide matching the other bespoke service/guide routes
(`download-catalogues`, `bulk-wholesale-supply`, etc.).

Design approved via two visual mockups. Locked decisions:

- **Copy is VERBATIM** from the production page — every paragraph word-for-word, no
  rewording. Layout/grouping only; words and order preserved (the single deviation is
  splitting the existing P8 at sentence boundaries so its light sentence anchors the
  "Light" factor — no words changed).
- Bespoke route overrides the catch-all; handle reserved in `[...slug]`.
- Sections: brand hero (intro) → "Why our order lasts" + a `12–36 months` emphasis
  callout → "Factors to consider" as a semantic `<dl>` factor rail (Light / Humidity /
  Odours), not identical icon cards → "How to prepare" → brand CTA with the verbatim
  range list and "Think tea, think TeaVision" sign-off.
- Primitives only (`Section`, `Eyebrow`, `Badge`, `Button`), design tokens only, `cn()`
  for conditional classes. No new image assets.

## Tasks

### 1. Reserve the handle + add the bespoke route
- Add `how-long-does-bulk-tea-last` to `RESERVED_HANDLES` in
  `src/app/(storefront)/pages/[...slug]/page.tsx` (the handle is a live Shopify page, so
  the catch-all would otherwise statically generate it and collide with the new route).
- `page.tsx` — indexable `metadata` (title, description, canonical) rendering `PageContent`.
- **verify:** `pnpm build` resolves the concrete route; no duplicate-route error.
- **done:** handle reserved, route file present.

### 2. Build the page sections
- `_lib/data.ts` — verbatim hero/longevity/factors/prepare/CTA copy + badges + FAQ pair.
- `_components/` — `page-content.tsx`, `hero-section.tsx`, `longevity-section.tsx`,
  `factors-section.tsx`, `prepare-section.tsx`, `cta-section.tsx`, `json-ld.tsx`.
- `_components/page-content.stories.tsx` — Default/Desktop/Mobile stories with `play` assertions.
- **verify:** `pnpm lint` + `pnpm typecheck` clean; page renders hero + longevity callout +
  three-factor rail + prepare + CTA; copy matches production verbatim.
- **done:** bespoke page matches the approved mockup with verbatim copy.

## must_haves

- truths:
  - `/pages/how-long-does-bulk-tea-last` is served by a concrete route, not the catch-all.
  - On-page body copy is verbatim from the production page.
- artifacts:
  - `src/app/(storefront)/pages/how-long-does-bulk-tea-last/page.tsx`
  - `src/app/(storefront)/pages/how-long-does-bulk-tea-last/_lib/data.ts`
  - `src/app/(storefront)/pages/how-long-does-bulk-tea-last/_components/*`
- key_links:
  - `src/app/(storefront)/pages/[...slug]/page.tsx` RESERVED_HANDLES
  - `src/components/ui` (Section, Eyebrow, Badge, Button)
